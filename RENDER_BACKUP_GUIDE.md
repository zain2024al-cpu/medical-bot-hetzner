# 💾 دليل النسخ الاحتياطي على Render

## ✅ نظام النسخ الاحتياطي

تم إضافة نظام نسخ احتياطي محلي يعمل على Render لحماية قاعدة البيانات من الفقدان.

---

## 🔄 كيف يعمل النظام

### 1. النسخ الاحتياطي التلقائي:

```
⏰ كل 10 دقائق:
   ✅ نسخة احتياطية محلية تلقائية
   ✅ يحفظ آخر 10 نسخ احتياطية
   ✅ يحذف النسخ القديمة تلقائياً
```

### 2. النسخ الاحتياطي اليومي:

```
📅 كل يوم الساعة 3:00 صباحاً:
   ✅ نسخة احتياطية يومية
   ✅ تُحفظ في مجلد /app/db/backups
```

### 3. عند إعادة النشر:

```
🔄 إعادة النشر:
   1. Container جديد يبدأ
   2. قاعدة البيانات تُحمّل من medical_reports_initial.db ✅
   3. النسخ الاحتياطية موجودة في /app/db/backups ✅
   4. البيانات محفوظة! ✅
```

---

## 📁 مواقع النسخ الاحتياطية

### على Render:

1. **قاعدة البيانات المحلية:**
   ```
   /app/db/medical_reports.db
   ⚠️ مؤقت (يضيع عند إعادة النشر)
   ```

2. **النسخ الاحتياطية المحلية:**
   ```
   /app/db/backups/backup_YYYYMMDD_HHMMSS.db
   ✅ محفوظة في Render Disk (إذا كان مربوط)
   ⚠️ قد تضيع إذا لم يكن Disk مربوط
   ```

3. **قاعدة البيانات الأولية:**
   ```
   /app/db/medical_reports_initial.db
   ✅ موجودة في Git
   ✅ تُحمّل تلقائياً عند البدء
   ```

---

## 🔧 إعداد Render Disk (موصى به)

### خطوات الإعداد:

1. **إنشاء Disk في Render:**
   - اذهب إلى Render Dashboard
   - اضغط **"New +"** → **"Disk"**
   - **Name:** `medical-reports-db`
   - **Size:** `1 GB` (أو أكثر)
   - **Mount Path:** `/app/db`

2. **ربط Disk بالـ Service:**
   - اذهب إلى إعدادات الـ Service
   - في قسم **"Disks"**، اضغط **"Link Disk"**
   - اختر الـ Disk الذي أنشأته
   - **Mount Path:** `/app/db`

3. **النتيجة:**
   - ✅ قاعدة البيانات تُحفظ في Disk
   - ✅ النسخ الاحتياطية تُحفظ في Disk
   - ✅ البيانات لا تضيع عند إعادة النشر

---

## 📊 استخدام النسخ الاحتياطي

### إنشاء نسخة احتياطية يدوياً:

```python
from services.render_backup import create_local_backup

# إنشاء نسخة احتياطية
backup_path = create_local_backup()
if backup_path:
    print(f"✅ تم إنشاء النسخة الاحتياطية: {backup_path}")
```

### قائمة النسخ الاحتياطية:

```python
from services.render_backup import list_local_backups

# عرض جميع النسخ الاحتياطية
backups = list_local_backups()
for backup in backups:
    print(f"📁 {backup['filename']} - {backup['size_kb']:.2f} KB")
```

### استعادة من نسخة احتياطية:

```python
from services.render_backup import restore_from_local_backup

# استعادة من نسخة احتياطية
success = restore_from_local_backup("backup_20251206_204500.db")
if success:
    print("✅ تم استعادة قاعدة البيانات")
```

---

## ⚠️ ملاحظات مهمة

### 1. Render Disk:
- ✅ **موصى به بشدة** لحفظ البيانات بشكل دائم
- ⚠️ بدون Disk، النسخ الاحتياطية قد تضيع عند إعادة النشر
- 💰 Disk له تكلفة إضافية (لكن صغيرة)

### 2. النسخ الاحتياطية المحلية:
- ✅ تُحفظ تلقائياً كل 10 دقائق
- ✅ آخر 10 نسخ محفوظة
- ⚠️ قد تضيع إذا لم يكن Disk مربوط

### 3. قاعدة البيانات الأولية:
- ✅ موجودة في Git (`medical_reports_initial.db`)
- ✅ تُحمّل تلقائياً عند البدء
- ✅ آمنة من الفقدان

---

## 🔍 التحقق من النسخ الاحتياطية

### من Render Dashboard:

1. اذهب إلى Render Dashboard
2. اختر الـ Service
3. اضغط **"Shell"** (إذا كان متاحاً)
4. شغّل:
   ```bash
   ls -lh /app/db/backups/
   ```

### من Logs:

ابحث عن:
```
✅ تم إنشاء نسخة احتياطية محلية
📁 المسار: /app/db/backups/backup_*.db
💾 الحجم: X KB
```

---

## 🆘 استكشاف الأخطاء

### المشكلة: النسخ الاحتياطية لا تُحفظ

**الحل:**
1. تحقق من Logs: `✅ تم إنشاء نسخة احتياطية محلية`
2. تحقق من وجود مجلد `/app/db/backups`
3. تحقق من الصلاحيات

### المشكلة: قاعدة البيانات تضيع بعد إعادة النشر

**الحل:**
1. ربط Render Disk بالـ Service
2. التأكد من أن `medical_reports_initial.db` موجود في Git
3. التحقق من Logs: `✅ Database loaded from initial file`

---

## ✅ الخلاصة

**النسخ الاحتياطي يعمل الآن:**
- ✅ نسخ احتياطي تلقائي كل 10 دقائق
- ✅ نسخ احتياطي يومي
- ✅ آخر 10 نسخ محفوظة
- ✅ قاعدة البيانات الأولية في Git

**لحماية أفضل:**
- ✅ ربط Render Disk
- ✅ رفع قاعدة البيانات إلى Git بشكل دوري

🎉 البيانات محمية الآن!

