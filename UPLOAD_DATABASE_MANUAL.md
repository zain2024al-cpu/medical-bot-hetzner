# 📤 رفع قاعدة البيانات يدوياً إلى Railway

## 🎯 الهدف:

رفع قاعدة البيانات المحلية (0.28 MB) إلى Railway.

---

## 📋 الطرق المتاحة:

### الطريقة 1: تثبيت المكتبة ثم الرفع (موصى به)

#### الخطوات:

1. **تثبيت المكتبة:**
   ```powershell
   pip install google-cloud-storage
   ```

2. **رفع قاعدة البيانات:**
   ```powershell
   python -c "from services.sqlite_backup import backup_now; backup_now()"
   ```

3. **إعادة النشر في Railway:**
   - اذهب إلى Railway → اضغط "Deploy"
   - قاعدة البيانات ستُستعاد تلقائياً

---

### الطريقة 2: رفع مباشر إلى Railway (بدون Cloud Storage)

#### الخطوات:

1. **تحضير قاعدة البيانات:**
   - تأكد من وجود `db/medical_reports.db`
   - الحجم: 0.28 MB

2. **رفع قاعدة البيانات إلى Railway:**
   - يمكن رفعها كملف في GitHub
   - أو استخدام Railway CLI
   - أو رفعها يدوياً

---

### الطريقة 3: استخدام Railway CLI (متقدم)

إذا كان لديك Railway CLI:

```bash
# رفع قاعدة البيانات
railway run python upload_database.py
```

---

## 🔧 الحل السريع:

### 1. تثبيت المكتبة:

```powershell
pip install google-cloud-storage
```

### 2. رفع قاعدة البيانات:

```powershell
python -c "from services.sqlite_backup import backup_now; backup_now()"
```

### 3. إعادة النشر:

- اذهب إلى Railway → اضغط "Deploy"
- قاعدة البيانات ستُستعاد تلقائياً

---

## ⚠️ إذا لم تكن لديك Google Cloud Credentials:

### الحل البديل:

1. **رفع قاعدة البيانات إلى GitHub:**
   - أضف `db/medical_reports.db` إلى GitHub
   - Railway سينسخها تلقائياً

2. **أو استخدام Railway Volume:**
   - إنشاء Volume في Railway
   - رفع قاعدة البيانات إليه

---

## 📋 قائمة التحقق:

- [ ] قاعدة البيانات المحلية موجودة (0.28 MB)
- [ ] تم تثبيت `google-cloud-storage` (للطريقة 1)
- [ ] تم رفع قاعدة البيانات
- [ ] تم إعادة النشر في Railway
- [ ] قاعدة البيانات مستعادة في Railway

---

**ابدأ بتثبيت المكتبة ثم الرفع!** 🚀

