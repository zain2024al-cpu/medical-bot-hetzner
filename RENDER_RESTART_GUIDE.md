# 🔄 دليل إعادة تشغيل/نشر البوت على Render

## 📋 طرق إعادة التشغيل

هناك عدة طرق لإعادة تشغيل البوت على Render:

---

## 🚀 الطريقة 1: إعادة النشر التلقائي (موصى به)

### أ. استخدام السكريبت:

```powershell
.\restart_render.ps1
```

السكريبت سيقوم بـ:
1. ✅ حفظ التغييرات في Git
2. ✅ رفع التغييرات إلى GitHub
3. ✅ Render سيعيد النشر تلقائياً

### ب. يدوياً:

```bash
# 1. حفظ التغييرات
git add .
git commit -m "تحديث وإعادة نشر"

# 2. رفع إلى GitHub
git push origin main

# 3. Render سيعيد النشر تلقائياً ✅
```

---

## 🔄 الطريقة 2: إعادة التشغيل من Render Dashboard

### خطوات:

1. **اذهب إلى Render Dashboard:**
   - [dashboard.render.com](https://dashboard.render.com)

2. **اختر الـ Service:**
   - اضغط على `medical-reports-bot`

3. **إعادة التشغيل:**
   - اضغط على **"Manual Deploy"** في القائمة العلوية
   - اختر **"Deploy latest commit"**
   - أو اضغط على **"Restart"** لإعادة تشغيل سريع

---

## 🔄 الطريقة 3: إعادة التشغيل السريع (بدون تحديث الكود)

### من Render Dashboard:

1. اذهب إلى الـ Service
2. اضغط على **"Restart"** في القائمة العلوية
3. سيتم إعادة تشغيل البوت فوراً

**ملاحظة:** هذه الطريقة لا تُحدّث الكود، فقط تعيد تشغيل البوت.

---

## 💾 تحديث قاعدة البيانات

### الطريقة 1: عبر Render Disk

1. **اذهب إلى Render Dashboard**
2. **اختر Disk:**
   - اضغط على `medical-reports-db` (أو اسم الـ Disk الخاص بك)
3. **رفع قاعدة البيانات:**
   - اضغط **"Upload"**
   - ارفع ملف `db/medical_reports.db`
4. **إعادة تشغيل الـ Service:**
   - اذهب إلى الـ Service
   - اضغط **"Restart"**

### الطريقة 2: عبر Git (مؤقت)

```bash
# 1. نسخ قاعدة البيانات
cp db/medical_reports.db db/medical_reports_initial.db

# 2. إضافة إلى Git
git add db/medical_reports_initial.db
git commit -m "تحديث قاعدة البيانات"
git push origin main

# 3. Render سيعيد النشر تلقائياً

# 4. بعد النشر، احذف الملف من Git
git rm db/medical_reports_initial.db
git commit -m "إزالة قاعدة البيانات من Git"
git push origin main
```

---

## 🔍 التحقق من النشر

### 1. فحص Logs:

1. اذهب إلى Render Dashboard
2. اضغط على الـ Service
3. اضغط **"Logs"**
4. تحقق من:
   ```
   ✅ تم تحميل X اسم مريض من قاعدة البيانات
   ✅ Database loaded: X KB
   ✅ تم تهيئة أسماء المرضى بنجاح
   ```

### 2. اختبار البوت:

1. افتح Telegram
2. ابحث عن البوت
3. أرسل `/start`
4. جرب إضافة تقرير جديد
5. تحقق من أن أسماء المرضى تظهر ✅

---

## ⚠️ ملاحظات مهمة

### 1. إعادة النشر التلقائي:
- ✅ Render يُحدّث تلقائياً عند push إلى GitHub
- ✅ لا حاجة لإعادة النشر يدوياً
- ⏳ قد يستغرق 2-5 دقائق

### 2. قاعدة البيانات:
- ✅ استخدم Render Disk لحفظ قاعدة البيانات بشكل دائم
- ✅ تأكد من أن `DATABASE_PATH=/app/db/medical_reports.db`
- ✅ تأكد من أن `AUTO_RESTORE_ON_STARTUP=true`

### 3. Webhook:
- ⚠️ بعد إعادة النشر، قد تحتاج لإعادة إعداد Webhook
- ✅ تحقق من أن Webhook يعمل:
  ```bash
  curl "https://api.telegram.org/bot<BOT_TOKEN>/getWebhookInfo"
  ```

---

## 🆘 استكشاف الأخطاء

### المشكلة: البوت لا يعمل بعد إعادة النشر

**الحل:**
1. تحقق من Logs في Render Dashboard
2. تحقق من متغيرات البيئة
3. تحقق من أن Webhook مُعدّ بشكل صحيح

### المشكلة: قاعدة البيانات لا تُحدّث

**الحل:**
1. تحقق من أن قاعدة البيانات موجودة في Render Disk
2. تأكد من أن `AUTO_RESTORE_ON_STARTUP=true`
3. أعد تشغيل الـ Service

### المشكلة: أسماء المرضى لا تظهر

**الحل:**
1. تحقق من Logs: `✅ تم تحميل X اسم مريض`
2. تحقق من أن قاعدة البيانات موجودة
3. أعد تشغيل الـ Service

---

## 📚 الملفات ذات الصلة

- `restart_render.ps1` - سكريبت إعادة النشر
- `RENDER_DEPLOYMENT_GUIDE.md` - دليل النشر الكامل
- `RENDER_QUICK_START.md` - دليل سريع

---

## ✅ الخلاصة

**لإعادة النشر:**
```powershell
.\restart_render.ps1
```

**أو يدوياً:**
```bash
git add .
git commit -m "تحديث"
git push origin main
```

Render سيعيد النشر تلقائياً! 🎉

