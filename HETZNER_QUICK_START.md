# 🚀 دليل البدء السريع - البوت على Hetzner VPS

## ⚡ نظرة سريعة (5 دقائق)

### الخطوة 1: إعداد السيرفر الأساسي
```bash
# على السيرفر (كـ root)
curl -fsSL https://raw.githubusercontent.com/YOUR_USERNAME/YOUR_REPO/main/hetzner-setup.sh | bash
```

### الخطوة 2: رفع المشروع
```bash
# على جهازك المحلي
chmod +x deploy-to-hetzner.sh
./deploy-to-hetzner.sh
```

### الخطوة 3: إعداد الأسرار في GitHub
```bash
# في GitHub Repository > Settings > Secrets and variables > Actions
SSH_PRIVATE_KEY=محتوى مفتاح SSH الخاص
HETZNER_HOST=عنوان_IP_السيرفر
```

### الخطوة 4: أول نشر
```bash
# ارفع أي تغيير ليتم النشر التلقائي
git add .
git commit -m "إعداد Hetzner deployment"
git push origin main
```

---

## 📋 قائمة المراجعة

### ✅ الإعداد الأساسي:
- [ ] VPS من Hetzner (CX11 أو أعلى)
- [ ] Ubuntu 22.04
- [ ] SSH Key مضاف
- [ ] جدار حماية مُفعل

### ✅ النشر الأولي:
- [ ] `hetzner-setup.sh` مُشغل
- [ ] المشروع مرفوع للسيرفر
- [ ] البوت يعمل (`sudo systemctl status medical-bot`)
- [ ] الأسرار في GitHub مُعدة

### ✅ النشر التلقائي:
- [ ] GitHub Actions مُفعل
- [ ] النشر يعمل عند `git push`
- [ ] البوت يُحدث تلقائياً

### ✅ المراقبة:
- [ ] النسخ الاحتياطي التلقائي (يومي)
- [ ] المراقبة تعمل (`~/monitor.sh`)
- [ ] التنبيهات مُعدة (اختياري)

---

## 🆘 استكشاف الأخطاء السريع

### ❌ البوت لا يبدأ:
```bash
# فحص السجلات
sudo journalctl -u medical-bot -n 20

# فحص ملف .env
ls -la ~/medical-bot/.env
```

### ❌ النشر التلقائي فشل:
```bash
# فحص GitHub Actions logs
# تأكد من الأسرار في GitHub
# تأكد من SSH key صحيح
```

### ❌ نفاد المساحة:
```bash
# فحص المساحة
df -h

# تنظيف
sudo apt autoremove -y
~/backup.sh  # حذف النسخ القديمة
```

---

## 📊 مراقبة سريعة

```bash
# حالة البوت
sudo systemctl status medical-bot

# السجلات
sudo journalctl -u medical-bot -f

# الموارد
htop

# النسخ الاحتياطي
ls -la ~/backups/
```

---

## 💰 التكلفة

- **Hetzner VPS CX11**: €2.89/شهر
- **Domain (اختياري)**: €10-15/سنة
- **المجموع**: €3.89/شهر فقط!

---

## 🎯 الخطوات التالية

1. **اختبر البوت**: `/start` في Telegram
2. **أضف النطاق**: (اختياري) لـ webhook
3. **إعداد التنبيهات**: أضف Discord/Telegram للإشعارات
4. **النسخ الاحتياطي**: تأكد من العمل اليومي

**🚀 استمتع ببوتك على Hetzner!**

---

## 📚 الملفات المهمة

- `HETZNER_SETUP_GUIDE.md` - الدليل التفصيلي
- `hetzner-setup.sh` - إعداد السيرفر
- `deploy-to-hetzner.sh` - النشر السريع
- `.github/workflows/deploy-hetzner.yml` - النشر التلقائي
- `backup.sh` - النسخ الاحتياطي
- `monitor.sh` - المراقبة
- `medical-bot.service` - خدمة systemd
