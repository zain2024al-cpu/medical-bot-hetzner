[phases.setup]
nixPkgs = ["python312", "nodejs", "gcc", "gfortran"]

[phases.install]
cmds = [
  "pip install --upgrade pip",
  "pip install -r requirements.txt",
  "npm install -g playwright",
  "playwright install chromium --with-deps"
]

[start]
cmd = "python app.py"
