# 🔄 تحديث البيانات وإعادة النشر على Render

## ⚡ الطريقة السريعة

### استخدم السكريبت:

```powershell
.\update_and_deploy_render.ps1
```

السكريبت سيقوم بـ:
1. ✅ التحقق من قاعدة البيانات المحلية
2. ✅ تحديث قاعدة البيانات (إذا طلبت)
3. ✅ حفظ التغييرات في Git
4. ✅ رفع التغييرات إلى GitHub
5. ✅ Render سيعيد النشر تلقائياً

---

## 📋 الخطوات التفصيلية

### 1. تحديث قاعدة البيانات (إذا لزم الأمر)

#### أ. التحقق من قاعدة البيانات المحلية:
```powershell
# التحقق من وجود قاعدة البيانات
Test-Path db/medical_reports.db

# عرض حجم قاعدة البيانات
(Get-Item db/medical_reports.db).Length / 1MB
```

#### ب. نسخ قاعدة البيانات للرفع:
```powershell
# نسخ قاعدة البيانات
Copy-Item db/medical_reports.db db/medical_reports_initial.db -Force
```

---

### 2. حفظ التغييرات

```powershell
# إضافة جميع التغييرات
git add .

# حفظ التغييرات
git commit -m "تحديث البيانات وإعادة نشر على Render"
```

---

### 3. رفع إلى GitHub

```powershell
# رفع التغييرات
git push origin main
```

**ملاحظة:** Render سيعيد النشر تلقائياً بعد الرفع! ✅

---

### 4. متابعة النشر

1. **اذهب إلى Render Dashboard:**
   - [dashboard.render.com](https://dashboard.render.com)

2. **راقب Logs:**
   - اضغط على الـ Service
   - اضغط **"Logs"**
   - انتظر ظهور:
     ```
     ✅ تم تحميل X اسم مريض من قاعدة البيانات
     ✅ Database loaded: X KB
     ✅ تم تهيئة أسماء المرضى بنجاح
     ```

3. **اختبر البوت:**
   - افتح Telegram
   - ابحث عن البوت
   - أرسل `/start`
   - جرب إضافة تقرير جديد
   - تحقق من أن أسماء المرضى تظهر ✅

---

## 🔄 بعد اكتمال النشر

### حذف قاعدة البيانات من Git (مهم!)

بعد اكتمال النشر، احذف قاعدة البيانات من Git:

```powershell
# حذف قاعدة البيانات من Git
git rm db/medical_reports_initial.db

# حفظ التغيير
git commit -m "إزالة قاعدة البيانات من Git"

# رفع التغيير
git push origin main
```

**لماذا؟**
- قاعدة البيانات كبيرة الحجم
- لا يجب حفظها في Git
- Render سيستخدم قاعدة البيانات من Render Disk

---

## ⚠️ ملاحظات مهمة

### 1. قاعدة البيانات:
- ✅ استخدم Render Disk لحفظ قاعدة البيانات بشكل دائم
- ✅ الملف `medical_reports_initial.db` مؤقت فقط للرفع
- ✅ احذفه من Git بعد النشر

### 2. التوقيت:
- ⏳ قد يستغرق النشر 2-5 دقائق
- ✅ Render يُحدّث تلقائياً عند push إلى GitHub
- ✅ لا حاجة لإعادة النشر يدوياً

### 3. التحقق:
- ✅ تحقق من Logs للتأكد من نجاح النشر
- ✅ اختبر البوت بعد النشر
- ✅ تحقق من أن أسماء المرضى تظهر

---

## 🆘 استكشاف الأخطاء

### المشكلة: قاعدة البيانات لا تُحدّث

**الحل:**
1. تحقق من أن قاعدة البيانات موجودة في Render Disk
2. تأكد من أن `AUTO_RESTORE_ON_STARTUP=true`
3. أعد تشغيل الـ Service من Dashboard

### المشكلة: النشر فشل

**الحل:**
1. تحقق من Logs في Render Dashboard
2. تحقق من متغيرات البيئة
3. تحقق من أن Dockerfile صحيح

### المشكلة: أسماء المرضى لا تظهر

**الحل:**
1. تحقق من Logs: `✅ تم تحميل X اسم مريض`
2. تحقق من أن قاعدة البيانات موجودة
3. أعد تشغيل الـ Service

---

## 📚 الملفات ذات الصلة

- `update_and_deploy_render.ps1` - سكريبت التحديث والنشر
- `restart_render.ps1` - سكريبت إعادة النشر السريع
- `RENDER_RESTART_GUIDE.md` - دليل إعادة التشغيل

---

## ✅ الخلاصة

**لتحديث البيانات وإعادة النشر:**

```powershell
.\update_and_deploy_render.ps1
```

**أو يدوياً:**

```bash
# 1. تحديث قاعدة البيانات (إذا لزم)
cp db/medical_reports.db db/medical_reports_initial.db

# 2. حفظ ورفع
git add .
git commit -m "تحديث البيانات"
git push origin main

# 3. Render سيعيد النشر تلقائياً ✅
```

🎉 تم!

