# سجل الصيانة — بوت المترجمين (تقارير طبية)

سجل مستمر لعلل مكتشفة أثناء التطوير، ما أُصلح منها ومتى، وما تبقّى مفتوحاً.
الهدف: مرجع سريع لجلسة صيانة لاحقة بدل إعادة اكتشاف نفس النمط من الصفر.

---

## ⚠️ النمط الجذري المتكرر (اقرأ هذا أولاً)

أي نوع إجراء/مسار جديد يُضاف للنظام (جلسة إشعاعي، المناظير، جلسات
العلاج...) يجب تسجيله في **كل** المواضع التالية، وإلا يظهر عطل صامت أو
رسالة خطأ عامة لا تدل على السبب الحقيقي:

| الملف | الوظيفة | عرض العطل إذا نُسي التسجيل |
|---|---|---|
| `bot/handlers/user/user_reports_add_new_system/flows/shared.py` → `get_editable_fields_by_flow_type()` | حقول التعديل **قبل** النشر | زر "✏️ مراجعة وتعديل التقرير" يعرض "لا توجد حقول مدخلة للتعديل" |
| `bot/handlers/user/user_reports_add_new_system/edit_handlers/before_publish/router.py` | تنفيذ التعديل الفعلي **قبل** النشر | زر التعديل يعرض "⚠️ قيد التطوير" |
| `bot/handlers/user/user_reports_edit.py` → `get_editable_fields_by_action_type()` | حقول التعديل **بعد** النشر | لا تظهر أي حقول قابلة للتعديل بعد نشر التقرير |
| `bot/handlers/user/user_reports_edit.py` → `handle_report_selection()`: قاموس `current_report_data` (حوالي السطر 907) | القيمة المعروضة فعلياً بجانب كل زر حقل، بعد النشر | الزر يعرض قيمة فارغة رغم وجود قيمة حقيقية بالقاعدة (القيمة تُقرأ من هذا القاموس أولاً، لا من `report` مباشرة) |
| ~~`bot/handlers/user/user_reports_edit.py` → `handle_field_selection()`: قاموس `field_names`~~ **أُزيل 2026-08-18 (إدخال ٥٠)** — يقرأ الآن من `get_editable_fields_by_action_type()` مباشرة (نفس مصدر أزرار الحقول) | التسمية العربية في عنوان شاشة "✏️ تعديل: ..." | ~~لم يعد بنداً منفصلاً — لا يمكن أن يُنسى لأنه لم يعد قائمة يدوية إطلاقاً~~ |
| `bot/handlers/user/user_reports_add_new_system/execute_smart_state_action.py` | إعادة عرض الشاشة عند ضغط "🔙 رجوع" | "⚠️ خطأ في التنقل" بدل الرجوع للسؤال السابق |
| `services/broadcast_service.py` → `format_report_message()` | بناء بطاقة التقرير المنشورة | حقول مفقودة/فارغة في البطاقة رغم حفظها بالقاعدة |
| ~~`bot/handlers/user/user_reports_add_new_system/flows/shared.py` → قوائم `valid_flow_types` (3 مواضع)~~ **أُزيلت 2026-08-18 (إدخال ٥١)** — مُشتقّة الآن من `field_registry.VALID_FLOW_TYPES` عبر `resolve_flow_type_for_confirmation()` موحَّدة. `get_translator_state()`/`get_confirm_state()` أنفسهما (قاموسا حالة منفصلان) **لم يُلمسا بعد** — لا يزالان يحتاجان تسجيلاً يدوياً منفصلاً | التحقق من صحة flow_type وربطه بحالة المحادثة الصحيحة | ~~لم يعد بنداً منفصلاً لقوائم الصلاحية~~ — لكن نسيان `get_translator_state`/`get_confirm_state` لنوع جديد لا يزال يرتدّ المسار لـ new_consult/بلا حالة تأكيد صحيحة |

**فحص سريع لأي نوع إجراء جديد**: ابحث عن اسم الـ flow_type الجديد في
كل ملف من القائمة أعلاه؛ أي ملف لا يحتوي عليه = عطل مؤجل.

---

## ⚠️ نمط جذري ثانٍ: عمود `doctor_decision` المركّب

`Report.doctor_decision` **ليس حقلاً واحداً** — يخزّن عدة حقول في نص
واحد مفصولة بـ`\n\n` بعلامات عربية:

```
التشخيص: ...\n\nقرار الطبيب: ...\n\nالفحوصات المطلوبة: ...
التشخيص: ...\n\nقرار الطبيب: ...\n\nوضع الحالة: ...\n\nملاحظات الرقود: ...\n\nنوع الترقيد: ...
تفاصيل العملية: ...\n\nاسم العملية بالإنجليزي: ...\n\nملاحظات: ...
```

السبب: بعض هذه الحقول **بلا أعمدة مخصصة إطلاقاً** في جدول `Report`
(ملاحظات الرقود، نوع الترقيد، نسبة النجاح، تفاصيل العملية...).

**القاعدة**: أي كتابة على `doctor_decision` يجب أن تكون **استبدال مقطع**
لا كتابة فوق النص كاملاً. استخدم `_replace_decision_segment()` في
`user_reports_edit.py`. الكتابة الكاملة تمحو كل المقاطع الأخرى من
القاعدة **نهائياً بلا استرجاع**، ثم تختفي من البطاقة عند إعادة النشر.

**تحذير إضافي**: تجنّب الاستدلال على المحتوى بكلمات عامة
(مثل `'تفاصيل' not in dd`) — كلمات عربية شائعة تُسقط نصوصاً صحيحة.
استخدم بادئات المقاطع المعروفة (`_DECISION_SEGMENT_PREFIXES`).

---

## ⚠️ نمط جذري ثالث: إضافة قسم "تشناي" جديد لأي شاشة

بُني مرتين حتى الآن (تقارير المترجمين `chennai_reports`، ثم الرعاية
الصحية `chennai_healthcare`) — نفس القالب بالضبط، **لا تُنشئ تدفقاً
مكرَّراً**، أعِد استخدام الشاشات الموجودة مع علم مدينة في `context.user_data`:

1. **وحدة صلاحية منفصلة** في `core/modules_bootstrap.py` (`registry.register(name="chennai_X", ...)`)
   — مستقلة عن الوحدة الاعتيادية، يمكن الجمع بين الاثنتين. `dynamic_user_kb`
   وشاشة "إدارة الوصول" تلتقطانها تلقائياً بمجرد التسجيل — لا تعديل هناك.
2. **زر دخول مستقل** يضبط علماً في `context.user_data` (مثل
   `hc_city`/`report_city`) قبل استدعاء **نفس** دالة عرض القائمة/بدء
   التدفق الاعتيادية — لا تُنشئ نسخة مكرَّرة من الشاشات. الدخول الاعتيادي
   يجب أن **يُصفِّر** العلم دائماً (`ud["x_city"] = ud.pop("_force_x_city", None)`)
   وإلا تتسرّب قيود جلسة تشناي سابقة لجلسة اعتيادية لاحقة لنفس المستخدم.
3. **فلترة المرضى**: عبر `city` في `shared/selectors/patient_selector`
   (أو `report_flow_patient_visible` لتدفق التقارير المستقل) — city=None
   = بلا قيد (تشناي يظهر ضمن الكل كسلوك افتراضي)، city="chennai" = تشناي
   حصراً. **لا تنسَ** البحث inline (`user_patient_search_inline.py` /
   `_data.py::report_flow_patient_visible`) — له مصدر بيانات مستقل تماماً
   عن القائمة المرقَّمة ويسرّب القيد بصمت إن لم يُحدَّث بالتوازي.
4. **توجيه مجموعة النشر**: يُشتق من **نوع المريض نفسه** (`patient.patient_type`)
   لا من أي زر/قسم دخول — فيبقى صحيحاً حتى لو اختير مريض تشناي من الشاشة
   الاعتيادية. اقرأ نوع المريض كنص **والجلسة مفتوحة** (⚠️ انظر عطل
   `DetachedInstanceError` أدناه) ومرِّره كنص، لا ككائن ORM.
5. **حراسة الوصول**: أي `_is_authorized`/`_MODULE_KEY` مكرَّر في عدة
   ملفات (نمط شائع في هذا المشروع) يجب تحديثه في **كل نسخة** ليقبل
   الوحدة الجديدة أيضاً — نسيان نسخة واحدة يحجب كل مستخدمي تشناي عن ذلك
   الجزء تحديداً بصمت.
6. **⚠️ تصادم نص الزر — ابحث عن `Regex(r"تشناي")` قبل اختيار أي اسم زر
   جديد يحوي كلمة "تشناي"**. هذا وقع فعلاً: زر "🏙️ الرعاية الصحية -
   تشناي" الجديد كان يُخطَف صامتاً بنمط `filters.Regex(r"تشناي")` غير
   المرتبط بأي كلمات أخرى — كان مسجَّلاً لقسم تقارير تشناي فقط لكنه
   يطابق **أي** رسالة نصية تحوي "تشناي" في أي مكان، فيوجّه الزر الجديد
   خطأً لـ`start_report_chennai` الذي يرفضه (لا يملك `chennai_reports`)
   بصمت تام — لا خطأ ظاهر للمستخدم، فقط سجل تحذير عابر. **القاعدة**: أي
   `filters.Regex` يطابق على كلمة "تشناي" وحدها دون تثبيتها بسياق كامل
   (كباقي كلمات الزر) هو عطل مؤجَّل لأي زر تشناي آخر يُضاف مستقبلاً —
   ثبّت النمط بعبارة الزر كاملة (مثال: `r"إضافة.*تقرير.*جديد.*تشناي"`
   لا `r"تشناي"` وحدها). أُصلح في `bot/handlers/user/user_reports_add_new_system/conversation_handler.py`
   (نقطتا الدخول والـfallbacks معاً).

---

## ✅ ما أُصلح (بترتيب زمني)

### 2026-07-30 — جلسات الأورام: اختيار متعدد + سلسلة أعطال مرتبطة
- **الميزة**: تحويل "🎗️ جلسات الأورام" من زر اختيار واحد لقائمة اختيار
  متعدد (كيماوي/مناعي/موجّه/إشعاعي معاً في تقرير واحد)، كل نوع بخطته
  المستقلة في `TreatmentPlan`. (`219b606`)
- **عطل مكتشف 1**: زر "✏️ مراجعة وتعديل التقرير" **قبل** النشر كان
  معطَّلاً تماماً لكل أنواع جلسات العلاج (كيماوي/مناعي/موجّه/غسيل الكلى/
  المدمج) — `get_editable_fields_by_flow_type` لم يكن له أي مدخل لها.
  **أُصلح** (`688264a`).
- **عطل مكتشف 2**: زر "🔙 رجوع" يعرض "⚠️ خطأ في التنقل" من أي خطوة داخل
  جلسات العلاج/الاختيار المتعدد — `execute_smart_state_action.py` لم
  يكن يعرف كيفية إعادة عرض هذه الحالات إطلاقاً. + شاشتان بلا زر رجوع
  إطلاقاً (بعد "نفس العدد لكل الدورات"، بعد اختيار "رقود"). **أُصلح**
  (`1bf2872`).
- **عطل مكتشف 3**: زر "✏️ تعديل التقرير" قبل النشر يعرض "⚠️ قيد
  التطوير" لكل جلسات العلاج **وأيضاً المناظير** — الراوتر
  (`edit_handlers/before_publish/router.py`) لم يكن يوجّه أي منهما لأي
  معالج فعلي (لم يُحدَّث منذ استخراجه من الكود الأصلي القديم، قبل بناء
  هاتين الميزتين). + "المناظير" كانت ناقصة من قائمة الحقول القابلة
  للتعديل قبل النشر أصلاً. **أُصلح** (`504eaae`).

### 2026-07-30 (لاحقاً) — اختفاء نصوص بعد إعادة نشر تقرير معدَّل
- **البلاغ**: "لا زال زر تعديل التقارير يظهرها مفقودة النصوص بعد النشر
  من جديد".
- **السبب الجذري**: الكتابة فوق `doctor_decision` المركّب كاملاً عند
  تعديل حقل «قرار الطبيب» (انظر القسم أعلاه) — تمحو التشخيص/الفحوصات/
  وضع الحالة/ملاحظات الرقود من القاعدة نهائياً.
- **سبب ثانوي**: شرط استخراج هشّ في `handle_republish`
  (`'تفاصيل' not in dd`) كان يُسقط أي قرار طبيب يحتوي كلمة «تفاصيل».
- **أُصلح** (`ba3aecd`): `_replace_decision_segment()` + فحص البادئات.
  تحقُّق: محاكاة كاملة لـ10 أنواع إجراءات + دورات تعديل متكررة +
  تقارير متضرّرة سابقاً (استعادت نصوصها).

### 2026-08-01 — انهيار الوصول لمستخدم أُلغيت عنه كل وحداته
- **سجل إنتاج**: `UNIQUE constraint failed: user_module_access...`
- **السبب الجذري**: الهجرة الكسولة في `get_user_modules` كانت تفحص
  السجلات **النشطة فقط**، فتُدرِج `user_reports` إدراجاً أعمى — بينما
  القيد `UNIQUE(tg_user_id, module_key)` يشمل السجلات **المُلغاة** أيضاً.
  وجود سجل مُلغى ⇒ IntegrityError ⇒ تُرجِع `[]` ⇒ **يفقد المستخدم كل
  أزراره**.
- **الحالة المشروعة التي فجّرته**: مترجم مخصَّص لقسم واحد (تشناي)
  أُلغيت عنه `user_reports` عمداً.
- **أُصلح** (`c2e41ba`): فحص كل السجلات؛ "كلها مُلغاة" = قرار أدمن صريح
  ⇒ لا هجرة كسولة ولا نقض للقرار. + `_insert_access` صار يُعيد تفعيل
  السجل الموجود بدل الإدراج الأعمى (حاجز أمان عام).
- **درس عام**: أي `INSERT` على جدول بقيد UNIQUE يجب أن يفحص وجود
  السجل **بصرف النظر عن `is_active`** — الأعلام المنطقية لا تُلغي قيود
  قاعدة البيانات.

### 2026-08-01 — حذف اسم مترجم: أربعة مصادر لا مصدر واحد
- **البلاغ**: حذف "ياسر"/"يحيى" المختصرين (يلتبسان بـ"ياسر ابو عمار"
  و"يحيى أبو حمزة").
- **الدرس الأهم**: الاسم يأتي من **أربعة** مصادر — حذفه من واحد لا يكفي:
  1. `data/translator_names.txt` (احتياطي + مصدر `sync_file_to_database`).
  2. `priority_order` المبرمَجة في `services/translators_service.py`.
  3. جدول `translators` في القاعدة.
  4. **قائمة الزرع التلقائي** في `flows/shared.py` — تُزرَع عند **كل
     إقلاع**، فتُعيد أي اسم حُذف من القاعدة بصفّ جديد. كانت هي السبب
     في أن الحذف "لا ينجح" مرتين متتاليتين.
- **ترتيب العلاج الإلزامي**: أغلق مصدر الزرع التلقائي (4) **أولاً**،
  ثم احذف من القاعدة، ثم أعد التشغيل. العكس يُعيد المشكلة بلا نهاية.
- **كيف كُشِف**: مقارنة معرّفات الصفوف قبل/بعد — الصفوف المحذوفة
  (514/515) عادت بأرقام جديدة (518/519) ⇒ إعادة إنشاء لا فشل حذف.
- **تنبيه على استدلال**: `translator_id` هو PRIMARY KEY، وSQLite يُسنده
  `max+1`. فبعد صفّ لمترجم حقيقي بمعرّف تيليجرام ضخم، تأخذ الصفوف
  المزروعة أرقاماً تاليةً تبدو كمعرّفات حقيقية. استدلال
  `>= 100_000` في السكربت أعطى إنذاراً كاذباً — لا يُعتمد عليه وحده.
- **أُصلح**: `fa72dc7`, `9cf737c`, `5578f31` + سكربت
  `scripts/delete_short_translator_names.py` (معاينة افتراضياً).

### 2026-08-01 — Button_data_invalid في قائمة المترجمين (حدّ 64 بايت)
- **السبب**: `callback_data` كان يحمل الاسم نفسه؛ الحرف العربي بايتان،
  فـ`simple_translator:appointment_reschedule:ياسر ابو عمار` = **65 بايت**
  ⇒ تفشل اللوحة كلها ولا تظهر الشاشة. ظهر فور اعتماد الأسماء الكاملة
  (المختصرة كانت 8 بايت فتمرّ).
- **قاعدة عامة**: لا تضع نصاً عربياً متغيّر الطول في `callback_data` —
  استخدم فهرساً. الحدّ 64 **بايت** لا 64 حرفاً.
- **فخّ صحة**: فرع IDX القديم كان يحلّ الفهرس مقابل قائمة **مختلفة**
  الترتيب عن المعروضة ⇒ كان سيختار مترجماً خاطئاً بصمت. يجب حفظ
  القائمة المعروضة في `context` والحلّ مقابلها.
- **أُصلح**: `441ad72`.

### 2026-08-02 — (تشخيص خاطئ، مُصحَّح) "يوم 31/07 يختفي من مسير الإخلاء"
- **البلاغ الأصلي**: المسير لا يُظهر تاريخ 31/07، بينما 30/07 فيه 14 سطراً
  و01/08 فيه سطران.
- **⚠️ التشخيص الذي طُبِّق ثم اتضح خطؤه**: افترضتُ أن السبب هو ختم سجلات
  الصرف بـ`datetime.utcnow()` بينما المستخدمون بتوقيت الهند (UTC+5:30)،
  فحوّلتُ 4 مواضع كتابة إلى `now_ist()` (commit `229e989`)، وصحّحتُ 214
  سجلاً موجوداً بإضافة 5:30 لختامها (`scripts/fix_dispense_utc_to_ist.py`).
  التوقيت تطابق ظاهرياً مع البيانات (سجلات 30/07 محفوظة 20:45–21:06 UTC
  = فجر 31/07 بتوقيت الهند) فبدا التفسير مقنعاً ومُتحقَّقاً.
- **✅ السبب الحقيقي (من المستخدم مباشرة)**: خطأ إدخال — المستخدم كتب
  تاريخ **31 أغسطس** بدل **31 يوليو** عند اختيار التاريخ من التقويم، فلم
  يظهر السجل في مسير شهر يوليو لأنه فعلاً يخصّ شهراً آخر بالكامل. لا علاقة
  للتوقيت (UTC/IST) بالمشكلة إطلاقاً.
- **التراجع**: `git revert 229e989` (commit `eb1fcfe`) — أعاد كل شيء إلى
  `datetime.utcnow()` كما كان. **سجلات الإنتاج الـ214 التي عُدِّلت
  تحتاج تصحيحاً عكسياً يدوياً على الخادم** (اطرح 5:30 من كل سجل غير مطابق
  للمنتصف تماماً) — راجع الالتزام السابق في هذا الملف قبل التصحيح إن
  احتجت التفاصيل.
- **الدرس الأهم**: تطابق نمط زمني في البيانات (فرق 5:30 ساعة بالضبط) هو
  دليل مغرٍ لكنه **ليس إثباتاً**. عند الشك في تاريخ إدخال محدد، الفحص
  الأول يجب أن يكون: ماذا اختار المستخدم فعلياً في خطوة «📅 اختر
  التاريخ» (اسأله أو راجع سجلات المحادثة) — لا افتراض نظري عن التوقيت.
  التصميم الأصلي (الاعتماد على «تاريخ التقرير» الذي يختاره المستخدم
  صراحة، لا على وقت الحفظ) كان صحيحاً من البداية ولا يحتاج تعديلاً.

### 2026-08-02 — تقارير تشناي تُنشر أحياناً في المجموعة الرئيسة
- **العَرَض**: تقارير المترجم ياسر ابو عمار (تشناي) تظهر أحياناً في
  المجموعة الرئيسة بدل مجموعة تشناي، بلا نمط واضح.
- **السبب الجذري**: `_resolve_target_group_id(patient)` كانت تقرأ
  `patient.patient_type` **بعد** `session.commit()` و`session.close()`
  في مسار النشر (`flows/shared.py`). `expire_on_commit=True` (الافتراضي،
  ولم يُغيَّر في `db/session.py`) يُبطل صفات الكائن عند `commit()`، وأي
  قراءة صفة بعد `close()` ترفع `DetachedInstanceError` — يبتلعها
  `except Exception` العام في الدالة نفسها فتُرجِع `None` بصمت ⇒
  المجموعة الاعتيادية. لا خطأ ظاهر في السجلات لأن الاستثناء يُسجَّل
  بمستوى `warning` لا `error`.
- **قاعدة عامة**: لا تُمرِّر كائن ORM لدالة تُستدعى بعد إغلاق الجلسة التي
  جلبته. خُذ لقطة من القيمة (نص/رقم) **والجلسة مفتوحة**، ومرِّر اللقطة لا
  الكائن. أي `except Exception` عام حول قراءة صفة ORM هو علامة خطر —
  يُخفي `DetachedInstanceError` تماماً كما أخفى هذا.
- **عيب مرتبط اكتُشف بالمناسبة**: البحث عن المريض في `save_report_to_database`
  كان بالاسم فقط (`filter_by(full_name=...)`) — عند تطابق اسم مريض عادي
  ومريض تشناي، `.first()` يختار الأول بصرف النظر عمّن اختاره المستخدم
  فعلاً؛ وأي فرق حرفي في الاسم يُنشئ مريضاً جديداً بنوع افتراضي (`general`).
  كلاهما يوجّه لمجموعة خاطئة. أُصلح بإعطاء الأولوية لـ`patient_id` الذي
  يحفظه منتقي المرضى فعلياً في `report_tmp`.
- **أُصلح**: `a779137` — `_resolve_target_group_id` في كلا الملفين
  (`flows/shared.py`, `user_reports_edit.py`) صار يقبل `patient_type`
  نصاً لا كائناً، واللقطة تُؤخذ قبل `commit()`/`close()`.

### 2026-08-02 — طباعة مسير الإخلاء تعرض بيانات كل المستخدمين لا مستخدم واحد
- **البلاغ**: "طباعة المسير من تاريخ إلى تاريخ يطبع حق الكل المستخدمين
  لا يطبع لهذا المستخدم فقط".
- **السبب**: `_get_evacuation_ledger_rows_sync` لم يكن بها أي فلترة حسب
  المستخدم إطلاقاً — أي مستخدم مُصرَّح له بوحدة `pharmacy_print` يطبع
  **كل** سجلات الصرف في الفترة المطلوبة، بصرف النظر عمّن أدخلها. هذا رغم
  أن نمط العزل («created_by == requester_id»، أدمن=الكل) موجود بالفعل
  ومطبَّق في `pharmacy_finance.list_pharmacy_source_records` لنفس نوع
  البيانات — لكنه لم يُطبَّق هنا عند الطباعة.
- **قرار عمل صريح من المستخدم**: العزل مطلوب حتى لو أدّى لتجزئة مسير
  اليوم الواحد على عدة مترجمين (بدل مستند شحن واحد شامل). راجع الفلترة
  إن تغيّر هذا القرار لاحقاً.
- **أُصلح**: `c161c43` — أُضيف `requester_id`/`is_admin` لـ
  `get_evacuation_ledger_rows`/`_get_evacuation_ledger_rows_sync`،
  يفلتران `MedicationRecord.created_by`/`SuppliesRecord.created_by` عند
  `is_admin=False`. `pharmacy_print/flow.py` يمرّر هوية طالب الطباعة.
  `requester_id=None` (الافتراضي) يبقي بلا فلترة — سكربتات التشخيص
  (`diag_manifest_date.py`, `diag_dispense_range.py`) غير متأثرة عمداً.

### 2026-08-02 — نوع إجراء جديد "🫁 معاملة الزراعة" (Chennai-only)
- **الميزة**: نوع إجراء جديد داخل قائمة "⚕️ نوع الإجراء" الموحَّدة (لا زر
  منفصل) — نوع الزراعة (كبد/كلى) → الجهة (اختيار متعدد: محكمة/محامي/
  مكتب تنسيق) → تفاصيل المعاملة (نص حر) → تاريخ العودة → سبب العودة →
  المترجم → نشر. **يظهر حصراً** عند `report_city == "chennai"` — مخفي
  تماماً في قائمة نوع الإجراء الاعتيادية. (`304f2bb`)
- **تطبيق النمط الجذري #1 صراحة قبل الوقوع فيه**: `broadcast_data` في
  `save_report_to_database`/`handle_republish` يُبنى يدوياً حقلاً حقلاً
  ولا يرث من `report_tmp`/`Report` تلقائياً — أُضيفت حقول الزراعة الثلاثة
  لكلا الموضعين مسبقاً (بدل اكتشاف "بطاقة فارغة رغم الحفظ الصحيح" لاحقاً
  كما حدث مع جلسات العلاج والمناظير سابقاً في هذا السجل).
- **أعمدة قاعدة بيانات جديدة**: `transplant_type`/`transplant_parties`/
  `transplant_details` على `Report` — migration في `db/maintenance.py`
  (`_migrate_column`)، بنفس نمط `endoscopy_type`/`treatment_plan_summary`
  تماماً. **ملاحظة مهمة اكتُشفت أثناء البحث**: migration جدول `reports`
  موجود في `db/maintenance.py` حصراً (`_migrate_column`)، **وليس** في
  `db/session.py` (`_add_column_if_missing`) كبقية الجداول — نمط مختلف
  لنفس الغرض، سبق أن ضلَّل البحث عن أعمدة `reports` القديمة.
- **قائمة تحقق أي نوع إجراء جديد يظهر لقسم واحد فقط (تشناي أو غيره)**:
  1) `PREDEFINED_ACTIONS` (`user_reports_add_helpers.py`).
  2) `_get_action_routing()` (`action_type_handlers.py`).
  3) تمرير `city`/`context` عبر **كل** استدعاءات `_build_action_type_keyboard`
     (4 مواضع) — دالة بلا `context` أصلاً قبل هذه الميزة.
  4) حالات جديدة في `states.py` + تسجيلها في `conversation_handler.py`
     (states dict + دالة `_xxx_handlers()` + استيراد الحالات).
  5) `get_translator_state`/`get_confirm_state` + **3** نسخ من
     `valid_flow_types` في `flows/shared.py`.
  6) `get_editable_fields_by_flow_type` (قبل النشر) و
     `get_editable_fields_by_action_type` (بعد النشر، بمفتاح `medical_action`
     لا `flow_type`).
  7) `_build_xxx_fields()` جديدة في `broadcast_service.py` + فرع
     `elif medical_action == '...'` في `format_report_message`.
  8) أعمدة DB جديدة (`db/models.py` + `db/maintenance.py`) إن لم تُعِد
     استخدام حقل موجود.
  9) حقول `broadcast_data` في **كلا** الموضعين: `save_report_to_database`
     و`handle_republish` (`user_reports_edit.py`).
  10) خرائط `execute_smart_state_action.py`: إما `step_name` العامة (لو
      الحالة تُعيد استخدام منطقاً مشتركاً كـFOLLOWUP_DATE/TRANSLATOR) أو
      فرع `elif target_step == ...` مخصَّص (لحالات فريدة بلا نظير عام).

### 2026-08-04 — تبسيط جذري لفورمه "🩸 جلسات غسيل الكلى"

بناءً على طلب المستخدم، حُذفت من مسار غسيل الكلى: شكوى المريض، قرار
الطبيب، ورفع دفتر الجلسات (كانت أُضيفت في تعديل سابق هذا الأسبوع). المسار
الآن: رقم الجلسة الحالية (يدوي) → **تاريخ الجلسة القادمة فقط** (تقويم بلا
وقت وبلا سبب) → المترجم/النشر. بقية أنواع جلسات العلاج (كيماوي/موجّه/
مناعي) غير متأثرة — لا تزال تمر بشكوى المريض/الملاحظات/تاريخ العودة+الوقت+
السبب كالمعتاد؛ الدوال المشتركة (`_prompt_complaint`/`_notes_keyboard`/
`_after_notes`) نُظِّفت من فروع dialysis الميتة بعد أن توقف المسار عن
استدعائها.

- **حالة جديدة** `TREATMENT_DIALYSIS_NEXT_DATE` حلّت محل
  `TREATMENT_DIALYSIS_UPLOAD` المحذوفة بالكامل (states.py، رقم 132) — تقويم
  مستقل تماماً (`_build_dialysis_next_date_markup`/`dlx_cal_*` callback
  prefix خاص) بدل إعادة استخدام تقويم `_render_followup_calendar` العام في
  `new_consult.py`، لأن ذاك التقويم مُقفَل بمنطق مشترك (تاريخ→وقت→سبب) عبر
  عشرات فروع `elif current_flow == ...` في 5+ دوال — تعديله لحالة واحدة
  فقط كان سيهدد كل الأنواع الأخرى. القاعدة عند "إزالة خطوة من مسار مبني
  على منطق تفرُّع عام مشترك": ابنِ نسخة مستقلة بدل تعديل الفرع العام، إن
  كان التبسيط يزيل خطوات (وقت/سبب) لا يعرفها الفرع العام.
- طبقة العرض: `broadcast_service.py::_build_treatment_session_fields`
  الآن تتحقق من `is_dialysis` وتبني سطر "📅 تاريخ الجلسة القادمة" مباشرة
  (بدل `_build_followup_fields` العامة التي تكتب "موعد العودة").
- الحقول القابلة للتعديل قبل/بعد النشر لـ`جلسات غسيل الكلى` أصبحت 3 فقط
  (`followup_date`, `no_report_reason`/`no_paper_report_reason`,
  `translator_name`) — لم يُدرَج `treatment_plan_summary` (رقم الجلسة)
  ضمنها عمداً لأنه نص مُنسَّق (`"🩸 **جلسات غسيل الكلى**\n\nرقم الجلسة
  الحالية: N"`) وليس رقماً حراً؛ تعديله عبر آلية "أرسل القيمة الجديدة"
  العامة كان سيستبدل التنسيق بالكامل بأي نص يكتبه المستخدم.

### 2026-08-04 — إصلاح: التعديل بعد النشر يعرض "لا يوجد" لحقول موجودة فعلياً

المستخدم أبلغ أن "التعديل بعد النشر لا يظهر نفس التقرير، يظهر بيانات
غير [صحيحة]". تحقّقتُ بمحاكاة فعلية لكل الـ21 نوع إجراء (وليس فقط قراءة
كود) واكتشفت السبب الدقيق: **ثالث موضع تسجيل خفي** لم يكن موثَّقاً سابقاً
(أُضيف الآن كصف في جدول "النمط الجذري المتكرر" أعلاه) — `handle_report_selection()`
تبني قاموس يدوي `context.user_data['current_report_data']` من حقول
`report` صراحةً واحداً تلو الآخر (وليس تلقائياً من `get_editable_fields_by_action_type()`)،
وهو ما تُقرأ منه فعلياً (أ) القيمة المعروضة بجانب زر كل حقل و(ب) "القيمة
الحالية" في شاشة "✏️ تعديل الحقل". و`handle_field_selection()` لديها
قاموس عربي منفصل `field_names` لعنوان تلك الشاشة.

**الأثر الفعلي المكتشف بالمحاكاة**:
- **معاملة الزراعة** (`transplant_type`/`transplant_parties`/`transplant_details`):
  كلا القاموسين ناقصان — الزر يعرض قيمة فارغة، وشاشة التعديل تعرض اسم
  الحقل الخام + "لا يوجد" رغم أن القيمة موجودة فعلياً في القاعدة. **الحفظ**
  نفسه لم يكن متأثراً (يمر عبر `else: setattr(report, field_name, new_value)`
  العام في `save_edit_to_database`، يعمل لأي عمود حقيقي).
- **المناظير** (`endoscopy_type`/`endoscopy_result`): القيمة تظهر صحيحة
  (كانت مُدرجة في `current_report_data`) لكن العنوان يعرض اسم الحقل الخام
  بدل تسمية عربية (كان ناقصاً من `field_names` فقط).
- **كل الأنواع الـ19 الأخرى**: تحقَّق أنها سليمة تماماً (قيمة + تسمية صحيحتان
  في كل من زر الاختيار وشاشة التعديل) — **ليست مشكلة عامة تشمل كل الأنواع**،
  بل محصورة تماماً بالحقول المضافة حديثاً هذا الأسبوع (الزراعة + المناظير)
  التي لم تُوصَّل بهذين القاموسين تحديداً.

**الإصلاح**: أُضيفت 3 مفاتيح لـ`current_report_data` (زراعة) و5 تسميات
لـ`field_names` (زراعة + مناظير). **تحقق**: سكربت آلي أنشأ تقريراً حقيقياً
في القاعدة لكل نوع إجراء (بقيم اختبار مميّزة لكل حقل)، شغّل
`handle_report_selection`/`handle_field_selection` الفعليتين، وقارن كل
زر وشاشة تعديل بالقيمة المتوقعة — 0 مشاكل على الـ21 نوعاً بعد الإصلاح.

### 2026-08-04 — تفعيل تعديل "رقم الجلسة الحالية" بعد النشر لجلسات العلاج

المستخدم طلب أن يصبح "رقم الجلسة الحالية" قابلاً للتعديل بعد النشر لكل
أنواع "💉 جلسات العلاج" (كيماوي/موجّه/مناعي وغسيل الكلى) — كان مستثنى
عمداً سابقاً بتعليق صريح: "تعديله هنا سيفصله عن الخطة الفعلية". الحل
الصحيح ليس مجرد إزالة الاستثناء (ذلك كان سيُنتج بالضبط الانفصال المحذَّر
منه) بل **توصيل التعديل بمصدر الحقيقة الفعلي**:

- **كيماوي/موجّه/مناعي** (لها `TreatmentPlan` حقيقية في
  `services/treatment_plan_service.py`): التعديل يستدعي
  `get_active_plan()` + `edit_plan(plan_id, {"current_session": N})` —
  تماماً كزر "✏️ تعديل الخطة" أثناء إنشاء تقرير جديد — ثم يُعاد بناء
  `Report.treatment_plan_summary` من `format_progress_text()` المحدَّثة.
  هذا يعني: تعديل الرقم من شاشة "بعد النشر" **يُحدِّث الخطة الفعلية**،
  فيبقى التقرير التالي لنفس المريض/النوع متابعاً من الرقم الصحيح.
- **غسيل الكلى** (بلا `TreatmentPlan` بالتصميم): الرقم مضمَّن فقط داخل
  نص `treatment_plan_summary` المخزَّن (`"🩸 **جلسات غسيل الكلى**\n\nرقم
  الجلسة الحالية: N"`)، فالتعديل يستبدل الرقم بـ regex
  (`_apply_session_number_edit` في `user_reports_edit.py`) مع الحفاظ على
  بقية التنسيق — نفس مبدأ "استبدال مقطع لا كتابة فوق النص كاملاً"
  الموثَّق أعلاه لعمود `doctor_decision` المركّب.

**التنفيذ**: حقل جديد `session_number` (رقم صحيح موجب فقط، تحقّق في
`handle_new_value`) أُضيف لأول قائمة حقول قابلة للتعديل في كل الأنواع
الأربعة، مع قيمته الحالية المعروضة (`_current_session_number_display`)
مشتقة حياً من `get_active_plan()`/regex لا من عمود منفصل. **لم يُضَف**
لمسار "التقرير المدمج" (اختيار متعدد لعدة أنواع علاج في تقرير واحد) —
قد يشمل أكثر من خطة واحدة فلا يوجد "رقم" واحد واضح يُعدَّل هناك.

**تحقق**: محاكاة كاملة لكلا المسارين — كيماوي (أنشأت `TreatmentPlan`
حقيقية بجلسة 3، عدَّلت إلى 7 عبر الشاشة، تأكدت أن `current_session` في
القاعدة أصبح 7 فعلياً وأن ملخص التقرير تحدَّث ليطابق) وغسيل الكلى
(نص جاهز بجلسة 9، عدَّلت إلى 15، تأكدت أن الاستبدال حافظ على العنوان
والتنسيق). أُعيد تشغيل سكربت تدقيق الـ21 نوعاً — 0 مشاكل.

### 2026-08-04 — إزالة بوابة "هل يوجد تقرير طبي؟" من مسار غسيل الكلى

بناءً على طلب المستخدم. البوابة (`build_medical_report_gate_keyboard`)
مُعرَّفة مرة واحدة في `show_translator_selection()` (`flows/shared.py`)
وتُعرَض لكل أنواع التقارير تلقائياً **إلا** ما هو مُستثنى صراحةً في
مجموعة `flows_without_gate` (كانت تحتوي `"appointment_reschedule"` فقط).
أُضيف `"treatment_dialysis"` لهذه المجموعة — تغيير سطر واحد، بلا حاجة
لأي تعديل في `treatment_sessions.py` نفسه (نقطة تسجيل مركزية واحدة).

**لم يُلمَس**: حقل `no_paper_report_reason` بقي في قائمة الحقول القابلة
للتعديل لغسيل الكلى (قبل/بعد النشر) — يظهر دائماً بغض النظر عن القيمة
(حالة خاصة موثَّقة في `_has_field_value_in_report`)، فيبقى متاحاً كخيار
يدوي حتى بلا مرور بالبوابة تلقائياً.

**تحقق**: محاكاة مباشرة — بعد اختيار "تاريخ الجلسة القادمة" لمسار غسيل
الكلى، الرسالة التالية هي شاشة اختيار المترجم مباشرة (لا بوابة). تأكدتُ
أيضاً أن الكيماوي/الموجّه/المناعي ما زالت تعرض البوابة كالمعتاد (لا تراجع).

### 2026-08-04 — إضافة "رقم الجلسة الحالية" للتعديل قبل النشر أيضاً

استكمال للتعديل بعد النشر (أعلاه) — المستخدم لاحظ أن "رقم الجلسة الحالية"
لا يظهر في قائمة الحقول القابلة للتعديل **قبل** النشر (شاشة "✏️ مراجعة
وتعديل التقرير" أثناء المحادثة الحية) لأي من الأنواع الأربعة.

المسار العام لتعديل حقول جلسات العلاج/المناظير قبل النشر واحد ومشترك
(`edit_handlers/before_publish/treatment_and_endoscopy_edit.py`) وهو
بسيط جداً بالتصميم: `data[field_key] = text` مباشرة — مناسب لحقول نصية
حرة (شكوى/ملاحظات) لكنه **غير مناسب إطلاقاً** لرقم الجلسة، الذي (كما في
التعديل بعد النشر) ليس مفتاحاً مستقلاً في `report_tmp` بل إما مضمَّناً
داخل نص `treatment_plan_summary` (dialysis) أو مشتقاً من `TreatmentPlan`
فعلية عبر `_tp_plan_id`/`patient_id` (كيماوي/موجّه/مناعي). الكتابة
المباشرة كانت ستُنشئ مفتاحاً `session_number` جديداً لا يقرأه أي كود عرض،
فيبدو التعديل وكأنه لم يُحفَظ.

**الإصلاح**: أُضيفت نسخة مطابقة تماماً لـ`_current_session_number_display`/
`_apply_session_number_edit` (من `user_reports_edit.py`) لكن تعمل على
`report_tmp` الحي بدل صف `Report` منشور — نفس المبدأ: كيماوي/موجّه/مناعي
تُحدِّث `TreatmentPlan` الفعلية عبر `get_active_plan`/`edit_plan` (فيبقى
أي تقرير تالٍ لنفس المريض متابعاً من الرقم الصحيح)، وdialysis تستبدل
الرقم داخل نص `treatment_plan_summary` مباشرة. حقل `session_number` جديد
أُضيف لأول قائمة حقول قابلة للتعديل في `get_editable_fields_by_flow_type`
لكل الأنواع الأربعة (`flows/shared.py`) — ما عدا `treatment_combined`
لنفس سبب استثنائه بعد النشر (قد يشمل أكثر من خطة).

**تحقق**: محاكاة حية كاملة — غسيل الكلى (ملخص جاهز بجلسة 4، عُدِّل إلى 11،
تأكدتُ من استبدال الرقم مع الحفاظ على التنسيق) وكيماوي (أُنشئت
`TreatmentPlan` حقيقية بجلسة 2، عُدِّلت إلى 5 عبر شاشة "قبل النشر"، تأكدتُ
أن `current_session` في القاعدة أصبح 5 فعلياً وأن `report_tmp['treatment_plan_summary']`
تحدَّث ليطابق).

### 2026-08-04 — إصلاح جذري: معاملة الزراعة تنقلب إلى "استشارة جديدة" أثناء التعديل قبل النشر

المستخدم أرسل لقطة شاشة: شاشة "تعديل التقرير" لتقرير زراعة تعرض حقول
غريبة تماماً (شكوى المريض، التشخيص الطبي، قرار الطبيب، الفحوصات...) —
هذه حقول **"استشارة جديدة"** لا "معاملة الزراعة". السبب لم يكن في شاشة
التعديل نفسها (`get_editable_fields_by_flow_type("transplant")` صحيحة
تماماً ومُتحقَّق منها سابقاً) — بل في **مكان أعمق وأخطر بكثير**: خطوة
اختيار "تاريخ العودة" لمعاملة الزراعة تُعيد استخدام تقويم `new_consult.py`
العام (بتصميم مقصود، موثَّق في الكود)، وهذا التقويم يقرر "ما الحالة
التالية بعد اختيار التاريخ/الوقت؟" عبر **سلسلة `elif current_flow == ...`
مكرَّرة 9 مرات** في 5 دوال مختلفة — ولم يُضَف `"transplant"` لأيٍّ منها
عند بناء الميزة! فكانت تسقط دائماً إلى الفرع الأخير `else:` وتُرجِع حالة
**new_consult** (`NEW_CONSULT_FOLLOWUP_TIME`/`NEW_CONSULT_FOLLOWUP_REASON`)
بدل `TRANSPLANT_FOLLOWUP_DATE`/`TRANSPLANT_FOLLOWUP_REASON`.

**لماذا هذا خطير جداً وصامت**: `ConversationHandler` في PTB يوجّه أي
رسالة تالية بناءً على **رقم الحالة المُرجَعة فقط** — لا يهم أي دالة
استُدعيت سابقاً. بمجرد أن أصبحت الحالة `NEW_CONSULT_FOLLOWUP_REASON`،
أي نص "سبب العودة" يكتبه المستخدم يذهب لمعالج **`new_consult` الخاص**
(`nc_followup_reason` المسجَّل لتلك الحالة تحديداً في
`conversation_handler.py`)، **وليس** `handle_transplant_followup_reason`
في `flows/transplant.py` — فيُعاد بناء كل شيء بمنطق new_consult، ويُستبدَل
`current_flow` بـ`"new_consult"` فعلياً، فتظهر حقول استشارة جديدة بدل
حقول الزراعة عند فتح "تعديل التقرير" لاحقاً.

**كيف اكتُشف**: محاكاة سابقة لتحقُّق ميزة الزراعة استدعت
`handle_transplant_followup_reason` **مباشرة** (تجاوزت مسار الحالة
الفعلي)، فلم تكشف الخلل رغم أنه كان موجوداً منذ البداية — درس مهم: محاكاة
دالة بمعزل عن `ConversationHandler` الفعلي قد تُخفي أخطاء توجيه الحالة
تماماً. الاكتشاف الفعلي جاء من تتبُّع **رقم الحالة المُرجَعة** خطوة بخطوة
عبر الدوال الحقيقية (`handle_new_consult_followup_calendar_day` ثم
`handle_new_consult_followup_time_skip`) بدل استدعاء `transplant.py`
مباشرة.

**الإصلاح**: أُضيف `elif current_flow == "transplant":` في كل الدوال
الخمس (9 مواضع) داخل `new_consult.py` — يُرجِع `TRANSPLANT_FOLLOWUP_DATE`
أثناء اختيار التاريخ/الوقت و`TRANSPLANT_FOLLOWUP_REASON` بعده، بنفس نمط
الفروع الموجودة مسبقاً لـ`treatment_chemo/targeted/immuno/dialysis`
و`endoscopy`. استُورد `TRANSPLANT_FOLLOWUP_DATE`/`TRANSPLANT_FOLLOWUP_REASON`
في أعلى الملف (لم يكونا مستورَدين إطلاقاً من قبل — علامة إضافية على أن
هذا لم يُلمَس عند بناء الميزة).

**قاعدة جديدة للنمط الجذري الأول (أعلاه)**: أي نوع إجراء جديد **يُعيد
استخدام** تقويم `new_consult.py` العام لتاريخ العودة (بدل بناء تقويمه
الخاص كما فعلت غسيل الكلى لاحقاً) يجب إضافته لكل الفروع التسعة في ذلك
الملف، لا فقط تسجيل حالاته في `conversation_handler.py`. فحص سريع: ابحث
عن `current_flow ==` أو `current_flow in (` في `new_consult.py`.

**تحقق**: أعدت تشغيل نفس محاكاة إنشاء تقرير زراعة كاملة، لكن هذه المرة
عبر الدوال الحقيقية (`handle_new_consult_followup_calendar_day` →
`handle_new_consult_followup_time_skip`) بدل استدعاء `transplant.py`
يدوياً — تأكدتُ أن رقم الحالة المُرجَعة أصبح `136`/`137`
(`TRANSPLANT_FOLLOWUP_DATE`/`TRANSPLANT_FOLLOWUP_REASON`) بدل `12`/`13`
(new_consult خطأً)، وأن `current_flow` يبقى `"transplant"` حتى شاشة
الملخص النهائية وزر "✏️ تعديل الحقول" (`edit:transplant`)، وأن قائمة
الحقول المعروضة صحيحة تماماً (نوع الزراعة/الجهة/تفاصيل المعاملة...).

### 2026-08-04 — إضافة "بحث مفتوح" لزر 📎 المرفقات الطبية

المستخدم واجه: يُدخل عدد فحوصات متوقَّع أقل من الفعلي (مثلاً 2 بدل 3)،
فبعد رفع الفحصين يُغلَق التقرير تلقائياً (`has_paper_report` يتحول من 2
→ 1)، ثم لا يستطيع رفع الفحص الثالث لأن قائمة "📎 المرفقات الطبية"
(`_get_reports_for_date` في `user_medical_attachments.py`) تعرض **فقط**
تقارير `has_paper_report == 2` — أي تقرير اكتمل أو لم يُطلَب له تقرير
ورقي أصلاً **يختفي من القائمة بلا أي رسالة خطأ**، فيبدو وكأنه لا يمكن
إضافة المزيد إطلاقاً.

**الإصلاح**: زر جديد "🔍 بحث عن تقرير بأي تاريخ (بلا قيود)" يظهر بجانب
الميزة الحالية (كل شاشات القائمة، بما فيها حالة "لا توجد تقارير"). عند
الضغط عليه يُحفَظ `ma_state["mode"] = "open"`، فتعرض `_get_reports_for_date`
(بمعامل جديد `open_mode`) **كل** تقارير هذا المترجم بذلك التاريخ بلا أي
فلترة على `has_paper_report` — بما فيها التقارير المكتملة. زر عكسي
"📌 التقارير المعلّقة فقط" يعيد `mode` إلى `"pending"` للرجوع للقائمة
الأصلية. تم توحيد الدالتين (`_show_today_reports`→`_show_reports_list`)
لتفادي تكرار منطق العرض/التصفّح بين الوضعين.

**نقطة مهمة**: رفع مرفق عبر الوضع المفتوح **لا يمسّ إطلاقاً** آلية
`PendingReport`/`expected_count`/`has_paper_report` — لا `increment_pending_upload`
ولا `complete_pending_upload` ولا تحديث `has_paper_report` (الشرط
`ma.get("mode") != "open"` في `_publish_attachments`). الهدف: إضافة مباشرة
بلا أي أثر جانبي على حالة التقرير أو على تقدُّم أي فحوصات أخرى لا تزال
منتظرة فعلياً عبر المسار العادي. زر "📂 فتح التقارير الطبية" على بطاقة
المجموعة الأصلية يظهر بشكل مستقل تماماً (يعتمد فقط على وجود ملفات مرفقة
فعلياً عبر `count_medical_attachment_files`)، فيبقى يعمل بشكل صحيح
بصرف النظر عن حالة `has_paper_report`.

**تحقق**: محاكاة حية كاملة — أنشأتُ تقريراً بـ`has_paper_report=1`
(مكتمل)، تأكدتُ أنه **لا يظهر** في القائمة الافتراضية، ثم ضغطتُ زر البحث
المفتوح واخترت نفس اليوم فظهر التقرير بشكل صحيح، اخترته ورفعت له ملفاً
ونشرته بنجاح، وتأكدتُ أن `has_paper_report` بقي `1` بلا أي تغيير بعد
الرفع عبر المسار المفتوح.

### 2026-08-04 — إصلاح: فشل صامت في حفظ الطبيب عند تصادم اسم قسم بين مستشفيين

من لوغ السيرفر: `UNIQUE constraint failed: departments.name` تلاه مباشرة
`PendingRollbackError` عند محاولة استعلام تالٍ بنفس الجلسة، ثم "✅ تم حفظ
اسم الطبيب" ظهرت للمستخدم رغم ذلك. **الطبيب لم يُحفَظ في القاعدة إطلاقاً**
رغم رسالة النجاح — فشل صامت خطير.

**السبب**: عمود `departments.name` عليه **فهرس UNIQUE على مستوى القاعدة
كاملة** (مكتشَف في الإنتاج فقط — **غير موجود في نموذج SQLAlchemy** ولا في
قاعدة التطوير المحلية، فهرس عادي `ix_departments_name` هناك فقط. نمط جذري
إضافي: عدم تطابق مخطَّط الإنتاج مع النموذج/قاعدة التطوير، شبيه بأنماط
migration الخفية الموثَّقة أعلاه). دالة `handle_doctor` في
`doctor_handlers.py` تبحث عن القسم **مقيّداً بـ`hospital_id`** فقط — فإن
لم يجده لهذا المستشفى تحديداً، تحاول **إنشاءه** حتى لو كان قسم بنفس الاسم
موجوداً فعلاً لمستشفى آخر (الأقسام مقصود أن تكون كياناً مشتركاً بالاسم عبر
كل المستشفيات، بنفس نمط `flows/shared.py`). الإنشاء يفشل بـ`IntegrityError`،
والمعالج القديم يكتفي بتسجيل تحذير **بلا `rollback()`** — فتبقى الجلسة
معطوبة (`PendingRollbackError`) عند أي استعلام تالٍ (هنا: تحميل
`resolved_hospital.id` كسول)، فيفشل حفظ الطبيب بالكامل صامتاً ضمن `except`
خارجي عام لا يُبلّغ المستخدم.

**الإصلاح**: (١) بحث عالمي عن القسم بالاسم (بلا تقييد `hospital_id`) قبل
أي محاولة إنشاء — يتفادى المشكلة من الأساس في الحالة الشائعة. (٢) إن فشل
الإنشاء رغم ذلك (سباق تزامني نادر): `s.rollback()` فوراً ثم إعادة البحث
بالاسم مرة أخرى (غالباً قسم أنشأه طلب متزامن آخر بين تحقُّقنا ومحاولتنا)
قبل الاستسلام. القاعدة العامة: **أي فشل `flush()/commit()` يُتبَع
بـ`rollback()` فوراً قبل أي استعلام آخر بنفس الجلسة** — وإلا تنهار كل
عملية لاحقة بصمت.

**تحقق**: أضفتُ فهرس UNIQUE مؤقتاً لقاعدة التطوير المحلية (لمطابقة
الإنتاج فعلياً بدل التخمين)، أنشأت قسماً باسم معيّن لمستشفى أ، ثم شغّلت
`handle_doctor` الفعلية لحفظ طبيب لمستشفى ب **بنفس اسم القسم** — قبل
الإصلاح كان هذا بالضبط يُنتج نفس الخطأين المتسلسلين من اللوغ. بعد الإصلاح:
لا استثناءات، الطبيب محفوظ فعلياً في القاعدة (لم يكن قبلاً)، ومرتبط
بالقسم المُعاد استخدامه من مستشفى أ بشكل صحيح. أُزيل الفهرس المؤقت بعد
التحقق لإعادة قاعدة التطوير لحالتها الأصلية.

### 2026-08-04 — تنظيف: إزالة "سبب عدم وجود تقرير طبي" من حقول تعديل غسيل الكلى

بقيّة سقطت عند إزالة بوابة "هل يوجد تقرير طبي؟" من مسار غسيل الكلى (أعلاه)
— الحقل نفسه ظل مُدرجاً في قوائم الحقول القابلة للتعديل (قبل وبعد النشر)
رغم أن المسار لم يعد يسأل عنه إطلاقاً، فيظهر خياراً بلا معنى. أُزيل من
`get_editable_fields_by_flow_type["treatment_dialysis"]`
(`flows/shared.py`) ومن فرع `'جلسات غسيل الكلى'` في
`get_editable_fields_by_action_type` (`user_reports_edit.py`) معاً —
القائمتان الآن: رقم الجلسة الحالية، تاريخ الجلسة القادمة، المترجم — فقط.

### 2026-08-04 — توحيد حقول التقارير: مصدر واحد للفورمه الأصلية + التعديل قبل/بعد النشر (كل الأنواع)

**الطلب**: "لماذا لا تبرمج البوت على هذا القانون اي فورمه في نوع الاجراء
في التعديل قبل النشر وبعد النشر تكون مطابق للفورمه الاولية" — طلب صريح
لتوحيد النمط الجذري المتكرر أعلى هذا الملف، لكل الأنواع **بما فيها
القديمة** (وليس فقط الأنواع الجديدة السهلة). خطة كاملة في
`C:\Users\nalgu\.claude\plans\linked-tumbling-galaxy.md`.

**البنية الجديدة**: `field_registry.py` (ملف جديد) — قاموس واحد
`REPORT_FIELD_REGISTRY` لكل الأنواع الـ22، كل حقل له مفتاح واحد نهائي +
عمود DB واحد. `get_editable_fields_by_flow_type` (قبل النشر) و
`get_editable_fields_by_action_type` (بعد النشر) أصبحتا دالتين رقيقتين
تقرآن منه، بدل ~500 سطر مكتوبة يدوياً في ملفين منفصلين لم يتزامنا أبداً.

**أعمدة DB جديدة** (11 عمود): `operation_details`, `operation_name_en`,
`success_rate`, `benefit_rate`, `admission_reason`, `discharge_type`,
`admission_summary`, `therapy_details`, `device_details`,
`admission_notes`, `admission_type` — كانت هذه الحقول تُخزَّن **فقط**
داخل نص `doctor_decision` المركّب بلا عمود مستقل (النمط الجذري الثاني
أعلى هذا الملف)، فتُفقَد أي قراءة منفصلة لها.

**اكتشاف مهم أثناء التنفيذ**: توحيد قائمة الأزرار (`get_editable_fields_by_flow_type`)
لا يكفي وحده — شاشة التعديل الفعلية **قبل** النشر لا تزال تمر عبر 13 ملف
معالج منفصل (`edit_handlers/before_publish/*_edit.py`، كل نوع بملفه
الخاص بمفاتيح مكتوبة يدوياً)، توجَّه إليها عبر `router.py`. تدقيق شامل
(Explore agent) لهذه الملفات الـ13 كشف خللين حقيقيين حيّين:
- **`complaint` مقابل `complaint_text`**: تعديل الشكوى عبر الزر الجديد
  (استشارة جديدة/عودة دورية/متابعة في الرقود/طوارئ) يكتب في `complaint_text`
  فقط — بينما الحفظ عند النشر كان يقرأ `complaint` القديم. **التعديل
  يظهر صحيحاً في شاشة المراجعة قبل النشر، لكن عند النشر الفعلي يُحفَظ
  النص الأصلي غير المعدَّل** — فقدان بيانات صامت.
- **`status` مقابل `case_status`** (طوارئ فقط): أسوأ من السابق — لا أثر
  مرئي للتعديل في أي مكان إطلاقاً (لا شاشة المراجعة ولا التقرير المنشور).

**أُصلح** (`865bed4`): `save_report_to_database` تفضّل الآن المفتاح
الجديد (`complaint_text`/`case_status`) قبل القديم في كل مكان يُقرأ منه
(بناء `doctor_decision` المركّب + `broadcast_data`)، وتفضّل
`recommendations` على `treatment_plan` الفارغ (استشارة أخيرة — أحد
الأخطاء الخمسة الأصلية المكتشفة عند بناء الخطة). كما تكتب الحقول
الجديدة أعلاه لأعمدتها المخصصة مباشرة.

**بعد النشر** (`2b0b3ae`): `handle_report_selection`/`handle_republish`
في `user_reports_edit.py` تفضّلان الآن العمود المخصص على النص المستخرَج
من `doctor_decision` — النص يبقى fallback فقط للتقارير المنشورة قبل هذا
التعديل. `save_edit_to_database` أصبحت تكتب مباشرة إلى العمود المخصص عند
تعديل هذه الحقول (كانت فقط تُعيد بناء `doctor_decision` المركّب، فيبقى
العمود الجديد بلا قيمة أبداً — كان سيصبح خللاً جديداً بمجرد إضافة
الأعمدة لولا هذا الإصلاح). أُصلح أيضاً خلل ميت: تعديل `operation_details`/
`admission_notes`/`admission_type` لتقرير طوارئ بعد النشر كان **لا يفعل
شيئاً إطلاقاً** (يطابق شرط فرع إعادة البناء لكن لا يوجد فرع فرعي لـ"طوارئ"
داخله). أُضيفت أيضاً `discharge_type`/`admission_notes`/`admission_type`
لقاموس `current_report_data` — كانت غائبة تماماً فيظهر الزر بمعاينة فارغة
رغم أن التعديل يعمل.

**Backfill** (`8d92949`): `scripts/backfill_legacy_report_fields.py`
(معاينة افتراضياً، `--apply` للتنفيذ) — يملأ الأعمدة الجديدة للتقارير
المنشورة قبل هذا التعديل بنفس منطق الاستخراج من `doctor_decision`. **لم
يُشغَّل على قاعدة الإنتاج بعد** — يجب تشغيله على السيرفر بعد النشر (بعد
أخذ نسخة احتياطية)، قبل حذف كود الاستخراج القديم مستقبلاً.

**تحقق**: محاكاة حية مباشرة عبر الدوال الفعلية (لا افتراضات) لكل تعديل —
تأكيد أن تعديل الشكوى/حالة الطوارئ يُحفَظ فعلاً عند النشر، أن التوصيات
تظهر بعد نشر استشارة أخيرة، أن تعديل `operation_details` بعد النشر
يُحدِّث العمود فعلاً ويظهر محدَّثاً في شاشة التعديل **وفي إعادة النشر**،
وأن تقريراً بنمط قديم (نص مركّب فقط، بلا عمود) لا يزال يعمل عبر fallback
النص. 173/173 اختبار + `import bot.handlers_registry` بعد كل مرحلة.

**متبقٍ** (مهام #124-125 في قائمة الجلسة): تشغيل الـ backfill فعلياً على
الإنتاج بعد النشر، ثم حذف كود استخراج `doctor_decision` القديم (~150 سطر)
بعد التحقق من نجاح الترحيل، ثم تدقيق نهائي شامل لكل الأنواع الـ17.

### 2026-08-03/04 — إصلاح: زر الرجوع من قائمة نوع الإجراء يقفز للأمام بدل الرجوع للقسم

**البلاغ**: من شاشة اختيار الطبيب، ضغط "رجوع" يرجع صحيحاً للقسم. لكن بعد
اختيار الطبيب والانتقال لقائمة نوع الإجراء، ضغط "رجوع" يرجع صحيحاً لشاشة
الطبيب — **لكن الضغط مرة ثانية من هناك يقفز للأمام لقائمة نوع الإجراء**
بدل الرجوع للقسم كما هو متوقَّع.

**السبب الجذري**: زر "رجوع" في قائمة نوع الإجراء (`go_to_search_doctor_screen`)
كان مسجَّلاً عبر `_tracked(handle_go_to_state, R_ACTION_TYPE)` —
`_tracked()` **يدفع** الحالة الحالية على مكدس التنقل، وهذا صحيح فقط
للمعالجات التي تتحرك **للأمام**؛ هذا الزر يتحرك للخلف. القيمة المدفوعة
(`R_ACTION_TYPE`) تتشارك رقمها عمداً مع `NEW_CONSULT_COMPLAINT` (تعليق
موثَّق مسبقاً في `conversation_handler.py` — PTB يدمج مفاتيح state
المتكررة)، فيتراكم على المكدس إدخال زائد. ضغطة "رجوع" الحقيقية التالية
تسحب هذا الإدخال الزائد فتُعرض قائمة نوع الإجراء من جديد بدل الاستمرار
للقسم. كل أزرار الرجوع الأخرى في هذا المسار
(`go_to_hospital_selection`/`go_to_department_selection`) مسجَّلة مباشرة
عبر `handle_smart_back_navigation` (يسحب من المكدس فقط، لا يدفع إليه) —
هذا الزر وحده كان الاستثناء.

**الإصلاح**: تسجيل نفس الزر عبر `handle_smart_back_navigation` مباشرة،
بلا `_tracked()`، مطابقاً لبقية أزرار الرجوع. (`b62c339`)

**تحقق**: محاكاة مباشرة لنفس تسلسل التنقل من البلاغ عبر الدوال الفعلية
(قسم → اختيار طبيب → قائمة نوع الإجراء → رجوع → رجوع) — الضغطة الأولى
تهبط على شاشة الطبيب بمكدس سليم `[0,2,3,4]` (بلا الإدخال الزائد 7)،
الضغطة الثانية تهبط على شاشة القسم كما هو متوقَّع، لا قفز للأمام.

### 2026-08-03 (لاحقاً) — إصلاح: استيراد دائري كان يُعطِّل صامتاً حقول التعديل قبل النشر (نظام draft)

**اكتُشف من لوغ الإنتاج الفعلي** (وليس محلياً): تحذير
`⚠️ Cannot import get_editable_fields_by_action_type from user_reports_edit`
عند إقلاع البوت على السيرفر.

**لماذا لم أكتشفه قبل النشر**: التحقق الذي اعتمدته طوال مراحل توحيد
الحقول (`python -c "import bot.handlers_registry"`) **لا يُشغِّل فعلياً**
`register_all_handlers()` — تلك الدالة تُستدعى فقط من `app.py::main()`
عند التشغيل الحقيقي. الاستيراد وحده لا يمرّ بنفس سلسلة الاستيراد
الفعلية، فأعطى انطباعاً خاطئاً بالسلامة.

**السبب الجذري**: استيراد أضفتُه أعلى `user_reports_edit.py` (المرحلة 4
من توحيد الحقول): `from ...user_reports_add_new_system.field_registry import ...`.
استيراد أي وحدة فرعية من حزمة `user_reports_add_new_system` يُجبر
Python على تحميل `__init__.py`/`conversation_handler.py` الخاصين بها
أولاً — وهذان يستوردان `edit_handlers/draft/handlers.py`، التي بدورها
تستورد **نفس** `user_reports_edit.py` (لا تزال قيد التحميل الأول، لم
تُعرَّف فيها الدالة بعد) لجلب `get_editable_fields_by_action_type` —
استيراد دائري حقيقي. `try/except` دفاعي موجود مسبقاً يلتقط الفشل
ويستبدل الدالة بـ`lambda x: []` بصمت — يعني عملياً أن **قائمة الحقول
القابلة للتعديل في نظام "draft" (`edit_handlers/draft/handlers.py`) كانت
تُعرَض فارغة دائماً في الإنتاج** منذ نشر تلك المرحلة.

**الإصلاح**: نقل هذا الاستيراد ليكون محلياً **داخل** الدالة
`get_editable_fields_by_action_type` بدل أعلى الملف — بحلول وقت استدعاء
الدالة فعلياً تكون كل الوحدات قد اكتمل تحميلها، فلا يتكرر التعارض
الدائري. (`1bfaa17`)

**تحقق**: استدعيت `register_all_handlers()` فعلياً (لا مجرد استيراد
الوحدة) مقابل تطبيق PTB حقيقي — قبل الإصلاح: التحذير يظهر. بعد الإصلاح:
لا تحذير، والدالة المسجَّلة في `edit_handlers/draft/handlers.py` ترجع
فعلياً حقول `field_registry` الصحيحة (8 حقول لاستشارة جديدة) بدل `[]`.

**درس مهم لهذه الجلسة**: `python -c "import bot.handlers_registry"`
**لا يكفي وحده** للتحقق من سلامة الاستيراد — يجب استدعاء
`register_all_handlers()` فعلياً (أو تشغيل البوت الحقيقي) للحصول على
تأكيد صحيح، لأن `handlers_registry.py` لا يستورد شيئاً على مستوى
الملف، كل الاستيرادات الفعلية تحدث داخل الدالة نفسها عند استدعائها فقط.

### 2026-08-04 — إصلاح: زر "حالة التقرير الطبي" في التعديل قبل النشر لا يعكس الاختيار الفعلي

**البلاغ**: تقرير مناظير، اختار المستخدم "🟡 لم يجهز بعد" في بوابة "هل
يوجد تقرير طبي؟"، لكن شاشة "التعديل قبل النشر" أظهرت زر "سبب عدم وجود
تقرير طبي" بلا قيمة — يوحي بحالة مختلفة تماماً عمّا اختير فعلاً. وعند
الضغط عليه، يفتح مباشرة صندوق نص "أدخل السبب الجديد" بدل عرض نفس الخيارات
الثلاثة الأصلية (✅ يوجد / 🟡 لم يجهز بعد / ❌ لا يوجد) التي تسمح بتغيير
الاختيار وتشغيل نفس تفرعه الأصلي (رفع ملفات / سؤال عدد الفحوصات / سبب).

**السبب الجذري**: حقل بوابة "هل يوجد تقرير طبي؟" حقل **تفرّعي بثلاث
حالات**، لكن `field_registry.py` مثّله بمفتاح واحد (`no_paper_report_reason`)
بتسمية ثابتة توحي بحالة "لا يوجد" فقط، والاعتراض الخاص به في
`edit_handlers/before_publish/router.py` كان يقفز مباشرة لطلب نص حر بلا
شرط، متجاهلاً الحالتين الأُخريين تماماً.

**الإصلاح** (`8d60593`):
- التسمية أصبحت محايدة: "📎 حالة التقرير الطبي" بدل "📋 سبب عدم وجود تقرير طبي".
- معاينة القيمة في قائمة الحقول (`show_edit_fields_menu`) تعكس الآن الحالة
  الفعلية (✅/🟡/❌) بدل قراءة `no_paper_report_reason` وحده دائماً.
- الضغط على الزر يعيد فتح نفس بوابة الإدخال الأصلية بثلاثة أزرارها
  (`build_medical_report_gate_keyboard`)، وأي اختيار يُشغِّل نفس معالِجه
  الأصلي بالضبط (`handle_medical_report_choice` وما بعده) — لا مسار مختصر.
- بعد إتمام أي فرع (رفع/عدد/سبب) في سياق التعديل، يعود لشاشة الملخص (نفس
  سلوك أي حقل آخر يُعدَّل) بدل المتابعة لاختيار المترجم — عبر علم جديد
  `_medical_report_edit_return`.
- الرجوع من شاشات الرفع/السبب الفرعية في سياق التعديل يعود لقائمة الحقول
  القابلة للتعديل مباشرة، لا لإعادة عرض البوابة الأصلية بلوحتها التي لا
  تعرف عن سياق التعديل (كانت ستُنتج نفس نمط عطل مكدس التنقل المُصلَح أعلاه
  عند ضغطة رجوع ثانية).

**تحقق**: محاكاة حية لتقرير مناظير بحالة "لم يجهز بعد" فعلياً عبر الدوال
الحقيقية — الزر يعرض "🟡 لم يجهز بعد" بشكل صحيح، الضغط عليه يعيد فتح
الخيارات الثلاثة، اختيار "لا يوجد" يطلب السبب بشكل صحيح، إتمامه يعود
للملخص لا لاختيار المترجم، والرجوع من شاشتي الرفع/السبب في وضع التعديل
يعود لقائمة الحقول بدل شاشة بلا مخرج. 173 اختبار + فحص تسجيل حقيقي كامل
(`register_all_handlers`) ناجحان.

### 2026-08-04 — إصلاح: 'الإجراءات التي تمت أثناء المنظار' غائب تماماً من التعديل قبل وبعد النشر

**البلاغ**: نفس الحقل الرابع في فورمه المناظير (بعد نوع المنظار ونتيجته)
— "الإجراءات التي تمت أثناء المنظار" (اختيار متعدد: خزعة/بوليب/توسيع.../
أخرى) — لا يظهر إطلاقاً لا في التعديل قبل النشر ولا بعده.

**السبب الجذري**: عمود DB حقيقي (`Report.endoscopy_procedures`، مخزَّن
كـ JSON `{"list":[...],"other":...}`) وخطوة حقيقية في فورمه الإدخال
(`flows/endoscopy.py`)، لكنه **لم يُضَف إطلاقاً** إلى مدخل "endoscopy" في
`field_registry.py` عند بناء السجل الموحَّد — فلا يظهر زره في أي من
شاشتي التعديل رغم أنه يُحفَظ ويُعرَض بشكل صحيح في البطاقة المنشورة.

**الإصلاح** (`d3ddfcf`):
- أُضيف الحقل للسجل + دالة مشتركة `format_endoscopy_procedures()` تحوّل
  JSON المخزَّن لنص عربي مقروء ("أخذ خزعة، توسيع تضيق") بدل عرض JSON خام.
- **قبل النشر**: الضغط على الزر يعيد فتح **نفس** شاشة الاختيار المتعدد
  الأصلية (checkboxes) معلَّمة مسبقاً حسب القيمة الحالية، بنفس آلية
  `_medical_report_edit_return` (زر رجوع يعود لقائمة الحقول، الإتمام يعود
  للملخص) — مطابقة كاملة للفورمه الأصلية.
- **بعد النشر**: لا آلية اختيار متعدد في نظام التعديل بعد النشر إطلاقاً
  (نظام مختلف تماماً)، فالتعديل هناك نص حر بسيط — لكن **يُغلَّف تلقائياً**
  في نفس بنية JSON قبل الحفظ. بدون هذا الإصلاح: بطاقة إعادة النشر
  (`broadcast_service.py`) تحلّل هذا العمود كـ JSON دائماً، فأي نص حر غير
  JSON صالح كان سيختفي **صامتاً** ويُستبدَل بـ"منظار تشخيصي" — نفس نمط فقدان
  البيانات الصامت الذي أُصلح مراراً هذه الجلسة.

**تحقق**: تقرير تجريبي بإجراءات محفوظة مسبقاً (خزعة + توسيع + ملاحظة
"أخرى") — الزر قبل النشر يعرض معاينة مقروءة صحيحة، الضغط عليه يعيد فتح
الاختيارات معلَّمة بشكل صحيح (بما فيها "أخرى")، تبديل خيار جديد يحافظ على
زر رجوع بوضع التعديل الصحيح. التعديل بعد النشر عبر القاعدة الفعلية: النص
الحر المُدخَل يُحفَظ كـ JSON صالح، وبطاقة إعادة النشر تعرض النص الجديد
فعلياً (تأكدتُ أنه كان سيختفي بدون التغليف). 173 اختبار + فحص تسجيل حقيقي
كامل ناجحان.

### 2026-08-04 (لاحقاً) — تطوير: نفس شاشة الاختيار المتعدد لتعديل الإجراءات بعد النشر أيضاً

**الطلب**: بعد الإصلاح أعلاه، سأل المستخدم صراحة إن كانت شاشة التعديل
بعد النشر تعرض نفس الأزرار الأصلية أم نصاً حراً أبسط — أجبتُ أن بعد
النشر نص حر فقط (لأن نظام التعديل بعد النشر لا يملك آلية اختيار متعدد
لأي حقل إطلاقاً)، وعرضت بناءها كعمل إضافي حقيقي. المستخدم طلب: "نعم
اريد نفسها".

**البناء**: حالتان جديدتان في `ConversationHandler` الخاص بـ
`user_reports_edit.py` (`EDIT_ENDOSCOPY_PROCEDURES`,
`EDIT_ENDOSCOPY_PROCEDURES_OTHER`) — أول آلية اختيار متعدد لأي حقل في
نظام التعديل بعد النشر بأكمله. تعيد استخدام نفس قائمة الخيارات
(`flows/endoscopy.py::ENDOSCOPY_PROCEDURE_OPTIONS`) ونفس منطق toggle/
done/نص "أخرى"، معلَّمة مسبقاً من القيمة الفعلية المخزَّنة في القاعدة
(تحويل عكسي من التسميات المخزَّنة في JSON إلى رموزها). بعد الانتهاء،
تُعاد استخدام آلية "معاينة ثم تأكيد" القياسية المستخدمة لكل حقل آخر بعد
النشر (`confirm_text_edit`/`save_edit_to_database`) — القيمة الجديدة
تُعرَض كنص مقروء في شاشة التأكيد، وJSON الفعلي يُمرَّر عبر
`_endoscopy_procedures_new_json` حتى يبقى الحفظ الفعلي بنية JSON صحيحة
لا نصاً حراً مُغلَّفاً (fallback المرحلة السابقة).

**تحقق**: محاكاة كاملة عبر القاعدة الفعلية — فتح الحقل يعرض الاختيارات
معلَّمة بشكل صحيح من القيمة المحفوظة، تبديل اختيار جديد يعمل، الإنهاء
(بمساري "بدون أخرى" و"مع أخرى") يبني JSON صحيحاً ويعرض معاينة مقروءة في
شاشة التأكيد، والحفظ النهائي يخزّن البنية الكاملة الصحيحة. 173 اختبار +
فحص تسجيل حقيقي كامل ناجحان.

### 2026-08-04 — إكمال تدفق الواصلين + إصلاح 3 أعطال بيانات صامتة

**الطلب**: إكمال تدفق "الواصلين" ليطابق الفورمه المقصودة، ثم المغادرين.
**تدقيق الكود كشف أن معظم البنية موجودة** (من مهام #71-76) — الناقص 6 أمور
محدَّدة فقط، وليس إعادة بناء. الخطة الكاملة في
`C:\Users\nalgu\.claude\plans\linked-tumbling-galaxy.md`.

**قرارات ثبّتها المستخدم**: الحقول الأربعة الإضافية (صورة الإقامة/تاريخ
انتهائها/عنوان السكن/الخدمات) **تبقى** — وحدة `residency` تقرأ أول اثنين
لإنشاء ملفات المقيمين، وحذفهما كان سيكسرها. الفيزا+الختم رفعة واحدة.
الملاحظات/الجهة/المختص **لكل شخص** (مريض ومرافقوه = وحدة). العدد = عدد
المرضى فقط.

**ما بُني**:
- `shared/calendar_picker.py`: `quick_jump` اختياري (**مُطفأ افتراضياً** —
  الـ33 مستدعياً القائمين لا يتأثرون) + `build_year_picker`/
  `build_month_picker`. تواريخ الانتهاء البعيدة تُختار في **4 ضغطات** بدل ~96.
- تعدد المرافقين (`STEP_C_MORE`) — كان التدفق يُنهي المريض فور أول مرافق.
- كتلة ختام لكل مريض: ملاحظات → الجهة الموصلة (أزرار من
  `ESCORT_ENTITY_MAP` الجديدة) → المختص (مع إبراز مختص المريض السابق).
- دمج الفيزا+ختم الدخول في رفعة واحدة (عمود `entry_stamp_file_id` يبقى
  للسجلات القديمة).

**ثلاثة أعطال بيانات حقيقية اكتُشفت أثناء التتبّع** (لم تكن في الطلب):
1. **`ArrivalCompanion` بلا عمود `visa_expiry` إطلاقاً** رغم أن التدفق
   يسأل عنه — فكانت القيمة تُكتب في `residence_expiry` بالخطأ: تضيع تأشيرة
   المرافق **وتُفسد تاريخ إقامته معاً**. أُضيف العمود + migration، وأُصلح
   مفتاح الكتابة، وأُضيف للحفظ (لم يكن يُحفَظ أصلاً).
2. **أسهم ◀️/▶️ في التقويم ميتة تماماً** في 3 مسارات (المغادرون، الخدمات
   العامة، ملفات الإقامة): `build_calendar` يُصدر `cal_prev`/`cal_next`
   بينما تلك المسارات تلتقط `cal_nav` فقط — اسم لا يُصدره أحد. النقر يسقط
   بلا معالج، فلا يمكن اختيار تاريخ خارج الشهر الحالي إطلاقاً.
3. المختص كان سؤالاً واحداً للدفعة — أصبح عموداً لكل مريض
   (`specialist_id`/`specialist_label` + migration)، وترويسة الدفعة تُشتق
   منهم ("متعدد" عند الاختلاف).

**المغادرون**: مصدر الأسماء **لم يُغيَّر** (رداً على سؤال المستخدم) — يقرأ
أصلاً من أرشيف الواصلين عبر `get_active_arrivals()` ويُعلّمهم مغادرين عبر
`mark_patients_departed()`. هذا الصحيح: لا مغادرة لمن لم يصل، والربط يُبقي
الأرشيف متسقاً. التغيير الوحيد كان إصلاح أسهم التقويم أعلاه.

**تحقق**: محاكاة حية كاملة عبر الـdispatcher ومعالجي النص والصور —
مريضان، الأول بمرافقَين، مع فحص الخطوة عند كل انتقال، ثم **قراءة كل صف من
القاعدة** بعد الحفظ: المختص والجهة لكل مريض صحيحان، المرافقان محفوظان
بتاريخ تأشيرتهما سليماً، ختم الدخول فارغ، والدفعة "متعدد". ثم
`create_profiles_from_arrival_batch` على نفس الدفعة — ملفات المقيمين
أُنشئت بنفس النجاح (`expiry_date` وصورتا الجواز/الفيزا صحيحة) ⇒ **لا انحدار
في وحدة الإقامة**. 173 اختبار + فحص `register_all_handlers()` الحقيقي.

**متبقٍ**: المستخدم سيزوّدنا بقائمة "الجهة الموصلة" الحقيقية — عندها
يُعدَّل `ESCORT_ENTITY_MAP` في `constants.py` وحده لا غير.

---

### 2026-08-05 — وحدة «📤 الرفع والمتابعة» + تصدير الأرشيف + إصلاح زر PDF

**أ) تصدير أرشيف الإقامات (Excel + PDF)**
`services/residency_archive_excel.py` و`residency_archive_pdf.py` —
المرضى ومرافقوهم مع تواريخ انتهاء الإقامة والجواز، ملوَّنة (🔴 منتهية،
🟠 ≤30 يوم، 🟡 ≤90 يوم)، والمرافق يلي مريضه. **كلاهما يقرأ من
`collect_archive_rows()` نفسها** فلا يمكن أن يختلفا.

**ب) وحدة «الرفع والمتابعة»** (`modules/residency/uploads/` — بادئة `rnu:`)

⚠️ **الدرس الأهم**: ثلثا ما طُلب كان موجوداً بأسماء أخرى — «تم الرفع
وجاري الانتظار» = `renewal_submitted` القديم (المسمّى «تم التقديم»)،
و«رفع الملف الجديد + التقويم للجميع» = مسار `rnr:` كاملاً. الجديد فعلياً
حالة واحدة (`extension_received`) وعمود واحد (`form_c_file_id`).
**قبل بناء أي «متتبِّع» جديد، ابحث عن الحالة المكافئة الموجودة.**

سلسلة الحالات الآن:
`active → renewal_submitted → extension_received → issued`
ومصدرها الوحيد `PAPERS_ADVANCE` في `modules/residency/constants.py` —
تقرأ منه الشاشة (أي زر تعرض) والمستودع (إلى أين ينقل) معاً.

**ج) 🚨 فخّ الحالة الجديدة — أخطر ما في هذه الجولة**
`trackable_statuses` كانت **مكتوبة يدوياً في استعلامين منفصلين** داخل
`followup/repository.py`. حالة جديدة تُضاف لأحدهما وتُنسى في الآخر
**تُخفي المرضى من «المتابعة» أو من التنبيه اليومي بلا أي خطأ في اللوق**.
وُحِّدت في `TRACKABLE_STATUSES` + `COMPANION_PENDING_STATUSES`.
**اختبار التحقق يؤكد صراحةً** أن مريضاً في `extension_received` ما زال
يظهر في `get_expiring_soon()` وفي `build_alert_message()`.

**د) 🐛 زر «📄 ملف PDF» كان معطّلاً منذ إنشائه**
`services/residency_pdf_builder.py` يستدعي قالب Jinja
`templates/residency_patient_package.html` — **ومجلد `templates/` غير
موجود إطلاقاً** (`TemplateNotFound`، مؤكَّد بالتشغيل) — فوق WeasyPrint
المعطّل على السيرفر. أُعيد بناؤه في `services/residency_profile_pdf.py`
على reportlab وحُذف الملف الميت.

**تذكير دائم لأي PDF عربي في هذا المشروع** (تكرّر ثلاث مرات الآن):
1. الخط **يجب** أن يكون المضمَّن في `assets/fonts/` لا خط نظام — خطوط
   ويندوز غير موجودة على السيرفر فيسقط reportlab لـHelvetica وتظهر كل
   العربية **مربعات**.
2. reportlab **لا يعكس ترتيب الأعمدة** للـRTL — تُعرَّف منطقياً وتُعكَس
   صراحةً عند الرسم.
3. **لا WeasyPrint** — مكتبات نظامه تعطّلت فعلياً على السيرفر.

**تبسيط**: القائمة الرئيسية نزلت من 4 أزرار إلى 3؛ «المتابعة»
و«التحديثات المعلقة» صارتا تبويبين داخل الوحدة. شاشة الأوراق تعطي كل
مريض **زراً واحداً** يتغيّر نصّه حسب مرحلته، وضغطة واحدة تنقل المريض
**وكل مرافقيه** معاً. زر `↩️` يتراجع خطوة ويقرأ الحالة السابقة من
`ResidencyUpdate` (لا عمود مكرَّر).

**متبقٍ**: المرافقون المضافون قبل وجود `companion_of_id` بلا ربط بمريضهم
فلن يظهروا تلقائياً — تحتاج شاشة ربط في الأدمن إن أُريدت. كذلك
`RESIDENCY_GROUP_ID` يجب ضبطه في `.env` على السيرفر لوصول التنبيهات
للمجموعة.

---

### 2026-08-05 — 🐛 البوت يردّ داخل مجموعات المتابعة (أزرار النظام تظهر للجميع)

**العَرَض**: أزرار البوت تظهر داخل مجموعة المتابعة الطبية، رغم أن
المجموعات **تستقبل التقارير فقط** والنشر يتم من المحادثة الخاصة.

**السبب**: **48 من أصل 50** معالِج رسائل/أوامر مسجَّلة **بلا
`filters.ChatType.PRIVATE`**. أي `/start` أو رسالة تطابق نص زر قائمة
داخل المجموعة تجعل البوت يردّ هناك — ولوحة المفاتيح المرسَلة لمجموعة
تظهر **لكل أعضائها**.

**الإصلاح**: حارس واحد `bot/group_guard.py` في **group -100** (أسبق من
كل المجموعات، أدناها -1) يرفع `ApplicationHandlerStop` لأي رسالة من
محادثة غير خاصة.

⚠️ **لماذا حارس واحد لا 48 تعديلاً**: 48 موضعاً = 48 فرصة للنسيان،
**وكل معالِج جديد مستقبلاً كان سيعيد فتح الثغرة بصمت**. نقطة واحدة تغطّي
الموجود والقادم.

**ما لا يتأثّر عمداً** (مُتحقَّق منه بالتشغيل):
- **نشر التقارير للمجموعات** — الإرسال صادر ولا يمرّ بالمعالِجات إطلاقاً.
- **أزرار بطاقة التقرير** (`medfiles:` و`view_reschedule:`) — تحديثات
  `CallbackQuery` لا `Message`، ونوع مختلف تماماً لا يطابقه فلتر الرسائل.

**فحص قبل التنفيذ**: لا معالِج واحد يرشّح على نوع المجموعة، وكل الأوامر
الـ21 مسارات محادثة خاصة، ولا يوجد أمر لجلب معرّف المجموعة — فالحجب آمن.

**قاعدة لاحقاً**: لا حاجة لإضافة `ChatType.PRIVATE` لأي معالِج جديد —
الحارس يكفي. إن احتيج مستقبلاً أمرٌ يعمل **داخل** مجموعة، يجب استثناؤه
صراحةً في `group_guard.py` لا إلغاء الحارس.

---

## 🔍 تدقيقات مكتملة — نظيفة (لا تُعَد)

### 2026-07-30 — أزرار التعديل في بوت الرعاية الصحية: **سليمة بالكامل**
فُحصت وظيفياً 37 زر تعديل عبر 6 وحدات (medications, supplies,
woundcare, medical_followup, other, pharmacy_finance): كلها لها معالجات،
تفتح الخطوة الصحيحة، تضبط `edit_from_review=True` (فتعود للمراجعة)،
ولا تفقد أي حقل. **لا حاجة لإصلاح.**

**لماذا لم تُصب بعطل بوت المترجمين؟** سببان معماريان:
1. لا يوجد عمود مركّب — كل حقل مستقل في الجلسة والقاعدة.
2. زر التعديل **يعيد فتح نفس خطوة الإدخال الأصلية** بدل معالج تعديل
   منفصل مكرَّر، فلا يوجد "مساران للحقيقة" يتباعدان.

**التعديل بعد النشر في الرعاية الصحية موجود في مكان واحد فقط**:
«📅 تعديل تقرير بتاريخ» داخل مالية الصيدلية (`pharmacy_finance`).
فُحص أيضاً: تعديل أي حقل يحفظ صحيحاً، يُبقي بقية الحقول سليمة، يُحدِّث
نفس السجل بلا تكرار. (`net_amount` حقل مُشتقّ = المبلغ − التخفيض،
فتغيّره مع `invoice_total` سلوك صحيح لا عطل.)

بقية وحدات الرعاية الصحية **بلا تعديل بعد النشر إطلاقاً** — التعديل
متاح فقط قبل التأكيد. (نقص وظيفي محتمل، وليس عطلاً.)

---

### 2026-08-18 — `user_inline_menu.py`/`user_shortcuts.py`: كود ميت **مقصود**، لا خلل حي
أثناء تدقيق تغطية الأزرار (إدخال ٥٣) ظهر أن أزرار `user_action:*`/
`category:*` (قائمة inline بديلة + اختصارات أوامر) بلا أي معالِج مسجَّل
فعلياً. تحقّقتُ: `bot/handlers_registry.py:143-147` يعطّلهما عمداً
بتعليق صريح ("المستخدم يفضل الأزرار الثابتة")، ولا يوجد أي مستدعٍ حي
آخر لأي من الملفين (تحقّقتُ عبر بحث شامل). **لا مستخدم حقيقي يصل لهذه
الأزرار إطلاقاً** — كود ميت مقصود من قرار سابق، لا يحتاج أي إجراء.

---

## 🔓 مفتوح — لم يُصلح بعد

_(لا بنود مفتوحة حالياً — أُغلق آخرها: تعديل حقول معاملة الزراعة قبل
النشر، انظر قسم 2026-08-13 (١٣) أدناه.)_

---

### 2026-08-05 — 🐛 `BadRequest: Can't parse entities` عند تعديل حقل نصّي قبل النشر (16 ملفاً)

**اكتُشف عبر تقرير أخطاء البوت اليومي** (`error-report-2026-08-05.txt`):
خطآن من نوع واحد في `new_consult_edit.py:132`، الدالة
`handle_new_consult_edit_field_selection`:
```
telegram.error.BadRequest: Can't parse entities: can't find end of the
entity starting at byte offset 29
```

**السبب**: شاشات "تعديل قبل النشر" تعرض القيمة الحالية لأي حقل نصّي حرّ
(شكوى/تشخيص/قرار الطبيب/ملاحظات...) داخل رسالة بـ `parse_mode="Markdown"`
**بلا أي تهريب**. أي محرف واحد غير متزاوج من `` _ * ` [ `` في نص المترجم
الحر (شائع في نص طبي: "الحالة مستقرة\* بانتظار المتابعة" أو "جرعة 500\_مجم")
يُسقط تحليل Telegram للرسالة بالكامل، فيظهر للمترجم خطأ عام بدل شاشة
التعديل — بصمت، بلا أي تلميح للسبب الحقيقي.

**النطاق**: نفس الشكل البرمجي *حرفياً* مكرَّر في **16 ملفاً** تحت
`edit_handlers/before_publish/` (نمط تكرار الحقول الموثَّق أعلاه في هذا
السجل — "النمط الجذري المتكرر"). لم يُصادف الخطأ إلا في `new_consult`
لأن مترجماً واحداً صدف أن كتب محرفاً خاصاً، لكن الثغرة كانت قائمة في
الـ16 جميعاً بلا استثناء.

**الإصلاح**: `escape_md_v1()` جديدة في
`bot/handlers/user/user_reports_add_new_system/utils.py` — تستدعي
`telegram.helpers.escape_markdown(text, version=1)` (التطبيق الرسمي
لقواعد Telegram نفسها، لا دالة يدوية قد تنحرف عنها). تُستدعى في كل
الملفات الـ16 مباشرة بعد بناء `current_value_display` وقبل حشرها في
الرسالة — تعديل ميكانيكي بحث-واستبدال واحد عبر كل الملفات دفعة واحدة
(نفس أسلوب سلسلة `except Exception` سابقاً في هذه الجلسة).

⚠️ **`user_reports_edit.py` (التعديل بعد النشر) به نفس الشكل تقريباً في
عدة مواضع (~1449, 1451, 1475, 1867, 1869) ولم يُلمَس بعد** — أحد
المواضع يستخدم كتلة \`\`\`code\`\`\` حيث الهروب العشوائي قد يُظهر شرطة
مائلة حرفية بدل حرف مهرَّب، فيحتاج معالجة أدقّ لا نسخاً للحل الميكانيكي
أعلاه. يستحق جولة منفصلة.

**التحقق**: أُعيد إنتاج القيمة الخام التي أسقطت البوت فعلياً وتشغيل
المعالِج الحقيقي — النص الخارج لـTelegram صار `\*` و`\_` بدل `*` و`_`
الخام (مطابق تماماً لمخرَج `escape_markdown` الرسمي)، وتأكدنا أن نصاً
عادياً بلا محارف خاصة لا يتغيّر إطلاقاً (لا هروب زائد). 183 اختباراً +
فحص `register_all_handlers()` حقيقي.

---

### 2026-08-06 — 🐛 التقويم لا يُظهر السنة بوضوح + 🔧 أسماء المرافقين لا تظهر في اختيار المغادرين

**١) التقويم (`shared/calendar_picker.py`)** — شاشات إدخال تاريخ انتهاء
الجواز/الإقامة (في `arrivals/flow.py`، `quick_jump=True`) تحشر 5 أزرار
في صف تنقّل واحد: `[«] [◀️] [الشهر السنة] [▶️] [»]`. زر "الشهر السنة"
(الوحيد الذي يحمل رقم السنة) يضيق حتى نحو خُمس عرض الصف فيختفي نصه
عملياً على شاشات الجوال — **ليست مشكلة حد أحرف كما بدت للمستخدم، بل
ضغط تخطيطي**؛ المسار العادي (`quick_jump=False`, 3 أزرار فقط) لا يعاني
من هذا لأن التسمية تأخذ ثلث الصف. **الإصلاح**: تقسيم الصف الواحد إلى
صفّين — صف الشهر بـ3 أزرار (مطابق تماماً للمسار العادي) + صف مستقل
لقفز السنة بزرّين واسعين بنص صريح ("« سنة سابقة" / "سنة تالية »") بدل
الرمزين المجرَّدين "«"/"»". لا تغيير في `callback_data` إطلاقاً (نفس
`cal_yprev`/`cal_ynext`/`cal_years`) فلا حاجة لمس أي معالِج ديسباتش —
تعديل تخطيطي بحت في `build_calendar()`.

**٢) اختيار المغادرين (`modules/general_services/departures/flow.py`)** —
تبيّن أن الاختيار المتعدد للمرضى **موجود بالفعل** (`shared/multiselect`,
`min_select=1` بلا `max_select`) خلافاً لِما بدا من صياغة الطلب، لكن
أسماء المرافقين لا تظهر أبداً كنص — فقط كعدد بجانب اسم المريض
(`"أحمد (+ مرافق واحد)"`). طُرح على المستخدم سؤال توضيحي (عرض الأسماء
فقط، أم اختيار مستقل للمرافقين عن مريضهم — الخيار الثاني يتطلب أعمدة
DB جديدة لأن `ArrivalCompanion` لا يملك `arrival_status`/
`departure_record_id` إطلاقاً). اختار المستخدم **عرض الأسماء فقط**.
**الإصلاح**: `ActiveArrivalEntry.display_label` في
`modules/general_services/arrivals/repository.py` يبني الآن
`"أحمد (+ خالد، سالم)"` من `companion_names` (كانت محمَّلة فعلاً من
`get_active_arrivals()` ولكن غير مستخدَمة إلا للعدّ). `display_label`
مستخدَم في مكان واحد فقط (`departures/flow.py:117`) فالتغيير معزول
تماماً بلا أثر جانبي على شاشات أخرى.

**التحقق**: بُنيت لوحة مفاتيح حقيقية عبر `build_calendar(2026, 8, ...,
quick_jump=True)` وطُبعت بنيتها بالكامل؛ اختُبر `display_label` بثلاث
حالات (بلا مرافق / مرافق واحد / مرافقان). 183 اختباراً +
`register_all_handlers()` حقيقي عبر `Application` فعلي.

---

### 2026-08-06 (٢) — 🔧 إخفاء المرافقين من شاشة «إدارة أسماء المرضى» في الادمن

**الطلب**: عند إضافة مريض بمرافقين من الادمن (`🤝 مريض جديد مع مرافقين`)،
لا داعي لظهور المرافقين كأسطر مستقلة في شاشة «أسماء المرضى» (عرض/تعديل/
حذف) — يكفي ظهور اسم المريض فقط هناك، بينما يبقى المرافقون ظاهرين كما
هم الآن أثناء الإدخال الفعلي في زر «الواصلين».

**السبب الجذري**: `get_patients_paginated()`/`get_patients_count()` في
`services/patients_service.py` كانتا تستعلمان عن جدول `Patient` بلا أي
فلتر على `patient_type` إطلاقاً، فتُرجعان كل الصفوف — المرضى العاديون
**و**المرافقون (`patient_type="companion"`) معاً — لأربع شاشات إدارة
في `bot/handlers/admin/admin_schedule_management.py` (`handle_manage_patients`
سطر 558، `handle_view_patient_names` سطر 602، `handle_delete_patient_name`
سطر 1044، `handle_edit_patient_name` سطر 1156)، فتظهر أسماء المرافقين
كصفوف عادية لا تُميَّز عن المرضى.

**الإصلاح**: أضفنا فلتر `patient_type != "companion"` لكلتا الدالتين.
⚠️ **فخ اكتُشِف أثناء الكتابة**: `patient_type` فارغ (`NULL`) لكل مريض
عادي (القيمة الافتراضية) — و`!=` في SQL لا تُطابق صفوف `NULL` (تُقيَّم
إلى `NULL` لا `TRUE`)، فلو تُرِك الفلتر `!= "companion"` وحده لاختفى
**كل** المرضى العاديين من القائمة، لا المرافقون فقط. أُصلِح بإضافة
`or_(Patient.patient_type != "companion", Patient.patient_type.is_(None))`
صراحة.

**لم يُلمَس**: `get_companions_for_patient()` (مسار مستقل تماماً يغذّي
اختيار المريض في `arrivals/flow.py:510`) ولا `patient_selector` بعلَم
`only_companion_flow=True` (مسار ثالث منفصل في `arrivals/flow.py:221,304`
و`public_services/flow.py:106`) — فظهور المرافقين أثناء الإدخال الفعلي
في «الواصلين» يبقى كما هو تماماً.

**التحقق**: محاكاة حية عبر قاعدة بيانات SQLite حقيقية منفصلة (لا قاعدة
الإنتاج) — أُدخِل مريضان عاديان (`patient_type=None`)، مريض صرف أدوية
(`pharmacy_only`)، ووالد بمرافقَين (`companion_parent` + مرافقان
`companion`)؛ تأكدنا عبر `assert` أن المرافقَين غائبان تماماً من نتيجة
`get_patients_paginated()`/`get_patients_count()` بينما الأربعة الباقون
(بمن فيهم المريضان بـ`NULL` والوالد نفسه) ظاهرون جميعاً. 183 اختباراً.

---

### 2026-08-06 (٣) — 🐛 تقرير تقييم الرعاية الصحية: أعمدة مقلوبة + نصوص عربية معكوسة

الملف: `modules/healthcare/evaluation/pdf_builder.py`.

**١) ترتيب أعمدة الجداول كان LTR** — ReportLab يرسم العمود رقم 0 في أقصى
**اليسار** دائماً. الجداول مكتوبة بترتيبها المنطقي العربي (البيان ثم
القيمة)، فكان "البيان" ينتهي يساراً و"القيمة" يميناً؛ والقارئ العربي
يبدأ من اليمين فيرى **القيمة أولاً ثم البيان** — معكوس تماماً، في كل
جداول التقرير (تحليل التوثيق، الأقسام الطبية، تحليل النشاط، مراحل
المجارحة، تفاصيل الحالات). أُصلِح مركزياً داخل `_make_table()` بعكس
الأعمدة **والعروض معها**، فتبقى مواضع الاستدعاء مكتوبة بترتيبها المنطقي
الطبيعي. أوامر `TableStyle` كلها صفّية النطاق `((0,r) → (-1,r))` فلا
يتأثر أيٌّ منها بالعكس، ومحاذاة كل خلية محمولة في نمط فقرتها. عُكِست
كذلك بطاقات الملخص و`sec_table` (لا تمران عبر `_make_table`).

**٢) بعض النصوص العربية معكوسة دون بقيتها** — `_ar()` كانت تستدعي
`get_display()` **بلا** `base_dir`، فتستنتج المكتبة اتجاه الفقرة من أول
حرف قوي في النص. نص طبي حر مثل `"500mg باراسيتامول"` أو
`"Ceftriaxone حقن وريدية"` (اسم دواء لاتيني أولاً — شائع جداً في حقلَي
`detail` و`patient_name`) أول حرف قوي فيه لاتيني ⇒ يُعامَل كفقرة LTR
فيوضع الجزء العربي في الطرف الخاطئ ويظهر "معكوساً"، بينما جاره في نفس
العمود (اسم عربي صرف) يظهر سليماً — وهذا بالضبط سبب "بعضها معكوسة"
دون بقيتها.

**الإصلاح**: `base_dir="R"` صراحةً + **تخطّي النص الخالي من العربية
تماماً**. ⚠️ الشقّ الثاني ضروري ولا يُحذَف: تمرير
`"2026/07/01 — 2026/08/05"` عبر `get_display` بأساس RTL **يقلب ترتيب
التاريخين فعلياً** (تحقّقنا: يخرج `"2026/08/05 — 2026/07/01"`)، فلو
ثُبِّت الأساس على R بلا استثناء الأرقام لاستُبدِل عطب بعطب. وُحِّدت
تسميات رسوم matplotlib على نفس `_ar` بدل `get_display` المباشر، وأُزيل
`_ar` مزدوج على `period_str` (كان غير ضار لأن النص رقمي بحت، لكنه فخّ
ينفجر لحظة إضافة أي حرف عربي لذلك السطر).

**التحقق**: وُلِّد PDF حقيقي ببيانات واقعية تتضمن
`"500mg باراسيتامول"`، ورُسِّمت صفحاته صوراً وفُحِصت بصرياً: "البيان"
يميناً و"القيمة" يساراً، وأعمدة "تفاصيل الحالات" بترتيب
التاريخ→المريض→الخدمة→القسم→التفاصيل→صور من اليمين لليسار، والتواريخ
سليمة غير مقلوبة. 183 اختباراً.

⚠️ **نفس دالة `_ar` منسوخة حرفياً بلا `base_dir` في 6 ملفات أخرى**
(`comprehensive_report_pdf`, `data_analysis_pdf`, `patient_report_pdf`,
`pharmacy_evacuation_pdf`, `residency_archive_pdf`,
`pdf_generation/pdf_renderer_arabic`) — نفس النمط الجذري المتكرر
المسجَّل في هذا السجل. لم تُلمَس (خارج نطاق الطلب)، وتستحق جولة موحّدة
لاحقاً؛ شقّ `base_dir` آمن عليها جميعاً، أما عكس الأعمدة فيحتاج فحصاً
لكل ملف على حدة.

---

### 2026-08-06 (٤) — 🐛 الواصلين: الإدخال اليدوي لا يُقبَل (رسالة معدَّلة ⇒ `update.message = None`)

**العَرَض**: في تدفق الواصلين، خطوة "السكن" (`p_residence_address`) وغيرها
لا تقبل الإدخال اليدوي إطلاقاً — لا ردّ ولا خطأ ولا شيء — بينما زر
"تخطي" يعمل بشكل سليم.

**الدليل من سجلّ الإنتاج**: بعد حفظ الجلسة على الخطوة وعرض الشاشة، تظهر
عند كتابة المستخدم **فقط** `[daily_patients.text] FIRED` (المجموعة 9)
ولا يظهر أي سطر `[arrivals.text]` ولا حتى `[ArrivalSession.load]` —
مع أن `ArrivalSession.load` تسجّل عند **كل** استدعاء (حتى عند غياب
المفتاح). أي أن `_handle_text_input` ارتدّ **قبل** سطر تحميل الجلسة.

**السبب الجذري**: `filters.TEXT` الذي سُجِّل به المعالِج **يقبل الرسائل
المعدَّلة أيضاً** — تحقّقنا عملياً ببناء تحديث `edited_message` حقيقي:
`MessageHandler(filters.TEXT & ~filters.COMMAND).check_update(u)` ⇒
`True` بينما `u.message is None`. فكان الشرط `if not update.message:
return` يبتلع الرسالة **صامتاً تماماً**. وزر "تخطي" يعمل لأنه
`CallbackQuery` لا `Message` — وهذا بالضبط سبب التفاوت الذي لاحظه
المستخدم.

**الإصلاح**: `update.effective_message` بدل `update.message` في معالِجات
النصوص. **هذا العطب نفسه كان مُصلَحاً سلفاً في
`admin_daily_patients.py`** (تعليقه يشرحه حرفياً) ولم يُعمَّم على البقية
— نفس النمط الجذري المتكرر المسجَّل في هذا السجل.

**النطاق — أُصلحت 4 ملفات، ثلاثة منها كانت أسوأ من "صامت"**:
| الملف | السلوك قبل الإصلاح |
|---|---|
| `general_services/arrivals/flow.py` | ارتداد صامت (له حارس `if not update.message`) |
| `general_services/departures/flow.py` | **`AttributeError`** — بلا حارس إطلاقاً، يذهب مباشرة لـ`update.message.text` |
| `general_services/public_services/flow.py` | **`AttributeError`** — بلا حارس إطلاقاً |
| `residency/profiles/flow.py` | ارتداد صامت (بحث الإقامات وإدخال الأسماء) |

**التحقق**: شُغِّل المعالِج الحقيقي على الخطوة المُبلَّغ عنها بالحالتين —
رسالة عادية ورسالة معدَّلة — وكلتاهما تُقبَل الآن وتُحفَظ القيمة
(`✅ ACCEPTED`)، بينما الرسالة المعدَّلة كانت تُسقَط قبل الإصلاح
(`❌ DROPPED`). 183 اختباراً + `register_all_handlers()` حقيقي.

---

### 2026-08-06 (٥) — 🛡️ الخدمات العامة: زر النشر يظهر بدل شاشة المختص

**البلاغ**: في الوحدة الثالثة «🧾 الخدمات العامة» لا تظهر شاشة اختيار
المختص بعد الملاحظات — يظهر زر النشر مباشرة.

**ما تبيّن بالفحص**: المسار في `main` **سليم** ولم نتمكّن من إعادة إنتاج
العطب — حوكيت الحالتان الخارجتان من خطوة الملاحظات بالدوال الحقيقية
(كتابة ملاحظة نصاً، وضغط «⏭️ تخطي») وكلتاهما تنتقلان إلى
`STEP_SPECIALIST` وتعرضان شاشة المختص بزرَّيها من `STAFF_MAP`. كما أن
`PublicServiceSession.create()` يصفّر `edit_from_review=False`.

**السبب المرجَّح**: `edit_from_review` عالقة `True` من جلسة سابقة مهجورة
(دخل المستخدم «✏️ الملاحظات» من شاشة المراجعة ثم ترك التدفق دون إكماله).
عندها تقفز خطوة الملاحظات مباشرة إلى `_go_to_review` فيظهر زر النشر بلا
مختص إطلاقاً.

⚠️ **والأخطر مما بُلِّغ عنه**: معالِج `confirm` (النشر) **لا يتحقّق من أي
حقل قبل الحفظ** — كان ينشر السجل ويحفظه بـ`specialist_label` فارغ
**بصمت** متى وصل المستخدم للمراجعة بأي طريق دون اختيار مختص.

**الإصلاح**: حارس واحد في `_go_to_review` — نقطة الاختناق الوحيدة التي
تمرّ منها **كل** الطرق المؤدية لشاشة المراجعة (الملاحظات، تخطي
الملاحظات، الصور، عدد البنود، نوع الخدمة). إن كان `specialist_label`
فارغاً تُعرَض شاشة المختص بدل المراجعة وتُضبَط الخطوة على
`STEP_SPECIALIST`. فلا يمكن بلوغ زر النشر بلا مختص مهما كان المسار أو
حالة العلَم.

**التحقق**: ثلاث حالات بالدوال الحقيقية — (أ) تدفق جديد بلا مختص ⇒ شاشة
المختص ✅ (ب) `edit_from_review=True` عالقة بلا مختص ⇒ شاشة المختص بدل
النشر ✅ (وهي حالة البلاغ بالضبط) (ج) تعديل ملاحظات من المراجعة والمختص
مُختار سلفاً ⇒ شاشة المراجعة بزر النشر ✅ (لا ارتداد). 183 اختباراً +
`register_all_handlers()` حقيقي.

✅ **`departures/flow.py` طُبِّق فيه نفس الحارس** (بطلب المستخدم مباشرة بعد
الإصلاح أعلاه) — كان يحمل الثغرة نفسها: `confirm` بلا أي تحقّق، ولا مسار
"تخطي المختص" في التدفق فالحقل إلزامي دائماً. حوكيت نفس الحالات الثلاث
بالدوال الحقيقية ونتائجها مطابقة: (أ) و(ب) ⇒ شاشة المختص، (ج) ⇒ شاشة
المراجعة بزر النشر.

📌 **الوحدات التي لا تزال بلا هذا الحارس**: `arrivals` (المختص فيها ضمن
كتلة ختام كل مريض لا حقلاً واحداً في المراجعة، فبنيتها مختلفة وتحتاج
فحصاً منفصلاً)، وكل وحدات `healthcare` — لم تُفحص بعد لهذه الثغرة تحديداً.

---

### 2026-08-06 (٦) — 🧹 لوحة أزرار البوت عالقة داخل مجموعات المتابعة

**العَرَض**: أزرار قائمة البوت (لوحة سفلية) ظاهرة **لكل** أعضاء مجموعة
«المتابعة الطبية»، وضغط أيٍّ منها يُرسل نصّه كرسالة عادية للمجموعة فقط
دون أن يفعل شيئاً.

**التشخيص — ليس عطباً جارياً بل أثراً متبقّياً**: لوحة الأزرار في تيليجرام
ليست جزءاً من الرسالة؛ فبمجرّد إرسالها لمحادثة تبقى ظاهرة لأعضائها **إلى
الأبد** حتى تُمسَح صراحةً بـ`ReplyKeyboardRemove`. ولأنها أُرسلت بلا
`selective` فهي تنطبق على كل الأعضاء — وهذا وحده يفسّر «تظهر للكل» دون
الحاجة لمسار حيّ. أُرسلت أصلاً قبل وجود `bot/group_guard.py`، حين كان
البوت يردّ داخل المجموعات وأي ردّ يحمل القائمة يثبّتها هناك.

**تأكيد أن لا مسار حيّ يعيد تثبيتها** — ثلاث نقاط فُحصت جميعاً:
- الحارس `group_guard` نشط فعلاً: مُرِّرت رسالة مجموعة حقيقية عبر كل
  المعالِجات المسجَّلة، فكان أول من يلتقطها **المجموعة ‎-100** (الحارس
  يرفع `ApplicationHandlerStop`) في `group`/`supergroup`، بينما لا يطابق
  في `private` — أي لا معالِج يستطيع الردّ بلوحة داخل مجموعة.
- المسارات الوحيدة التي **تعمل** داخل المجموعات (أزرار بطاقة التقرير
  `medfiles:` و`view_reschedule:`، و`universal_fallback`) لا يُرسل أيٌّ
  منها لوحة أزرار؛ الأخير يكتفي بـ`query.answer()`.
- بثّ التقارير يُرفق `InlineKeyboardMarkup` فقط، لا `ReplyKeyboardMarkup`.

**العلاج**: `scripts/clear_group_reply_keyboard.py` — سكربت لمرة واحدة
(لا تعديل في الكود، فالكود سليم) يرسل `ReplyKeyboardRemove` لكل مجموعة
مضبوطة **ويترك رسالته**. نُفِّذ على الإنتاج و**اختفت اللوحة فعلياً** من
المجموعات الأربع بعد إعادة تشغيل تطبيق تيليجرام على الجهاز.

⚠️ **أربعة أخطاء في السكربت لم يكشف أياً منها إلا التشغيل الحقيقي** — لا
`py_compile` ولا مراجعة الكود؛ تُذكَر لأن كلاً منها فخّ متكرّر:
1. `text="​"` (مسافة صفرية) ⇒ تيليجرام يرفضها بـ
   `Text must be non-empty` فتفشل كل الإرسالات. و`ReplyKeyboardRemove`
   لا يمكن إرساله بلا رسالة تحمله ⇒ لا بدّ من نصّ مرئي.
2. `REPORTS_GROUP_ID` يحمل **معرّفين مفصولين بفاصلة**
   (`-100...388,-100...845`) وكانت السلسلة تُمرَّر كمعرّف واحد. الآن
   تُفصَل، ويُحذف المكرَّر حسب **المعرّف الفعلي** لا النصّ الخام — فالمعرّف
   نفسه مشترك بين `REPORTS_GROUP_ID` و`MEDICAL_REPORTS_GROUP_ID`.
3. الملخّص كان يطبع «يجب أن تكون اللوحة اختفت» حتى مع فشل 4/4 — تقرير
   نجاح كاذب. صار يعلن الفشل ويخرج بكود غير صفري.
4. 🔑 **الأهم — حذف رسالة المسح كان يُلغي المسح نفسه**: بعد إصلاح (1) و(2)
   أبلغ السكربت «4/4 ✅» بينما بقيت اللوحة ظاهرة على الهاتف. السبب أن
   عميل تيليجرام يستنتج اللوحة الحالية من **آخر رسالة تحمل لوحة** في
   المحادثة؛ فحذف الرسالة الحاملة لـ`ReplyKeyboardRemove` يُسقط الأمر
   معها فيرجع العميل لآخر رسالة ثبّتت لوحة قبلها وتظهر الأزرار مجدداً.
   الـAPI كان يقبل الإرسال فعلاً — ثم نُبطِله بأنفسنا بعده بسطر واحد.
   ⇒ **رسالة المسح تبقى في المجموعة دائماً**، وحذفها لاحقاً يُعيد المشكلة.
   (`--delete-message` يستعيد السلوك القديم لمن يريده، مع تحذير.)

📌 **درسان عامّان**: (أ) «نجح الاستدعاء» ≠ «تحقّق الأثر» — تقرير النجاح
هنا كان صادقاً في الطبقة الخطأ؛ التحقق الوحيد المعتبَر كان فتح التطبيق
والنظر. (ب) لا يُحكَم على اختفاء اللوحة قبل **إعادة تشغيل تطبيق تيليجرام**،
فهو يخزّنها مؤقتاً في العميل لا في الخادم.

---

### 2026-08-06 (٧) — ✅ زر "🔙 رجوع" في جلسة إشعاعي والمناظير (كان مفتوحاً)

**العَرَض**: "⚠️ خطأ في التنقل" عند الضغط على 🔙 رجوع من **أي** خطوة في
مسارَي «جلسة إشعاعي» و«المناظير».

**السبب**: `execute_smart_state_action.py` — الدالة التي تُعيد عرض الشاشة
المستهدفة — لم تكن تعرف أياً من حالات المسارين. تحقّقنا عملياً من النسخة
السابقة: **صفر** إشارة لـ`RADIATION_THERAPY_*` أو `ENDOSCOPY_*` في الملف
كلّه، فكانت الحالات الـ17 جميعها تسقط لفرع `else` الأخير الذي يطبع رسالة
الخطأ. نفس عطل `1bf2872` (جلسات العلاج/الأورام) لكن لمسارين لم يُغطَّيا
حينها.

**الإصلاح — على مستويين حسب طبيعة الشاشة:**

1. **11 حقلاً تغطّيها الفروع العامة** (مترجم/تأكيد/تواريخ/أسباب/ملاحظات/
   شكوى) ⇒ أُضيفت لخريطة `state_value_to_name` باسمها العام فقط، فتلتقطها
   الفروع الموجودة أصلاً بلا كود جديد.
2. **7 شاشات ذات محتوى خاص** (نوع الإشعاعي، عدد الجلسات الكلي، العدد
   الجديد، سبب التعديل، نوع المنظار، نتيجة المنظار، الإجراءات، إجراء
   "أخرى") ⇒ فروع `elif target_step == …` مخصَّصة في نهاية السلسلة.

⚠️ **نقطة تصميمية تُنسى بسهولة**: الشاشات الخاصة **يجب ألّا تُضاف** لخريطة
`state_value_to_name`. تركها بلا اسم يُبقي `step_name` رقماً، فلا يلتقطها
أي فرع عام بالخطأ (`'TYPE' in step_name` مثلاً كان سيبتلع «نوع المنظار»
ويعرض شاشة أخرى)، ويصل التنفيذ لفرعها المخصَّص. نفس نمط
`TRANSPLANT_*`/`ONCOLOGY_*`.

✅ **شاشة الإجراءات تُعاد ببناء `_build_procedures_keyboard(selected)`
بالاختيارات المحفوظة** لا بلوحة فارغة — الرجوع لخطوة اختيار متعدد يجب أن
يُبقي ما أشّر عليه المستخدم.

**التحقق**: شُغِّلت `execute_smart_state_action` الحقيقية على **الحالات
الـ17 كلّها** واحدةً واحدة — **17/17 تعرض شاشتها الصحيحة، صفر "خطأ في
التنقل"**. وفُحص القاموس بـAST: 106 مفتاحاً بلا اسم مكرَّر، ولا تصادم بين
قيم الحالات الجديدة والقديمة (المفاتيح المكرَّرة في dict تُدهَس بصمت).
183 اختباراً + `register_all_handlers()` حقيقي.

---

### 2026-08-06 (٨) — 🐛 التعديل بعد النشر: دالة تهريب Markdown خاطئة + كتلة ``` تنكسر

الملف: `bot/handlers/user/user_reports_edit.py`.

**ثلاثة عيوب متمايزة، لكلٍّ علاج مختلف — والخلط بينها هو أصل المشكلة:**

**١) دالة `escape_markdown` محلية تُهرّب محارف الإصدار الخطأ.** كانت
معرَّفة داخل الملف وتُهرّب 18 محرفاً من قواعد **MarkdownV2**
(`. - ( ) ! # + = | { } ~ >` …)، بينما **كل** رسائل هذا الملف تُرسَل بـ
`ParseMode.MARKDOWN` (النسخة القديمة) التي لا تعرف من هذه المحارف شيئاً.
النتيجة: الشرطة المائلة تصل للمترجم **حرفية ومرئية**. تحقّقنا:
`"2026-08-05 الساعة 10:30"` كان يظهر `2026\-08\-05 الساعة 10:30` في كل
شاشة تأكيد تعديل تاريخ. **حُذفت الدالة نهائياً** (لا اكتُفي بتصحيح
مواضع استدعائها) حتى لا يعود أحد لاستخدامها.

**٢) نفس الدالة كانت تُطبَّق داخل كتل ```.** وهذا خطأ مضاعف: داخل كتلة
الكود يعامل تيليجرام المحتوى حرفياً، فمحارف `_ * [` **آمنة أصلاً بلا
تهريب**؛ وتهريبها يُظهر شرطات مائلة لا وجود لها في نص المترجم.

**٣) العلامة الخلفية داخل كتلة ``` تكسر الرسالة كلها.** وهذا هو العطب
الوظيفي الحقيقي (لا التجميلي): قيمة مثل ``"نتيجة `الفحص` طبيعية"`` تحوي
علامتين خلفيتين تُغلقان الكتلة قبل أوانها ⇒ `BadRequest` وتسقط الشاشة.
وهذا بالضبط ما جعلنا نُرجئ هذا الملف عند إصلاح الـ16 ملفاً في
2026-08-05: نسخ الحل الميكانيكي إليه كان سيزيد الطين بلّة.

**العلاج — دالتان لا واحدة، بحسب موقع القيمة:**
| الموقع | الدالة | لماذا |
|---|---|---|
| نصّ عادي داخل رسالة Markdown | `escape_md_v1` | تهريب `_ * ` [` فقط (التطبيق الرسمي) |
| داخل كتلة ` ``` ` | `_safe_code_block` | تحييد العلامة الخلفية **فقط** |

`_safe_code_block` تستبدل `` ` `` بعلامة اقتباس مفردة مائلة (U+2018) —
تُشبهها بصرياً ولا تعني شيئاً لتيليجرام.

**التحقق**: شُغِّلت الشاشات الحقيقية الثلاث (`handle_field_selection`،
`confirm_text_edit`، `confirm_date_edit`) بقيم خبيثة — قيمة فيها علامات
خلفية وأخرى فيها `_ * [` غير متزاوجة. النتيجة: **الكتل متوازنة (``` زوجية)،
صفر علامة خلفية شاردة، صفر شرطة مائلة مرئية**، والتاريخ يظهر
`2026-07-01` سليماً. وأُعيد إنتاج العيوب الثلاثة على الكود القديم للتأكد
أنها كانت حقيقية لا مفترَضة. 183 اختباراً +
`register_all_handlers()` حقيقي.

⚠️ **ما زال مفتوحاً**: محاكاة end-to-end كاملة لدورة التعديل بعد النشر
(اختيار تقرير ⇒ تعديل كل نوع حقل ⇒ حفظ ⇒ إعادة نشر ⇒ بطاقة المجموعة).
ما عولج هنا هو طبقة العرض/التهريب فقط.

---

### 2026-08-06 (٩) — 🐛 بطاقة المجموعة: شرطات مائلة مرئية + بطاقة تنكسر باسم فيه `_`

**نفس دالة التهريب المعطوبة — نسخة ثالثة** في
`services/broadcast_service.py:45` (بعد `user_reports_edit.py`). تُهرّب
محارف **MarkdownV2** بينما البطاقة تُرسَل بـ`ParseMode.MARKDOWN` القديم،
فتصل الشرطة المائلة لأعضاء المجموعة **حرفية**:
```
🏥 المستشفى: Apollo Hospital \(Chennai\)
👨‍⚕️ اسم الطبيب: د\. سرور
```
كان هذا ظاهراً **لكل أعضاء المجموعات** في أي تقرير فيه قوس أو نقطة في
اسم مستشفى أو طبيب — أي في أغلب التقارير عملياً.

**وأخطر منه: التهريب غير متّسق.** اسم المترجم كان مُهرَّباً في موضع واحد
و**خاماً في 7 مواضع**. فاسم فيه `_` أو `*` يكسر تحليل الرسالة ⇒ يسقط
`broadcast_service` لخطة بديلة (`parse_mode=None`) فتُعاد البطاقة كلها
بلا تنسيق و**علامات `**` مكشوفة حرفياً**. أي أن العطب لا يُسقط التقرير
بل يُشوّهه بصمت — ولهذا لم يُبلَّغ عنه كخطأ.

**الإصلاح — أصغر تغيير يغطّي أكبر مساحة:**
1. استُبدل **جسم** `escape_markdown` بالتطبيق الرسمي
   (`telegram.helpers.escape_markdown(version=1)`) مع إبقاء الاسم —
   فصُحِّحت **68 موضع استدعاء** دفعة واحدة بلا لمس أيٍّ منها.
2. أُضيف التهريب للمواضع الـ7 الخام لاسم المترجم.

⚠️ **لم يُمسّ الشكل إطلاقاً** — بطلب المستخدم صراحةً بعد استعراض 4
تصاميم بديلة: الترتيب والرموز والفواصل كما هي، التغيير في التهريب فقط.

**التحقق**: (أ) البطاقة العادية: **0 شرطة مائلة** بدل 3، والأسماء تظهر
`Apollo Hospital (Chennai)` و`د. سرور` سليمة. (ب) قيم خبيثة
(`احمد_الاشول`, `Apollo *Main*`, `د. سرور [قسم`): كل `_ [ *` القادمة من
البيانات مُهرَّبة، والوحيدة غير المُهرَّبة هي علامات `**` المقصودة للعناوين
وعددها **زوجي** فالتحليل متوازن. (ج) مسح على **15 نوع إجراء** بقيم خبيثة
في كل حقل: **15/15 ناجحة**، صفر محرف خام، وكل أعداد `*` زوجية.
183 اختباراً + `register_all_handlers()` حقيقي.

📌 **الدرس**: هذه ثالث نسخة من نفس الدالة الخاطئة في المشروع. النسختان
الأخريان حُذفتا؛ هذه بقيت باسمها لكثرة مستدعيها لكن جسمها صار يفوّض
للتطبيق الرسمي. **لا تُكتب دالة تهريب Markdown يدوياً في هذا المشروع
مجدداً** — `telegram.helpers.escape_markdown` هي المصدر الوحيد.

---

### 2026-08-07 — ✅ محاكاة end-to-end لدورة التعديل بعد النشر (كانت مفتوحة)

آخر بند مفتوح في هذا السجل. المسار **لم يُختبر end-to-end قطّ** — فُحصت
منه سابقاً طبقة العرض/التهريب فقط.

**النتيجة: 9/9 نجحت — لا عطب في المسار.** الدورة كاملة تعمل: اختيار
تقرير ⇒ اختيار حقل نصّي وعرض قيمته ⇒ إدخال قيمة جديدة ومعاينتها ⇒ الحفظ
**ووصوله لقاعدة البيانات فعلياً** ⇒ العودة لقائمة الحقول ⇒ حفظ حقل تاريخ
⇒ إعادة النشر وبناء البطاقة ⇒ ظهور القيمة المعدَّلة فيها ⇒ سلامة تهريبها.

القيمة المستخدَمة في الاختبار تحوي عمداً `التهاب معدة* مزمن_مع ارتجاع` —
محارف كانت تكسر الرسالة قبل إصلاحات التهريب (أقسام ٨ و٩ أعلاه)، فالاختبار
يغطّيها ويؤكّد الإصلاح في مساره الحقيقي لا في عزلة.

**حُفِظ كسكربت دائم**: `scripts/e2e_post_publish_edit.py`.

⚠️ **ولماذا سكربت لا اختبار pytest** — قرار مقصود لا كسل:
`user_reports_edit.py` يلتقط `SessionLocal` **وقت الاستيراد**،
و`broadcast_service` يستوردها من جديد داخل الدوال **وقت الاستدعاء**؛
بينما `tests/test_rbac_smoke.py` يُعيد ربط `db.session.SessionLocal`
**عالمياً** لقاعدة في الذاكرة. فوضع هذه الدورة في pytest يجعل نتيجتها
رهينة ترتيب استيراد الملفات — نفس تلوّث الاختبارات الموثَّق في
`tests/test_report_ownership.py`. إدخال اختبار متذبذب إلى مجموعة من 183
اختباراً تمرّ نظيفة كان سيكلّف أكثر مما يفيد. السكربت يعزل نفسه بقاعدة
مؤقتة في `tempfile.mkdtemp()` ولا يلمس قاعدة الإنتاج بأي حال.

📌 **يبقى خارج نطاق هذه الجولة**: تغطية كل أنواع الحقول (المترجم،
الاختيار المتعدد للمناظير، حقول جلسات العلاج) — غُطّي حقل نصّي وحقل تاريخ
كعيّنتين مُمثِّلتين لآليتَي الحفظ المختلفتين.

---

### 2026-08-07 (٢) — ✅ تعميم إصلاح اتجاه النص العربي على بواني PDF الستة

استكمال لقسم 2026-08-06 (٣) الذي أصلح تقرير التقييم وحده وسجّل أن **نفس
دالة `_ar` منسوخة بلا `base_dir` في 6 ملفات أخرى**.

**ما تبيّن عند الفحص — الملفات لم تكن متساوية:**
| الحالة | الملفات |
|---|---|
| ينقصها **الحارسان** (تخطّي غير العربي + `base_dir`) | `comprehensive_report_pdf`, `data_analysis_pdf`, `patient_report_pdf` |
| ينقصها **`base_dir` فقط** (تتخطّى غير العربي أصلاً) | `pharmacy_evacuation_pdf`, `residency_archive_pdf` |
| صيغة مختلفة تماماً (`ArabicTextRenderer.reshape_text`) | `pdf_generation/pdf_renderer_arabic` |

فلم يكن الإصلاح نسخاً واحداً على الكل — عولج كلٌّ بما ينقصه. ووُحِّدت
تسميات رسوم matplotlib على `_ar` بدل `get_display` المباشر (لم يبقَ في
`services/` أي استدعاء مباشر بلا `base_dir`).

⚠️ **تكرَّر فخّ `from __future__`**: إدراج `import re` آلياً وضعه **قبل**
`from __future__ import annotations` في ملفين، و`py_compile` وحده كشفه
(`ast.parse` يقبله). نفس الدرس المسجَّل سابقاً في هذا السجل — ولهذا
`compileall` جزء ثابت من التحقق بعد أي تعديل آلي.

**التحقق — مقارنة بالمرجع لا فحص ذاتي**: قورنت الدوال الست بدالة
`modules/healthcare/evaluation/pdf_builder._ar` (المرجع المتحقَّق منه
بصرياً سابقاً) على 7 حالات تشمل `"500mg باراسيتامول"` و
`"2026/07/01 — 2026/08/05"` و`"د. سرور"` ⇒ **6/6 مطابقة تماماً، صفر
اختلاف**. ثم وُلِّد مسير إخلاء حقيقي ورُسِّم صورةً وفُحِص بصرياً:
`باراسيتامول 500mg` تقرأ RTL سليمة، والتواريخ وأرقام الفواتير سليمة.
183 اختباراً + `register_all_handlers()` حقيقي.

📌 **لم تُمسّ ترتيبات الأعمدة** في أيٍّ من الملفات الستة — بعضها قد يحتاج
عكساً مثل تقرير التقييم، لكن ذلك يحتاج فحصاً بصرياً لكل تقرير على حدة
وهو خارج نطاق هذه الجولة (إصلاح اتجاه النص فقط).

---

### 2026-08-07 (٣) — 🐛 النسخة **الخامسة** من عطب الرسالة المعدَّلة (إدارة المستخدمين)

اكتُشف أثناء تقرير مراجعة لوحدة إدارة المستخدمين طلبه المستخدم.

**الموضع**: `admin_users_management.py::handle_translator_name_for_approved_user`
— خطوة إدخال اسم المترجم بعد الموافقة على مستخدم جديد. كانت تقرأ
`update.message.text` مباشرة.

**الأثر**: إن **عدّل** الأدمن الاسم بعد إرساله (بدل كتابته من جديد) —
وهو تصرّف طبيعي عند التصحيح — ينفجر المعالج بـ`AttributeError` ويبقى
الأدمن عالقاً في حالة انتظار الاسم بلا مخرج سوى «تخطي»، فلا يُربَط
المستخدم المعتمَد بدليل المترجمين ولا يظهر اسمه عند إنشاء التقارير.

**أُكِّد عملياً** بتشغيل الدالة على تحديث `edited_message`:
`'NoneType' object has no attribute 'text'`.

**الإصلاح**: `update.effective_message` + حارس `None` (يُبقي الحالة على
`AWAIT_TRANSLATOR_NAME` بدل إنهائها) + توحيد الردود الثلاثة على `msg`.
تُحقِّق بالحالتين — عادية ومعدَّلة — كلتاهما تُعالَج والمحادثة تنتهي سليمة.

📌 **الحصيلة: خمس نسخ من هذا العطب** عولجت اليوم (arrivals · departures ·
public_services · residency/profiles · admin_users_management)، إضافةً
لـ`admin_daily_patients` الذي كان مُصلَحاً سلفاً. **القاعدة**: أي
`MessageHandler` بفلتر `filters.TEXT` يجب أن يقرأ من
`update.effective_message` لا `update.message` — الفلتر يقبل الرسائل
المعدَّلة، والثاني يكون `None` فيها.

**لم يُعالَج في هذه الجولة** (رُصِد في نفس التقرير، خارج ما طُلب):
- زر «❌ رفض» يحذف سجل المستخدم نهائياً (`s.delete`) بلا تأكيد ولا أثر
  تدقيقي — ضغطة خاطئة تفقد المستخدم وتاريخه.
- نسخة رابعة من دالة تهريب Markdown (`_esc` داخل `_render_user`) —
  **صحيحة** هذه المرة (تهرّب `_ * ` [` فقط) لكنها تكرار لـ`escape_md_v1`.

---

### 2026-08-07 (٤) — 🔎 تكرار اسم نفس الشخص في قائمة المستخدمين (سبب + تشخيص)

**البلاغ**: في شاشة إدارة المستخدمين، الاسم نفسه يظهر مرتين — مثال حقيقي
أعطاه المستخدم: «ادريس» ظاهر في قاعدة البيانات، لكن نفس الشخص ظهر أيضاً
باسمه الخام من تيليجرام «Edress»، وكأنه شخصان.

**الآلية الكاملة (تتبُّع كود، لا تخمين):**
1. `ensure_default_translators()` ([shared.py:104](bot/handlers/user/user_reports_add_new_system/flows/shared.py:104))
   تزرع 18 اسماً افتراضياً — منها **«ادريس» حرفياً** — في
   `TranslatorDirectory` **عند كل إقلاع للبوت**، بلا `translator_id` حقيقي
   (SQLite يعطيها رقماً وهمياً صغيراً تلقائياً بحكم كونه المفتاح الأساسي).
2. حين بدأ الشخص الحقيقي استخدام البوت، أُنشئ له صف `users` بآيدي تيليجرام
   حقيقي واسم ظاهر (`full_name`) هو ما يعرضه بروفايله فعلياً — «Edress».
3. `_db_add_translator_ex()` ([admin_translators_management.py:38](bot/handlers/admin/admin_translators_management.py:38))
   تدمج تلقائياً حين يطابق الاسم المُدخَل عند الموافقة الاسمَ الافتراضي
   **حرفياً بلا أي تسامح** (`==` بلا تطبيع). أي فرق بسيط — مسافة زائدة،
   أو كتابة الاسم بالإنجليزية بدل العربية — يمنع الدمج، فيُنشأ صفّ دليل
   **جديد ومنفصل**، ويبقى صفّ «ادريس» الوهمي معلَّقاً بلا صاحب حقيقي.
   القائمة تعرض الاثنين لأنها تكرِّر جدول `users` لا `TranslatorDirectory`.

**الإصلاح المباشر لما طُلب حرفياً** («اريد تربط كل اسم بالايدي الذي سجل
فيه»): آيدي تيليجرام يظهر الآن بجانب كل اسم في شاشتَي «إدارة المستخدمين»
و«إدارة الصلاحيات» (`admin_users_management.py::_list_kb` و`_perm_list_kb`) —
مثال: `✅ ادريس · 7345544036`. تحقَّق بتشغيل الدالة الحقيقية وقياس طول
الأزرار (17–19 حرفاً، بعيد جداً عن حدّ تيليجرام 64).

⚠️ **لماذا لا يوجد كشف تلقائي للزوج «ادريس»/«Edress»**: الشكلان متباعدان
نصّياً بلا أي رابط برمجي (لغتان مختلفتان تماماً) — لا مطابقة نصّية معقولة
تكشف هذا التحويل الصوتي. البديل العملي: `scripts/diag_user_names.py`
وُسِّع بقسم ⑥ يطبع **كل** المستخدمين المعتمَدين (الاسم المعروض، آيدي
تيليجرام، عدد التقارير المنشورة باسمه، والاسم الخام إن اختلف) مرتَّبين
أبجدياً — فيسهل على الأدمن (الذي يعرف الأشخاص فعلياً) اكتشاف الزوج بصرياً،
ويستعين بعدد التقارير لتقرير أيّ الحسابين يُبقي (الأكثر نشاطاً غالباً هو
الصحيح) قبل تجميد الآخر يدوياً عبر «🔒 تجميد» الموجود أصلاً.

**التحقق**: 183 اختباراً + `register_all_handlers()` حقيقي + تشغيل فعلي
لـ`diag_user_names.py` على قاعدة محلية (20 مستخدماً معتمَداً، كل الأسماء
الافتراضية الـ18 مربوطة فعلياً بآيديات حقيقية — لا عطب محلياً، لكن الآلية
مؤكَّدة من الكود وتنتظر تشغيلاً على الإنتاج لكشف الحالات الفعلية).

**تحديث بعد التشغيل الفعلي على الإنتاج** (64 مستخدماً، 35 صف دليل):
المخرَج كشف **٤ فئات** لا فئة واحدة —
① 4 أشخاص باسمين مختلفين طبيعياً (لا مشكلة، منهم «ادريس» بـ492 تقريراً
سليم تماماً) ·
② أشباح واضحة (`مستخدم`، `Edrees` منفصل عن ادريس الحقيقي) ·
③ أزواج متشابهة (`Zain`×2، `نجم`/`نجم الدين`، `زين`/`زين العابدين`…)
تحتاج معرفة الأدمن الشخصية ولا يمكن حسمها برمجياً ·
④ **مفاجأة**: أسماء بآيديات وهمية (`هاشم`=234 تقريراً، `عزالدين`=250،
`عصام`=185…) **ليست أشباحاً** — مترجمون حقيقيون عملوا بالنظام عبر
مطابقة الاسم النصّي القديم قبل ربطهم بحساب تيليجرام، وحذف صفوفهم يفقدهم
نسبة تقاريرهم الحقيقية. لم يُتَّخذ أي إجراء حذف — القرار يحتاج الأدمن.

---

### 2026-08-07 (٥) — ✅ ربط تلقائي متحفِّظ لهوية المترجم عند اختياره فعلياً

**الطلب**: حين يختار مستخدم اسماً من قائمة المترجمين أثناء إنشاء تقرير
(مثال: «آدم» يختار اسم «ادم»)، يُفهَم أن آيدي تيليجرام هذا هو صاحب الاسم
الحقيقي — فيُربَط تلقائياً، علاجاً جذرياً ومستمراً لمشكلة الأشباح
المكتشَفة في القسم السابق (بدل تنظيف يدوي لمرة واحدة).

**التصميم — متحفِّظ عمداً، لا اجتهاد**: `claim_translator_for_submitter()`
جديدة في `services/translators_service.py`، مربوطة في نقطة اختيار
المترجم الوحيدة (`flows/shared.py` — معالِج `translator_idx:`). لا تربط
إلا حين يتحقق **كِلا** الشرطين:
1. **المُرسِل لا يملك اسماً معتمَداً آخر مسبقاً** — وإلا فاختياره اسماً
   مختلفاً الآن أقرب لإدخال نيابة عن زميل، لا اكتشاف هوية ذاتية.
2. **الآيدي الحالي لهذا الاسم لم يُستخدَم فعلياً من قِبل صاحبه** — يُتحقَّق
   عبر `Report.submitted_by_user_id` (الحقل الموثوق لهوية من نشر التقرير
   فعلياً، لا `translator_id` القابل للتلوّث بالمطابقة الاسمية القديمة؛
   انظر مفاجأة القسم السابق). وجود تقرير واحد منشور فعلياً بهذا الآيدي
   يعني هوية حقيقية أخرى — لا مخاطرة بسرقتها.

⚠️ **لماذا لا تُستخدَم `_has_real_telegram_id()` الموجودة أصلاً**: عتبتها
(`>= 100_000`) لا تميّز الأشباح الجديدة عن الحسابات الحقيقية — الآيديات
الوهمية المتسلسلة (`8310133494`...) أكبر من 100,000 أصلاً (SQLite
تكملها من قيمة `TRANSLATORS_SEED` الثابتة). الفحص الجديد يعتمد **نشاطاً
فعلياً موثَّقاً** (`submitted_by_user_id`) لا حجم الرقم.

**التنفيذ**: عند اختيار مترجم بنجاح، تُستدعى الدالة داخل `try/except`
معزولة تماماً — فشلها لا يكسر إنشاء التقرير مهما حدث. عند نجاح الربط
(`"linked"`) يُحدَّث `translator_id` في التقرير الحالي نفسه ليُنسَب
للمُرسِل الحقيقي فوراً، لا للآيدي الوهمي القديم.

**التحقق**: محاكاة الحالات الأربع كاملة بالدالة الحقيقية على قاعدة
منفصلة — **8/8 نجحت**: (أ) اسم مرتبط أصلاً ⇒ لا تغيير · (ب) شبح بلا أي
نشاط ⇒ يُربَط فعلياً (تحقّقنا: الصف القديم اختفى وصفّ جديد بآيدي المُرسِل
ظهر بنفس الاسم) · (ج) هدف له تقرير منشور فعلياً بآيديه ⇒ **رُفِض** ولم
يُمَسّ · (د) مُرسِل يملك اسماً آخر مسبقاً ⇒ **رُفِض** ولم يُمَسّ. 183
اختباراً + `register_all_handlers()` حقيقي.

📌 **أثر متوقَّع تدريجي لا فوري**: الميزة تعمل فقط عند اختيار مترجم
لتقرير **جديد** — لا تُصحِّح شيئاً بأثر رجعي. مع الاستخدام العادي للبوت،
الأشباح الحقيقية (② في القسم السابق) ستُستبدَل تلقائياً أول مرة يختار
صاحبها الحقيقي اسمه، بينما تبقى الحالات الملتبسة (③) وأسماء المترجمين
غير المسجَّلين بحساب حقيقي بعد (④) كما هي حتى يستخدموا البوت بأنفسهم أو
يتدخّل الأدمن يدوياً.

---

### 2026-08-07 (٦) — 🐛 التقارير المعلقة: تبقى معلَّقة بعد الرفع + زر تحديث يبدو ميتاً

فحص لشاشة «📋 التقارير المعلقة» بطلب المستخدم، على شكوى محدَّدة: بعض
التقارير يرفعها المستخدم فعلاً وتظل في القائمة، وسؤال عمّا إن كان زر
«🔄 تحديث» يعمل. **العطبان حقيقيان وكلاهما أُصلح.**

**① 🔴 مسار «البحث المفتوح» لا يُغلق السجل المعلَّق إطلاقاً**

في `user_medical_attachments.py` كان تتبّع الإكمال مشروطاً بـ
`if report_id and ma.get("mode") != "open":`. المستخدم الذي يرفع مرفق
تقرير **معلَّق فعلاً** عبر «🔍 بحث بلا قيود» (بدل قائمة المعلقة) لا
يُغلَق سجلّه أبداً — فيبقى في قائمة الأدمن إلى الأبد رغم رفعه. وهذا هو
بالضبط ما أبلغ عنه المستخدم.

النيّة الأصلية للشرط (موثَّقة في تعليقه) كانت: «ألّا يمسّ حالة تقرير
مكتمل أو غير معلَّق أصلاً». لكن `increment_pending_upload()` **تتحقّق
بنفسها** من وجود سجل معلَّق **نشط** وتعيد `(True, 1, 1)` بلا أي كتابة
حين لا يوجد — فالاستثناء كان يحمي من أثر جانبي **غير موجود أصلاً**،
بينما يمنع الإغلاق المشروع. أُزيل الشرط، وأُثبت عملياً أن الحالات الثلاث
تتصرّف صحيحاً: تقرير لم يكن معلَّقاً ⇒ لا كتابة · أُغلق مسبقاً ⇒ لا
كتابة · معلَّق فعلاً ⇒ يُغلَق.

**② 🔴 زر «🔄 تحديث» بلا أي استجابة مرئية**

`handle_pending_reports_callback` كان يستدعي `query.answer()` **فارغاً في
أعلاه** لكل الأفعال. وتيليجرام يسمح بالردّ على الاستعلام **مرة واحدة
فقط**؛ فحين يفشل `edit_message_text` لاحقاً بـ«message is not modified»
(القائمة لم تتغيّر)، تفشل محاولة `_handle_render_error` عرض «✅ القائمة
محدَّثة» **صامتة** — فلا يرى الأدمن شيئاً إطلاقاً ويبدو الزر معطَّلاً.

⚠️ **الزر كان يعمل وظيفياً طوال الوقت** (يُعيد الاستعلام من القاعدة
فعلاً، وتحقّقنا أن بيانات جديدة تظهر فوراً) — العطب في **التغذية
الراجعة** لا في المنطق. وهذا يفسّر لماذا بدا «لا يعمل» رغم أنه يعمل.

الإصلاح على مستويين: (أ) نُقلت ملكية الردّ لمسار العرض عبر `_answer_once()`
فيُستدعى **مرة واحدة** بالنصّ الصحيح في كل حالة (نجاح/غير معدَّلة/خطأ) ·
(ب) أُضيف سطر «🔄 آخر تحديث: HH:MM:SS» بتوقيت IST فيصير المحتوى مختلفاً
عادةً وينجح التعديل، ويرى الأدمن دليلاً مرئياً دائماً على أن التحديث تمّ.

**التحقق**: **8/8** بمحاكاة تحاكي قيد تيليجرام الحقيقي (`answer` مرة
واحدة، ورفض التعديل عند تطابق المحتوى) — أهمها: التحديث بلا تغيير صار
يعطي «✅ القائمة محدَّثة بالفعل» بعد أن كان `answers=[None]` · التحديث
بعد تغيير فعلي يعرض الجديد · الرفع يُغلق السجل · تقرير غير معلَّق لا
يتأثر إطلاقاً. وفُحص شرط الكود عبر **AST بعد تجريد التعليقات** لا مطابقة
نصّية (اختباري الأول أعطى فشلاً كاذباً لأن تعليقي التفسيري يحوي نصّ
الشرط المحذوف). 183 اختباراً + `register_all_handlers()` حقيقي.

📌 **بأثر رجعي**: التقارير العالقة حالياً بسبب ① لن تختفي وحدها — الإصلاح
يمنع تكرار المشكلة فقط. تُغلَق يدوياً برفع مرفق جديد لها، أو أخبرني
لأكتب سكربت إغلاق للسجلات التي لها مرفقات فعلية بالفعل.

### 2026-08-07 (٧) — 🔴 سبب ثالث للتقارير العالقة: سجلات يتيمة بعد حذف التقرير

أبلغ المستخدم عن صف بعينه لا يزال معلقاً منذ 7 أيام **ونوع الفحص فيه
«—»**. وهذه الـ«—» ليست تفصيلاً تجميلياً — إنها **الدليل الحاسم** على السبب.

`get_pending_reports()` تقرأ نوع الفحص من جدول `reports` عبر `report_id`،
وتستخدم `("—", "")` قيمةً افتراضية حين **لا تجد التقرير أصلاً**.
فظهور «—» يعني أن التقرير **محذوف**، وسجله المعلق بقي يتيماً.

**السبب**: مسارا حذف التقرير (المترجم في `user_reports_delete.py` والأدمن في
`admin_delete_reports.py`) **لا يمسّان `PendingReport` إطلاقاً** (تحقّقنا:
صفر إشارة للجدول في كلا الملفين). والنتيجة سجل **لا سبيل لإغلاقه
أبداً**: الإغلاق الوحيد يأتي من رفع مرفق، ولا يمكن رفع مرفق لتقرير
غير موجود — فيبقى في قائمة الأدمن إلى الأبد.

**الإصلاح**: `drop_pending_for_report()` جديدة في
`services/pending_reports_service.py`، موصولة بـ**كلا** مساري الحذف
داخل `try/except` معزولة (فشلها لا يكسر الحذف نفسه).

**وللمتراكم سابقاً**: `scripts/fix_stuck_pending_reports.py` — يصنّف كل
سجل معلّق لثلاث فئات ويعالج كلاً بما يناسبه:
| الفئة | العلامة | الإجراء |
|---|---|---|
| 🅐 تقريره محذوف | «نوع الفحص: —» | يُحذف سجل المتابعة |
| 🅑 مرفوع ومكتمل العدد | رُفِع ولم يُغلَق | يُغلَق (completed) |
| 🅓 مرفوع **جزئياً** | expected>1 والعدد لم يكتمل | **لا يُمَسّ** — قرار بشري |
| 🅒 لا مرفقات ولا حذف | معلّق بحق | **لا يُمَسّ** |

⚠️ لا يحذف السكربت أي تقرير ولا أي مرفق — يعمل حصراً على جدول
`pending_reports`، ويبدأ بوضع فحص لا يغيّر شيئاً (`--apply` للتنفيذ).

**التحقق**: قاعدة معزولة فيها **الفئات الثلاث معاً** — التصنيف
صحيح 3/3، وضع الفحص لم يغيّر شيئاً (3 معلقة قبله وبعده)، وبعد `--apply`:
اليتيم حُذف · المرفوع أُغلِق · المعلّق بحق لم يُمَسّ · و**التقارير
(2/2) والمرفقات (1/1) سليمة بالكامل**. 183 اختباراً + فحص تسجيل حقيقي.

⚠️ **تصحيح لاحق للسكربت قبل تشغيله على الإنتاج**: النسخة الأولى كانت
تُغلق **كل** سجل له مرفقات قسراً (`uploaded_count = expected_count`). وهذا خطأ:
حالة تنتظر 3 فحوصات ووصل منها واحد كانت ستُغلَق وتختفي من المتابعة
**وهي ناقصة فعلاً** — وهو بالضبط ما بُنيت آلية `expected_count` لمنعه
(انظر `increment_pending_upload`). فُصِلت الفئة 🅓 وصار السكربت يعرض
التقدّم (`uploaded/expected`) لكل سجل ويترك الناقص لقرار الأدمن.

**نتيجة الفحص على الإنتاج** (15 سجلاً معلقاً): 🅐 يتيمان · 🅑 خمسة
مرفوعة · 🅒 ثمانية معلّقة بحق. والصف الذي أبلغ عنه المستخدم
(`report#3750` محمد يحيى، 7 أيام، «—») ظهر في الفئة 🅐 — **تأكيد مباشر
للتشخيص على بيانات حقيقية**.

📌 **خلاصة الأسباب الثلاثة للتعليق** (أقسام ٦ و٧): البحث المفتوح لا
يُغلق · حذف التقرير يخلّف يتيماً · (وزر التحديث كان يعمل بلا تغذية راجعة).

---

### 2026-08-10 — 🔧 تصحيح عدّاد السجلات الجزئية (`--reconcile`)

**نتيجة `--apply` على الإنتاج**: حُذف يتيمان وأُغلق سجل واحد. ووقعت
**4 سجلات** في الفئة 🅓 — أي أن فصلها منع إغلاق 4 حالات ناقصة فعلاً،
منها `report#3796` بتقدّم 1/3.

**عطب ثانوي كشفته البيانات الحقيقية**: `report#3764` يعرض `0/2` **ورغم
ذلك له مرفق واحد**. العدّاد صفر لأن عطب البحث المفتوح (القسم ٦) منع
`increment_pending_upload` من العمل أصلاً — فالعدّاد **لا يعكس الواقع**
في السجلات التي أصابها العطب نفسه. والأثر العملي أن رفع الفحص الناقص
اليوم يجعله `1/2` فيبقى معلَّقاً رغم اكتماله فعلياً؛ سيحتاج رفعتين لا واحدة.

**الإصلاح**: وضع `--reconcile` يضبط `uploaded_count` على عدد المرفقات
الفعلية بحدّ أقصى `expected_count`، **بلا إغلاق أي سجل وبلا حذف**، ويمسّ
الفئة 🅓 وحدها. فتُغلَق هذه الحالات تلقائياً وبشكل صحيح عند رفع الباقي.

⚠️ **وهو تقدير لا يقين**: عدد الملفات ≠ عدد الفحوصات (ملف PDF واحد قد
يضمّ فحصين). لذلك إن بلغ العدّاد `expected_count` بعد التصحيح، يظهر
السجل في الفئة 🅑 عند الفحص التالي **لقرار الأدمن** — لا إغلاقاً صامتاً.

🔑 **درس في تصميم الأعلام**: النسخة الأولى من `--reconcile` كانت **تكتب
فوراً بلا معاينة** — كسرٌ لعُرف السكربت نفسه (التشغيل بلا `--apply` معاينة
آمنة). كشفه الاختبار حين وجد لقطة «ما قبل التنفيذ» وقد تغيّرت أصلاً.
صار `--reconcile` يختار العملية و`--apply` ينفّذها:
`--reconcile` معاينة · `--reconcile --apply` تنفيذ.

**التحقق**: قاعدة معزولة تحاكي شكل الإنتاج بالضبط (عدّاد 0/2 ومرفق
واحد) + 4 حالات حافّة — 12/12 نجحت: المعاينة لا تكتب · `0/2 ← 1/2` ·
المطابق أصلاً لا يُمَسّ · الملفات الزائدة تُحَدّ عند `expected` · اليتيم
والمعلَّق بحق خارج النطاق · **لا سجل أُغلق** · المرفقات (5/5) والتقارير
(4/4) سليمة · idempotent · والبالغ `expected` انتقل إلى 🅑 لا أُغلق.
183 اختباراً + تسجيل حقيقي (170 معالجاً).

---

### 2026-08-11 — 🐛 الواصلون: لا يقبل عنوان السكن يدوياً (تصادم المجموعة 10)

**البلاغ**: خطوة عنوان السكن في الواصلين لا تنتقل عند الكتابة، بينما زر
«تخطي» يعمل. أُبلغ عنه سابقاً وشُخّص خطأً على أنه عطب `update.message` مع
الرسائل المعدَّلة؛ فعاد الآن لأن **التشخيص الأول كان خاطئاً**.

**ما حسم التشخيص هو ما لم يظهر في السجل**: مع كل محاولة إدخال يظهر
`[daily_patients.text] FIRED` ولا يظهر `[arrivals.text]` إطلاقاً — أي أن
معالِج الواصلين **لا يُستدعى أصلاً**، فالعطب ليس في منطقه. وأكّدته محاكاة
حيّة للدوال الحقيقية: الخطوة تنتقل والعنوان يُحفظ بلا خلل.

**السبب الجذري**: PTB ينفّذ **أول معالِج مطابق في كل مجموعة فقط** ثم ينتقل
للمجموعة التالية. وكان في `group 10` ثلاثة معالِجات: اثنان لـ
`pharmacy_finance` وواحد للواصلين. فيفوز `pharmacy_finance.handle_text_input`
(العام: `TEXT & ~COMMAND`) ويصير معالِج الواصلين **كوداً ميتاً**. وزر
«تخطي» يعمل لأنه `CallbackQuery` لا `Message` فلا يمرّ بهذا المسار.

🔁 **وهذه المرة الثالثة لنفس النمط**: `pharmacy_finance` كانت في `group 0`
فابتلعها معالِج `woundcare` العام، فنُقلت إلى `group 10` — وهي مجموعة
الواصلين الحصرية — فابتلعت هي الواصلين. **هروبٌ من تصادم أوقع في تصادم.**
وكان في [`general_services/routing.py:13`](modules/general_services/routing.py:13)
تحذير مكتوب صراحةً بأن group 10 حصرية للواصلين — ولم يمنع شيئاً، لأن
التعليقات لا تُنفَّذ.

**الإصلاح**: `pharmacy_finance` إلى `group 7` — حرّة، وبين مجموعات الرعاية
الصحية (2 و4 و6 و8) حيث لكل وحدة مجموعة مستقلة. الوجود في مجموعة أسبق لا
يحجب الواصلين: المجموعات **كلها** تُنفَّذ، والحجب لا يقع إلا داخل المجموعة
الواحدة.

🛡️ **حارس دائم**: `tests/test_handler_group_collisions.py` يبني
`Application` حقيقياً ويسجّل كل المعالِجات فعلياً، ثم يمرّر تحديثات نموذجية
(نص · صورة · مستند صورة) ويسقط إن قبِل معالِجان في مجموعة واحدة التحديث
نفسه — مع اختبار مخصَّص لبقاء معالِج نصوص الواصلين أوّلَ من يصله النص.
**تُحقّق من أن الحارس يفشل فعلاً**: بإعادة `group=10` سقط الاختباران، وبعد
الإصلاح نجحا.

**التحقق**: مسح شامل للتصادمات على كل المجموعات ⇐ صفر بعد الإصلاح (كان 1) ·
محاكاة حيّة تؤكد وصول النص إلى **كلتا** الوحدتين (الواصلون `group 10`
والمالي `group 7`) لأن كلاً منهما يرتدّ عند غياب جلسته · 187 اختباراً.

📌 **الدرس**: عند «زر لا يستجيب»، غياب سطر سجلّ متوقَّع دليلٌ إيجابي أقوى من
وجود سطر خطأ — يعني أن المعالِج لم يُستدعَ، فابحث في التسجيل والمجموعات لا
في منطق المعالِج.

---

### 2026-08-11 — 🧾 الإقامات: تنظيف شاشات الملف + مرفق الصورة الشخصية

أربع ملاحظات من المستخدم على بوت الإقامات:

**① «الأيام المتبقية لا تظهر»** — والحساب كان **سليماً**. المحاكاة الحيّة
أظهرت أن شاشة **ملف المريض** تعرضها صحيحةً؛ الناقصة كانت شاشة **أرشيف
المرضى**، وهي تبني السطر من `format_expiry_warning_inline` التي **تُرجع
فراغاً إذا تبقّى أكثر من 30 يوماً**. فالحالة الجديدة (46 يوماً) تظهر بلا أي
أيام — تبدو كعطل وهي في الحقيقة حالة سليمة بعيدة عن الانتهاء. صارت القائمة
تعرض `format_days_remaining` **دائماً**، وكل مريض في كتلة مستقلة.

⚠️ **درس تشخيصي**: البلاغ قال «في الحالة التي أضفتها» فتوجّهتُ أولاً إلى
مسار الحفظ، والحفظ سليم (`visa_expiry` يُكتب في `expiry_date`). الفارق كان
في **أي شاشة** يُنظر إليها — فتشغيل كل الشاشات على نفس البيان هو ما حسمها.

**② «رقم الإقامة لم أطلبه»** — أُزيل من ملف المريض ومن كتلة المرافقين.
العمود باقٍ في DB وفي مسار التجديد؛ العرض فقط توقّف.

**③ «الوثائق مربعات»** — كانت أربع وثائق مكدَّسة في سطر واحد بمربعات فقط،
والمربع الأبيض لا يميّز «غير مرفوع» عن «لا ينطبق». صارت سطراً لكل وثيقة مع
نصّ صريح: `✅ يوجد` / `⬜ لا يوجد`.

**④ مرفق جديد**: عمود `photo_file_id` + ترحيله، وزر **«📎 إضافة مرفق»** في
ملف المريض يفتح شاشة فيها الصورة الشخصية وفورم C. كان فورم C متاحاً فقط عبر
«الرفع والمتابعة ← إضافة خدمة»، وهو مسار غير بديهي من الملف.

🔑 الشاشة **مقصورة على المرفقين اللذين لا يجمعهما مسار آخر**؛ الجواز
والتأشيرة والإقامة تُرفع ضمن مساراتها (الإضافة والتجديد) فلا تُكرَّر هنا —
وإلا صار للحقل الواحد مصدران متباينان، وهو نمط الخطأ المتكرر في هذا السجل.
والصورة موصولة بـ«📎 إرسال الوثائق» وإلا بقيت محفوظة بلا طريق لإخراجها.

**التحقق**: تشغيل حيّ لمريض بمرافق — الأرشيف يعرض 46 يوماً (كان فارغاً) ·
المرافق 13 يوماً ⚠️ · الوثائق الخمس بنصّ صريح · رفع صورة عبر `_on_photo`
الحقيقية ⇒ `photo_file_id='PHOTO123'` في DB والشاشة تقلب إلى «✅ يوجد» ·
زر `rnu:attach_1` يطابق النمط المسجَّل. والترحيل مُختبَر على جدول
`res_profiles` قديم بلا العمود ⇒ أُضيف بلا كسر. 187 اختباراً + 170 معالجاً.

---

### 2026-08-11 (٢) — 🐛 الإقامات: ملف من الواصلين بلا تاريخ، وحالة «نشطة» لإقامة منتهية

بلاغ ثانٍ على **ملف أُنشئ تلقائياً من الوصول** (لا يدوياً — وهذا مسار مختلف
تماماً عن الذي فُحص في القسم السابق): «📅 تاريخ الانتهاء: —» و«⏳ الأيام
المتبقية: —» مع «📊 الحالة: ✅ نشطة» رغم أن الإقامة منتهية منذ يوليو.

**السبب الأول** — `create_profiles_from_arrival_batch` كانت تنسخ **انتهاء
الإقامة وحده** (`ap.residence_expiry`). والواصل الجديد غالباً **بلا إقامة
بعد** فيتخطّى الحقل، بينما **انتهاء التأشيرة مُدخَل** وهو ما يحكم مدة بقائه.
فيُنشأ الملف بتاريخ فارغ. وهذا **تباين بين مسارَي الإنشاء**: الإضافة اليدوية
(`save_manual_batch`) تستخدم `visa_expiry` أصلاً. صار المسار التلقائي يرجع
إلى التأشيرة عند غياب الإقامة — للمريض **وللمرافق** معاً.

✅ وافق المستخدم على هذا المنطق نصّاً: «إذا لم يضف الإقامة يعتمد على تاريخ
انتهاء التأشيرة، وإذا رفع الإقامة والتاريخ يُعتمد عليها». والنصف الثاني
مضمون أصلاً: [`renewal/models.py:51`](modules/residency/renewal/models.py:51)
يكتب `profile.expiry_date = new_expiry_date` (و‍:100 للمرافقين)، فتغلب
الإقامة التأشيرةَ فور رفعها.

**السبب الثاني** — الحالة تُكتب مرة واحدة عند الإنشاء (`"active"`) و**لا
يعيد أحد حسابها بمرور الوقت**، فملف أُنشئ اليوم يبقى «✅ نشطة» إلى الأبد.
أُضيفت `effective_status()` في `modules/residency/views.py` تشتقّ الحالة من
التاريخ عند العرض.

🔑 **وتمسّ الحالات الزمنية وحدها** (`active`/`expiring`/`expired`). حالات
**سير العمل** (`renewal_submitted` وأخواتها) لا تُمَسّ: هي وقائع حدثت فعلاً —
«تم رفع الأوراق» تبقى صحيحة سواء انتهت الإقامة أم لا، واستبدالها بـ«منتهية»
يمحو معلومة حقيقية. الاشتقاق عند العرض لا في العمود، فالعمود المخزَّن يظل
سجلّ سير العمل بلا تلوّث.

**وللمتراكم**: `scripts/backfill_residency_expiry.py` يملأ `expiry_date`
الفارغ من التأشيرة، ولا يمسّ أي صف له تاريخ بالفعل، ويسرد ما يتعذّر تعبئته
لإدخاله يدوياً عبر زر «📅 تعديل تاريخ الانتهاء».

**التحقق**: إعادة إنتاج حالة المستخدم عبر `save_arrival_batch` ثم
`create_profiles_from_arrival_batch` الحقيقيتين (تأشيرة 20/07/2026 والإقامة
متخطّاة) ⇒ التاريخ ظهر، و«انتهت منذ 21 يوم»، والحالة «❌ منتهية» بدل
«نشطة» · 5/5 حالات لـ`effective_status` بما فيها عدم مسّ سير العمل ·
السكربت فحصاً ثم `--apply` على صف فارغ ⇒ عُبِّئ. 187 اختباراً + 170 معالجاً.

---

### 2026-08-11 (٣) — 🖼️ الإقامات: مصدر واحد لفورم C والصورة، وضبط الصورة على 4×6

طلب المستخدم فحصين: **①** أن يبقى فورم C والصورة الشخصية يُضافان من زر
«➕ إضافة خدمة» **حصراً** لا من زر آخر مضاف سابقاً في ملف المريض. **②** هل
يوجد زر لرفع الصورة الشخصية، وهل تُحفظ بمقاس 4×6.

**① أُزيل زرّ «📎 إضافة مرفق»** الذي أُضيف في الجلسة السابقة لملف المريض —
كان يفتح مساراً ثانياً للمرفقين نفسيهما، وهو بالضبط ما حذّر منه المستخدم:
مدخلان لحقل واحد. صار المسار الوحيد: «📤 الرفع والمتابعة ← ➕ إضافة خدمة».

**② لم يكن هناك زر لرفع الصورة الشخصية إطلاقاً** — رغم أن العمود
`photo_file_id` والدالة الخلفية `save_patient_photo` أُضيفا في الجلسة
السابقة، لم يُربَط بهما زرّ في شاشة «➕ إضافة خدمة» (كان فورم C وحده). أُضيف
زر «🖼️ رفع صورة شخصية» بجانب فورم C، مع سطر حالة («✅ مرفوعة» / «⬜ غير
مرفوعة») في نفس الشاشة.

**والمقاس**: لم يكن هناك أي ضبط — أياً كانت الصورة تُحفَظ كما هي. أُضيفت
`modules/residency/uploads/photo_processing.py::resize_to_4x6()`: تنزيل
الأصل ← قصّ من المنتصف لمطابقة نسبة 4:6 (لا تمديد يشوّه الوجه) ← تصغير
دقيق إلى 472×709 بكسل (= 4×6 سم عند 300 نقطة/إنش، دقة طباعة) ← إعادة رفع
إلى تيليجرام للحصول على `file_id` يمثّل **النسخة المضبوطة لا الأصل
الخام**. فشل المعالجة لا يُسقط الحفظ: يُحفظ الأصل ويُخبَر المستخدم صراحةً
أن الضبط لم يتم، بدل ضياع الصورة كلياً أو فشل صامت.

**التحقق**: 4 نِسَب مختلفة (عريضة/طويلة/مربّعة/أصغر من الهدف) ⇒ كلها
472×709 بالضبط = 4.00×6.00 سم عند 300dpi · تشغيل حيّ لـ`_on_photo` ببوت
وهمي يتحقق أن **البايتات المُرسَلة فعلاً بالمقاس المضبوط** (لا الأصل) ⇒
`photo_file_id` المحفوظ في DB هو ملف إعادة الرفع لا الملف الأصلي. Pillow
مثبَّتة أصلاً (`requirements.txt`) فلا تبعية جديدة. 187 اختباراً + 170 معالجاً.

---

### 2026-08-11 (٤) — 📄 الإقامات: ملف PDF يعرض صور الوثائق لا جدول بيانات

طلب المستخدم تحويل ملف «📄 ملف PDF» من تقرير بيانات (حالة/تواريخ/جدول
مرافقين/سجل أحداث بنصوص) إلى **حزمة وثائق** تُظهر الصور نفسها: جواز، تأشيرة،
إقامة (آخر نسخة مرفوعة فقط — لا كل النسخ)، وفورم C، ثم نفس المجموعة لكل
مرافق بالترتيب.

**إعادة بناء `services/residency_profile_pdf.py`**: حُذفت جداول الحالة
والمرافقين والسجل (لا علاقة لها بـ"الوثائق")، وصار الملف يبني قسماً لكل
شخص (المريض ثم كل مرافق، بفاصل صفحة بينهم) يضمّ صورة كل وثيقة متوفرة تحت
تسمية واضحة. الإقامة **آخر نسخة تلقائياً** بلا أي منطق إضافي — العمود
`latest_residency_file_id` لا يخزّن أصلاً غير أحدث ملف مرفوع (يُستبدَل عند
كل تجديد)، ففورم C وحده للمريض (وثيقة عائلية) ولا يتكرر مع كل مرافق.

**معالجة الصور**: `_prepare_image_flowable()` تصحّح دوران EXIF (صور
الجوال كثيراً ما تصل بدوران خاطئ بصرياً رغم بيانات "معتدلة")، وتحسب مقاساً
يحافظ على النسبة الأصلية دون تمدّد يشوّه محتوى الوثيقة، بحدّ تكبير 3x
لصورة صغيرة الدقة حتى لا تظهر مبكسلة.

**فشل جزئي لا كلي**: تحميل صورة واحدة (`_download_document_images` في
`documents.py`) أو معالجتها قد يفشل بلا إسقاط الحزمة كاملة — تُستبعَد تلك
الوثيقة وحدها ويُسجَّل تحذير، فيصل المستخدم حزمة ناقصة بدل «❌ فشل إنشاء
ملف PDF» على خلل في وثيقة واحدة من عشر.

🔑 **فصل الطبقات**: تحميل الصور I/O شبكي فبقي async في `documents.py`
(يملك `bot`)، وبناء PDF حسابي فبقي متزامناً صرفاً في `residency_profile_pdf.py`
(يُنفَّذ في executor كسابقه) — لا يعرف عن كائن البوت شيئاً، فقط
`images: dict[file_id, bytes]` جاهزة مسبقاً.

⚠️ **لا إيموجي في نصوص PDF**: الخط العربي المضمَّن نصّي بلا حروف تصويرية،
وreportlab لا يرسم إيموجي ملوَّنة — استُبعدت من كل تسميات هذا الملف، خلافاً
لنصوص شاشات البوت في تيليجرام حيث تُعرض بلا مشكلة.

**التحقق**: `_prepare_image_flowable` — نسبة عرض/ارتفاع محفوظة لصورة أفقية
وعمودية (فرق < 0.01) وضمن الحدود القصوى، وحدّ التكبير 3x فعّال · بناء PDF
كامل بمريض (4 وثائق) ومرافقَين (أحدهما بلا وثائق إطلاقاً، والآخر بصورة
تأشيرة **تالفة عمداً**) ⇒ 146KB، رأس `%PDF` سليم، 4 صفحات، **لا استثناء
رغم الصورة التالفة** · بناء بلا صور محمَّلة إطلاقاً ⇒ لا يسقط · تشغيل
`send_patient_pdf` الحقيقية ببوت وهمي (تحميل يفشل لملف واحد عمداً) ⇒
نفس حجم الملف الناجح تماماً — يثبت أن فشل التحميل الجزئي لا يُغيّر ناتج
البقية. 187 اختباراً + 170 معالجاً.

---

### 2026-08-11 (٥) — 🚫 الإقامات: مصدر الأسماء الوحيد هو الواصلون

طلب المستخدم صراحةً: «أي اسم ينشره الواصلون هو الذي يظهر في قائمة الإقامات،
لا يجلب الإقامات من الأدمن». الوحدة كانت تسمح بمصدرين — تلقائي من نشر
الواصلين (`source="arrivals"`) ويدوي عبر زر «➕ إضافة مريض جديد»
(`source="manual"`، الدالة `save_manual_batch`) — فيمكن لاسم مكتوب يدوياً
أن يدخل قائمة الإقامات ويتضارب مع الاسم الذي ينشره الواصلون لنفس الشخص.

**① أُزيل مدخل الإضافة اليدوية من الواجهة** في موضعين: زر «➕ إضافة مريض
جديد» في القائمة الرئيسية، وزر «➕ إضافة جديد» في شاشة الأرشيف — كلاهما
كان يفتح `rna:start`. الكود الخلفي (`save_manual_batch` وخطوات الويزارد في
`profiles/flow.py`) **لم يُحذف**، فقط لم يعد له مدخل من الواجهة — إن أراد
المستخدم حذفه نهائياً لاحقاً فهو أثر ميت واضح المعالم.

**② فُلترت كل استعلامات "القائمة/التصفح/التنبيه"** لتستبعد
`source="manual"` — لا يكفي إغلاق الباب الأمامي وحده، فسجلات يدوية سابقة
(إن وُجدت) كانت ستبقى ظاهرة في كل شاشة تعرض عدة ملفات دفعة واحدة:
`get_profiles_page` وَ`search_profiles` (الأرشيف والبحث) ·
`get_expiring_soon` وَ`get_passport_expiring_soon` وَ`get_dependent_pending`
(المتابعة والتنبيه اليومي — `residency_alerts_service.py` يستدعيهما
فيرِث الاستبعاد تلقائياً) · `get_papers_entries` وَ`get_hub_counts` (وحدة
الرفع والمتابعة، بما فيها منتقي "➕ إضافة خدمة" الذي يعيد استخدام
`get_profiles_page`) · `collect_archive_rows` (يغذّي تصديرَي Excel وPDF
كليهما). **لم تُفلتَر** الاستعلامات بمعرّف ملف مباشر (`get_profile_by_id`،
مسار التجديد بمعرّف) — فتح ملف موجود فعلاً بمعرّفه ليس "قائمة".

🔑 **حارس NULL مطلوب صراحةً**: `source != "manual"` وحدها تُسقط أي صف
`source IS NULL` من SQL (NULL غير معروف لا false) — وعمود `source` قيمته
الافتراضية `"arrivals"` في `db/models.py`، فسجل قديم بلا قيمة صريحة هو
اسمياً من الواصلين ويجب أن يبقى ظاهراً. كل موضع استخدم
`or_(source != "manual", source.is_(None))` — نفس الفخّ ونفس الحل
المسجَّلين سابقاً في هذا الملف لعمود `patient_type`.

**مرافقو الملف اليدوي**: استعلاما `get_expiring_soon` وَ
`get_passport_expiring_soon` يفحصان `ResidencyCompanion` مباشرةً بلا مرور
بالملف الأصل، فأُضيف `.join(ResidencyProfile)` مع نفس الفلتر — وإلا ظهر
مرافق شخص يدوي في تنبيه رغم اختفاء الشخص نفسه من القائمة.

**التحقق**: قاعدة معزولة بأربعة صفوف — واصل، يدوي، **قديم بمصدر NULL**،
ويدوي بحالة `dependent_pending` مع مرافق تابع — 9/9 فحوصاً: القائمة تستبعد
اليدوي وتُبقي NULL · البحث لا يجد الاسم اليدوي ويجد اسم الواصل · كل دوال
المتابعة الثلاث تستبعد اليدوي **ومرافقه** · عدّاد "المرافقون المعلقون"
صار صفراً (كان سيظهر 1 لولا الفلتر) · تصدير الأرشيف يستبعده. وتحقّق منفصل
أن الزرّين اختفيا فعلياً من كلتا الشاشتين. 187 اختباراً + 170 معالجاً.

---

### 2026-08-11 (٦) — 📁 الإقامات: حذف «الرفع والمتابعة» وتجميع كل شيء في ملف المريض

طلب المستخدم: لا حاجة لزر «📤 الرفع والمتابعة» المستقل — كل شيء عبر
«📁 أرشيف المرضى»، وعند فتح ملف مريض يريد أن يتابع من هناك مباشرة: هل تمّ
التقديم؟ هل الملف معلَّق بسبب مرافق لم يُستكمَل؟ وهل يمكن إضافة مرافق؟

**إعادة هيكلة تنقل لا تخفي فقط**: أُزيل زرّ «📤 الرفع والمتابعة» من القائمة
الرئيسية، وحُذفت شاشاته الأربع بالكامل من `modules/residency/uploads/{views,flow,repository}.py`
(`build_uploads_hub`, `build_papers_list`, `build_service_patient_list`,
`build_service_menu`, ودوالّ الاستعلام `get_papers_entries`/`get_hub_counts`
التي كانت تغذّيها فقط) — لا زر يفتحها فحذفها بلا رصيد إبقاء دون فائدة.

**① متابعة تقديم الأوراق صارت زراً على ملف المريض نفسه**: مصدر الحقيقة
لا يزال `PAPERS_ADVANCE` في `constants.py` كما كان، لكن الزر يُستدعى مباشرةً
بمعرّف الملف (`rnu:adv_{id}` / `rnu:undo_{id}`) بدل المرور بقائمة، وبعد
التنفيذ **يُعاد رسم ملف المريض نفسه لا قائمة** — المستخدم يبقى حيث كان.
هذا استبدل أيضاً زرّ «📋 تم التقديم» (`rnf:submitted_`) القديم الذي اكتُشف
أثناء هذا العمل أنه **معطوب فعلياً**: كان يكتب الحالة `renewal_submitted`
دائماً بلا مراعاة الحالة الحالية أو المرافقين، ثم يُعيد رسم شاشة «⏰
المتابعة» بدل ملف المريض الذي ضغط منه المستخدم أصلاً — مسارٌ ثانٍ مواز
لـ`PAPERS_ADVANCE` نفسه، وهو بالضبط نمط الانحراف المتكرر في هذا السجل.

**② `dependent_pending` يظهر ويُحلّ من نفس المكان**: يبقى وسمه القياسي
«⏳ مرافقون معلقون» على سطر الحالة، ويظهر زر «📋 استكمال المرافقين
المعلَّقين» **فقط في هذه الحالة تحديداً**، يعيد استخدام `rnf:complete_`
الموجود أصلاً (يُحوِّل مباشرة لمسار `rnr:` — لم يحتَج تعديلاً).

**③ «➕ إضافة مرافق» — قدرة جديدة كلياً**: لم يكن هناك أي مسار لإضافة
مرافق لملف موجود بعد إنشائه (المرافقون كانوا يُنشأون فقط مع مريضهم عند
الوصول). أُضيفت جلسة خفيفة بسياق واحد لا dataclass (اسم → تاريخ تأشيرة
اختياري بتقويم → جواز اختياري → تأشيرة اختيارية → حفظ)، مبنية على
`shared.uploads.collector` نفسه المُثبَت في مسارات فورم C/الصورة/التجديد.
التقويم استخدم بادئة `accal:` منفصلة تماماً عن `cal_pick:`/`cal_prev:`
العامة التي يستخدمها ويزارد الإضافة اليدوية المعطَّل (بلا بادئة مخصَّصة)،
تجنّباً لالتباس جلستين تشتركان في نفس `build_calendar()`. الإلغاء في أي
خطوة يُسقط المسودّة بالكامل (لا حفظ جزئي)؛ التخطي يتابع للخطوة التالية.
`add_companion_to_profile()` الجديدة في `profiles/models.py` تكتب
`ResidencyCompanion` + سجل `ResidencyUpdate` تدقيقي، و`expiry_date` يُعبَّأ
من التأشيرة مباشرةً (نفس رجوع الملف الرئيسي عند غياب الإقامة).

**④ فورم C والصورة الشخصية**: زرّاهما انتقلا من شاشة «➕ إضافة خدمة»
المحذوفة إلى ملف المريض مباشرة (`rnu:formc_`/`rnu:photo_` — نفس الأفعال
الخلفية بلا تغيير)، وشاشتا التأكيد بعد الرفع صارتا تعودان لملف المريض
(`rna:view_`) بدل الهَب المحذوف.

**⑤ «⏰ المتابعة» و«⏳ المرافقون المعلقون»** (شاشتا قائمة عابرة للمرضى، لا
معلومة عن مريض واحد) انتقل زرّاهما إلى شاشة الأرشيف نفسها بدل الاختفاء —
كانتا مفيدتين لترتيب الأعجل أولاً، وحذفهما كان سيفقد هذه الفائدة رغم أن
ملف المريض يحمل الآن نفس المعلومة فردياً.

**التحقق**: محاكاة حيّة كاملة بمريض حقيقي عبر مسار الواصلين — 41/41 فحصاً
عبر 3 سيناريوهات: (أ) القائمة الرئيسية زرّ واحد، الأرشيف يحمل زرّي
المتابعة/المعلقين بالعدّ الصحيح، ملف مريض بحالة `active` يعرض الزر الصحيح
بلا تراجع؛ تقدّم فعلي إلى `renewal_submitted` ثم `extension_received`
(ظهور زر التراجع وتبدّل نص الزر إلى «تسجيل الإقامة الجديدة» تلقائياً من
`PAPERS_ADVANCE`) ثم تراجع فعلي — **كل الأفعال أعادت رسم ملف المريض لا
قائمة** · إضافة مرافق كاملة (اسم حقيقي، تخطي تاريخ، تخطي جواز، تخطي تأشيرة)
⇒ حُفظ فعلاً في `ResidencyCompanion` · إلغاء منتصف الرفع ⇒ لا حفظ جزئي ·
`dependent_pending` يُظهر زر الاستكمال حصراً في هذه الحالة؛ (ب) مسار
التقويم الفعلي: زر يوم حقيقي ببادئة `accal:` (لا تصادم) ⇒ التاريخ يُحفظ
وينتقل تلقائياً لرفع الجواز؛ (ج) رفع فورم C فعلي ⇒ `form_c_file_id` محفوظ
والزر يستهدف `rna:view_` لا الهَب المحذوف، والإلغاء يعيد رسم ملف المريض.
187 اختباراً + 170 معالجاً (بلا تغيير في العدد — إعادة توجيه منطق داخل
مجموعات موجودة، لا معالِجات جديدة).

---

### 2026-08-11 (٧) — 🪪 تجديد الإقامة: سؤال كل مرافق باسمه + استئناف ذكي + تبسيط

طلب المستخدم إعادة تصميم زر «🪪 تجديد الإقامة»: بعد رفع وثيقة المريض،
يُسأل عن كل مرافق **باسمه** على حِدة (لا سؤال جماعي) — هل تم تجديد إقامته؟
نعم ⇒ تاريخ ثم وثيقة؛ لم تجهز بعد ⇒ زر بهذا النصّ يتابعها ويمكن إكمالها
لاحقاً «من نفس الزر» أو من زر مرافقين معلَّقين منفصل (طلب مني تحديد الصحيح).
ولا حاجة لسؤال رقم الإقامة أو الملاحظات — فقط تاريخ + وثيقة.

**فحص الكود قبل التصميم كشف** أن معظم الآليات موجودة فعلاً لكن مُجمَّعة
خطأً: سؤال المرافقين كان بوابة جماعية واحدة («يوجد N مرافق، تحدّث الآن أم
تخطّي مؤقتاً؟») بدل سؤال فردي؛ وزر «📋 استكمال المرافقين المعلَّقين»
(أُضيف الجلسة الماضية) كان يعمل بضبط `companions_only=True`، **لكن**
`get_profile_with_companions` كان يعيد **كل** المرافقين بلا استثناء —
فالاستئناف كان سيُعيد السؤال عن مرافق أُنجز فعلاً في تمريرة سابقة (حالته
`issued`). وزر «🪪 تجديد الإقامة» نفسه، إن ضُغط مباشرة على ملف
`dependent_pending`، كان يُعيد طلب تاريخ ووثيقة المريض من الصفر رغم
إنجازهما بالفعل — خطأ حقيقي لو ضُغط الزر «الخطأ» عن قصد.

**① سؤال فردي لكل مرافق**: `build_renewal_companions_prompt` (البوابة
الجماعية) حُذفت، واستُبدلت بـ`build_renewal_c_ready_prompt` جديدة: «هل تم
تجديد إقامة *{الاسم}*؟» مع زرَّي «✅ نعم» و«❌ لم تجهز بعد» — يُعرض تلقائياً
بعد وثيقة المريض مباشرة (أو بعد إنهاء كل مرافق، للتالي)، لا بعد بوابة
منفصلة.

**② «من نفس الزر» — الخيار الصحيح، والفجوة أُصلحت**: كلا المدخلين
(«🪪 تجديد الإقامة» مباشرة، أو «📋 استكمال المرافقين المعلَّقين») يعملان
الآن بشكل صحيح ومتطابق: `rnr:start_` يتحقق من حالة الملف نفسها
(`profile.status == "dependent_pending"`) ويُفعِّل `companions_only`
تلقائياً بغضّ النظر عن الزر الذي فتحه — فلا حاجة لتذكّر أيّهما "الصحيح"،
كلاهما يتصرّف بذكاء. و`get_profile_with_companions()` صار يقبل
`pending_only=True` فيستبعد أي مرافق `status="issued"` من القائمة، فلا
يُعاد سؤال من أُنجز فعلاً. **لا يُطبَّق هذا الاستبعاد في دورة تجديد جديدة
كاملة عمداً** — مرافق أُنجز في دورة سابقة يحتاج تجديداً جديداً في دورة
تالية، فالاستبعاد مقصور على استئناف **نفس** الدورة المتوقفة.

**③ تبسيط**: `STEP_RESIDENCY_NUMBER`/`STEP_C_RESIDENCY_NUMBER`/
`STEP_NOTES` أُزيلت من التدفق بالكامل — لا تُسأل الآن إطلاقاً، فقط تاريخ
+ وثيقة لكل شخص. شاشة المراجعة فقدت سطري «رقم الإقامة» و«الملاحظات».
حُذف معهما معالج نصوص «رقم الإقامة/الملاحظات» الميت في `profiles/flow.py`
(كان يتحقق من خطوات لم تعد موجودة أصلاً).

🔑 **كود ميت مكتشَف أثناء العمل**: `build_renewal_document_prompt`
وَ`build_renewal_c_document_prompt` كانتا مُستورَدتين في `flow.py` بلا أي
استدعاء فعلي — رفع الوثيقة يمرّ دائماً عبر شاشة `shared.uploads.collector`
الخاصة به لا عبر هاتين الدالتين، على الأرجح أثرٌ من قبل ربط الوحدة
بالمُجمِّع المشترك. حُذفتا مع الأزرار الميتة المرتبطة بهما
(`doc_done`/`skip_doc`/`c_doc_done`/`skip_c_doc` بقيت في المرسِل بلا ضرر
— لا زر حقيقي يُنتجها).

**التحقق**: قاعدة معزولة بمريض ومرافقَين — 27 فحصاً بمحاكاة حيّة كاملة:
تاريخ ← وثيقة (بلا سؤال رقم إقامة) ← سؤال المرافق الأول **باسمه الفعلي**
← نعم ← تقويم ← وثيقة ← سؤال المرافق الثاني تلقائياً ← لم تجهز بعد ←
**مراجعة مباشرة بلا ملاحظات** (سطرا الملاحظات/رقم الإقامة غائبان فعلياً)
← تأكيد ← فحص DB الفعلي: المريض `dependent_pending`، المرافق الأول
`issued`، الثاني بلا تغيير · ثم **استئناف بالضغط المباشر على «🪪 تجديد
الإقامة» لا زر الاستكمال** ⇒ لم يُعِد سؤال المريض، قفز مباشرة للمرافق
المتبقي وحده (الأول مُستبعَد لأنه `issued`) ⇒ إكمال ⇒ فحص DB: كلاهما
`issued`. وسيناريوهان إضافيان (5 فحوصاً): مريض بلا مرافقين ⇒ مراجعة
مباشرة بعد الوثيقة، واستئناف عبر `rnf:complete_` (الزر المخصَّص) يعمل
بنفس الصحة. المجموع 32/32. 187 اختباراً + 170 معالجاً.

### 2026-08-12 — 📋 الإقامات: تتبّع الطلبات الناقصة عند التقديم + تذكير يومي

**الطلب**: زر «📤 تم الرفع» (النقلة الأولى `active/expiring → renewal_submitted`)
كان ينشر للمجموعة بلا أي تحقّق من اكتمال الأوراق. طُلب: (١) قبل النشر
سؤال «هل تم رفع كامل الأوراق؟» نعم/لا، (٢) عند «لا» إدخال يدوي لوصف
الطلب الناقص، (٣) تذكير يومي للمجموعة الساعة 5:00 مساءً بكل طلب لم
يُرفَع بعد، (٤) إمكانية تسجيل طلب ناقص جديد لاحقاً (لا فقط عند التقديم)،
(٥) إمكانية رفع مستند الطلب لاحقاً مع نشر «تم رفع الطلب، جاري انتظار
الإقامة».

**⚠️ انحراف عن الصياغة الحرفية، مقصود**: طلب المستخدم وصف الوصول عبر «زر
المتابعة» بمنتقي مريض منفصل. بدل ذلك، وُضعت كل أفعال الطلب الناقص
(تسجيل جديد / رفع لإغلاق) كأزرار مباشرة في **ملف المريض نفسه**
(`build_profile_detail`)، اتّساقاً مع فلسفة «كل شيء عبر 📁 أرشيف المرضى»
التي أرساها المستخدم في جلسة 2026-08-11 (٦) عند حذف شاشة «الرفع
والمتابعة» كلياً. بناء منتقي مريض ثانٍ كان سيكرّر شاشة موجودة أصلاً
(البحث/الأرشيف) بمسار مختلف. **يُرجى تأكيد هذا الاختيار أو طلب تعديله.**

**التصميم**:
- جدول جديد `ResidencyMissingItem` (`db/models.py`): `profile_id`,
  `description`, `status` (pending/resolved), `file_id`, `created_by`,
  `created_at`, `resolved_by`, `resolved_at`. جدول جديد بالكامل فلا
  ترحيل مطلوب — `Base.metadata.create_all` يُنشئه تلقائياً.
- بوابة الاكتمال مقيَّدة بالنقلة الأولى فقط (`PAPERS_ADVANCE[status][1]
  == "renewal_submitted"`) — النقلات اللاحقة (استلام التمديد/تسجيل
  الإقامة) تبقى فورية كما كانت، لأن طلب المستخدم «عندما أقدّم الإقامة»
  يخصّ فعل التقديم تحديداً لا كل مرحلة.
- «لا» **لا توقف النقلة** — الأوراق قُدِّمت فعلاً وإن كانت ناقصة، فالحالة
  تتقدّم إلى `renewal_submitted` بينما يُسجَّل الطلب الناقص بالتوازي (قرار
  متعمَّد: «قدّمت لكن ناقص» حالة واقعية مختلفة عن «لم أقدّم شيئاً»).
- زرّا «📝 تسجيل طلب ناقص» (دائماً) و«📎 رفع الطلب المعلَّق» (طلب واحد) /
  «📎 رفع طلب معلَّق (N)» ← منتقي (أكثر من طلب) في `build_profile_detail`
  نفسه، أسفل قسم جديد يعرض الطلبات المعلَّقة إن وُجدت.
- `services/residency_missing_items_service.py` — نفس بنية
  `residency_alerts_service.py` حرفياً: `None` من `build_missing_items_message()`
  إذا لا يوجد شيء معلَّق (لا رسالة يومية فارغة)، إرسال للمجموعة ولكل
  مستخدم يملك صلاحية `residency` مستقلاً بمحاولة/استثناء لكل مستلم.
  مجدولة عبر `app.job_queue.run_daily` الساعة 17:00 (`app.py`).
- سياق نصّي خفيف بلا dataclass (`_CTX_MISSING_ITEM` في
  `profiles/flow.py`) — يُستخدَم من مكانين: زر «❌ لا، يوجد نقص» في
  `uploads/flow.py` (يضبط `advance_stage=True`) وزر «📝 تسجيل طلب ناقص»
  المستقل (`advance_stage=False`) — نفس معالج النص الواحد في
  `profiles/flow.py::_handle_text_input` يفرّق بينهما بهذا العلم فقط.

**التحقق**: `python -m py_compile` نظيف على 8 ملفات · 187 اختباراً +
170 معالجاً حقيقياً (`register_all_handlers` على `Application` فعلي) ·
محاكاة حيّة بقاعدة معزولة (10 فحوصاً): تسجيل مريض بمرافق ← بوابة
الاكتمال تُفعَّل فقط عند `active→renewal_submitted` ← مسار «لا»: تسجيل
الطلب **والنقلة تتقدّم معاً** ← `build_profile_detail` يعرض العدّاد
والوصف والزر الصحيح ← الرسالة اليومية تتضمّن الاسم والوصف ← تسجيل طلب
ثانٍ مستقل لا يمسّ الحالة ← الزر يتحوّل لمنتقي عند طلبين ← إغلاق الطلب
الأول عبر `resolve_missing_item` يُبقي الثاني معلَّقاً ← إغلاق الأخير
يُعيد الرسالة اليومية إلى `None` ← معرّف طلب غير موجود يعيد `None` بأمان
← النقلة التالية (`renewal_submitted→extension_received`) تمرّ فوراً
بلا بوابة كما هو متوقَّع.

### 2026-08-12 (٢) — 🪪 الإقامات: إرفاق ملف الإقامة الصادر مع إعلان النشر

**الطلب**: عند نشر «تم إصدار إقامة جديدة» للمجموعة (تأكيد مسار `rnr:`)،
يُرفَق ملف/صورة الإقامة الفعلي الذي رُفع للتو مع الإعلان، لا نصّاً فقط.

**التنفيذ**: `report_publisher.publish_event()` صار يقبل `attachments:
list[tuple[file_id, caption]]` اختيارياً — يُرسَل كل مرفَق عبر
`send_photo` بعد نصّ الإعلان (لكل من الأدمن حين `GS_NOTIFY_ADMINS` مفعَّل
وللمجموعة)، بمحاولة/استثناء منفصلة لكل مرفَق فلا يُسقط فشل واحد الباقي
ولا النصّ نفسه. الاستدعاءات القديمة بلا `attachments` غير متأثرة إطلاقاً
(معامل اختياري بقيمة افتراضية `None`).

في `renewal/flow.py` (فرع `confirm`): تُبنى قائمة المرفقات من
`session.document_file_id` (وثيقة المريض المرفوعة في هذه الدورة) + وثيقة
كل مرافق أُنجزت إقامته فعلاً (`completed_companions` حيث `skipped=False`
و`file_id` موجود) — لا المرافقون المؤجَّلون (`dependent_pending`)، فهؤلاء
لم تصدر وثيقتهم بعد.

**التحقق**: `python -m py_compile` نظيف · 187 اختباراً + 170 معالجاً ·
محاكاة ببوت وهمي (stub) يسجّل الاستدعاءات: تأكيد بمرفقين (مريض + مرافق)
⇒ رسالة نصّية واحدة للمجموعة ثم مرفقان بالترتيب الصحيح والتسمية الصحيحة؛
واستدعاء بلا `attachments` ⇒ نصّ فقط كالسابق تماماً (لا كسر للسلوك
القديم).

### 2026-08-12 (٣) — 📊 الإقامات: خياران قبل تصدير PDF/Excel (نطاق الأعمدة + وجهة الإرسال)

**الطلب**: زرّا «📄 PDF» و«📊 Excel» في شاشة الأرشيف كانا يُصدّران فوراً بلا
أي خيار. طُلب سؤالان قبل الإنشاء: (١) هل يتضمّن الملف تاريخ انتهاء الجواز
أم الإقامات فقط، (٢) هل يُنشَر في المجموعة أم يُرسَل للمستخدم فقط للاطلاع.

**التنفيذ**: بوّابتان متسلسلتان في `profiles/flow.py`، بلا أي حالة في
`context.user_data` — كل الاختيارين مُشفَّران في `callback_data` نفسها
(نفس نمط `cal_pick:`/`add_comp_cal_` القائم في الملف):
- الأزرار في `build_archive_list` صارت `export_ask_{pdf|xlsx}` (بدل
  `export`/`export_pdf` القديمين اللذين حُذفا).
- `export_ask_{fmt}` ⇒ `build_export_passport_prompt` — «مع تاريخ الجواز»
  أم «الإقامات فقط» ⇒ `export_dst_{fmt}_{y|n}`.
- `export_dst_{fmt}_{pas}` ⇒ `build_export_destination_prompt` — «نشر في
  المجموعة» أم «لي فقط» ⇒ `export_go_{fmt}_{pas}_{group|user}`.
- `export_go_{fmt}_{pas}_{dest}` ينشئ الملف فعلياً ويرسله: `group` عبر
  `bot.send_document` إلى `RESIDENCY_GROUP_ID` فقط (لا نسخة للمستخدم
  الطالب)، `user` عبر `query.message.reply_document` كالسلوك القديم تماماً.

`build_residency_archive_export()`/`build_residency_archive_pdf()`
(وبانيا Excel/PDF تحتهما) صارا يقبلان `include_passport: bool = True` —
عند `False` يُحذَف عمودا «انتهاء الجواز»/«المتبقّي» من الرأس والصفوف
والتلوين في كلا المخرجين، بلا تغيير على `collect_archive_rows()` نفسها
(لا تزال تجلب `pas_expiry` دائماً؛ الإسقاط عرضٌ فقط).

**التحقق**: `python -m py_compile` نظيف على 4 ملفات · 187 اختباراً +
170 معالجاً · محاكاة حيّة بقاعدة معزولة: بناء Excel/PDF فعليَّين بعمودي
جواز وبدونهما والتأكد من فرق عدد الأعمدة (`openpyxl.load_workbook`
لقراءة الرأس الفعلي) وأرشيف فارغ يعيد `(None, 0)` في الحالتين · محاكاة
مُرسِل كاملة بـ`_dispatch_inner` الفعلي وكائنات bot/query وهمية تسجّل كل
استدعاء: `export_ask_pdf` ⇒ شاشة السؤال الأول بالأزرار الصحيحة ⇐
`export_dst_pdf_n` ⇒ شاشة السؤال الثاني ⇐ `export_go_pdf_n_user` ⇒ ملف
PDF فعلي بلا عمود الجواز يصل للمستخدم فقط (لا استدعاء لـ`send_document`
على البوت) ⇐ `export_go_xlsx_y_group` ⇒ ملف Excel بعمود الجواز يُرسَل
لمجموعة `RESIDENCY_GROUP_ID` فقط (لا نسخة للمستخدم) — وفحص شامل لكل الثماني
تركيبات `{pdf,xlsx}×{y,n}×{group,user}` تُحلَّل بأمان (لا شرطات سفلية
داخل أي رمز فيها فتُقسَّم بأمان بـ`split("_")`).

### 2026-08-12 (٤) — ✏️ الواصلون: التعديل قبل النشر — كان غائباً تماماً

**الطلب**: "التعديل قبل النشر في زر الواصلين لا يوجد" — شاشة مراجعة دفعة
الوصول (`build_review`) كانت تعرض فقط «📢 نشر التقرير» و«❌ إلغاء»؛ لا وسيلة
لتصحيح حقل واحد أُدخل خطأً سوى إلغاء الدفعة **بأكملها** والبدء من الصفر.
سُئل المستخدم عن مستوى التعديل المطلوب ضمن دفعة قد تضم عدة مرضى ومرافقين
بعشرة حقول تقريباً لكل شخص، واختار: **تعديل كل حقل بمفرده** (نمط
pharmacy_finance الموجود، لا إعادة فتح الشخص كاملاً ولا تعديل عناوين فقط).

**التصميم — إعادة استخدام الآلة الخطية القائمة بدل بناء مسار موازٍ**:
كل حقل في الدفعة له أصلاً شاشة عرض (`_show_p_*`/`_show_c_*`) ومعالج حفظ
(في `cal_pick`/زر تخطٍّ/معالج النص/معالج الصور) — التعديل لا يبني أياً من
هذه من جديد، بل:
- ثلاثة حقول جديدة في `ArrivalSession` (`session.py`): `edit_from_review`،
  `edit_patient_index`، `edit_companion_index`.
- عند فتح حقل من المراجعة: يُحمَّل `completed_patients[i]` **مؤقتاً** في
  `session.current_patient` (ومرافقه إن وُجد في `current_companion`)، وتُضبط
  `session.step` على خطوة الحقل العادية، ثم تُعرَض شاشتها الأصلية كما هي.
- عند اكتمال الحقل: بدل الانتقال للخطوة التالية في المسار الخطي، `_finish_field_edit()`
  الجديدة تكتب النسخة المعدَّلة في مكانها **بعينه** ضمن `completed_patients`
  (والمرافق ضمن `companions[j]` إن وُجد)، تصفّر أعلام التعديل، وتُعيد العرض
  لشاشة المراجعة مباشرة — بغضّ النظر عمّا كانت ستؤول إليه الخطوة التالية
  أصلاً.
- التطبيق: فحص واحد سطر `if session.edit_from_review: await _finish_field_edit(...); return`
  أُدرج في **كل** موضع يحفظ قيمة حقل عبر المسارات الأربعة (تقويم `cal_pick`،
  أزرار التخطي، معالج النص، معالج الصور) — 21 حقلاً (13 للمريض + 8 للمرافق)
  عبر نحو 35 موضعاً، مطابقاً تماماً لعدد/موقع المسارات القائمة أصلاً فلا
  شاشات جديدة ولا تكرار للتحقّق.

⚠️ **الحالة الخاصة الحرجة الوحيدة — المختص**: زر اختيار المختص يستدعي عادة
`_finish_patient_and_advance` الذي بدوره `finish_current_patient()` —
وهذه **تُلحِق** `current_patient` كمريضٍ **جديد** بآخر `completed_patients`
(لا تُحدِّث موجوداً بمكانه). لو تُرك هذا الفرع بلا تدخّل أثناء التعديل، كل
تعديل لحقل «المختص» كان سيُنتج **مريضاً مكرَّراً** في الدفعة بدل تصحيح
الأصلي. أُضيف الفحص **قبل** هذا الاستدعاء تحديداً، فيوجَّه لـ`_finish_field_edit`
عوضاً عنه.

**غير قابل للتعديل عمداً**: اسم المريض/المرافق — اختياره إلزامي من
سجل المرضى ويجرّ معه `companion_queue` تلقائياً؛ تغييره بعد اكتمال الدفعة
يتطلب إعادة بناء تلك القائمة بالكامل، خارج نطاق «تصحيح حقل واحد».

**الشاشات الجديدة** (`views.py`): `build_edit_menu` (قائمة حقول المريض +
أزرار المرافقين إن وُجدوا) و`build_edit_cmenu` (قائمة حقول مرافق واحد)،
وزر «✏️ تعديل بيانات {الاسم}» واحد لكل مريض في `build_review` قبل صفّ
النشر/الإلغاء.

**التحقق**: `python -m py_compile` نظيف · 187 اختباراً + 170 معالجاً
(العدد لم يتغيّر — التعديل فروع جديدة داخل معالِج `gsa:` القائم لا معالِج
منفصل) · محاكاة حيّة بـ`_dispatch_callback_inner`/`_handle_text_input`/
`_handle_photo_input` الفعلية وكائنات query/message وهمية على دفعة بمريضين
ومرافق واحد: تعديل حقل نصّي (ملاحظات) ← كتابة صحيحة + عودة للمراجعة + عدم
مسّ المريض الآخر؛ تعديل حقل تقويم (انتهاء تأشيرة) عبر `cal_pick` فعلي؛
تعديل حقل صورة (جواز) عبر معالج الصور الفعلي؛ تعديل عبر زر تخطٍّ (صورة
الإقامة)؛ **تعديل المختص والتأكد الصريح أن عدد المرضى بقي 2 لا 3** (الفحص
الحرج أعلاه)؛ تعديل حقل مرافق (عنوان السكن) والتأكد من كتابته في
`companions[0]` تحديداً؛ `review_show` يعيد العرض بلا أي أثر جانبي؛ تعديل
الجهة الموصلة عبر زر. وفحص انحدار منفصل بجلسة عادية (بلا تعديل) يؤكد أن
`cal_pick` يتقدّم بالمسار الخطي القديم تماماً كما كان، وأن `build_review`
لمريض واحد بلا مرافقين يعرض زرّ تعديل واحد بلا فاصل مرافقين.

### 2026-08-12 (٥) — 📤 الخدمات والإقامات: من نشر التقرير يراه فعلياً لا رسالة نجاح مختصرة فقط

**الطلب**: "يكون التقرير للخدمات والاقامات اثناء النشر يظهر للمستخدم" — من
ينفّذ عملية نشر (واصلين/مغادرون/خدمات عامة، أو أحداث الإقامة: تقديم/استلام
تمديد/إصدار/طلب ناقص) لم يكن يرى **نص التقرير نفسه**، بل رسالة نجاح مختصرة
فقط («✅ تم الحفظ والنشر»). كان يصل التقرير الفعلي فقط لو كان ضمن `ADMIN_IDS`
**و**`GS_NOTIFY_ADMINS=1` — أي أن أي مستخدم بصلاحية وحدة فقط (لا أدمن كامل)،
أو أي أدمن أثناء تعطيل `GS_NOTIFY_ADMINS` للاختبار، لم يكن يرى ما نشره إطلاقاً.

**التنفيذ — نقطة واحدة لكل ناشر بدل تعديل كل نقطة استدعاء**:
- `modules/general_services/report_publisher.py::publish()` — يرسل التقرير
  الآن لـ`data.created_by_id` دائماً (بعد نصّ التقرير نفسه، لا رسالة بديلة).
  `created_by_id` كان موجوداً أصلاً في `GSPublishData` وتُمرِّره الوحدات
  الثلاث (`arrivals`/`departures`/`public_services`) بالفعل، فتصحيح الدالة
  المركزية كفى — **لا حاجة لمس أي من الوحدات الثلاث**.
- `modules/residency/report_publisher.py::publish_event()` — أُضيف معامل
  اختياري جديد `submitted_by: int | None`، ويُرسَل له التقرير (والمرفقات
  إن وُجدت) بنفس الطريقة. بخلاف `GSPublishData`، `publish_event` لا يحمل
  هوية الفاعل ضمنياً، فاستُدعي التمرير الصريح في **كل** نقاط الاستدعاء
  الخمس: `renewal/flow.py` (تأكيد الإصدار)، `uploads/flow.py::_notify()`
  (تقدّم المرحلة + تقديم الأوراق الكامل — استُخدِم معامل `submitted_by`
  الجديد في الدالة المساعدة نفسها بدل تكراره)، و`profiles/flow.py` الثلاث
  (رفع طلب ناقص لإغلاقه، تسجيل طلب مع تقدّم الحالة، تسجيل طلب مستقل).
- **إزالة التكرار**: في كلا الدالتين، يُجمَع من استُدعي فعلاً في حلقة الأدمن
  ضمن `set`، ونسخة المستخدم تُرسَل فقط إن لم يكن معرّفه في تلك المجموعة —
  فالأدمن الذي نشر تقريره الخاص لا يستلم رسالتين متطابقتين.
- **توافق خلفي كامل**: كلا المعاملين اختياري بقيمة افتراضية `None`/غير
  ممرَّر ⇐ أي استدعاء قديم أو مستقبلي بلا تمرير الفاعل يتصرّف كما كان تماماً
  (لا رسالة إضافية، لا استثناء).

**نطاق الأثر**: يشمل تلقائياً واصلين/مغادرون/خدمات عامة (عبر `publish()`
المركزية) وكل أحداث الإقامة الخمسة أعلاه (عبر `publish_event()`) — لم
تُمَس وحدات الرعاية الصحية الست (`healthcare/*`) لأن الطلب حدَّد «الخدمات
والإقامات» تحديداً ولها ناشرها الخاص (`healthcare` report_publisher منفصل
تماماً، لم يُفحَص هذه الجلسة).

**التحقق**: `python -m py_compile` نظيف على 5 ملفات · 187 اختباراً + 170
معالجاً (العدد لم يتغيّر — تعديل داخل دوال نشر موجودة لا معالجات جديدة) ·
محاكاة حيّة ببوت وهمي يسجّل كل استدعاء `send_message`/`send_photo`: (١)
ناشر عادي (لا أدمن) يستلم التقرير الكامل مع الأدمن والمجموعة معاً — نفس
النصّ حرفياً في الرسائل الثلاث؛ (٢) ناشر هو نفسه الأدمن الوحيد ⇒ نسخة واحدة
فقط لا اثنتان؛ (٣) `created_by_id=None` لا يُسقط الاستدعاء ولا يُضيف مستلماً
وهمياً؛ (٤) نفس الثلاثة لـ`publish_event`/`submitted_by` على وحدة الإقامة؛
(٥) استدعاء `publish_event` بلا `submitted_by` إطلاقاً يُنتج **نفس قائمة
المستلمين حرفياً** كما قبل هذا التغيير (فحص توافق خلفي صريح).

### 2026-08-12 (٦) — 💉 جلسات الكيماوي: رقم الجلسة ضمن الدورة + إصلاح تسمية رقم الدورة

**الطلب**: "يكون يظهر كم عدد الدورات الكلي, وثم كم رقم هذا الدورة, وثم كم
رقم هذا الجلسة... لا داعي ليكون في كم عدد جلسات كل دورة" — مع التركيز على
ظهور هذه الأرقام في شاشتَي التعديل قبل/بعد النشر أيضاً.

**اكتشاف أثناء التدقيق**: مسار الكيماوي الفعلي (`_start_simple_session_flow`
بمفتاح `"chemo"`) لا يستخدم أسئلة الدورات القديمة (`CHEMO_CYCLES_*`) إطلاقاً
منذ إزالتها جلسة سابقة — كان هذا التعليق أعلى الملف متروكاً بالخطأ يصف
سلوكاً قديماً. الموجود فعلياً: عدد الدورات الكلي (`TreatmentPlan.total_sessions`)
ورقم الدورة الحالية (`current_session`, تلقائي +1 كل تقرير) — أي أن (١) و(٢)
من طلب المستخدم **كانا موجودين أصلاً**، لكن (٣) «رقم الجلسة ضمن الدورة» لم
يكن موجوداً بتاتاً، ولا أثر له في شاشتَي التعديل.

**التنفيذ**:
- عمود جديد `Report.chemo_session_number` (Integer، مهاجَر عبر
  `_migrate_column`) — رقم **يدوي مستقل تماماً** لكل تقرير، بلا أي تتبّع
  خطة وبلا عدّ كلي لجلسات كل دورة (بطلب المستخدم صراحةً — الفرق الجوهري عن
  `current_session` الذي يُعدّ تلقائياً ومرتبط بـ`TreatmentPlan`).
- حالة محادثة جديدة `TREATMENT_CHEMO_SESSION_NUMBER` (states.py) — تُسأل في
  `flows/treatment_sessions.py` فور الضغط على «✅ متابعة» في شاشة عرض الخطة،
  **للكيماوي فقط** (`_treatment_key == "chemo"`)؛ targeted/immuno/dialysis
  تتخطّاها مباشرة لشكوى المريض كما كانت تماماً — مسجَّلة في
  `conversation_handler.py` وموصولة برجوعها في `execute_smart_state_action.py`
  (نفس نمط الفحص المتكرر هذه الجلسة: خطوة جديدة يجب وصلها في كل الأماكن).
- **إصلاح تسمية جانبي مكتشَف أثناء العمل**: حقل التعديل القديم
  `session_number` ("🔢 رقم الجلسة الحالية") يُعدِّل فعلياً `current_session`
  — وهو للكيماوي تحديداً يمثّل رقم **الدورة** لا الجلسة (`unit_labels`
  يُخرج "دورة" للكيماوي)، فكان زر التعديل يحمل تسمية مضلِّلة. أُضيف مفتاح
  منفصل `chemo_cycle_number` ("🔢 رقم الدورة الحالية") بنفس منطق القراءة/
  الكتابة تماماً (نفس `_current_session_number_display`/`_apply_session_number_edit`
  في كلا ملفَّي التعديل)، خاص بالكيماوي وحده — `session_number` يبقى كما هو
  بلا أي تغيير لـtargeted/immuno/dialysis حيث تسميته صحيحة أصلاً.
- `field_registry.py`: قائمة `treatment_chemo` صارت
  `[chemo_cycle_number, chemo_session_number, complaint_text, notes, ...]`
  (8 حقول بدل 7). `chemo_session_number` يمرّ بالكامل عبر المسارات العامة
  الموجودة أصلاً في كلا شاشتَي التعديل (`data.get(key)`/`setattr(report, key)`)
  بلا أي منطق خاص — فقط تحقّق رقم صحيح موجب أُضيف صراحة، ونفس اسم المفتاح
  في `report_tmp` وعمود `Report` (`chemo_session_number`) فيعمل تلقائياً.
- `flows/shared.py::save_report_to_database` + `broadcast_data` — كلاهما
  يُبنى يدوياً حقلاً حقلاً ولا يرث من `data` تلقائياً (نفس نمط
  `treatment_plan_summary` الموثَّق أعلاه)، فأُضيف `chemo_session_number`
  في كليهما بجانب `treatment_plan_summary` مباشرة — وإلا لظهرت البطاقة
  فارغة من الحقل الجديد عند النشر الأول رغم حفظه بشكل صحيح.
- `user_reports_edit.py` (إعادة النشر بعد التعديل): نفس الإضافة في
  `broadcast_data` — تجنّباً لعطب "الحقل يختفي عند إعادة النشر" المسجَّل
  سابقاً لحقول أخرى (`treatment_plan_summary`, `endoscopy_*`, `transplant_*`).
- `services/broadcast_service.py::_build_treatment_session_fields` — سطر
  جديد "🔢 رقم الجلسة ضمن الدورة" يظهر فقط إن كانت القيمة موجودة (بقية أنواع
  العلاج لا تملك هذا المفتاح إطلاقاً فيبقى غائباً تلقائياً لها).
- اختبار داخلي `test_editable_fields_mapping()` في `user_reports_edit.py`
  محدَّث: العلاج الكيماوي 7→8 حقول.

**التحقق**: `python -m py_compile` نظيف على 10 ملفات · 187 اختباراً + 170
معالجاً (العدد لم يتغيّر — حالة جديدة داخل `ConversationHandler` موجود لا
معالج منفصل) · تشغيل `test_editable_fields_mapping()` مباشرة يؤكد 8 حقول
للكيماوي و7 لبقية الأنواع (فشل غير متعلق مسبق في عدّاد المناظير 9≠8 — عطب
موجود قبل هذه الجلسة، غير مُلامَس هنا) · محاكاة حيّة بقاعدة معزولة بدوال
حقيقية: `_migrate_column` يضيف العمود فعلياً على DB جديدة وقيمته تُقرأ/تُكتب
بنجاح · `handle_treatment_plan_display_choice` الفعلية توجّه الكيماوي فقط
لحالة رقم الجلسة الجديدة بينما targeted تتخطّاها تماماً (فحص انحدار صريح) ·
`handle_chemo_session_number` الفعلية: رفض نص غير رقمي، وقبول رقم صحيح
وتخزينه كـ`int` والانتقال لشكوى المريض · `execute_smart_state_action`
الفعلية تعيد رسم شاشة رقم الجلسة عند الرجوع إليها · `field_registry.py`
يؤكد أن الكيماوي يستخدم `chemo_cycle_number`/`chemo_session_number` بدل
`session_number`، وأن targeted ما زال يستخدم `session_number` بلا تغيير ·
مسارا القراءة/الكتابة قبل النشر (`_current_session_number_display` لـ
`chemo_cycle_number`، والمسار العام لـ`chemo_session_number`) وبعد النشر
(`setattr` على صفّ `Report` حقيقي في القاعدة) كلاهما يعمل بشكل صحيح ·
بطاقة البث الفعلية (`format_report_message`) تعرض السطر الجديد للكيماوي
فقط وتغيب تماماً لغيره.

### 2026-08-12 (٧) — 🕵️ تقرير أخطاء اليوم: فجوة توقيت تُضيع أخطاء آخر الليل نهائياً

**الطلب**: "لماذا توقف ارسال التنبيهات بخصوص الاخطاء؟" — لم يصل تقرير
2026-08-10 ولا 2026-08-11 رغم وقوع أخطاء فعلية.

**التشخيص (عبر SSH فعلي على السيرفر — `botuser@217.217.248.239`،
`medical-bot-hetzner`)**: سجلّ `[error_digest]` في `medbot-error.log` أظهر
الإرسال يحدث الساعة **20:25** توقيت السيرفر يومياً — بينما الكود يحدّد
الإرسال 23:55 بتوقيت `Asia/Kolkata` صراحةً (`app.py`). السيرفر بتوقيت
أوروبا (CEST، +2) لا الهند (+5:30) — والفارق بينهما 3.5 ساعة **يطابق
تماماً** الفارق بين 20:25 و23:55. أي: التوقيت المجدوَل صحيح 100%، لكنه
عُرض في اللوق بتوقيت السيرفر المحلي فبدا "20:25" — لا خلل في الجدولة.

**الخلل الحقيقي**: `services/error_digest.py` يستخدم `date.today()`
الساذجة (توقيت نظام السيرفر، CEST) في **موضعين**: (أ) `_DigestHandler.emit()`
عند تسمية ملف اليوم الذي يُكتَب فيه كل خطأ، و(ب) `send_daily_error_report()`
عند تحديد أي ملف يُقرأ لبناء التقرير. بما أن الإرسال يقع الساعة 20:25 CEST
(=23:55 IST) بينما "منتصف الليل" الذي يُغيِّر اسم ملف اليوم لا يقع إلا
الساعة 00:00 CEST (=03:30 IST اليوم التالي) — تنشأ **فجوة 3 ساعات و35
دقيقة** كل ليلة (20:25–23:59 بتوقيت السيرفر) يُكتَب فيها أي خطأ جديد في
ملف "اليوم نفسه" **بعد** أن أُرسل تقريره فعلاً. فلا يظهر في تقرير اليوم
(أُرسل قبل وقوعه) ولا تقرير الغد (يقرأ ملف الغد، لا اليوم) — يضيع نهائياً
بصمت. فحص فعلي على السيرفر أكّد هذا حرفياً: `errors-2026-08-10.jsonl`
و`errors-2026-08-11.jsonl` كليهما يحتويان أخطاء فعلية بتوقيت لاحق للإرسال
(21:25 و22:35 توقيت السيرفر على التوالي)، بينما سجلّ ذلك اليوم يقول
صراحةً "لا أخطاء، لا إرسال" — تناقض يثبت الفجوة، لا غياب الأخطاء فعلاً.

⚠️ **نفس نمط الخلل المسجَّل سابقاً هذه الجلسة** (المهام #93–#96: توقيت
صرف الأدوية بالصيدلية كان UTC بدل IST) — خادم بتوقيت غير IST بينما منطق
البوت كله مبني على افتراض IST؛ أي كود جديد يستخدم `date.today()`/
`datetime.now()` الساذجة بدل قراءة صريحة لتوقيت `Asia/Kolkata` معرَّض لنفس
الفخّ.

**الإصلاح**: دالة جديدة `_today_local()` في `error_digest.py` تقرأ التاريخ
عبر `pytz.timezone(TIMEZONE)` (من `config/settings.py`، افتراضياً
`Asia/Kolkata`) بدل `date.today()` الساذجة — بها احتياط `try/except` يعود
لتوقيت النظام لو تعذّر `pytz`/الإعداد (فشل الالتقاط بالكامل أسوأ من فجوة
توقيت). استُبدلت بها **كل** استخدامات `date.today()` الثلاثة في الملف:
كتابة ملف اليوم (`_DigestHandler.emit`)، تحديد يوم التقرير المُرسَل
(`send_daily_error_report`)، وحدّ التنظيف الشهري (`cleanup_old`) — الآن
الثلاثة تتفق على نفس "اليوم" بتوقيت IST، فتصبح الفجوة **5 دقائق فقط**
(من 23:55 إلى 00:00 IST) بدل 3 ساعات ونصف — مطابقاً أخيراً لنيّة التعليق
الأصلي أعلى الملف: "يرسل للأدمن آخر اليوم ملفاً واحداً يلخّص ما حدث".

**غير مُصلَح بعد (يحتاج قراراً من المستخدم)**: تقريرا 2026-08-10 و
2026-08-11 الضائعان لا يزالان في `errors-*.jsonl` على السيرفر (لم
يُحذَفا — `cleanup_old` عمرها 30 يوماً) ويمكن إرسالهما يدوياً عبر استدعاء
`send_daily_error_report(application, day=date(2026,8,10))` لكل منهما
بعد النشر، إن رغب المستخدم باستعادتهما.

**التحقق**: `python -m py_compile` نظيف · 187 اختباراً (العدد لم يتغيّر —
تعديل داخلي في دالة موجودة) · محاكاة حيّة: `_today_local()` يطابق تاريخ
IST الفعلي لا تاريخ نظام التشغيل المحلي؛ خطأ مُسجَّل فوراً يُقرأ بنجاح
تحت نفس مفتاح اليوم (لا فجوة)؛ `build_report_file` يبني تقريراً يحوي
الخطأ فور وقوعه لا بعد فوات الأوان؛ `cleanup_old` يستخدم نفس مرجع
"اليوم" المبني على IST لحساب حدّ الـ30 يوماً.

### 2026-08-12 (٨) — 🗑️ تعديل التقارير قبل النشر: حذف زر "⏭️ بدون تاريخ عودة" من تقويم التعديل

**الطلب**: عند تعديل موعد العودة قبل/بعد النشر، يظهر تقويم + زر "⏭️ بدون
تاريخ عودة" يمسح التاريخ تماماً — المستخدم لا يريد هذا الزر إطلاقاً؛ إن
أراد تعديل التاريخ فمن التقويم فقط.

**التدقيق**: الزر موجود في مكان واحد فقط فعلياً —
`edit_handlers/draft/handlers.py::_build_draft_edit_calendar_markup`
(تقويم تعديل موعد العودة **قبل النشر**، المستخدَم أيضاً من
`treatment_and_endoscopy_edit.py` لجلسات العلاج/المناظير عبر
`_render_draft_edit_followup_calendar`). تقويم **بعد النشر**
(`user_reports_edit.py`, حالة `EDIT_DATE_CALENDAR`) جرى فحصه بالكامل ولا
يحتوي زراً مماثلاً — فقط "🔙 رجوع" و"❌ إلغاء"؛ لا حاجة لتعديل هناك.

**التنفيذ**: حذف الزر من `_build_draft_edit_calendar_markup`، وحذف
معالِجه `handle_draft_edit_cal_skip` بالكامل (لا تعديل تعطيل فقط — كود
ميت لا يستدعيه شيء بعد حذف الزر)، مع كل نقاط تسجيله الأربع: استيراده في
`conversation_handler.py`، مدخله في قاموس `sh[...]`،
`CallbackQueryHandler(pattern="^draft_edit_cal_skip$")`، واستيراده
وتصديره في `edit_handlers/draft/__init__.py` (نُسي هذا الموضع الرابع
أول مرة فسبَّب `ImportError` عند تحميل الحزمة — اكتُشف بمحاكاة حيّة
فعلية استوردت الدالة الهدف مباشرة، لا بفحص نصّي وحده).

⚠️ **تنبيه أدوات**: الفحص الأول عبر `register_all_handlers` على
`Application` حقيقية أظهر 170 معالِجاً بلا خطأ رغم وجود هذا الـ
`ImportError` فعلياً — بسبب bytecode مخبَّأ قديم (`__pycache__`) لم
يُبطَل. تكرار الفحص بعد حذف كل `__pycache__` يدوياً كشف الخطأ فوراً.
**الدرس**: على Windows/هذه البيئة تحديداً، لا يُعتمَد على فحص واحد ناجح
وحده لتغييرات على حزم `__init__.py` — يُعاد الفحص بعد `find . -name
__pycache__ -exec rm -rf {} +` صراحةً.

**التحقق**: `python -m py_compile` نظيف على 3 ملفات · 187 اختباراً +
170 معالجاً (بعد حذف `__pycache__` بالكامل وإعادة الفحص) · محاكاة حيّة
فعلية: `_build_draft_edit_calendar_markup` لا يُنتج زر "بدون تاريخ عودة"
ولا `callback_data` باسم `draft_edit_cal_skip` في أي مكان بلوحة المفاتيح؛
أزرار التنقّل (◀️ السابق/▶️ التالي) وأزرار الأيام و"🔙 رجوع لقائمة الحقول"
كلها سليمة بلا تأثر؛ الدالة المحذوفة غير موجودة إطلاقاً على الوحدة
(`hasattr` يعيد `False`) — لا مجرد غير قابلة للوصول.

### 2026-08-12 (٩) — 📅 موعد العودة عند إنشاء تقرير جديد: كان يقبل أي نص كتاريخ فعلي

**الطلب**: "عند الوصول الى التاريخ المفترض الاختيار من التقويم، لكن الآن
يقبل اختيار من التقويم وإذا أدخلت نص يدوي يقبله مباشرة وينتقل إلى سبب
العودة" — مُبلَّغ فور إصلاح زر "بدون تاريخ عودة" السابق (٨)، فبدا للوهلة
الأولى وكأنه انحدار من ذلك التغيير.

**التشخيص**: تأكَّد عبر `git show` أن تعديل (٨) لم يمسّ أي `MessageHandler`
نصّي — لا في السابق ولا الآن. الخلل فعلياً **قديم ومنفصل تماماً**، وموجود
في شاشة أخرى ثالثة لم تُمَس: تقويم موعد العودة أثناء **إنشاء تقرير جديد**
(لا التعديل قبل/بعد النشر، وكلاهما تم التأكد أنهما سليمان تماماً — التعديل
قبل النشر لا يملك أي معالِج نصّي إطلاقاً لهذه الخطوة فيُتجاهَل النص بصمت،
والتعديل بعد النشر يرفض النص فعلياً عبر `handle_text_during_date_calendar`
الموجودة والسليمة).

`handle_followup_date_text_input` في `date_time_handlers.py` (يُسجَّل عبر
`_followup_date_state_handlers()` في **كل** حالة "تاريخ العودة" لكل مسارات
الإنشاء تقريباً — استشارة جديدة/طوارئ/عملية/ترقيد/خروج/علاج طبيعي/أجهزة
تعويضية/مناظير/جلسات علاج/زراعة) كانت تتحقّق فقط من طول النص (`len(text)
>= 2`) بلا أي تحليل تاريخ فعلي — أي نصّين حرفين فأكثر ("xx"، "22"، أي شيء)
يُقبَل، يُخزَّن حرفياً كما كُتب في `followup_date`، وينتقل التدفّق مباشرة
لخطوة "سبب العودة" كأنه تاريخ صحيح مُختار من التقويم فعلاً. رسالة الرفض
القديمة نفسها كانت تقترح أمثلة نصّية ("بعد أسبوع"، "الأحد القادم") توحي
بأن قبول النص اليدوي كان مقصوداً يوماً ما، لكن منطق تحليله الفعلي **لم
يُبنَ إطلاقاً** — فجوة قديمة (منذ commit `13078e2`/`753ca6d`، مايو 2026)
سبقت هذه الجلسة بأشهر، لا علاقة لها بتعديل (٨).

**الإصلاح**: نفس قاعدة الشاشتين الأخريين بالضبط — رفض أي نص يدوي كلياً
(بلا استثناء لطول أو محتوى)، عرض تحذير، وإعادة رسم التقويم نفسه عبر
`_render_followup_calendar` الموجودة (مشتركة فعلياً بين كل هذه المسارات
فأصلحتها تغطية واحدة)، والبقاء على نفس حالة المحادثة الحالية — مطابقاً
تماماً لـ`handle_text_during_date_calendar` في `user_reports_edit.py`.
دالة واحدة مسجَّلة في كل هذه الحالات، فالإصلاح يغطّيها جميعاً معاً.

**التحقق**: `python -m py_compile` نظيف · 187 اختباراً + 170 معالجاً
(بعد حذف `__pycache__` بالكامل — الدرس من (٨) طُبِّق هنا مباشرة) · محاكاة
حيّة فعلية بدالة المعالِج الحقيقية: نص "xx" ونص شبيه بتاريخ حقيقي
("15/1/2026") كلاهما يُرفَضان بلا فرق (لا استثناء لأي نمط نص)، `followup_date`
يبقى غير موجود إطلاقاً في `report_tmp` بعد الرفض (لا تخزين جزئي)، التدفّق
يبقى على نفس حالة المحادثة الحالية بدل القفز لسبب العودة، ونفس السلوك
يتكرّر بمسار مختلف تماماً (طوارئ) للتأكد أن الإصلاح عام لا خاص بمسار واحد.

### 2026-08-12 (١٠) — 💉 رقم الجلسة ضمن الدورة كان غائباً عند اختيار الكيماوي ضمن اختيار مُدمَج

**الطلب**: "في زر الجلسات جلسات الكيماوي لم يظهر التعديل الجديد لبعض
المترجمين، برغم أنه يفعل /start" — ميزة (٦) هذه الجلسة (رقم الجلسة ضمن
الدورة) لم تظهر لبعض المترجمين تحديداً رغم إعادة التشغيل.

**التشخيص**: تأكَّد أولاً عبر SSH فعلي أن آخر commit مدفوع مُنشَر فعلاً على
السيرفر (`68d3980`، نفس commit الميزة، والعملية تعمل منذ ذلك النشر بلا
إعادة تشغيل لاحقة) — فليست مشكلة نشر. البحث الفعلي كشف السبب: شاشة
"🎗️ جلسات الأورام" تسمح باختيار **أكثر من نوع علاج معاً** (كيماوي + مناعي
مثلاً) عبر checkbox. حين يُختار **نوع واحد فقط**، يُفوَّض مباشرة لـ
`start_chemo_flow` — نفس المسار الذي أُضيفت له الميزة الجديدة، فيعمل بشكل
صحيح. حين يُختار **نوعان أو أكثر معاً**، يعمل "طابور" منفصل تماماً في
`flows/oncology_multiselect.py` — لا يمرّ عبر `treatment_sessions.py`
إطلاقاً — وهذا الطابور **لم يكن يتضمّن أي استدعاء** لسؤال "رقم الجلسة ضمن
الدورة" الجديد في أي من فرعيه (خطة نشطة تتقدّم تلقائياً، أو خطة جديدة
بإدخال العدد الكلي + الرقم الحالي يدوياً). فالمترجم الذي يختار الكيماوي
**وحده** يرى الميزة، والذي يختاره **مع نوع آخر** لا يراها إطلاقاً — يطابق
تماماً "لبعض المترجمين" في البلاغ.

**الإصلاح**: حالة محادثة جديدة `ONCOLOGY_QUEUE_CHEMO_SESSION` مستقلة عن
`TREATMENT_CHEMO_SESSION_NUMBER` (معالِجات الطابور منفصلة كلياً عن
`treatment_sessions.py`، فلا يمكن مشاركة الحالة نفسها). أُضيفت الدالة
المساعدة `_prompt_chemo_session_number_queue` ومعالِجها
`handle_oncology_queue_chemo_session` في `oncology_multiselect.py`، ووُصلا
في **الفرعين كليهما**: (أ) الخطة النشطة المتقدِّمة تلقائياً في
`_process_next_in_queue`، و(ب) الخطة الجديدة بعد إدخال العدد الكلي والرقم
الحالي في `handle_oncology_queue_current` — كلاهما يتحقّق أولاً
`if key == "chemo":` قبل التحويل، فبقية الأنواع (مناعي/موجّه/إشعاعي) غير
متأثرة إطلاقاً. `chemo_committed_in_queue` (المسار القديم لخطط الدورات،
لا يزال يُستدعى نظرياً عند تعديل خطة قديمة داخل طابور) وُصل أيضاً لنفس
الاتساق. لا حاجة لأي تعديل في `flows/shared.py`/`broadcast_service.py` —
كلاهما يقرآن `chemo_session_number` من `report_tmp`/`Report` عمومياً بلا
شرط على `flow_type`، فيعملان تلقائياً بمجرد استلام القيمة من أي مصدر.

**التحقق**: `python -m py_compile` نظيف على 4 ملفات · 187 اختباراً + 170
معالجاً (بعد حذف `__pycache__` بالكامل) · محاكاة حيّة فعلية بقاعدة معزولة:
مسار "خطة جديدة" (كيماوي + مناعي معاً) — إدخال العدد الكلي ← رقم الدورة
الحالية ← **يتحوَّل الآن لسؤال رقم الجلسة ضمن الدورة بدل القفز مباشرة
لطريقة الإعطاء** ← تخزينه كـ`int` صحيح والانتقال الطبيعي؛ رفض رقم غير
صحيح مع البقاء على نفس الحالة؛ مسار "خطة نشطة" (تقدّم تلقائي) لمريض له
خطة كيماوي فعلية في القاعدة — يتحوَّل لنفس السؤال الجديد أيضاً؛ وفحص
انحدار صريح أن نوعاً آخر (مناعي) ضمن نفس آلية الطابور يبقى بلا أي تغيير
— ينتقل مباشرة لطريقة الإعطاء كما كان تماماً قبل هذا الإصلاح.

### 2026-08-12 (١١) — 🎗️ تقارير الأورام المُدمَجة (كيماوي + مناعي مثلاً) تنقلب إلى «استشارة جديدة» عند لمس تقويم تاريخ العودة

**الطلب**: "المشكلة الكبيرة، في زر جلسات الأورام، عند اختيار أكثر من خيار
فرضاً مناعي وكيماوي، يحصل تغيّر في الفورمة الجديدة ليس نفس ما تدخل، كل
شيء منفصل" — وبعد استيضاح: "يظهر الفورمة القديمة وليست الجديدة"، أي أن
البطاقة المنشورة تظهر بشكل تقرير "استشارة جديدة" العادي بدل بطاقة جلسات
العلاج المخصَّصة، رغم أن المترجم أدخل بيانات جلسات علاج مُدمَجة فعلياً.

**التشخيص**: `flows/oncology_multiselect.py::_finish_queue()` يضبط
`current_flow = "treatment_combined"` و`medical_action` مُركَّباً مثل
"العلاج الكيماوي + العلاج المناعي" (بفاصل " + "). لكن `flows/new_consult.py`
يحتوي سلسلة معالِجات مشتركة لتقويم/وقت/سبب تاريخ العودة (يُعاد استخدامها
من عدة تدفّقات عبر استيراد مباشر، تماماً كما حدث سابقاً مع "الزراعة" —
سجل 2026-08-04) تتفرّع داخلياً على `current_flow` عبر سلاسل
`elif current_flow in (...)` طويلة، وكانت هذه السلاسل تتضمّن
`"treatment_chemo"`, `"treatment_targeted"`, `"treatment_immuno"`,
`"treatment_dialysis"` — **لكن ليس `"treatment_combined"`** — في **9 مواضع**
منفصلة (الأسطر 476, 558, 598, 684, 733, 812, 849, 912, 948)، تغطي معالِجات
اختيار يوم التقويم، اختيار الساعة، اختيار الدقيقة، وتخطي الوقت، بما فيها
فرع الرجوع عند خطأ إدخال. أي مترجم يبدأ تقريراً بنوعين علاج مُدمَجين ثم
يصل لخطوة تاريخ العودة (خطوة تمرّ عبرها كل التقارير تقريباً) كان يسقط
بصمت في الفرع الافتراضي `else: current_state = NEW_CONSULT_FOLLOWUP_DATE`
— فتنتقل المحادثة كلياً لآلة حالات "استشارة جديدة" بدل "جلسات العلاج".
لاحقاً في `save_report_to_database`، شبكة الأمان التي تُصحِّح
`flow_type` من `current_flow` (`flows/shared.py`) لا تتفعّل لأن القيمة
الفاسدة `"new_consult"` هي نفسها قيمة صالحة قانونياً، وقاموس
`action_names` في نفس الدالة يستبدل `final_medical_action` المُدمَج
الصحيح بـ`"استشارة جديدة"` تلقائياً بمجرد أن `flow_type` صار
`"new_consult"` — وهذا هو بالضبط ما وصفه المستخدم بـ"الفورمة القديمة".

**الإصلاح**: أُضيف `"treatment_combined"` إلى الـ 9 سلاسل جميعها في
`flows/new_consult.py`، فتُعامَل الآن معاملة `treatment_chemo` وأخواتها
تماماً وتوجَّه لحالات `TREATMENT_FOLLOWUP_DATE`/`TREATMENT_FOLLOWUP_REASON`
الصحيحة. لم يُلمَس `handle_new_consult_followup_reason` (الذي يستدعي
`show_translator_selection(..., "new_consult")` صراحة) لأنه بعد هذا
الإصلاح لن يستقبل تقارير `treatment_combined` إطلاقاً — ستُحوَّل قبله.

**التحقق**: `python -m py_compile` نظيف · بعد حذف `__pycache__` بالكامل:
187 اختباراً + 170 معالجاً حقيقياً عبر `register_all_handlers` فعلي · محاكاة
حيّة فعلية بدوال المعالِج الحقيقية لتقرير أورام مُدمَج
(`medical_action="العلاج الكيماوي + العلاج المناعي"`): اختيار يوم من
التقويم يوجّه الآن لـ`TREATMENT_FOLLOWUP_DATE` (كان يسقط بصمت لـ
`NEW_CONSULT_FOLLOWUP_TIME`)، `current_flow`/`medical_action` يبقيان
سليمَين بلا فساد بعد الاستدعاء؛ اختيار ساعة يوجّه لـ`TREATMENT_FOLLOWUP_REASON`
(كان يسقط لـ`NEW_CONSULT_FOLLOWUP_REASON`)؛ إدخال تاريخ فاسد من callback
data يبقى على `TREATMENT_FOLLOWUP_DATE` نفسها بدل القفز؛ وفحص انحدار
صريح أن تقرير "استشارة جديدة" عادي (`current_flow="new_consult"`) غير
متأثر إطلاقاً ويستمر بالتوجّه الصحيح كما كان تماماً.

### 2026-08-13 (١٢) — 🔍 تدقيق شامل لنمط «القيمة الناقصة من سلسلة flow_type» + إصلاح حالة حقيقية أخرى: الأجهزة التعويضية

**الطلب**: "افحص هذا المشكله اذا تكررت في عده اماكن لا اعلم بها" — بعد
إصلاح (١١)، طلب المستخدم تدقيقاً استباقياً على كامل الكودبيس للتأكد من
عدم وجود حالات أخرى غير مكتشَفة من نفس نمط الخطأ (سلسلة `elif current_flow
in (...)` أو قاموس بحسب flow_type ناقص قيمة صالحة، فيسقط التقرير بصمت
لمسار "استشارة جديدة" الخاطئ) — وهو نمط وُثِّق ثلاث مرات حتى الآن: الزراعة
(2026-08-04)، والأورام المُدمَجة (إدخال ١١ أعلاه).

**منهجية التدقيق**: تدقيق شامل لكل ملفات `flows/`, `edit_handlers/`,
`shared.py`, `execute_smart_state_action.py`, `field_registry.py`،
و`broadcast_service.py`، بمقارنة كل سلسلة/قاموس يتفرّع على flow_type مقابل
القائمة الكاملة لأنواع التدفّق الـ22 الصحيحة (من مفاتيح
`REPORT_FIELD_REGISTRY`)، مع تتبّع فعلي لكل نتيجة مرشَّحة (من أين تُستدعى؟
هل flow_type المفقود يصلها فعلاً؟) بدل الاكتفاء بالمطابقة النصية.

**النتيجة — حالة حقيقية واحدة أخرى قابلة للاستغلال فعلياً**: نفس الـ9
سلاسل (+ موضع عاشر، السطر 393) في `flows/new_consult.py` التي أُصلحت
لـ`treatment_combined` في (١١) كانت تتحقق من `current_flow == "device"`
فقط — **لكن `flows/rehab.py:107` يضبط `current_flow = "rehab_device"`
عند اختيار "🦾 أجهزة تعويضية"** (ليس `"device"` — إعادة التسمية لـ
`"device"` تحدث فقط لاحقاً، داخل مسار *تعديل ما بعد الإنشاء* في
`edit_handlers/before_publish/rehab_device_edit.py`، وليس أثناء الإنشاء
نفسه). بعكس `flows/shared.py` التي تعامل `"rehab_device"` و`"device"`
كمرادفين بالكامل في كل مكان (`get_confirm_state`، `get_translator_state`،
`action_names`، `elif flow_type in ["rehab_device", "device"]:` بالسطر
2002)، فإن `new_consult.py` فقط أخطأت هذا التوحيد. **مسار استغلال حقيقي
مؤكَّد**: مترجم يُدخل تقرير "أجهزة تعويضية" ← يصل لتاريخ العودة ← يختار
يوماً من التقويم (الحالة `DEVICE_FOLLOWUP_DATE` مربوطة فعلياً بنفس دوال
`new_consult.py` المشتركة في `conversation_handler.py:1063-1068`) ←
يسقط بصمت لمسار "استشارة جديدة" تماماً كما حدث مع الأورام المُدمَجة.

**الإصلاح**: نفس شكل إصلاح (١١) حرفياً — تغيير الـ10 مواضع من
`elif current_flow == "device":` إلى
`elif current_flow in ("device", "rehab_device"):` في `new_consult.py`.
كما أُضيف `"transplant": "معاملة الزراعة"` إلى قاموس `flow_to_action`
الاحتياطي داخل `show_final_summary` (`flows/shared.py`، كان ناقصاً رغم
وجوده في القاموس الشقيق `action_names` في نفس الملف) — إصلاح احتياطي
غير قابل للاستغلال حالياً (`flows/transplant.py` يضبط `medical_action`
دائماً مع `current_flow`، فلا يُستدعى هذا المسار الاحتياطي فعلياً) لكن
يُغلق فجوة كانت ستظهر لو تغيّر ذلك مستقبلاً. **لم يُلمَس** `action_names`
نفسه لإضافة `"treatment_combined"` كما اقترح الفحص الأولي — تحقّقتُ
يدوياً أن غيابها **متعمَّد وصحيح**: قيمة `treatment_combined` الفعلية
ديناميكية (مثل "العلاج الكيماوي + العلاج المناعي") ولا يوجد نص ثابت
واحد يمثّلها، فإضافة مفتاح ثابت هنا كانت ستُفسِد `final_medical_action`
الصحيح بدل حمايته — نفس آلية الفساد الموصوفة في (١١) لكن بالعكس.

**حالات أخرى مرصودة لكن غير مُصلَحة الآن (موثَّقة في قسم "🔓 مفتوح"
أدناه)**: (أ) `flows/shared.py:2597` — `if flow_type == "rehab_device":`
لا يقبل `"device"` أيضاً (بعكس السطر 2002 المماثل) — تحقّقتُ أن هذا **كود
ميت حالياً**: لا يوجد أي موضع في كامل `user_reports_add_new_system/`
يضبط `current_flow`/`flow_type` فعلياً إلى `"device"` الحرفية (حتى
`rehab_device_edit.py` الذي يحمل هذا الاسم يُثبِّت `flow_type =
"rehab_device"` داخلياً بصرف النظر عن المدخل). (ب) `edit_handlers/
before_publish/router.py` — سلسلتا `route_edit_field_selection`
و`route_edit_field_input` تغطيان كل أنواع التدفّق **إلا "transplant"**
تماماً (صفر ذكر لـ"transplant" في كامل مجلد `edit_handlers/`) — لكن
مسارها الافتراضي `else` يعرض "⚠️ قيد التطوير" وينهي المحادثة بوضوح بدل
السقوط الصامت لمسار خاطئ، فهي عطل واضح (تعديل حقل محدَّد لتقرير "معاملة
الزراعة" قبل النشر غير مُنفَّذ إطلاقاً) لا فساد صامت — أُبقيت مفتوحة
لتحتاج بناء مخصَّصاً وليست إصلاحاً ميكانيكياً بسيطاً كبقية هذا الإدخال.

**التحقق**: `python -m py_compile` نظيف على الملفين · بعد حذف
`__pycache__` بالكامل: 187 اختباراً + 170 معالجاً حقيقياً عبر
`register_all_handlers` فعلي · محاكاة حيّة فعلية بدوال المعالِج الحقيقية
لتقرير "أجهزة تعويضية" (`current_flow="rehab_device"`): اختيار يوم من
التقويم يوجّه الآن لـ`DEVICE_FOLLOWUP_DATE` (كان يسقط بصمت لـ
`NEW_CONSULT_FOLLOWUP_TIME`)، `current_flow` يبقى سليماً بلا فساد؛
اختيار ساعة يوجّه لـ`DEVICE_FOLLOWUP_REASON` (كان يسقط لـ
`NEW_CONSULT_FOLLOWUP_REASON`)؛ فحص انحدار صريح أن المرادف `"device"`
(مسار التعديل القديم) وتقرير "استشارة جديدة" عادي كلاهما غير متأثرَين.

### 2026-08-13 (١٣) — 🫁 تفعيل تعديل حقول "معاملة الزراعة" قبل النشر

**الطلب**: إغلاق البند المفتوح الموثَّق في (١٢) — تعديل حقل محدَّد لتقرير
"معاملة الزراعة" (transplant) قبل النشر كان يعرض "⚠️ قيد التطوير"
وينهي المحادثة بدل فتح شاشة التعديل.

**السبب**: `edit_handlers/before_publish/router.py` يوجّه حسب flow_type
عبر سلسلة `if/elif` تغطي كل الأنواع الأخرى، وآخر فرع فيها
`elif flow_type in TREATMENT_AND_ENDOSCOPY_FLOWS:` — وهي مجموعة معرَّفة
في `treatment_and_endoscopy_edit.py` (نفس المعالِج العام المشترك المُشار
إليه في (١١)/(١٢) لأن حقول الزراعة نصية بسيطة بلا استخلاص مركّب، تماماً
كحقول جلسات العلاج/المناظير) — لكن `"transplant"` لم يكن عضواً فيها
إطلاقاً، فيسقط لفرع `else` الافتراضي.

**الإصلاح**: أُضيف `"transplant"` إلى `TREATMENT_AND_ENDOSCOPY_FLOWS`
في `treatment_and_endoscopy_edit.py` (سطر واحد)، وأُضيفت تسميات عرض
الحقول الثلاثة (`transplant_type`, `transplant_parties`,
`transplant_details`) إلى قاموس `_FIELD_NAMES` في الملف نفسه. لم يُلمَس
`router.py` إطلاقاً — العضوية في المجموعة كافية لأن كلا الفرعين هناك
(`route_edit_field_selection`/`route_edit_field_input`) يتحقّقان من
`TREATMENT_AND_ENDOSCOPY_FLOWS` مباشرة. لا حاجة لملف/معالِج مخصَّص جديد
لأن حقول الزراعة الثلاثة نصوص حرة بلا أي منطق خاص (لا رقم جلسة، لا خطة
علاج، لا قائمة اختيار) — تمرّ بالكامل عبر الفرع العام الموجود أصلاً في
`handle_treatment_endoscopy_edit_field_input`.

**التحقق**: `python -m py_compile` نظيف · بعد حذف `__pycache__` بالكامل:
187 اختباراً + 170 معالجاً حقيقياً عبر `register_all_handlers` فعلي ·
محاكاة حيّة فعلية عبر `route_edit_field_selection`/`route_edit_field_input`
الحقيقيتين: اختيار حقل "📝 تفاصيل المعاملة" لتقرير زراعة يفتح الآن شاشة
تعديل حقيقية (بدل "قيد التطوير")، وإدخال قيمة جديدة يحفظها في
`report_tmp['transplant_details']` ويعيد الحالة إلى `TRANSPLANT_CONFIRM`
بنجاح.

### 2026-08-14 (١٤) — 🐛 `BadRequest: Message is not modified` عند الضغط مرتين على «📋 تقديم» في شاشة المتابعة (بوت الإقامات)

**اكتُشف عبر لوق الإنتاج الفعلي** الذي أرسله المستخدم بعد سؤاله عن سلامة
زرَّي «⏰ المتابعة» و«⏳ المرافقون المعلقون» (تحقّقت وقتها فقط بمحاكاة —
هذه الحالة تحديداً لم تُختبَر لأنها تتطلب حالة DB سابقة معيّنة):
```
telegram.error.BadRequest: Message is not modified: specified new message
content and reply markup are exactly the same as a current content and
reply markup of the message
```
في `modules/residency/followup/flow.py`، معالِج `rnf:submitted_`.

**التشخيص**: زر «📋 تقديم» في قائمة «⏰ المتابعة» لا يختفي ولا يتغيّر
نصّه بعد الضغط عليه — يبقى معروضاً بنفس الشكل حتى بعد أن يصير الملف
`renewal_submitted` فعلاً. فإذا ضغطه المترجم **مرة ثانية** على نفس
الملف (سواء سهواً أو لأنه لا يرى أي مؤشّر أن الطلب سُجِّل)،
`mark_renewal_submitted` يُشغَّل مجدداً بلا ضرر فعلي (`old_status ==
new_status == "renewal_submitted"`)، لكن القائمة المُعاد بناؤها بعده
تصير **مطابقة بايتاً بايت** لما هو معروض بالفعل على الشاشة (نفس الأيقونة
📤، نفس الأزرار) — فيرفض تيليجرام التعديل. هذا الاستثناء لم يكن
مُعالَجاً في `followup/flow.py` (بخلاف كل معالِجات الإقامة الأخرى:
`profiles/flow.py`, `renewal/flow.py`, `routing_nav.py`, `uploads/flow.py`
—جميعها لديها بالفعل نمط `_safe_edit` يتجاهل هذا الخطأ تحديداً؛
`followup/flow.py` وحده كان ناقصاً هذه الحماية القياسية)، فيسقط لفرع
`except Exception` الخارجي ويُسجَّل traceback كامل بصمت — لا يتأثر
المستخدم عملياً (التوست «✅ تم تسجيل التقديم» يظهر بغضّ النظر لأنه يُرسَل
قبل محاولة التعديل) لكن يُلوِّث لوق الأخطاء بلا داعٍ في كل تكرار.

**الإصلاح**: أُضيفت دالة `_safe_edit` مطابقة تماماً للنمط المستخدَم أصلاً
في `routing_nav.py` إلى `followup/flow.py`، وحلّت محل استدعائي
`query.edit_message_text` المباشرين (في `submitted_c_` و`submitted_`).

**التحقق**: `python -m py_compile` نظيف · بعد حذف `__pycache__` بالكامل:
187 اختباراً + 170 معالجاً حقيقياً · محاكاة حيّة فعلية عبر
`_dispatch_callback` الحقيقي بقاعدة معزولة: أُعيد إنتاج البلاغ **بالضبط**
(ضغطة أولى تنقل الحالة من `expiring` إلى `renewal_submitted` فينجح
التعديل عادياً لأن الأيقونة تغيّرت فعلاً؛ ضغطة ثانية على نفس الملف الآن
`renewal_submitted` بالفعل فتصير القائمة المُعاد بناؤها مطابقة تماماً)
— قبل الإصلاح كانت الضغطة الثانية تُطلِق نفس الاستثناء غير المُعالَج من
اللوق الحقيقي؛ بعده تمرّ بصمت مع بقاء التوست يعمل كالمعتاد.

### 2026-08-14 (١٥) — 🖼️ دعم رفع صورة شخصية للمرافقين (لا للمريض فقط)

**الطلب**: زر "🖼️ رفع صورة شخصية" في ملف المريض كان يستهدف المريض فقط
دائماً بلا أي تمييز — لا شاشة اختيار اسم، ولا حتى عمود في قاعدة
البيانات لتخزين صورة أي مرافق. المستخدم طلب إضافة اختيار "لمن الصورة؟"
يعرض أسماء المريض وكل مرافق.

**الإصلاح**:
- عمود جديد `ResidencyCompanion.photo_file_id` (`db/models.py` +
  `_migrate_column` في `db/maintenance.py`) — بخلاف فورم C الذي يبقى
  عمداً مستنداً واحداً للعائلة، الصورة الشخصية تخصّ كل شخص على حدة.
- شاشة اختيار جديدة `build_photo_target_picker` (`uploads/views.py`) —
  تظهر **فقط إذا كان للملف مرافقون** (لا داعي لتخيير المستخدم بين خيار
  واحد فقط)؛ بلا مرافقين يبقى السلوك القديم كما هو تماماً (رفع مباشر
  للمريض).
- زر الملف (`rnu:photo_{id}`) صار يتحقق أولاً من وجود مرافقين: يعرض
  الشاشة إن وُجدوا، وإلا يفتح الرفع مباشرة. اختيار شخص من الشاشة يمرّ
  بمسار جديد `rnu:photosel_{profile_id}_{"p"|companion_id}`.
- `_CTX_PHOTO_PROFILE` (profile_id فقط) استُبدل بـ`_CTX_PHOTO_TARGET`
  (قاموس `{profile_id, companion_id}`) لحمل هوية المستهدَف عبر جلسة
  الرفع (نفس القيد التقني الذي حتّم هذا النمط أصلاً: collector يعيد
  الملفات فقط ولا يحمل سياق المتصل).
- دالة مستودع جديدة `save_companion_photo` (`uploads/repository.py`)
  توازي `save_patient_photo` الموجودة، تكتب على `ResidencyCompanion`
  بدل `ResidencyProfile` وتسجّل `companion_id` في السجل التدقيقي.
- عنوان شاشة الرفع نفسها يذكر اسم الهدف صراحةً ("أرسل الصورة الشخصية
  لـ «فلان»") ليعرف المترجم لمن يرفع دون لبس.

**التحقق**: `python -m py_compile` نظيف على 6 ملفات · بعد حذف
`__pycache__` بالكامل: 187 اختباراً + 170 معالجاً حقيقياً · محاكاة حيّة
فعلية بقاعدة معزولة: مريض بلا مرافقين ← الرفع يفتح مباشرة له بلا شاشة
اختيار (لا انحدار)؛ مريض بمرافقَين ← الشاشة تعرض الثلاثة بالاسم
بـcallback_data صحيح لكل واحد؛ اختيار مرافق ← عنوان الرفع يذكر اسمه
والسياق يحمل `companion_id` الصحيح؛ محاكاة اكتمال رفع فعلي لمرافق ←
`ResidencyCompanion.photo_file_id` يُحفَظ للمرافق المستهدَف تحديداً،
بلا أي أثر على المرافق الآخر أو على صورة المريض نفسه. محاكاة منفصلة
تؤكد أن `_migrate_column` يضيف العمود فعلياً على قاعدة بمخطط قديم (عمود
محذوف يدوياً ثم استدعاء `check_db_health_startup` الحقيقي يُعيده).

### 2026-08-14 (١٦) — 🗂️ زر موحَّد "رفع وثيقة" (يضم الصورة/فورم C + جواز/فيزا/تذكرة جديدة)

**الطلب**: زر جديد موحَّد يضم زرَّي "🖼️ صورة شخصية" و"📄 فورم C"
الموجودين، بالإضافة إلى أي وثيقة "تُرفع من الواصلين" ولم تُرفع فعلاً
لأن المترجم ضغط "⏭️ تخطي" بالخطأ أثناء الواصلين (جواز/فيزا/تذكرة).

**التشخيص المبدئي (بفحص قبل التنفيذ)**: جواز السفر والفيزا لم يكن لهما
أي مسار رفع/تصحيح بعد الإنشاء إطلاقاً — فقط نسخة لمرة واحدة تُنقَل من
الواصلين عند إنشاء الملف. التذاكر لم تكن موجودة إطلاقاً على الإقامات
(فقط `tickets_file_id` على جداول الواصلين). الإقامة نفسها قابلة
للتحديث لكن فقط عبر مسار "🪪 تجديد الإقامة" الكامل. اكتُشف أيضاً تعليق
موثِّق قراراً سابقاً متعمَّداً باستبعاد الجواز/الفيزا/الإقامة من زر
الرفع الموحَّد "فلا يصير للحقل الواحد مصدران" — عُرض على المستخدم صراحةً
قبل المتابعة: أكّد استثناء الإقامة (تبقى حصراً على مسار التجديد)، ووافق
على إضافة التذاكر رغم حاجتها لعمودين جديدين.

**الإصلاح**:
- عمودان جديدان `tickets_file_id` على `ResidencyProfile` و
  `ResidencyCompanion` + ترحيلهما.
- قاموس `DOCUMENT_TYPES` واحد (`uploads/views.py`) — مصدر الحقيقة
  الوحيد لأيقونة/تسمية جواز/فيزا/تذكرة، يُستورَد من كل من الشاشات
  والمستودع فلا تنحرف التسميات.
- زرّا "📄 فورم C" و"🖼️ صورة شخصية" المنفصلان على ملف المريض حُذفا
  واستُبدلا بزر واحد "🗂️ رفع وثيقة" (`rnu:docmenu_`) يفتح قائمة بـ5
  خيارات — الصورة وفورم C يوجَّهان **لنفس معالِجيهما الأصليين بلا أي
  تعديل** (نفس callback_data تماماً)، وجواز/فيزا/تذكرة مسار عام جديد
  `rnu:doc_{type}_` ← (شاشة اختيار «لمن الوثيقة؟» إن وُجد مرافقون، رفع
  مباشر بلا سؤال إن لم يوجد أي مرافق) ← `rnu:docsel_{type}_{id}_{who}`.
- دالة مستودع عامة واحدة `save_document(doc_type, ...)` تكتب على عمود
  `{doc_type}_file_id` المطابق على `ResidencyProfile` أو
  `ResidencyCompanion` حسب الهدف — لا تكرار لكل نوع.
- معالِج نتيجة رفع عام واحد `_on_document` (بلا معالجة الصورة الخاصة
  بـ`_on_photo` — الجواز/الفيزا/التذكرة تُحفَظ كما وصلت).

**التحقق**: `python -m py_compile` نظيف على 7 ملفات · بعد حذف
`__pycache__` بالكامل: 187 اختباراً + 170 معالجاً حقيقياً (بلا زيادة —
الميزة تمدّد معالِج `rnu:` الموجود بدل إضافة CallbackQueryHandler
جديد) · محاكاة حيّة فعلية بقاعدة معزولة: شاشة الملف تعرض الزر الموحَّد
الواحد فقط (لا أثر للزرَّين القديمين)؛ القائمة تعرض الخيارات الخمسة
بـcallback_data صحيح، خياَرا الصورة/فورم C يستخدمان نفس callback_data
القديم تماماً؛ جواز بلا مرافقين ← رفع مباشر للمريض؛ فيزا بمرافقين ←
شاشة اختيار بالاسمين؛ اختيار مرافق ← عنوان الرفع يذكر اسمه ونوع
الوثيقة؛ حفظ فعلي لفيزا مرافق ← تُكتَب على `ResidencyCompanion` المستهدَف
تحديداً بلا أثر على المريض؛ حفظ فعلي لتذكرة المريض ← تُكتَب على
`ResidencyProfile.tickets_file_id`. محاكاة منفصلة تؤكد أن الترحيل يضيف
`tickets_file_id` على كلا الجدولين على قاعدة بمخطط قديم.

### 2026-08-14 (١٧) — 👋 رسالة ترحيب /start العامة لا تذكر بوت الإقامات

**الطلب**: مستخدم في بوت الإقامات يضغط /start فيرى "مرحباً بك — نظام
إدارة التقارير الطبية" العام، بدل رسالة تذكر أنه في بوت الإقامات
تحديداً (كما يحدث فعلاً لمستخدمي الرعاية الصحية: "في بوت الرعاية الصحية
الذكية").

**التشخيص**: `core/routing/landing.py::resolve_user_landing_interface`
يحسب "دور هبوط" واحد لكل مستخدم بترتيب أولوية `admin > translator >
healthcare > public` — و"healthcare" وحدها لها فرع فرعي مخصَّص في رسالة
الترحيب (`user_start.py`)؛ "residency" لم يكن دوراً قائماً بذاته إطلاقاً،
فأي مستخدم لديه وحدة الإقامات فقط (بلا user_reports وبلا healthcare) كان
يسقط إلى "public" ويرى نفس النص العام المخصَّص أصلاً للمترجمين.

**الإصلاح**: أُضيف "residency" كدور هبوط ثالث بنفس أسلوب "healthcare"
تماماً — بعد healthcare في ترتيب الأولوية (`admin > translator >
healthcare > residency > public`)، فلا يتأثر أي مستخدم لديه بالفعل
user_reports أو healthcare بهذا التغيير. أُضيف فرع مطابق في
`user_start.py` يعرض "في بوت الإقامات". تحقّقت أن `handle_start_main_menu`
(معالِج الزر القديم "▶️ ابدأ") لا يحتاج فرعاً جديداً — فرعه `else`
يستدعي `dynamic_user_kb()` التي تبني القائمة من وحدات المستخدم الفعلية
بصرف النظر عن اسم الدور، فتعمل بشكل صحيح للإقامات تلقائياً دون أي تعديل
(صُحِّح التعليق المصاحب فقط ليعكس ذلك بدقة).

**التحقق**: `python -m py_compile` نظيف · بعد حذف `__pycache__` بالكامل:
187 اختباراً + 170 معالجاً حقيقياً · محاكاة حيّة فعلية بقاعدة بيانات
معزولة (نفس أسلوب `tests/test_rbac_smoke.py`) عبر `grant_module` الحقيقية
و`resolve_user_landing_interface` الحقيقية: مستخدم إقامات فقط ← يحصل
الآن على `role="residency"` (كان `"public"` قبل الإصلاح)؛ مترجم يملك
الإقامات أيضاً ← يبقى `"translator"` بلا تغيير (ترتيب الأولوية سليم)؛
مستخدم رعاية صحية فقط ← يبقى `"healthcare"` بلا تغيير؛ مستخدم بلا أي
وحدة ← يبقى `"public"` بلا تغيير — لا انحدار على أي دور موجود سابقاً.

### 2026-08-14 (١٨) — 🚀 توحيد زر "▶️ ابدأ الآن" عبر الإقامات/الخدمات العامة/الرعاية الصحية

**الطلب**: إضافة زر "ابدأ الآن" لبوت الإقامات وبوت الخدمات العامة وبوت
الرعاية الصحية، بنفس سلوك زر "🚀 ابدأ" في بوت المترجمين — تحديث الصفحة
ثم البدء من جديد.

**التشخيص**: بوت الرعاية الصحية كان يملك بالفعل زراً بنفس الاسم تقريباً
("▶️ ابدأ الآن") لكن **بسلوك مختلف عمداً وموثَّق**: يفتح قائمة الرعاية
الصحية مباشرة (اختصار)، لا يعيد إرسال رسالة الترحيب ولا يعيد بناء لوحة
المفاتيح من الصفر مثل /start الكامل. عُرض هذا التمييز على المستخدم
صراحةً قبل المتابعة — أكّد أنه يريد توحيد السلوك ليطابق المترجمين
تماماً، وليس الإبقاء على الاختصار القديم.

**الإصلاح**:
- `core/modules_bootstrap.py`: أُضيف `"▶️ ابدأ الآن"` إلى `menu_buttons`
  وصفّ جديد في `keyboard_rows` لكل من `general_services` و`residency`
  (لم يكن لهما أي زر مماثل من قبل).
- `bot/handlers/user/user_start.py::register()`: معالِج عام واحد جديد
  `MessageHandler(filters.Regex("^▶️ ابدأ الآن$"), user_start)` — يغطّي
  الثلاثة أزرار (الرعاية الصحية/الخدمات العامة/الإقامات) بتسجيل واحد،
  بنفس نمط "🚀 ابدأ" الموجود أصلاً للمترجمين.
- `modules/healthcare/menu.py`: حُذفت دالة `_show_menu` وتسجيلها بالكامل
  (كانت المعالِج المخصَّص القديم لنفس النص بسلوك الاختصار) — بقاء
  التسجيلين معاً كان سيُسقِط أحدهما بصمت (نفس نمط تعارض المعالِجات
  المسجَّلة سابقاً هذه الجلسة). `_show_menu_chennai` (زر مختلف تماماً:
  "🏙️ الرعاية الصحية - تشناي") لم يُلمَس.

**ملاحظة تقنية غير مؤثِّرة وظيفياً**: بما أن ثلاث وحدات (`healthcare`,
`general_services`, `residency`) تُعلن الآن نفس نص الزر في
`menu_buttons`، فإن `registry.resolve_button()` (يُستخدَم فقط داخل
`core/conversation/interrupt.py` لتحديد `extra_wipe_keys`/hooks عند
مقاطعة محادثة نشطة) يُرجِع الوحدة الأخيرة المسجَّلة فقط ("last wins") —
لا يهم عملياً هنا لأن none من الوحدات الثلاث تُعرِّف `on_activate`/
`on_deactivate`، و`user_start()` نفسها تصفّر `context.user_data` بالكامل
بلا شرط فور تشغيلها بصرف النظر عن أي `wipe_keys` جزئية استُخدمت قبلها.
نفس النمط (زر واحد مُعلَن في أكثر من وحدة) موجود أصلاً بين `user_reports`
و`chennai_reports` لزر "🚀 ابدأ" — لا شيء جديد. حُدِّث اختبار
`test_bootstrap_registers_healthcare` (كان يفترض ملكية حصرية لـ
healthcare) ليتحقق من إعلان الزر في الوحدات الثلاث بدل نتيجة
`resolve_button` غير الحاسمة.

**التحقق**: `python -m py_compile` نظيف · بعد حذف `__pycache__` بالكامل:
187 اختباراً + 170 معالجاً حقيقياً (العدد الكلي بلا تغيير — إزالة معالِج
من healthcare قابلها إضافة معالِج عام واحد) · محاكاة حيّة فعلية عبر
`register_all_handlers` الحقيقي: تأكيد أن **معالِجاً واحداً بالضبط**
مسجَّل لنص "▶️ ابدأ الآن" وأنه `user_start` (لا تعارض، لا معالِج ميت) ·
محاكاة منفصلة عبر `dynamic_user_kb` الحقيقية + `grant_module` الحقيقية:
مستخدم إقامات فقط ومستخدم خدمات عامة فقط يريان الآن الزر الجديد في لوحة
مفاتيحهما، ومستخدم رعاية صحية فقط لا يزال يراه (لا انحدار).

### 2026-08-14 (١٩) — 🚐 الجهة الموصلة بعد الملاحظات في تقرير الواصلين

**الطلب**: "في زر الواصلين أثناء نشر التقرير، أريد تظهر [الجهة الموصلة]
بعد [الملاحظات] في التقرير".

**التنفيذ**: وُجدت ثلاث شاشات مرتبطة كانت تعرض الحقلين بترتيب مختلف
عن بعضها (تناقض قائم أصلاً بين شاشة المراجعة والتقرير المنشور فعلياً):
- **نص التقرير المنشور فعلياً** (`arrivals/flow.py`، حلقة بناء `body`
  لكل مريض): كان `escort → address → services → notes`؛ صار
  `address → services → notes → escort`.
- **شاشة المراجعة قبل النشر** (`build_review` في `arrivals/views.py`):
  كان `escort → specialist → notes`؛ صار `notes → escort → specialist`.
- **قائمة أزرار "✏️ تعديل بيانات"** (`_P_EDIT_FIELDS`): كانت **صحيحة
  أصلاً** — `notes → services → escort → specialist` — لم تُعدَّل
  إجمالاً (محاولة أولى نقلت "escort" ليلتصق مباشرة بـ"notes" فكسرت هذا
  الترتيب الصحيح؛ رُجِع عنها فوراً بعد اكتشاف التعليق الموثِّق في
  `flow.py` عند `STEP_P_INDIV_NOTES`: "كتلة الختام لكل مريض: ملاحظات ←
  الخدمات المقدَّمة ← الجهة الموصلة ← المختص" — وهو **الترتيب الحقيقي
  المعتمَد فعلياً في تدفّق الإدخال نفسه**، فالجهة الموصلة كانت أصلاً بعد
  الملاحظات هناك بفارق حقل واحد لا خطأ).

**الدرس**: التعليقات القريبة من كود ذي صلة (حتى لو بدت وصفاً لتدفّق
مختلف — هنا: ترتيب *أسئلة* الإدخال لا *عرض* التقرير) تكشف أحياناً
تناقضاً بين تصميمين متوازيين لنفس المفهوم — التحقّق منها قبل "الإصلاح"
منع كسر ترتيب صحيح أصلاً في `_P_EDIT_FIELDS`.

**التحقق**: `python -m py_compile` نظيف على الملفين · بعد حذف
`__pycache__` بالكامل: 187 اختباراً + 170 معالجاً حقيقياً · محاكاة حيّة
فعلية بـ`build_review` الحقيقية (Session بمريض واحد بكل الحقول الثلاثة
معبَّأة): تأكيد أن الجهة الموصلة تظهر مباشرة بعد الملاحظات ومباشرة قبل
المختص في النص الفعلي المُولَّد؛ فحص مباشر لترتيب `_P_EDIT_FIELDS`
الحقيقي يؤكد بقاءه `notes → services → escort → specialist`؛ وفحص نص
`flow.py` مباشرة يؤكد أن سطر الجهة الموصلة صار يلي سطر الملاحظات مباشرة
في بنية التقرير الفعلية المنشورة.

### 2026-08-14 (٢٠) — 🎗️ إلغاء الحفظ/التقدّم التلقائي في زر "جلسات الأورام" المدمج

**الطلب**: "في زر جلسات الأورام، ألغِ لي ميزة الحفظ التلقائي للجلسات،
أريد بكل مرة تطلب من المستخدم أن يدخل من جديد الخطة والدورة".

**التشخيص**: `oncology_multiselect.py` (زر "🎗️ جلسات الأورام" المدمج
لاختيار عدة أنواع علاج معاً) كان يستدعي `get_active_plan()` عند بداية كل
نوع في الطابور؛ إن وُجدت خطة نشطة سابقة لنفس المريض/النوع، يستدعي
`advance_plan()` فوراً (يزيد رقم الجلسة/الدورة الحالية +1 تلقائياً بلا أي
سؤال للمستخدم) ويعرض ملخص التقدّم مباشرة — يتخطّى تماماً سؤالي "كم العدد
الكلي؟" و"ما رقم الجلسة الحالية؟". هذا سلوك **مختلف عمداً** عن نقاط
الدخول الفردية (`treatment_sessions.py::_start_simple_session_flow`
للكيماوي/الموجَّه/المناعي المفردة، وكذلك `radiation_therapy.py`) التي
تملك منطق تقدّم تلقائي مماثلاً خاصاً بها ولم يطلب المستخدم لمسها — الطلب
مخصَّص صراحةً بزر "جلسات الأورام" المدمج، فبقيت تلك المسارات كما هي.

**الإصلاح** (`bot/handlers/user/user_reports_add_new_system/flows/oncology_multiselect.py`):
- `_process_next_in_queue`: حُذف فرع `if plan: advance_plan(...)` بالكامل
  — الدالة الآن تعرض سؤال "كم العدد الكلي؟" (`_QUEUE_TOTAL_PROMPTS`)
  دائماً لكل نوع في الطابور، بصرف النظر عن وجود خطة نشطة سابقة أم لا.
- `handle_oncology_queue_current`: عند حفظ الرقمين المُدخَلين يدوياً
  (العدد الكلي + رقم الجلسة الحالية)، تُفحَص الخطة النشطة أولاً عبر
  `get_active_plan()`؛ إن وُجدت، تُحدَّث هي نفسها بالقيمتين الجديدتين عبر
  `edit_plan()` (بدل إنشاء صف "نشط" ثانٍ موازٍ عبر `create_plan()`) — يحافظ
  هذا على قاعدة "خطة نشطة واحدة لكل مريض/نوع" ويكتب سجلاً تدقيقياً كاملاً
  (قبل/بعد) في `TreatmentPlanChangeLog` بسبب `"إعادة إدخال يدوية للخطة
  والدورة (أُلغي التقدّم التلقائي)"`؛ بلا خطة سابقة، يستمر المسار القديم
  عبر `create_plan()` بلا تغيير.
- الاستيراد من `services.treatment_plan_service` عُدِّل بإسقاط
  `advance_plan` (لم تعد مستخدَمة في هذا الملف) وإضافة `edit_plan`.

**التحقق**: `python -m py_compile` نظيف · بعد حذف `__pycache__` بالكامل:
187 اختباراً + 170 معالجاً حقيقياً (بلا تغيير في العدد) · محاكاة حيّة
فعلية بقاعدة بيانات معزولة عبر الدوال الحقيقية: خطة كيماوي نشطة مسبقاً
لمريض (عدد=6، جلسة حالية=2) ← دخول طابور الأورام لنفس النوع يعرض سؤال
"كم العدد الكلي؟" فوراً (لا تقدّم تلقائي ولا رسالة ملخص) ← إدخال عدد=8
وجلسة=3 يدوياً ← التأكد أن **صفاً نشطاً واحداً بالضبط** موجود بنفس معرّف
الخطة الأصلي (لم يُنشَأ صف مكرَّر) وبالقيمتين الجديدتين (8/3) ← التأكد من
وجود صف `TreatmentPlanChangeLog` يوثّق التعديل اليدوي ← اختبار مستقل لنوع
بلا خطة سابقة (إشعاع) يؤكد أن `create_plan()` ما زال يعمل بشكل طبيعي عند
عدم وجود خطة نشطة.

### 2026-08-14 (٢١) — 🔍 تدقيق شامل للأنواع الـ17 مقابل field_registry.py: 3 أخطاء حقيقية في التعديل بعد النشر (أبرزها «قرار الطبيب» لخمسة أنواع)

**الطلب** (مهمة #125 المؤجَّلة من جلسة توحيد الحقول 2026-08-04): تدقيق كل
أنواع التقارير مقابل `field_registry.py` والتأكد أن التعديل قبل/بعد النشر
يعمل فعلياً، لا افتراضاً.

**المنهجية**: لكل مفتاح في `field_registry.py` له `db_column` مختلف عن
`key`، أو مفتاح لا يطابق اسم عمود حقيقي في `Report` — هذا هو **شكل الخلل
المتكرر** المكتشَف ثلاث مرات سابقاً هذه الجلسة (complaint/status/
recommendations) — تتبُّع كامل مسار الزر (بناء القائمة ← `callback_data`
← `save_edit_to_database`) للتأكد أن الاسم المُرسَل فعلياً يطابق فرعاً
حقيقياً، لا `else` العام النهائي (`setattr(report, field_name, new_value)`
— يعمل بصمت فقط إذا كان `field_name` اسم عمود Report حقيقي؛ إن لم يكن،
يضبط خاصية بايثون وهمية غير مُعرَّفة كعمود، لا تُحفَظ عند `commit()` أبداً
ولا ترفع أي استثناء يكشف الخطأ).

**ثلاثة أخطاء حقيقية حيّة مكتشَفة ومُصلَحة** (`bot/handlers/user/user_reports_edit.py::save_edit_to_database`):

1. **`tests` (🧪 الفحوصات والأشعة، استشارة جديدة فقط)**: عمود
   `field_registry.py` المخصَّص `medications`، لكن لا فرع `field_name ==
   'tests'` إلا لـ«استشارة مع قرار عملية» — يسقط في `else` العام.
   **الإصلاح**: فرع جديد يكتب `report.medications` ويحدِّث مقطع «الفحوصات
   المطلوبة:» داخل `doctor_decision` المركّب مع الحفاظ على بقية المقاطع.
2. **`recommendations` (💡 التوصيات الطبية، استشارة أخيرة فقط)**: عمود
   `field_registry.py` المخصَّص `treatment_plan`، و`Report` لا يملك عمود
   `recommendations` إطلاقاً (الاسم موجود فقط في جدول `monthly_evaluations`
   منفصل تماماً) — يسقط في `else` العام. **الإصلاح**: فرع جديد يكتب
   `report.treatment_plan` ويحدِّث مقطع «التوصيات الطبية:».
3. **`decision` (📝 قرار الطبيب — الخلل الأوسع أثراً)**: `field_registry.py`
   يعرِّف هذا الحقل بمفتاح `decision` (`db_column="doctor_decision"`) لخمسة
   أنواع: استشارة جديدة، مراجعة/عودة دورية، متابعة في الرقود، طوارئ،
   استشارة أخيرة. الفرع الوحيد الذي كان يطابق `field_name == 'decision'`
   مقيَّد بـ«استشارة مع قرار عملية» فقط؛ وفرع آخر يطابق `field_name ==
   'doctor_decision'` (اسم مختلف) للحفاظ على مقطع القرار — **لكن الأزرار
   الفعلية المبنية عبر `get_editable_fields_by_action_type` (والتي تقرأ من
   `field_registry.py`) ترسل `callback_data` بالمفتاح الحرفي `decision`،
   لا `doctor_decision`** — فتسقط الأنواع الخمسة جميعاً في `else` العام.
   **تعديل «قرار الطبيب» بعد النشر لأشهر خمسة أنواع تقارير في البوت كان
   يختفي صامتاً دائماً، بلا أي خطأ ظاهر للمستخدم.** **الإصلاح**: الفرع
   الموجود لـ`doctor_decision` وُسِّع ليطابق `field_name in ('doctor_decision',
   'decision')` معاً، فيستخدم `_replace_decision_segment()` الموجودة أصلاً
   (تستبدل مقطع «قرار الطبيب:» فقط، تحافظ على التشخيص/الفحوصات/وضع الحالة
   الأخرى في نفس النص).

**تحقَّق أنها سليمة فعلاً (لا حاجة لإصلاح)**: `discharge_type`،
`device_details`، `admission_type`/`admission_notes`/`operation_details`
(طوارئ)، `case_status` — كل هذه أسماء مفاتيحها **مطابقة حرفياً** لأعمدة
`Report` الحقيقية، فـ`else` العام (`setattr`) يحفظها بصمت وبنجاح فعلاً؛
`endoscopy_procedures` (JSON) و`session_number`/`chemo_cycle_number`/
`chemo_session_number` لها فروع مخصصة موجودة مسبقاً وسليمة؛ الحقول
الأساسية (التشخيص، الشكوى، الملاحظات، نوع الأشعة، حقول تأجيل الموعد،
حقول الزراعة الثلاثة) جميعها أسماء أعمدة حقيقية مباشرة.

**التحقق**: ثلاث محاكاة حيّة منفصلة عبر `save_edit_to_database` الحقيقية
(لا افتراض) — كل واحدة أعادت إنتاج الخلل أولاً (قيمة العمود المخصَّص لم
تتغيَّر بعد التعديل) قبل الإصلاح، ثم أكَّدت الإصلاح بعده: `tests`→
`medications` لاستشارة جديدة، `recommendations`→`treatment_plan` لاستشارة
أخيرة، `decision`→مقطع `doctor_decision` لكل من استشارة جديدة **وطوارئ**
(مع التأكد أن تعديل القرار لا يمحو مقطعي التشخيص ووضع الحالة المجاورين).
بعد حذف `__pycache__` بالكامل: 187 اختباراً + 170 معالجاً حقيقياً (بلا
تغيير في العدد — تعديل منطق داخلي فقط، لا تسجيل معالِجات جديدة).

**متبقٍ من مهمة #124**: تشغيل `scripts/backfill_legacy_report_fields.py
--apply` فعلياً على قاعدة بيانات الإنتاج (يتطلب نسخة احتياطية أولاً —
مسؤولية المستخدم على السيرفر)، ثم حذف كود استخراج `doctor_decision`
القديم بعد التحقق من نجاح الترحيل.

### 2026-08-14 (٢٢) — 🔢 إلغاء سؤال "العدد الكلي" نهائياً للكيماوي/الموجّه/المناعي — إدخال يدوي بحت بلا أي TreatmentPlan

**الطلب**: "في جلسات الأورام أريد أن ألغي زر المجموع الكلي، لا داعي أن
يكون يدخل المجموع الكلي سواء كيماوي مناعي موجّه".

**التوضيح المطلوب** (عبر أسئلة توضيحية قبل التنفيذ): (أ) من أي مسار
بالضبط؟ ← "من الاثنين معاً" (الأزرار المفردة كيماوي/مناعي/موجّه، **و**
الزر المدمج "🎗️ جلسات الأورام" عند اختيار نوع واحد أو أكثر منها).
(ب) ماذا يبقى من نظام تتبّع الخطة (TreatmentPlan) لهذه الأنواع الثلاثة
بعد حذف السؤال؟ ← "يُلغى بالكامل — إدخال يدوي بحت"، بنفس فلسفة غسيل الكلى
تماماً (لا خطة محفوظة، لا عدّ كلي، كل تقرير من الصفر).

**السياق**: هذا الطلب امتداد مباشر للإصلاح السابق في نفس الجلسة (٢٠) —
هناك أُلغي **التقدّم/الحفظ التلقائي** فقط (المستخدم أصبح يُسأل عن العدد
الكلي والرقم الحالي من جديد في كل مرة بدل القراءة الصامتة من خطة سابقة).
هنا يُلغى **مفهوم العدد الكلي نفسه** — لم يعد له أي فائدة فعلية بعد
إلغاء التقدّم التلقائي أصلاً، فحُذف السؤال بالكامل وليس التلقائية فقط،
ومعه حُذف مفهوم TreatmentPlan ذاته لهذه الأنواع الثلاثة (الإشعاعي وحده
استُثني — لم يُطلَب تغييره، ولا يزال يعتمد على TreatmentPlan فعلياً
لحساب `radiation_therapy_remaining`).

**الإصلاح** — أربعة ملفات، بنفس نمط غسيل الكلى تماماً (نص لقطة مباشر في
`treatment_plan_summary`، مطابَق لاحقاً بتعبير نمطي، بلا أي صف
TreatmentPlan جديد):

- **`flows/treatment_sessions.py`** (الأزرار المفردة): `_start_simple_session_flow`
  (يخدم كيماوي/موجّه/مناعي معاً) أُعيدت كتابتها بالكامل — حُذف فحص
  `get_active_plan`/`advance_plan` وسؤال "كم العدد الكلي؟"؛ تسأل الآن
  مباشرة "🔢 رقم الجلسة/الدورة الحالية؟" (الكلمة الصحيحة حسب النوع عبر
  `unit_labels`). دالة جديدة `_build_manual_session_summary()` تبني نص
  اللقطة. `handle_treatment_plan_setup` (نفس الحالة `TREATMENT_PLAN_SETUP`
  المُعاد استخدامها بمعنى جديد بدل حالات جديدة) لم تعد تُنشئ/تُعدِّل أي
  خطة — تكتب اللقطة النصية مباشرة وتنتقل: الكيماوي ← سؤال رقم الجلسة ضمن
  الدورة (كان يدوياً بحتاً أصلاً، لم يُمَس)؛ البقية ← الشكوى مباشرة.
  شاشة "✏️ تعديل الخطة" (`_show_plan_display` وما بعدها) ومعالِجات
  `CHEMO_CYCLES_*` (نمط الدورات القديم) أصبحت **غير قابلة للوصول** من
  إنشاء تقرير جديد — بقيت مسجَّلة كرمز خامل (خطط قديمة في قاعدة البيانات
  تبقى كما هي، غير قابلة للتعديل عبر هذه الشاشة بعد الآن؛ حذفها الفعلي
  مهمة منفصلة مستقبلية، نفس نمط تنظيف الكود الميت الموثَّق أعلاه).
- **`flows/oncology_multiselect.py`** (الزر المدمج، طابور 2+ أنواع):
  `_QUEUE_TOTAL_PROMPTS` صار خاصاً بالإشعاعي فقط؛ قاموس جديد
  `_QUEUE_CURRENT_ONLY_PROMPTS` لكيماوي/مناعي/موجّه. `_process_next_in_queue`
  يتفرّع حسب النوع: الإشعاعي (كما كان: عدد كلي ثم حالي، `ONCOLOGY_QUEUE_TOTAL`)
  مقابل البقية (رقم حالي فقط مباشرة، `ONCOLOGY_QUEUE_CURRENT`).
  `handle_oncology_queue_current` يتفرّع بالمثل: لكيماوي/مناعي/موجّه يبني
  اللقطة النصية عبر `_build_manual_session_summary` المستوردة من
  `treatment_sessions.py` بلا أي `create_plan`/`edit_plan`؛ الإشعاعي وحده
  يستمر بمنطق TreatmentPlan الأصلي دون أي تغيير.
- **`user_reports_edit.py`** (تعديل بعد النشر) **و**
  **`edit_handlers/before_publish/treatment_and_endoscopy_edit.py`**
  (تعديل قبل النشر/المسودة): `_current_session_number_display`/
  `_apply_session_number_edit` في كلا الملفين — التعبير النمطي وُسِّع من
  `رقم الجلسة الحالية:` (غسيل الكلى فقط) إلى `رقم (?:الجلسة|الدورة)
  الحالية:` (يطابق الكلمتين معاً). **الأولوية الآن دائماً للنص المباشر**
  في `treatment_plan_summary` (المصدر الوحيد للتقارير الجديدة)؛
  `get_active_plan`/`edit_plan` يُحاوَلان فقط كـ **fallback** — للتوافق
  مع تقارير/خططاً حقيقية نُشرت **قبل** هذا التغيير، فتبقى قابلة للعرض
  والتعديل بلا انحدار.

**التحقق**: `python -m py_compile` نظيف على الملفات الأربعة · بعد حذف
`__pycache__` بالكامل: 187 اختباراً + 170 معالجاً حقيقياً (بلا تغيير في
العدد) · محاكاة حيّة شاملة عبر الدوال الفعلية: الزر المفرد (موجّه) لا
يسأل عن عدد كلي، يسأل مباشرة عن الرقم الحالي، ولا يُنشئ أي صف
TreatmentPlan؛ الزر المفرد (كيماوي) يسأل عن رقم الدورة الحالية (بالكلمة
الصحيحة) ثم رقم الجلسة ضمن الدورة كالمعتاد؛ الزر المدمج بكيماوي فقط ثم
بمناعي فقط — كلاهما بلا سؤال عدد كلي وبلا أي صف TreatmentPlan (تحقّق
مباشر: `count() == 0` على جدول `treatment_plans`)؛ الزر المدمج بالإشعاعي
وحده — **لم يتأثر**، ما زال يسأل عن العدد الكلي كما كان؛ تعديل بعد
النشر لتقرير موجّه يقرأ الرقم الحالي من النص مباشرة (بلا أي خطة) ويحفظ
تعديلاً جديداً بنجاح؛ نفس التحقق لدالتَي التعديل قبل النشر (المسودة)
لتقرير كيماوي.

### 2026-08-14 (٢٣) — 🏥 زر "طريقة الإعطاء" (عيادة يومية/رقود) لا يظهر عند اختيار نوع واحد فقط من جلسات الأورام

**البلاغ**: "حقل أو زر طريقة الإعطاء هذه المرة، رعاية يومية أو رقود، في
جلسات الأورام — هذا لا يظهر إلا إذا اخترت أكثر من خيار (كيماوي ومناعي
أو غيره)، أما إذا اخترت كيماوي فقط لا تظهر".

**السبب الجذري**: سؤال "🏥 طريقة الإعطاء هذه المرة؟" (`_ask_delivery_mode`
في `oncology_multiselect.py`) كان مبنياً ومسجَّلاً **فقط** داخل مسار
الطابور (اختيار نوعين أو أكثر من شاشة "🎗️ جلسات الأورام"). أما الأزرار
المفردة (كيماوي/موجّه/مناعي، سواء من قائمة الإجراءات مباشرة أو من نفس
شاشة "🎗️ جلسات الأورام" عند اختيار نوع واحد فقط — وكلاهما يُفوَّض لنفس
دوال `treatment_sessions.py`) كانت تنتقل مباشرة من رقم الجلسة/الدورة
الحالية إلى الشكوى (`_prompt_complaint`)، بلا استدعاء هذا السؤال إطلاقاً
— لم يكن موجوداً هناك من الأساس، لا خللاً في شرط عرضه.

**الإصلاح**: (النطاق: كيماوي/موجّه/مناعي — نفس الثلاثة أنواع من الإصلاح
السابق (٢٢)؛ الإشعاعي لم يُطلَب تغييره ويبقى كما هو)
- `flows/treatment_sessions.py`: `handle_treatment_plan_setup` (فرع
  الموجّه/المناعي) و`handle_chemo_session_number` (بعد رقم الجلسة ضمن
  الدورة للكيماوي) يستدعيان الآن `_ask_delivery_mode` (استيراد محلي من
  `oncology_multiselect.py` لتفادي استيراد دائري) بدل `_prompt_complaint`
  مباشرة.
- `flows/oncology_multiselect.py`: `_ask_delivery_mode`/
  `handle_oncology_delivery_mode`/`ONCOLOGY_DELIVERY_MODE`/
  `ONCOLOGY_DELIVERY_DAYS` مُشتركة أصلاً (حالات محادثة عامة مسجَّلة مرة
  واحدة في `conversation_handler.py`، لا تخص الطابور تحديداً) فأُعيد
  استخدامها كما هي بلا تعديل. الدالتان اللتان تفترضان وجود طابور فعلياً
  عُدِّلتا لتمييز "الوضع المفرد" (لا `_onc_queue` إطلاقاً) عن وضع الطابور:
  `handle_oncology_delivery_days` تُلحِق نص "🛏️ رقود (N أيام متوقعة)"
  مباشرة بـ`treatment_plan_summary` بدل قاموس `_onc_summaries` غير
  الموجود في الوضع المفرد؛ `_advance_queue_after_delivery` تتحقق أولاً من
  `data.get("_onc_queue") is None` وتنتقل مباشرة للشكوى (بدل استدعاء
  `_process_next_in_queue` الذي كان سيبني بيانات طابور وهمية ويكسر
  التقرير).

**التحقق**: `python -m py_compile` نظيف على الملفين · بعد حذف
`__pycache__` بالكامل: 187 اختباراً + 170 معالجاً حقيقياً (بلا تغيير في
العدد — الحالتان مسجَّلتان أصلاً) · محاكاة حيّة عبر الدوال الفعلية: زر
موجّه مفرد (بلا طابور إطلاقاً) يسأل الآن عن طريقة الإعطاء فعلاً، اختيار
"رقود" يسأل عن عدد الأيام، يُحفَظ في نص الخطة، وينتقل للشكوى مباشرة
(بلا أي مفتاح `_onc_queue` في البيانات إطلاقاً)؛ زر كيماوي مفرد يسأل عن
طريقة الإعطاء بعد رقم الجلسة ضمن الدورة، اختيار "عيادة يومية" ينتقل
للشكوى فوراً؛ الطابور المدمج (كيماوي+موجّه معاً) **لم يتأثر** — بعد
الإجابة عن طريقة الإعطاء للكيماوي، ينتقل صحيحاً للنوع التالي في الطابور
(الموجّه) بدل القفز للشكوى مباشرة.

### 2026-08-14 (٢٤) — 📋 شاشة "الأسماء المعلّقة" في بوت الواصلين (جزء أ من دورة معلّق↔أرشيف↔معلّق)

**الطلب**: ربط بوت الخدمات العامة (الواصلين) بالإقامات في دورة حياة
كاملة: اسم إداري معلّق ← بيانات مكتملة (إقامة معلّقة بانتظار الصورة/فورم
C) ← أرشفة بعد اكتمالهما ← معلّق من جديد عند اقتراب الانتهاء. بعد تدقيق
شامل للآلية الحالية (خطة كاملة في
`C:\Users\nalgu\.claude\plans\linked-tumbling-galaxy.md`) واتفاق على 4
قرارات تصميم صريحة مع المستخدم، نُفِّذ الجزء الأول من 3.

**التشخيص**: لا يوجد أي مفهوم "معلّق" ظاهر اليوم — اسم يُضاف عبر "🤝
مريض جديد مع مرافقين" (`admin_schedule_management.py`) يُكتب في
`Patient` بلا أي علم يميّزه عن أي اسم آخر؛ محدِّد شاشة اختيار الاسم في
"🛬 الوصول" (`patient_selector` بعلم `only_companion_flow=True`) يعرض
هذه الأسماء فقط ضمنياً (بلا استخدامها بعد) لكن بلا أي شاشة تُظهر ذلك
صراحة للمستخدم.

**الإصلاح**:
- `db/models.py` + `db/maintenance.py`: عمود جديد
  `Patient.pending_arrival` (Boolean, default False) + ترحيله
  (`_migrate_column(conn, "patients", "pending_arrival", "BOOLEAN")`).
- `services/patients_service.py::add_patient()`: بارامتر جديد
  `pending_arrival: bool = False`.
- `admin_schedule_management.py`: تمرير `pending_arrival=True` عند
  إضافة المريض والمرافقين عبر "🤝 مريض جديد مع مرافقين" (نقطتان —
  `handle_start_patient_with_companions`/`handle_companion_name_input`).
  الإضافة العامة "➕ إضافة اسم جديد" لا تتأثر (تبقى `False` كالمعتاد).
- `services/patients_service.py`: دالتان جديدتان —
  `get_pending_arrival_names()` (تجمع كل عائلة `companion_parent`
  مع مرافقيها `pending_arrival=True`) و
  `clear_pending_arrival_by_names(names)` (مطابقة بالاسم الكامل — نفس
  أسلوب فحص التكرار في `add_patient` نفسها، لأن `session.completed_patients`
  في الواصلين لا يحمل معرّف السجل، فقط الاسم المُختار).
- `modules/general_services/arrivals/flow.py`: عند تأكيد دفعة وصول
  (`STEP_REVIEW`)، استدعاء جديد fire-and-forget لـ
  `clear_pending_arrival_by_names()` بأسماء كل مريض ومرافقيه في الدفعة
  — بجانب `create_profiles_from_arrival_batch`/`sync_arrivals_to_patient_registry`
  الموجودين، بنفس نمط try/except اللاحرج.
- `modules/general_services/arrivals/views.py`: زر جديد "📋 الأسماء
  المعلّقة" في شاشة "🛬 الوصول" الفرعية + `build_pending_names_list()`
  (عرض فقط، بلا أزرار إجراء — الاستخدام الفعلي يبقى عبر منتقي "➕ تسجيل
  دفعة وصول جديدة" العادي).
- `modules/general_services/arrivals/flow.py`: إضافة `action ==
  "pending_names"` لموزِّع `gsa:` الموجود (`_dispatch_callback_inner`).

**التحقق**: `python -m py_compile` نظيف على كل الملفات · بعد حذف
`__pycache__` بالكامل: 187 اختباراً + 170 معالجاً حقيقياً (بلا تغيير —
الزر الجديد يمر عبر موزِّع `gsa:` الموجود، لا معالِج جديد) · محاكاة حيّة
عبر الدوال الفعلية: إضافة عائلة (مريض + مرافقان) عبر `add_patient`
الحقيقية بـ`pending_arrival=True` ⇒ ظهورها في `get_pending_arrival_names()`
بالضبط (مريض + المرافقان)؛ اسم عام مُضاف بلا `pending_arrival` لا يظهر
إطلاقاً؛ استدعاء `clear_pending_arrival_by_names()` لاسمين من الثلاثة
(محاكاة دفعة وصول استخدمتهما فقط) ⇒ التأكيد أن العائلة تختفي كاملة من
القائمة (المريض نفسه لم يعد معلَّقاً) بينما المرافق الثالث المتخطَّى
يبقى `pending_arrival=True` فعلياً في قاعدة البيانات (تحقّق مباشر) ·
اختبار منفصل: بناء جدول `patients` بالبنية القديمة (بلا العمود) والتأكد
أن `_migrate_column` الحقيقية تضيفه بنجاح — يحاكي ترقية قاعدة بيانات
إنتاج فعلية.

**متبقٍ (الجزءان ب/ج من نفس الخطة)**: حالتا `pending_documents`/
`archived` الجديدتان في الإقامات + زر الأرشفة اليدوي + شاشة "الملفات
المعلّقة (وثائق ناقصة)" + توسيع `TRACKABLE_STATUSES` — الخطة الكاملة
موثَّقة في ملف الخطة أعلاه.

### 2026-08-14 (٢٥) — 📋🗄️ حالتا "بانتظار الوثائق"/"مؤرشفة" + زر الأرشفة اليدوي (الجزء ب من دورة معلّق↔أرشيف↔معلّق)

**الطلب**: تكملة الجزء الثاني من الخطة الموثَّقة في (٢٤) — ملف إقامة قادم
من "🛬 الوصول" يبدأ بحالة "بانتظار الوثائق" بدل "نشطة" مباشرة، ولا ينتقل
لحالة "مؤرشفة" إلا باعتماد يدوي صريح بعد اكتمال الصورة الشخصية وفورم C
معاً.

**الإصلاح**:
- `modules/residency/constants.py`: حالتان جديدتان في
  `RESIDENCY_STATUS_LABELS`/`RESIDENCY_STATUS_ICONS` — `pending_documents`
  ("📋 بانتظار الصورة/فورم C") و`archived` ("🗄️ مؤرشفة"). لم تُلمَس
  `TRACKABLE_STATUSES` بعد (الجزء ج القادم).
- `modules/residency/profiles/models.py::save_profile()`: بارامتر جديد
  `status: str = "active"` (الافتراضي يحافظ على كل مستدعٍ قديم بلا أي
  تغيير سلوك — تحقَّق مباشرةً بمحاكاة حيّة).
- `create_profiles_from_arrival_batch()`: يكتب `status="pending_documents"`
  بدل `"active"` لكل ملف جديد ناتج من دفعة وصول (من الآن فصاعداً فقط —
  الدالة تُنشئ فقط، لا تُعدِّل ملفات موجودة). حالة `ResidencyCompanion`
  تبقى `"active"` بلا تغيير (فورم C حقل عائلي واحد على المريض فقط، قرار
  سابق موثَّق — لا داعٍ لحالة "بانتظار وثائق" مستقلة للمرافقين).
- `admin_schedule_management.py::_create_residency_profile_for_companion_flow`:
  يمرِّر الآن `status="pending_documents"` أيضاً (نفس المنطق — ملف
  فارغ من المستندات عند الإنشاء عبر "🤝 مريض جديد مع مرافقين").
- دالة جديدة `modules/residency/profiles/models.py::mark_archived()` —
  تتحقق مباشرة (دفاع بعمق، رغم أن الزر لا يظهر أصلاً إلا حين تتحقق
  الشروط) من وجود `photo_file_id` و`form_c_file_id` معاً قبل ضبط
  `status="archived"` وتسجيل `ResidencyUpdate` (نفس نمط
  `mark_renewal_submitted` الموجود في `followup/repository.py`).
- `modules/residency/profiles/views.py::build_profile_detail()`: زر
  شرطي جديد "✅ اعتماد ونقل إلى الأرشيف" — يظهر فقط حين الوثيقتان
  موجودتان و`status != "archived"`.
- `modules/residency/profiles/flow.py`: معالِج جديد
  `rna:archive_confirm_{id}` يستدعي `mark_archived` ويعيد رسم شاشة ملف
  المريض (نفس نمط `view_{id}` الموجود).

**لم يُلمَس عمداً**: مسار `rnr:` (التجديد) بالكامل — تجديد ناجح لاحق
يضبط `status="issued"` كما كان تماماً (لا حالة "archived" جديدة تتداخل
معه)؛ بما أن التجديد لا يمسّ `photo_file_id`/`form_c_file_id`، يظهر زر
الأرشفة من جديد تلقائياً بعد كل تجديد ليُعتمَد يدوياً مجدداً — سلوك
متّسق مع "يدوي دائماً" الذي اختاره المستخدم، بلا استثناء خاص.

**التحقق**: `python -m py_compile` نظيف على كل الملفات · بعد حذف
`__pycache__` بالكامل: 187 اختباراً + 170 معالجاً حقيقياً (بلا تغيير —
المعالِج الجديد يمر عبر موزِّع `rna:` الموجود) · محاكاة حيّة شاملة عبر
الدوال الفعلية: محاكاة دفعة وصول حقيقية عبر `create_profiles_from_arrival_batch`
⇒ التأكيد أن الملف الناتج `status="pending_documents"` لا `"active"` ⇒
زر الأرشفة **لا يظهر** في `build_profile_detail` الحقيقية طالما
الوثيقتان ناقصتان ⇒ `mark_archived()` **يرفض** الاعتماد ويُعيد `False`
في هذه الحالة (دفاع بالعمق يعمل فعلاً) ⇒ رفع الصورة وفورم C (محاكاة
مباشرة للعمودين) ⇒ الزر **يظهر** الآن فعلاً ⇒ الاعتماد ينجح، الحالة
تصبح `"archived"`، صف `ResidencyUpdate` صحيح
(`old_status="pending_documents"`, `new_status="archived"`)، والزر
**يختفي** من الشاشة بعدها مباشرة ⇒ `save_profile()` بلا تمرير `status`
صراحة لا يزال يُنتِج `"active"` كالمعتاد (لا انحدار على أي مستدعٍ قديم).

**متبقٍ (الجزء ج، الأخير)**: شاشة "📋 الملفات المعلّقة (وثائق ناقصة)"
الجديدة + إضافة `"archived"` إلى `TRACKABLE_STATUSES` (يجعل ملفاً
مؤرشَفاً اقترب انتهاؤه يظهر تلقائياً في شاشة "⏰ المتابعة" الموجودة —
مشتقّ حيّاً بلا كتابة على العمود، كما تقرَّر).

### 2026-08-14 (٢٦) — 📋 شاشة "الملفات المعلّقة (وثائق ناقصة)" + عودة الأرشيف لـ"معلّق" حيّاً عند اقتراب الانتهاء (الجزء ج، الأخير — إغلاق دورة معلّق↔أرشيف↔معلّق)

**الطلب**: إتمام الجزء الثالث والأخير من الخطة الموثَّقة في (٢٤)/(٢٥):
شاشة تعرض الملفات التي ما زالت بانتظار الصورة/فورم C، وجعل ملفاً مؤرشَفاً
يعود للظهور في قوائم المتابعة تلقائياً عند اقتراب انتهاء إقامته — بلا أي
تغيير فعلي على العمود المخزَّن (قرار المستخدم: "مشتقّة حيّاً").

**الإصلاح**:
- `modules/residency/constants.py`: `"archived"` أُضيفت إلى
  `TRACKABLE_STATUSES` — وحدها (لا `pending_documents`، ليست زمنية).
  هذا وحده كافٍ لجعل `get_expiring_soon()`/`get_passport_expiring_soon()`
  الموجودتين فعلاً في `followup/repository.py` تلتقطان أي ملف مؤرشَف
  اقترب انتهاء إقامته أو جوازه — بلا أي استعلام جديد، نفس فلسفة
  `effective_status()` بالضبط.
- `modules/residency/followup/repository.py`: دالة استعلام جديدة
  `get_pending_documents()` (+ `PendingDocumentsEntry` dataclass) —
  `ResidencyProfile` حيث `status == "pending_documents"`، أحدثها أولاً.
- `modules/residency/followup/views.py`: `build_pending_documents_list()`
  جديدة — تعرض كل ملف مع ✅/⬜ لكل من الصورة وفورم C، وزر "📂 فتح الملف"
  لكل صف (يفتح `rna:view_{id}` الموجود مباشرة لرفع الناقص).
- `modules/residency/routing_nav.py`: فرع جديد `action == "pending_docs"`
  (نفس نمط `"followup"`/`"pending"` تماماً)؛ `action == "archive"` يمرِّر
  الآن `pending_docs_count` أيضاً لـ`build_archive_list`.
- `modules/residency/profiles/flow.py`: نفس التمرير عند تنقّل الصفحات
  (`page_` action) — مصدر ثانٍ لنفس الشاشة كان سيبقى بالعدّاد القديم لولا
  التحديث هنا أيضاً.
- `modules/residency/profiles/views.py::build_archive_list()`: بارامتر
  جديد `pending_docs_count` + زر "📋 الملفات المعلّقة (N)" في شاشة
  الأرشيف الرئيسية، بجانب "⏰ المتابعة"/"⏳ المرافقون المعلقون".

**التحقق**: `python -m py_compile` نظيف على كل الملفات · بعد حذف
`__pycache__` بالكامل: 187 اختباراً + 170 معالجاً حقيقياً (بلا تغيير —
الفرع الجديد يمر عبر موزِّعَي `rn:`/`rna:` الموجودين) · محاكاة حيّة
شاملة عبر الدوال الفعلية تغطي **الدورة الكاملة من البداية للنهاية**:
ملف بـ`status="pending_documents"` يظهر في `get_pending_documents()`
الحقيقية وفي `build_pending_documents_list()` ⇒ ملف آخر بحالة "active"
عادية **لا** يظهر في نفس القائمة (لا تسرّب) ⇒ رفع الصورة وفورم C ثم
`mark_archived()` الحقيقية ⇒ تعديل `expiry_date` مباشرة ليكون خلال 10
أيام ⇒ التأكيد أن العمود المخزَّن يبقى **حرفياً `"archived"`** (لا كتابة
عليه) بينما الملف نفسه يظهر الآن تلقائياً في `get_expiring_soon()`
الحقيقية (نفس شاشة "⏰ المتابعة" التي يستخدمها المترجمون يومياً) ⇒
وفي نفس الوقت **اختفى** من `get_pending_documents()` (لم يعد "بانتظار
وثائق" — الوثائق مكتملة، القضية الآن اقتراب الانتهاء لا نقص المستندات).

**ملخّص الدورة الكاملة المُنفَّذة عبر الأجزاء الثلاثة (٢٤/٢٥/٢٦)**:
اسم إداري (`Patient.pending_arrival=True`) ← يظهر في "📋 الأسماء
المعلّقة" ببوت الواصلين ← يُستخدَم في تقرير وصول فعلي (`pending_arrival`
يُلغى تلقائياً) ← ملف إقامة `status="pending_documents"` يظهر في "📋
الملفات المعلّقة" بالإقامات ← رفع الصورة وفورم C يُظهر زر الاعتماد ←
اعتماد يدوي ← `status="archived"` ← عند اقتراب الانتهاء يعود للظهور
تلقائياً (حيّاً، بلا تغيير على العمود) في "⏰ المتابعة" ← تجديد عبر
`rnr:` الموجود يعيد `status="issued"` ويُظهر زر الاعتماد من جديد
للدورة القادمة.

### 2026-08-14 (٢٧) — 🐛 مرافقو "مريض جديد مع مرافقين" لا يظهرون للحذف ولا يُحذَفون مع المريض

**البلاغ**: "عند إضافة اسم مريض مع المرافقين من الأدمن... وثم أريد حذف
مريض، لا يظهر اسم المرافقين لحذفهم، ينحذف المريض فقط... وإذا حذفت
المريض لا ينحذف المرافق معه".

**التشخيص**: خللان مرتبطان، كلاهما أثر جانبي غير مقصود لإصلاح سابق
موثَّق (٢٠٢٦-٠٨-٠٦ (٢)) كان قد استبعد المرافقين عمداً من شاشات "أسماء
المرضى" (عرض/تعديل/حذف) لتجنّب ظهورهم كأسطر مستقلة مربكة — لكن هذا ترك
شاشة **الحذف تحديداً** بلا أي طريقة للوصول إلى مرافق لحذفه بمفرده:
1. `get_patients_paginated()` (تُغذّي `handle_delete_patient_name`)
   تستبعد `patient_type="companion"` بتصميم — فلا يظهر أي مرافق كصفّ
   مستقل في قائمة الحذف إطلاقاً، لا وسيلة أخرى للوصول إليه.
2. `services/patients_service.py::delete_patient()` كانت تحذف صفاً واحداً
   بمعرّفه فقط (`session.delete(patient)`) بلا أي فحص لوجود مرافقين
   مرتبطين به عبر `companion_of_id` — حذف "companion_parent" كان يترك
   صفوف مرافقيه يتيمة في قاعدة البيانات إلى الأبد، بلا أي شاشة تعرضهم
   ليُحذَفوا لاحقاً يدوياً.

**الإصلاح**:
- `services/patients_service.py::delete_patient()`: قبل حذف المريض،
  يستعلم عن كل صف `Patient` حيث `companion_of_id == patient_id` ويحذفهم
  معه في نفس المعاملة. حذف مرافق بمفرده (لا يملك هو نفسه مرافقين) يبقى
  حذفاً لصف واحد كما كان — لا تسلسل عكسي (حذف مرافق لا يحذف المريض).
- `bot/handlers/admin/admin_schedule_management.py::handle_delete_patient_name`:
  لكل مريض بنوع `"companion_parent"` في القائمة، تُستدعى
  `get_companions_for_patient()` الموجودة أصلاً (نفس الدالة التي تُغذّي
  تعبئة المرافقين التلقائية في "الواصلين") وتُعرَض أزرار حذف فرعية
  مُزاحة ("   └ 🗑️ الاسم (مرافق)") مباشرة تحت زر المريض — بلا تعديل على
  `get_patients_paginated()` نفسها ولا على شاشتَي العرض/التعديل (القرار
  السابق بإخفاء المرافقين كأسطر مستقلة يبقى قائماً هناك، لم يُطلَب
  تغييره). زر المرافق الفرعي يستخدم نفس `callback_data=f"del_patient:{id}"`
  الموجود مسبقاً — يعمل مباشرة بلا أي معالِج أو حالة محادثة جديدة.

**التحقق**: `python -m py_compile` نظيف على الملفين · بعد حذف
`__pycache__` بالكامل: 187 اختباراً (بلا كسر لاختبار 2026-08-06 (٢) الذي
يتحقق أن `get_patients_paginated()` نفسها ما زالت تستبعد المرافقين — لم
تُلمَس) + 170 معالجاً حقيقياً (بلا تغيير — لا معالِج جديد) · محاكاة حيّة
عبر الدوال الفعلية: مريض "companion_parent" بمرافقَين حقيقيَين ⇒ شاشة
`handle_delete_patient_name` الحقيقية تعرض الآن ثلاثة أزرار (المريض
والمرافقان مُزاحَين، كلاهما يحمل "(مرافق)") ⇒ حذف المريض عبر
`delete_patient()` الحقيقية يحذف المرافقَين معه فعلياً في قاعدة
البيانات (تحقّق مباشر: لا أثر لأي من الصفوف الثلاثة) بينما مريض غير
مرتبط يبقى دون تأثير ⇒ اختبار مستقل: حذف مرافق بمفرده (بلا مريضه) ينجح
ولا يمسّ المريض الأصلي إطلاقاً (لا تسلسل عكسي).

### 2026-08-14 (٢٨) — 🔧 إخفاء أسماء المرافقين من شاشة "الأسماء المعلّقة" المترجم-facing

**الطلب**: "عند إضافة اسم مريض مع المرافقين، لا داعي لظهور أسماء
المرافقين للمستخدمين — يظهر اسم المريض فقط".

**السياق**: هذا يخصّ شاشة "📋 الأسماء المعلّقة" الجديدة (أُضيفت في
٢٠٢٦-٠٨-١٤ (٢٤)) داخل "🛬 الوصول" — متاحة لأي مستخدم general_services
(مترجم أو أدمن)، بخلاف زر حذف المرافقين المُضاف اليوم أيضاً (٢٧) وهو
**أدمن فقط** (`bot/handlers/admin/`) ولخدمة حاجة مختلفة تماماً (حذف
مرافق بمفرده) — لم يُطلَب تغييره ولم يُلمَس. الطلب هنا يُطبِّق نفس فلسفة
"لا داعي لظهور المرافقين كأسطر مستقلة" الموثَّقة أصلاً لشاشات الأدمن
(٢٠٢٦-٠٨-٠٦ (٢))، ممتدّة الآن لهذه الشاشة الجديدة المواجِهة للمستخدمين.

**الإصلاح**: `modules/general_services/arrivals/views.py::build_pending_names_list()`
— حُذف سطر `   └ 👥 {comp_name}` لكل مرافق؛ تعرض الآن اسم المريض فقط
(`👤 {fam['name']}`). `services/patients_service.py::get_pending_arrival_names()`
لم تُلمَس (لا تزال تُرجع بيانات المرافقين، غير مُستخدَمة الآن في العرض
فقط — تبسيط أبسط وأقل خطراً من تعديل طبقة البيانات).

**التحقق**: `python -m py_compile` نظيف · بعد حذف `__pycache__` بالكامل:
187 اختباراً + 170 معالجاً حقيقياً (بلا تغيير) · محاكاة حيّة عبر
`build_pending_names_list()` الحقيقية: مريض بمرافقَين حقيقيَين ⇒ اسم
المريض يظهر في النص، واسما المرافقين **غائبان تماماً** عن النص الناتج.

### 2026-08-14 (٢٩) — 🧭 إعادة هيكلة قائمة الإقامات الرئيسية: زرّان مباشرة بدل "📁 أرشيف المرضى" الوحيد

**الطلب**: وصف كامل لدورة الحياة المطلوبة — اسم يُكمَل في الواصلين
فيختفي منها ويظهر في المغادرين/الإقامات، وعند دخول "🪪 الإقامة" يظهر
زرّان: "📋 الملفات المعلّقة" (يستلم القادم من الواصلين، يُكمَل فيه الصورة
وفورم C) و"⏰ المتابعة". بعد أسئلة توضيحية، تأكَّد: الزرّان يحلّان محل
"📁 أرشيف المرضى" كنقطة الدخول مباشرة على القائمة الرئيسية؛ "📁 أرشيف
المرضى" (القائمة الكاملة + Excel/PDF) و"⏳ المرافقون المعلقون" لا
يُحذَفان — يصبحان زرّين **داخل شاشة "⏰ المتابعة"** بدل القائمة الرئيسية.

**السياق**: الجزء الأول (اختفاء الاسم من الواصلين عند إتمامه) كان مكتملاً
فعلاً من (٢٤) — `clear_pending_arrival_by_names()` تُنفَّذ عند تأكيد أي
دفعة وصول. هذا الإصلاح يخصّ إعادة الهيكلة الملاحية فقط.

**الإصلاح** — تغيير شجرة التنقّل بالكامل مع الحفاظ على كل شاشة كما هي
محتوىً (لا حذف لأي وظيفة، فقط تغيير أماكن الأزرار وأهداف "رجوع"):
- `modules/residency/profiles/views.py::build_residency_main_menu()`:
  بارامترا `pending_docs_count`/`expiring_count` جديدان — تعرض الآن
  "📋 الملفات المعلّقة (N)" (→ `rn:pending_docs`) و"⏰ المتابعة (N)"
  (→ `rn:followup`) بدل الزرّ الوحيد القديم.
- `build_archive_list()`: حُذفت صفوف "⏰ المتابعة"/"⏳ المرافقون
  المعلقون"/"📋 الملفات المعلّقة" (لم تعد بحاجة لهذه الأزرار — الوصول
  إليها الآن من القائمة الرئيسية ومن شاشة المتابعة)؛ البارامترات
  `expiring_count`/`pending_count`/`pending_docs_count` أُزيلت من
  التوقيع (لم تعد مستخدَمة)؛ زرّ "⬅️ رجوع" يعود الآن لـ`rn:followup`
  بدل `rn:main` (الأرشيف صار يُفتَح من داخل المتابعة، لا نقطة دخول
  مستقلة).
- `modules/residency/followup/views.py::build_followup_list()`: بارامتر
  `pending_count` جديد؛ صفّ أزرار جديد `[📁 أرشيف المرضى, ⏳ المرافقون
  المعلقون (N)]` يظهر في كلا حالتَي القائمة (فارغة/غير فارغة)؛ "⬅️ رجوع"
  يعود الآن لـ`rn:main`.
- `build_pending_list()`/`build_pending_documents_list()`: أهداف "رجوع"
  حُدِّثت لتطابق الشجرة الجديدة — الأولى → `rn:followup` (تُفتَح من
  داخلها)، والثانية → `rn:main` (تُفتَح من القائمة الرئيسية مباشرة).
- `modules/residency/routing_nav.py` (`_dispatch`) و`modules/residency/menu.py`
  و`modules/residency/profiles/flow.py` (`page_` لتنقّل صفحات الأرشيف):
  كل نقاط الاستدعاء الثلاث لـ`build_residency_main_menu`/`build_archive_list`/
  `build_followup_list` عُدِّلت لتمرير/عدم تمرير العدادات بما يطابق
  التوقيعات الجديدة.

**التحقق**: `python -m py_compile` نظيف على الملفات الخمسة · بعد حذف
`__pycache__` بالكامل: 187 اختباراً + 170 معالجاً حقيقياً (بلا تغيير —
لا معالِج جديد، فقط منطق عرض/تنقّل) · محاكاة حيّة شاملة عبر `_dispatch`
الحقيقية لكل الشاشات الخمس: القائمة الرئيسية تعرض زرّين بالضبط بعدَّين
حقيقيَّين صحيحين (ملف واحد بانتظار وثائق، ملف واحد قريب الانتهاء)؛ شاشة
"المتابعة" تستضيف الآن زرّي "📁 أرشيف المرضى"/"⏳ المرافقون المعلقون"
فعلياً وتعود لـ`rn:main`؛ شاشة "الأرشيف" لم تعد تكرّر أياً من الأزرار
الثلاثة وتعود الآن لـ`rn:followup`؛ "الملفات المعلّقة" تعود لـ`rn:main`؛
"المرافقون المعلقون" تعود لـ`rn:followup` — الشجرة الكاملة تعمل كما
صُمِّمت بلا أي رابط معطَّل.

### 2026-08-14 (٣٠) — 🐛 اسم مرافق يتطابق مع مريض موجود مسبقاً ⇒ لا يُربَط بمريضه إطلاقاً (لا يظهر للحذف، ولا يُحذَف معه)

**البلاغ**: "زر حذف المرافقين لم يظهر، الآن أحذف اسم مريض ويبقى اسم
المرافقين موجود" — بعد إصلاح (٢٧) مباشرة، وعلى مريض **جديد** أُضيف
*بعد* نشر ذلك الإصلاح فعلياً على السيرفر (استُبعِد احتمال بيانات قديمة/
عدم نشر بمحاكاتين حيّتين منفصلتين أعادتا إنتاج التدفق الإداري الحقيقي
كاملاً من الصفر بنجاح تام، فلم يتبقَّ إلا فحص حالة حافة لم تُختبَر بعد).

**التشخيص**: خلل حقيقي أعمق وأقدم من إصلاح (٢٧) نفسه — `add_patient()`
(`services/patients_service.py`) تفحص وجود الاسم مسبقاً في `Patient`
بلا أي شرط على `patient_type`: *أي* اسم مطابق حرفياً (مريض عادي من تقرير
طبي سابق، مريض `pharmacy_only`، أو حتى مرافق آخر) يجعلها تُعيد الـ`id`
الموجود **بلا تحديث** `patient_type`/`companion_of_id` إطلاقاً — حتى لو
استُدعيت بقيم جديدة. التعليق في `handle_companion_name_input` كان
يزعم فعلاً "أسماء المرافقين لا تُفحص للتكرار عمداً"، لكن هذا لم يكن
مُنفَّذاً فعلياً في الكود قط — نية موثَّقة بلا تطبيق. أُعيد الإنتاج
بمحاكاة حيّة: زرع مريض عادي باسم "منى سالم" (كأنها من تقرير طبي)، ثم
تشغيل تدفق "🤝 مريض جديد مع مرافقين" الحقيقي كاملاً بمرافق باسم "منى
سالم" — النتيجة: صف المريض الأصلي بقي `patient_type=None`/
`companion_of_id=None` تماماً، فلا تربطها أي علاقة بالمريض الجديد،
فتختفي من شاشة الحذف تحته ولا تُحذَف معه.

**الإصلاح**:
- `services/patients_service.py::add_patient()`: بارامتر جديد
  `skip_dedup: bool = False` — عند `True` يتخطّى فحص "الاسم موجود
  مسبقاً" كلياً وينشئ صفاً جديداً دائماً. الافتراضي `False` يحافظ على
  سلوك كل مستدعٍ آخر (لا تكرار لأسماء المرضى العاديين — سلوك مقصود
  ومطلوب لتلك الحالة، لم يُمَس).
- `admin_schedule_management.py::handle_companion_name_input`: تمرير
  `skip_dedup=True` — يضمن أن كل مرافق يحصل على صف `Patient` **مستقل
  دائماً**، بصرف النظر عن تطابق اسمه مع أي صف موجود، فيُربَط بمريضه
  بشكل صحيح دون أي شرط. المريض الأصلي (إن وُجد تطابق) يبقى بلا أي مساس —
  لا خطر على بياناته أو تصنيفه.

**التحقق**: `python -m py_compile` نظيف على الملفين · بعد حذف
`__pycache__` بالكامل: 187 اختباراً + 170 معالجاً حقيقياً (بلا تغيير) ·
محاكاة حيّة تُعيد إنتاج البلاغ بالضبط: زرع مريض عادي "منى سالم" ⇒ تشغيل
تدفق "مريض جديد مع مرافقين" الحقيقي بمرافق بنفس الاسم ⇒ **قبل** الإصلاح:
المريض الأصلي بقي غير مرتبط تماماً (الخلل يتكرر فعلياً) ⇒ **بعد**
الإصلاح: صفّ جديد مستقل يُنشَأ ويُربَط بـ`companion_of_id` الصحيح، بينما
المريض الأصلي يبقى دون أي تغيير على الإطلاق؛ إعادة تشغيل محاكاتَي
(٢٧) الكاملتين (شاشة الحذف + التدفق الإداري الحقيقي من الصفر) للتأكد
من عدم حدوث أي انحدار — كلتاهما ناجحتان كما كانتا.

### 2026-08-14 (٣١) — 🐛 قائمة الرعاية الصحية لا تظهر إطلاقاً — "▶️ ابدأ الآن" لا تفتح شيئاً (حلقة مفرغة)

**البلاغ**: "في مشكلة في بوت الرعاية الصحية لا يظهر لي القائمة لإدخال
التقرير، يبدأ يضغط ابدأ الآن فقط ولا تظهر القائمة" — مستخدم رعاية صحية
يضغط "▶️ ابدأ الآن" فيُعاد بناء نفس الترحيب/اللوحة بلا أي طريق لفتح
قائمة أنواع التقارير الفعلية (المجارحة/المتابعة الطبية/صرف الأدوية/
المستلزمات/إجراءات أخرى).

**التشخيص**: انحدار من التزام سابق في هذه الجلسة نفسها وحَّد سلوك
"▶️ ابدأ الآن" عبر ثلاث وحدات (healthcare/general_services/residency)
ليصبح إعادة تشغيل كاملة فقط (عبر `user_start`)، بنفس نمط "🚀 ابدأ" في
بوت المترجمين — وهو سلوك صحيح ومطلوب بذاته. لكن `general_services`
و`residency` كلٌّ منهما يملك أصلاً زراً منفصلاً مخصَّصاً يفتح قائمته
الفعلية ("🔧 الخدمات العامة"/"🪪 الإقامة")، إلى جانب "▶️ ابدأ الآن" —
بينما الرعاية الصحية **لم يكن لديها زر منفصل قط**؛ الدالة القديمة
`modules/healthcare/menu.py::_show_menu` كانت تُسجَّل مباشرة على نص
"▶️ ابدأ الآن" نفسه لتفتح قائمة الرعاية الصحية. حين وُحِّد ذلك النص
ليصبح إعادة تشغيل فقط، حُذفت آلية الفتح تلك بلا أي بديل — فبقي مستخدم
الرعاية الصحية بلا أي طريق لفتح قائمته إطلاقاً (لوحة مفاتيح بزر واحد
فقط، ضغطه يعيد نفس اللوحة). تأكَّد هذا مباشرة بقراءة
`core/modules_bootstrap.py` (سطور ٦٠-٢٠٦) ومقارنة الأزرار الثلاثة
الفعلية لكل وحدة.

**الإصلاح**:
- `modules/healthcare/menu.py`: زرّ جديد `HEALTHCARE_BUTTON = "🏥
  الرعاية الصحية"` ودالة `_show_menu()` جديدة (بنفس نمط `_show_menu_chennai`
  الموجودة تماماً — تحقّق RBAC عبر `user_has_module(tg_id, "healthcare")`،
  فعند الرفض تُعاد التوجيه إلى `user_start` بدل أي خطأ) تستدعي
  `build_healthcare_menu()` بلا مدينة محدَّدة وترسل القائمة الفعلية
  مباشرة. `register_menu_handler()` يسجِّل `MessageHandler` جديداً لهذا
  النص في group 0، بجانب معالِج تشناي الموجود.
- `core/modules_bootstrap.py`: تسجيل وحدة `healthcare` عُدِّل —
  `menu_buttons`/`keyboard_rows` صارا يضمّان زرَّين ("🏥 الرعاية الصحية"
  و"▶️ ابدأ الآن") بدل زرّ واحد، تماماً بنفس نمط `general_services`
  (`{"🔧 الخدمات العامة", "▶️ ابدأ الآن"}`) و`residency`
  (`{"🪪 الإقامة", "▶️ ابدأ الآن"}`) — لم يُلمَس أي منهما.

**التحقق**: `python -m py_compile` نظيف على الملفين · محاكاة حيّة عبر
الدوال الفعلية: `dynamic_user_kb()` لمستخدم رعاية صحية فقط يُنتج الآن
صفَّين منفصلين يحتويان الزرَّين معاً؛ ضغط "🏥 الرعاية الصحية" الفعلي
عبر `_show_menu()` يستدعي `build_healthcare_menu()` الحقيقية ويعيد
القائمة التفاعلية الصحيحة بأزرارها الخمسة؛ مستخدم بلا صلاحية "healthcare"
يُعاد توجيهه إلى `user_start` بأمان دون أي تسريب للقائمة · بعد حذف
`__pycache__` بالكامل: 187 اختباراً + **171** معالجاً حقيقياً (زيادة
معالِج واحد فقط عن ١٧٠ — `MessageHandler` الجديد للزر، مؤكَّدة عبر
`register_all_handlers(app)` حقيقية على `Application` حقيقي، لا تخمين).

⚠️ **ملاحظة جانبية غير متعلقة بهذا الإصلاح**: أثناء محاكاة مسار الرفض
(مستخدم بلا صلاحية `healthcare` يُعاد توجيهه إلى `user_start`) اكتُشِف
خلل حقيقي منفصل تماماً في `bot/handlers/user/user_start.py`: أي
مستخدم تيليجرام **جديد كلياً** (لا صف `Translator` له بعد، وليس أدمن)
يضغط `/start` يواجه `UnboundLocalError` على `logger` (المتغيّر يُعاد
تعيينه محلياً داخل الدالة في نقطة أخرى فيصبح محلياً على كامل نطاقها)
— يُنشَأ صف `Translator` فعلاً في قاعدة البيانات لكن إشعار الأدمن
للموافقة لا يُرسَل أبداً، والمستخدم يرى رسالة خطأ عامة بدل رسالة "طلب
انضمام جديد". لم يُصلَح ضمن هذا الإصلاح (غير متعلق بقائمة الرعاية
الصحية) — أُبلِغ عنه كمهمة منفصلة.

### 2026-08-15 (٣٢) — تطوير: دمج "➕ تسجيل دفعة وصول جديدة" داخل "📋 الأسماء المعلّقة"

**الطلب**: "في الوصول لا داعي لزر تسجيل دفعه وصول جديده، من زر الاسماء
المعلقة، يتم تسجيل بيانات المرضى ويتحولوا الى الاقامات" — إزالة زر
"➕ تسجيل دفعة وصول جديدة" من منيو "🛬 الوصول"؛ الضغط على اسم من "📋
الأسماء المعلّقة" يبدأ تسجيل بياناته مباشرة ويحوّله إلى إقامة.

**التحليل**: منتقي الأسماء داخل "➕ تسجيل دفعة وصول جديدة"
(`patient_selector.enter(..., only_companion_flow=True)`) كان مقيَّداً
أصلاً على نفس مجموعة الأسماء الظاهرة في "📋 الأسماء المعلّقة" — الزر لم
يكن يضيف أي قدرة فعلية غير موجودة، فقط خطوة تنقّل زائدة (منيو فرعي ←
سؤال تاريخ ← سؤال عدد المرضى (1-20) ← نفس منتقي الأسماء من جديد). شاشة
"الأسماء المعلّقة" نفسها كانت عرضاً فقط بلا أزرار إجراء، وتوثِّق نصّاً
صريحاً أن "الاستخدام الفعلي يبقى عبر المنتقي العادي".

**قرارات ثبَّتها المستخدم عبر أسئلة توضيحية**: ضغطة واحدة = مريض واحد
بالضبط (لا اختيار متعدد لدفعة بعدة أسماء)؛ سؤال "تاريخ الوصول" يبقى أول
خطوة بعد الضغط على الاسم.

**التنفيذ**:
- `modules/general_services/arrivals/session.py`: حقلان جديدان على
  `ArrivalSession` — `preselected_patient_id`/`preselected_patient_name`
  (مضمونا الوجود دائماً الآن، إذ كل جلسة تُنشأ حصراً من الضغط على اسم
  معلّق)؛ حذف `STEP_PATIENT_COUNT` (خطوة محذوفة كلياً).
- `modules/general_services/arrivals/views.py::build_pending_names_list()`:
  كل اسم أصبح زرّاً (`gsa:pending_pick_<id>`) بدل نص عرض فقط؛ زر "⬅️
  رجوع" يعود لـ`gs:main` مباشرة. حذف `build_arrivals_menu()` و
  `build_patient_count_prompt()` كلياً (لا مستدعٍ لهما بعد الآن).
- `modules/general_services/arrivals/flow.py`: فرع جديد
  `gsa:pending_pick_<id>` — يتحقق من أن الاسم ما زال معلَّقاً فعلاً (نفس
  مصدر الحقيقة `get_pending_arrival_names()` المستخدَم في العرض، فلا
  تعارض)، ثم ينشئ جلسة بـ`patient_count=1` ومعرِّف/اسم المريض المختار،
  ويعرض مباشرة سؤال تاريخ الوصول. استُخلصت دالة مشتركة جديدة
  `_apply_known_patient()` من جسم `_on_p_name_selected()` (تجلب مرافقي
  المريض من السجل وتنتقل لـ`STEP_P_ARRIVAL_DATE`) — تُستدعى الآن من
  مسارين: اختيار الاسم عبر المنتقي العام (زر "⬅️ رجوع" ← إعادة اختيار،
  يبقى يعمل كخيار تصحيح يدوي دون تغيير) **و** من كل نقاط تأكيد التاريخ
  (اليوم/تقويم/إدخال يدوي) بدل الانتقال للخطوة المحذوفة "عدد المرضى".
  حُذفت الفروع الميتة `gsa:start`، `gsa:arrivals` الداخلي،
  `gsa:back_to_count` (كان ميتاً أصلاً — لا زر يُصدره)، ومعالِج
  `count_1..count_20`.
- `modules/general_services/routing_nav.py`: الفرع الوحيد المسؤول فعلياً
  عن الزر الرئيسي "🛬 الوصول" **وكل** أزرار "❌ إلغاء" عبر التدفق (جميعها
  تستخدم نفس `gs:arrivals` — أكثر من ٣٠ موضعاً في `views.py`، تأكَّدت
  بالبحث الكامل) عُدِّل ليفتح `build_pending_names_list()` مباشرة بدل
  المنيو الفرعي المحذوف — تعديل ملف واحد كفى لإعادة توجيه كل هذه الأزرار
  دفعة واحدة، دون لمس كل شاشة على حدة.

**التحقق**: `python -m py_compile` نظيف على الملفات الأربعة · بحث كامل
في `tests/` أكَّد عدم استخدام أيٍّ من الدوال/الثوابت المحذوفة (آمن
للحذف الكامل) · بعد حذف `__pycache__` بالكامل: 187 اختباراً + **171**
معالجاً حقيقياً (بلا تغيير — كل التعديلات فروع داخلية في معالِج مُسجَّل
أصلاً، لا معالِج جديد ولا محذوف) · محاكاة حيّة شاملة عبر الدوال الفعلية:
زرع مريض+مرافق حقيقي عبر "🤝 مريض جديد مع مرافقين" ⇒ محاكاة `gs:arrivals`
(زر "🛬 الوصول") ⇒ تأكَّد ظهور "📋 الأسماء المعلّقة" **مباشرة** بزرّ لكل
اسم، بلا زر "start"، وزر رجوع يستهدف `gs:main` ⇒ الضغط على
`gsa:pending_pick_<id>` ⇒ تأكَّد إنشاء الجلسة بـ`preselected_patient_id`/
`preselected_patient_name` الصحيحين وعرض سؤال التاريخ ⇒ محاكاة "اليوم"
⇒ تأكَّد القفز **مباشرة** لـ`STEP_P_ARRIVAL_DATE` (تخطّي كامل لخطوة عدد
المرضى المحذوفة) مع اسم المريض ومرافقه الصحيحين من `companion_queue`
تلقائياً ⇒ محاكاة معرِّف غير موجود/مُستخدَم حديثاً (`pending_pick_999999`)
⇒ تأكَّد رسالة تحذير واضحة بدل عطل.

### 2026-08-15 (٣٣) — 🐛 ملف الإقامة يظهر قبل تسجيل الوصول فعلياً + اسم المرافق يظهر ضمنه للمستخدمين

**البلاغ**: "الان اضفت اسم جديد من زر مريض جديد مع المرافقين وظهر في
الاسماء المعلقة في الوصول، وظهر كذلك في الاقامات، المفروض يظهر في
الاقامات إلا بعد أن أخرجه من الوصول من الأسماء المعلقة، لا يظهران مع
بعض بنفس الوقت، ينتقل من مرحلة إلى مرحلة" + "اسم المرافق لا زال يظهر
للمستخدمين... المفروض يظهر فقط اسم المريض لا يظهر المريض والمرافق".

**التشخيص**: `admin_schedule_management.py::_create_residency_profile_for_companion_flow()`
كانت تُستدعى **فوراً** عند إضافة الاسم عبر "🤝 مريض جديد مع مرافقين"
(نقطتا استدعاء: عند الإجابة "لا" على "هل يوجد مرافق؟"، وعند اكتمال أسماء
كل المرافقين) — تُنشئ `ResidencyProfile`/`ResidencyCompanion` مباشرة،
بينما `Patient.pending_arrival` يبقى `True` (لم يُستخدَم الاسم في "🛬
الوصول" بعد). النتيجة: الاسم يظهر في **الشاشتين معاً في آنٍ واحد** —
"📋 الأسماء المعلّقة" (الوصول) و"📋 الملفات المعلّقة" (الإقامة) — رغم أن
دورة الحياة المصمَّمة أصلاً (راجع MAINTENANCE_LOG (٢٨)-(٣٠)) تفترض
مرحلتين متعاقبتين لا متزامنتين. وبما أن ملف الإقامة موجود فعلاً، فأي
مستخدم بصلاحية "الإقامة" يقدر يفتحه (`📂 فتح الملف`) ويرى
`build_profile_detail()` الذي يعرض المرافق باسمه صراحةً — قبل أن يبدأ
تسجيل وصوله الفعلي حتى. أعيد إنتاج الحالتين معاً بمحاكاة حيّة عبر
الدوال الفعلية (إضافة عبر التدفق الإداري الحقيقي، ثم استدعاء كل شاشات
الإقامة التي يراها مستخدم عادي) قبل أي إصلاح، فتأكَّد وجود
`ResidencyProfile`/`ResidencyCompanion` فوراً واسم المرافق ظاهراً في
تفاصيل الملف.

**الإصلاح**: حذف `_create_residency_profile_for_companion_flow()` بالكامل
من `admin_schedule_management.py` (الدالة ونقطتا استدعائها الاثنتان) —
لم يعد يُنشَأ أي `ResidencyProfile` عند الإضافة عبر "🤝 مريض جديد مع
مرافقين". إنشاء الملف السكني يبقى **حصراً** عند تأكيد "🛬 الوصول"
الفعلي (`create_profiles_from_arrival_batch()` الموجودة أصلاً في
`modules/general_services/arrivals/flow.py`، بلا أي تعديل عليها) — تلك
اللحظة نفسها التي يُمسَح فيها `pending_arrival` (`clear_pending_arrival_by_names`)
فيختفي الاسم من "الأسماء المعلّقة". النتيجتان تحدثان معاً ذرّياً عند
التأكيد، فلا تداخل بين المرحلتين إطلاقاً — واسم المرافق لا يصبح مرئياً
لأي مستخدم قبل تلك اللحظة، لأن لا ملف إقامة موجود أصلاً ليُفتَح.

**التحقق**: `python -m py_compile` نظيف · بعد حذف `__pycache__` بالكامل:
187 اختباراً + 171 معالجاً حقيقياً (بلا تغيير — تعديل منطق داخلي فقط) ·
محاكاة حيّة بمرحلتين عبر الدوال الفعلية: **المرحلة ١** (فور الإضافة عبر
"🤝 مريض جديد مع مرافقين") — تأكَّد ظهور الاسم في `get_pending_arrival_names()`
فقط، وعدم وجود أي صفّ في `ResidencyProfile`/`ResidencyCompanion` إطلاقاً،
و`get_pending_documents()` (شاشة "الملفات المعلّقة") فارغة تماماً ·
**المرحلة ٢** (بعد تأكيد "🛬 الوصول" عبر `save_arrival_batch` +
`create_profiles_from_arrival_batch` + `clear_pending_arrival_by_names`
الحقيقية) — تأكَّد اختفاء الاسم فوراً من `get_pending_arrival_names()`
وظهوره في اللحظة نفسها في `get_pending_documents()` — لا نقطة زمنية
واحدة يظهر فيها الاسم في الشاشتين معاً، ولا نقطة يظهر فيها اسم المرافق
لمستخدم قبل تسجيل الوصول الفعلي.

### 2026-08-15 (٣٤) — 🐛 اسم المرافق يظهر للمترجمين في قائمة اختيار المريض عند إضافة تقرير

**البلاغ**: "للمترجمين أثناء إضافته مع المرافق من زر مريض جديد مع مرافق،
ثم يدخل المترجمين ليرفع تقرير، يظهر له أسماء المرافقين — نريد فقط أسماء
المرضى يظهروا بدون المرافقين" — عقب توضيح أن المشكلة السابقة (٣٣) عن
شاشات الإقامة لا تخص هذه، بل تدفق **تقارير المترجمين** تحديداً.

**التشخيص**: `shared/selectors/patient_selector/_data.py` يحتوي دالتين
منفصلتين لفلترة ظهور المرضى: `_type_visible()` (تُستخدَم في منتقي
"الخدمات العامة/الإقامة/الرعاية الصحية" الحديث — تُخفي `patient_type=
"companion"` افتراضياً بشرط `include_companions=True` صراحة) و
`report_flow_patient_visible()` — **مصدر الحقيقة المنفصل تماماً** المستخدَم
حصراً في تدفق تقارير المترجمين (`patient_handlers.py` للقائمة المرقَّمة
و`user_patient_search_inline.py` للبحث inline، كلاهما يستدعيانها). هذه
الدالة الثانية كانت تستثني فقط `pharmacy_only` و`chennai` — **لم تكن
تستثني `companion` إطلاقاً** — فيظهر أي مرافق أُضيف عبر "🤝 مريض جديد مع
مرافقين" كصف مستقل قابل للاختيار في قائمة/بحث المرضى عند إضافة تقرير
طبي، رغم أنه ليس مريضاً يُكتَب له تقرير أصلاً.

**الإصلاح**: `report_flow_patient_visible()` — إضافة `_COMPANION` إلى
مجموعة الأنواع المستبعَدة (`pt not in (_PHARMACY_ONLY, _CHENNAI,
_COMPANION)`). `companion_parent` (المريض الرئيسي نفسه في ذلك التدفق)
يبقى ظاهراً بشكل طبيعي كأي مريض عادي — لم يُمَسّ، فهو مريض حقيقي يحتاج
تقارير كبقية المرضى. إصلاح في نقطة واحدة يغطي القائمة المرقَّمة والبحث
inline معاً (نفس الدالة المشتركة، تماماً كما يوثِّق تعليقها الأصلي).

**التحقق**: `python -m py_compile` نظيف · بعد حذف `__pycache__` بالكامل:
187 اختباراً + 171 معالجاً حقيقياً (بلا تغيير — تعديل منطق فلترة داخلي
فقط) · محاكاة حيّة: زرع مريض حقيقي (`companion_parent`) + مرافق
(`companion`) عبر التدفق الإداري الحقيقي + مريض عادي منفصل للمقارنة ⇒
استدعاء `report_flow_patient_visible()` الحقيقية على الثلاثة ⇒ المريض
الحقيقي والمريض العادي ظاهران، المرافق مستبعَد وحده تماماً — بلا أي
انحدار على ظهور المرضى العاديين.

### 2026-08-15 (٣٥) — 🐛 ملفات إقامة يتيمة متبقية من الخلل القديم (٣٣) + حذف المريض لا يحذف ملفه المعلَّق

**البلاغ**: "لا زال في الملفات المعلقة في الإقامات يظهر اسم المريض وهو
عادة معلّق في الوصول... وإذا تم حذفه من الأدمن ينحذف من الوصول، لكن
يظل في الإقامات ملفاً معلقاً" — بعد التأكد أن الإصلاح (٣٣) نُشر فعلاً
قبل الاختبار.

**التشخيص**: بتحليل الكود تأكَّد أنه لا يوجد أي مسار حالي يقدر ينشئ
ملف إقامة لاسم لا يزال pending_arrival=True — فطُلِب من المستخدم تشغيل
سكربت تشخيصي (`diag_pending_name_state.py`) على الاسم المُبلَّغ عنه
مباشرة على السيرفر. النتيجة: `Patient` لهذا الاسم **محذوف بالفعل (0
صفوف)** — أي أن المستخدم كان قد حذفه من الأدمن مسبقاً — بينما
`ResidencyProfile` لا يزال موجوداً بـ`source='companion_flow'`
و`created_at` يسبق نشر الإصلاح (٣٣). أي: هذا صفّ **متبقٍّ فعلياً من
الخلل القديم قبل الإصلاح**، وليس خللاً جديداً — لكنه كشف فجوة حقيقية
منفصلة: `delete_patient()` (`services/patients_service.py`) لم يكن
يمسّ جدول `ResidencyProfile` إطلاقاً رغم أن الربط بينهما بالاسم فقط
(لا FK) — فأي ملف إقامة (قديم أو جديد) يبقى يتيماً للأبد بعد حذف
المريض المرتبط به، بلا أي طريقة للوصول إليه أو حذفه لاحقاً.

**الإصلاح**:
- `services/patients_service.py::delete_patient()`: عند حذف مريض، يُحذَف
  معه أيضاً أي `ResidencyProfile` (+`ResidencyCompanion`+`ResidencyUpdate`
  التابعة) مطابق بالاسم **بشرط أن يكون لا يزال فارغاً تماماً** —
  `status="pending_documents"` **و** لا صورة شخصية **و** لا فورم C
  مرفوعين. ملفات ذات تقدّم حقيقي (وثيقة واحدة مرفوعة فأكثر، أو حالة
  غير pending_documents) لا تُمَسّ إطلاقاً مهما كان — الحذف يقتصر حصراً
  على الملفات الفارغة تماماً (تحقَّق بمحاكاة حيّة تشمل الحالتين معاً).
- `scripts/cleanup_stale_companion_flow_residency.py` (جديد، معاينة/
  `--apply`): تنظيف لمرة واحدة لكل ملفات الإقامة المتبقية بـ
  `source='companion_flow'` في قاعدة الإنتاج — هذا المصدر لم يعد
  يُكتَب من أي كود حالي إطلاقاً (تأكَّد بحث كامل)، فأي صفّ به هو
  بالضرورة بقية من الخلل القديم، بصرف النظر عن وجود Patient مطابق له
  من عدمه (يشمل حالة اليتيم الكامل المكتشَفة في هذا البلاغ).
- `scripts/diag_pending_name_state.py` (جديد، قراءة فقط بالكامل): أداة
  تشخيص عامة — تعرض كل صفوف Patient/ResidencyProfile/ResidencyCompanion
  المطابقة لاسم معيّن مع created_at/source، لتمييز البيانات القديمة
  المتبقية من خلل جديد فعلي بسرعة دون تخمين، بُنيت واستُخدمت فعلاً
  لتشخيص هذا البلاغ بالذات.

**التحقق**: `python -m py_compile` نظيف على الملفات الثلاثة · بعد حذف
`__pycache__` بالكامل: 187 اختباراً + 171 معالجاً حقيقياً (بلا تغيير) ·
محاكاة حيّة عبر `delete_patient()` الحقيقية بثلاث حالات معاً: (أ) مريض
بملف إقامة فارغ تماماً (pending_documents، بلا صورة/فورم C) ⇒ يُحذَف
الملف ومرافقوه معه، (ب) مريض بملف له تقدّم حقيقي (صورة مرفوعة فعلاً،
ولو كانت الحالة لا تزال pending_documents لعدم اكتمال فورم C) ⇒ الملف
يبقى دون أي مساس، (ج) مريض عادي بلا أي ملف إقامة إطلاقاً ⇒ حذف نظيف بلا
أي خطأ · تشخيص فعلي على بيانات الإنتاج الحقيقية أكَّد صحة الفرضية قبل
كتابة أي إصلاح (لا تخمين).

### 2026-08-15 (٣٦) — 🔨 إعادة بناء كاملة لبوت الإقامة (كود + بيانات) — المرحلة ١: ملف معلّق + تذكير يدوي بتاريخ محدَّد

**الطلب**: "أريد حذف بوت الإقامات كامل وثم البناء من جديد لأنه تشتّت
البوت كثير" — بعد تراكم إصلاحات متتالية على دورة الإقامة عبر هذه
الجلسة (٢٨-٣٥) انتهى الأمر بخطأ من الذات (سكربت تنظيف حذف بالخطأ
ملفَّين حقيقيَّين فعّالين — `id=1` بحالة "نشطة" و`id=2` بحالة "تجديد
مُرسَل" — لعدم تقييد الحذف بشرط "فارغ فعلاً"، عولج بطلب نسخة احتياطية
فورية ثم بقرار المستخدم إعادة بناء القسم كاملاً بدل الترقيع). قرار
المستخدم الصريح بعد أسئلة توضيحية: **حذف الكود والبيانات معاً**، على
مراحل — ميزة واحدة تُبنى وتُختبَر بالكامل قبل الانتقال للتالية.

**النسخة الاحتياطية** (قبل أي حذف، طلب المستخدم صراحةً):
`db/backups/backup_before_residency_rebuild_20260814_213139.db` — تحفظ
كل بيانات البوت (لا الإقامة فقط) لحظة ما قبل هذا التغيير.

**نطاق المرحلة الأولى** (حدَّده المستخدم بدقة بعد أسئلة توضيحية):
- ملف إقامة يُنشأ تلقائياً عند تأكيد "🛬 الوصول" **فقط** (نفس نقطة
  الإطلاق القديمة، مؤكَّدة صراحةً — لا تغيير هنا) — يظهر في "🪪 الإقامة"
  كملف معلّق بانتظار الصورة الشخصية.
- مستخدم الإقامة يفتح الملف ويختار **تاريخ تنبيه يدوياً من التقويم**
  (لا حساب تلقائي من تاريخ الانتهاء — قرار صريح).
- بدءاً من ذلك التاريخ: **رسالة تلغرام يومية تلقائية** لمجموعة الإقامة
  (`RESIDENCY_GROUP_ID`، معرَّفة مسبقاً وأُعيد استخدامها كما هي) تذكّر
  بتجديد الفيزا/الإقامة للمريض ومرافقيه، تتكرر يومياً.
- تتوقف حصراً عند ضغط **الأدمن** (لا أي مستخدم آخر) زرّاً جديداً:
  "✅ تم تقديم طلب الإقامة لهذا المريض والمرافقين".
- زرّ "🪪 الإقامة" يبقى بنفس الاسم/الموقع — يفتح "الملفات المعلّقة"
  مباشرة (شاشة واحدة فقط الآن، لا منيو وسيط).
- **مؤجَّل عمداً لمراحل لاحقة منفصلة**: فورم سي، الأرشيف، التجديد
  (renewal الرسمي)، المرافقون المعلّقون، تنبيهات انتهاء الجواز/الإقامة
  التلقائية القديمة، تصدير Excel/PDF.

**الحذف (بعد تدقيق شامل لكل نقاط الربط قبل أي حذف)**:
- `modules/residency/` بالكامل (profiles/, followup/, renewal/,
  uploads/, والملفات العلوية) — المخطط القديم (source/status متعددة/
  renewal/فورم سي) حُذف كلياً.
- `services/residency_alerts_service.py`,
  `services/residency_missing_items_service.py`,
  `services/residency_archive_pdf.py`, `services/residency_archive_excel.py`,
  `services/residency_profile_pdf.py` (يستورد من الملف المحذوف — أصبح
  معطَّلاً)، `scripts/backfill_residency_expiry.py`.
- سكربتات تشخيص/تنظيف مؤقتة كانت مبنية على المخطط القديم تحديداً
  (`scripts/diag_pending_name_state.py`، `scripts/cleanup_stale_companion_flow_residency.py`)
  — لم تعد قابلة للتطبيق على المخطط الجديد، حُذفت.
  `scripts/delete_companion_flow_names.py` (سكربت تنظيف Patient سابق
  غير متعلّق مباشرة، لكنه يستورد `ResidencyUpdate` المحذوفة) حُذف أيضاً
  لنفس السبب.
- `db/models.py`: حذف 4 كلاسات قديمة (`ResidencyProfile` بحقول
  source/status/residency_number/…، `ResidencyCompanion` المطابقة،
  `ResidencyUpdate` سجلّ تدقيق، `ResidencyMissingItem` تتبّع فورم سي) —
  استُبدِلَت بكلاسين مبسَّطين فقط (انظر أدناه).
- `app.py`: حذف مهمّتَي `run_daily` القديمتين (`daily_residency_alerts`
  ٩ص، `daily_residency_missing_items` ٥م) — استُبدِلَتا بمهمّة واحدة.

**بلا تغيير (نقاط ربط تأكَّد بقاؤها صحيحة عبر البحث الشامل)**:
`bot/handlers_registry.py` (نفس `register_all` بنفس التوقيع)،
`core/modules_bootstrap.py` (نفس مفتاح "residency" ونفس الزرّين)،
`core/routing/landing.py` (نفس مكانة الوحدة في سلسلة الأولوية)،
`tests/test_healthcare_smoke.py` (يتأكَّد فقط من وجود الزرّين — يبقى
ناجحاً بلا أي تعديل).

**البناء الجديد**:
- `db/models.py` — `ResidencyProfile` مبسَّط جذرياً: `name`,
  `photo_file_id`, `reminder_date` (ISO، يدوي)، `reminder_active`،
  `submitted` (بدل النظام القديم للحالات المتعددة). `ResidencyCompanion`:
  `profile_id` + `name` فقط.
- `modules/residency/` وحدة جديدة **مسطَّحة** (لا تفريعات profiles/
  followup/…) — `constants.py`, `models.py` (الكتابة:
  `create_profiles_from_arrival`, `set_reminder_date`, `mark_submitted`,
  `save_photo`, `delete_stub_profile_by_name`), `repository.py`
  (القراءة، بنمط dataclass `ProfileRow` — يتجنّب خطأ
  `DetachedInstanceError` المكتشَف فعلياً أثناء الاختبار عند إرجاع كائنات
  ORM بعد إغلاق الجلسة)، `views.py`, `flow.py` (معالِج `rn:` واحد +
  استقبال الصورة)، `menu.py`، `routing.py`. أُعيد استخدام
  `photo_processing.py::resize_to_4x6` كما هو (دالة نقية مستقلة).
- `services/residency_reminders_service.py` — مهمّة يومية واحدة جديدة
  (نمط `build_message()`/`send_daily_...()` نفسه المستخدَم في الملف
  القديم للاختبارية)، ترسل لمجموعة الإقامة فقط.
- `scripts/drop_old_residency_tables.py` (معاينة/`--apply`) — يُسقِط
  الجداول الأربعة القديمة من قاعدة الإنتاج؛ `res_profiles`/`res_companions`
  تُنشَأ تلقائياً بالمخطط الجديد عند أول لمسة لقاعدة البيانات بعدها
  (`Base.metadata.create_all` — لا يُعدِّل جدولاً موجوداً، فالإسقاط
  ضروري).
- نقاط الربط المُعدَّلة (لا محذوفة): `modules/general_services/arrivals/flow.py`
  (نفس نقطة تأكيد "🛬 الوصول"، تستدعي `create_profiles_from_arrival`
  الجديدة بدل `create_profiles_from_arrival_batch`)، و
  `services/patients_service.py::delete_patient()` (يستدعي
  `delete_stub_profile_by_name` الجديدة بدل الاستعلام المباشر على
  الجداول القديمة).

**التحقق**: `python -m py_compile` نظيف على كل الملفات · بعد حذف
`__pycache__` بالكامل: **187 اختباراً** (بلا تغيير) + **166 معالجاً**
حقيقياً (انخفض من ١٧١ — الوحدة الجديدة أبسط بكثير: معالِج كولباك واحد
+ معالِج صورة واحد بدل 4 موزّعات فرعية سابقاً) · محاكاة حيّة شاملة
للدورة الكاملة عبر الدوال الفعلية: تأكيد وصول حقيقي ⇒ إنشاء
`ResidencyProfile`+`ResidencyCompanion` تلقائياً ⇒ "🪪 الإقامة" يعرض
الملف مباشرة ⇒ فتح تفاصيله ⇒ رفع صورة حقيقية (`resize_to_4x6` على
بايتات JPEG فعلية، إعادة الرفع للحصول على `file_id` جديد) ⇒ اختيار
تاريخ تنبيه (اليوم) عبر مسار التقويم الحقيقي الكامل (`rn:remind_` ثم
`rn:cal_pick:...`) ⇒ `reminder_active=True` ⇒ استدعاء المهمّة اليومية
الحقيقية ⇒ رسالة صحيحة تصل لمجموعة الإقامة تتضمّن المريض ومرافقه ⇒
محاكاة ضغط "تم تقديم الطلب" **كمستخدم عادي غير أدمن** ⇒ رفض صحيح، لا
تغيير ⇒ نفس الضغط **كأدمن** ⇒ `submitted=True`، يختفي من القائمة
المعلّقة، والمهمّة اليومية اللاحقة لا تشمله بعد الآن · محاكاة منفصلة
لـ`delete_patient()` بثلاث حالات على المخطط الجديد (ملف فارغ يُحذَف،
ملف بصورة مرفوعة يبقى دون مساس، مريض بلا ملف إقامة يُحذَف بلا خطأ) ·
اختُبِر سكربت إسقاط الجداول فعلياً على قاعدة اختبار تحتوي الجداول
القديمة الأربعة معاً، وتأكَّد أن `res_profiles`/`res_companions` تُنشَأ
تلقائياً بالمخطط الجديد المضبوط (تأكيد أسماء الأعمدة) فور أول لمسة
لقاعدة البيانات بعد الإسقاط.

⚠️ **قبل النشر على الخادم**: يجب تشغيل
`scripts/drop_old_residency_tables.py --apply` **قبل** `pm2 restart
all` — وإلا سيحاول الكود الجديد التعامل مع أعمدة غير موجودة في الجداول
القديمة (النسخة الاحتياطية الكاملة موجودة مسبقاً كخط رجعة).

### 2026-08-15 (٣٧) — 🔨 توسيع "🪪 الإقامة" إلى نظام دورة حياة كامل بحالات مستقلة لكل شخص

**الطلب**: مواصفة تفصيلية كاملة من المستخدم لنظام إقامة كامل — كل شخص
(المريض وكل مرافق) له دورة حياة مستقلة تماماً عبر 5 حالات: 🟡 معلّق من
الوصول → 🟢 نشط → 🔴 معلّق انتهاء → 🔵 تم التقديم → 🟣 تم الإصدار →
(تأكيد الإصدار) → 🟢 نشط من جديد، مع بقاء المريض ومرافقيه مرتبطين
بصرياً بنفس "الطلب" في كل الشاشات. بيانات المريض/المرافقين تصل جاهزة
من "🛬 الوصول" — **لا** سؤال "هل يوجد مرافق؟" ولا زر إضافة مرافقين
مستقل داخل الإقامة إطلاقاً. طلب المستخدم صراحةً: تطوير نفس
`modules/residency/` القائم (لا نظام منفصل)، بعد فحص شامل للمشروع أولاً.

**التدقيق قبل التصميم** (عبر Explore agent + قراءة مباشرة): المخطط
السابق (`ResidencyProfile`/`ResidencyCompanion`، المرحلة الأولى من نفس
اليوم) لم يكن يعطي المرافقين أي حالة/صورة/تاريخ مستقل — نفس الفجوة
موجودة في `ArrivalCompanion` (بلا عمود حالة، خلافاً لـ`ArrivalPatient.
arrival_status`) — تأكَّد أنه **لا يوجد أي نمط "أب/تابع بحالة مستقلة
لكل صفّ" سابق في كامل المشروع**، وأن المهمّة اليومية الحالية
(`daily_residency_reminders`) قراءة+إرسال فقط، بلا أي كتابة مجدولة على
القاعدة سابقاً.

**قرارات ثبَّتها المستخدم صراحةً بعد أسئلة توضيحية**:
- زر "🪪 الإقامة" يبقى بنفس الاسم/الرمز (رغم استخدام "🏠 الإقامات" عرضاً
  في رسالة الطلب).
- **حُذفت الرسائل التلغرامية التلقائية لمجموعة الإقامة نهائياً** —
  "نكتفي بوصولها للمستخدم في البوت فقط" — التنبيه الآن هو ظهور
  العدّادات/القوائم داخل البوت عند فتحه، لا دفعاً فعلياً عبر رسائل.

**التصميم**:
- `db/models.py`: حذف `ResidencyProfile`/`ResidencyCompanion` (مخطط
  المرحلة الأولى)، إضافة `ResidencyPerson` (`res_persons`) — جدول واحد
  موحَّد بعلاقة ذاتية `parent_id` (`None`=المريض/جذر الطلب، معرَّف=مرافق
  تابع) يحمل `status`/`photo_file_id`/`reminder_date`/`expiry_date`/
  `residency_file_id` لكل شخص على حدة — و`ResidencyStatusLog`
  (`res_status_log`) سجلّ تدقيق لكل انتقال حالة.
- `modules/residency/models.py`: إعادة كتابة كاملة — `create_profiles_from_arrival`
  (نفس التوقيع بالضبط، **لا تعديل** في نقطة الاستدعاء بـ`arrivals/flow.py`)،
  `bulk_activate_request` (تفعيل الطلب كاملاً دفعة واحدة)، `mark_submitted`،
  `start_issuance`/`set_issuance_expiry`/`save_issuance_file`/`confirm_issuance`
  (دورة الإصدار الأربع خطوات)، `delete_stub_person_by_name` — كل دالة
  تسجّل الانتقال في `res_status_log`.
- `modules/residency/flow.py`: تسلسل تسجيل دخول تدريجي واحد فقط
  (🟡 الوحيد الذي يحتاج جلسة خطية في `context.user_data["_rn_onboard"]`
  المُشتقّة حيّاً من حالة البيانات نفسها لا مخزَّنة بشكل منفصل — صورة
  ثم تاريخ تنبيه لكل شخص بالتسلسل تلقائياً بلا أي سؤال عن وجود مرافقين)؛
  شاشات الحالات الأربع الأخرى (🟢🔴🔵🟣) تعرض **الطلبات** (تجميع بصري)
  لكن تفتح **شاشة تفاصيل موحَّدة واحدة** (`build_family_detail`) تُستخدَم
  من كل نقاط الدخول (القوائم الخمس، البحث، النتائج) وتعرض زرّ الإجراء
  المناسب لحالة **كل شخص على حدة**. "🔵 تم التقديم" يبقى **أدمن فقط**
  (استمرار لنفس قاعدة المرحلة الأولى).
- `services/residency_status_service.py` (جديد) — `run_daily_expiry_check()`:
  مهمّة يومية واحدة (٩ص، تحلّ محلّ `daily_residency_reminders` المحذوفة
  بالكامل مع ملفها) تحوّل كل شخص `ACTIVE` وصل تاريخ تنبيهه إلى
  `EXPIRY_PENDING` — **كتابة فقط على القاعدة، بلا أي `bot.send_message`
  إطلاقاً** (أول مهمّة كتابة مجدولة في المشروع، طبقاً للتدقيق).
- `scripts/migrate_to_res_persons.py` (جديد، معاينة/`--apply`) — ينقل
  أي بيانات فعلية من `res_profiles`/`res_companions` (مخطط المرحلة
  الأولى) إلى `res_persons` (`submitted=True`→`SUBMITTED`، وإلا
  `WAITING_ARRIVAL`؛ المرافقون يرثون حالة والدهم بما أنهم لم يكن لهم
  حالة مستقلة أصلاً)، ثم يُسقِط الجدولين القديمين.
- `services/patients_service.py::delete_patient()` — يستدعي
  `delete_stub_person_by_name` الجديدة (تحذف فقط أشخاصاً `WAITING_ARRIVAL`
  بلا صورة بعد؛ أي تقدّم حقيقي محفوظ دائماً).

**التحقق**: `python -m py_compile` نظيف على كل الملفات · بعد حذف
`__pycache__` بالكامل: **187 اختباراً** (بلا تغيير) + **167 معالجاً**
حقيقياً (+1 عن نظام المرحلة الأولى — معالِج نصّ جديد للبحث) · محاكاة
حيّة شاملة كاملة عبر الدوال الفعلية غطّت الدورة بأكملها: تأكيد وصول
بمريض + مرافقَين ⇒ 3 أشخاص `WAITING_ARRIVAL` ⇒ فتح الطلب من 🟡 ⇒ تسلسل
صورة+تاريخ تنبيه للمريض تلقائياً ثم للمرافقَين **بلا أي سؤال عن وجود
مرافقين** ⇒ شاشة مراجعة ⇒ "✅ حفظ الإقامة" ⇒ الثلاثة يصيرون `ACTIVE`
معاً + 3 سجلّات دقيقة في `res_status_log` ⇒ اختيار تاريخ تنبيه مستقبلي
للمريض والمرافق الأول، وتاريخ اليوم للمرافق الثاني فقط ⇒ تشغيل
`run_daily_expiry_check()` الحقيقية ⇒ **المرافق الثاني وحده** يصير
`EXPIRY_PENDING`، المريض والمرافق الأول يبقيان `ACTIVE` دون أي مساس
(تحقّق مباشر لمبدأ الاستقلالية) ⇒ مستخدم غير أدمن يُرفَض من "تم
التقديم" ⇒ أدمن ينجح ⇒ "تم الإصدار" ⇒ تاريخ انتهاء جديد (عبر التقويم
الحقيقي) + رفع ملف حقيقي (**بلا** ضبط مقاس 4×6 — يختلف عن صورة التسجيل
الشخصية عمداً) ⇒ "✅ تأكيد الإصدار" ⇒ يعود ذلك الشخص وحده إلى `ACTIVE`
بـ`reminder_date` فارغ من جديد، بقية العائلة دون أي مساس طوال العملية
⇒ البحث عن اسم مرافق يُحَل بنجاح لجذر عائلته ⇒ السجلّ يعكس كل الانتقالات
العشرة بالترتيب الصحيح · محاكاة منفصلة لـ`delete_patient()` (شخص فارغ
يُحذَف مع تابعه، شخص بصورة يبقى دون مساس) · اختُبِر سكربت الهجرة فعلياً
(مريضان + مرافق واحد بمخطط المرحلة الأولى) وتأكَّد نقلهم بصورة صحيحة
(الحالة المُشتقَّة من `submitted`، المرافق مرتبط بمعرّف والده الجديد)
وإسقاط الجدولين القديمين.

⚠️ **قبل النشر على الخادم**: تشغيل `scripts/migrate_to_res_persons.py
--apply` **قبل** `pm2 restart all` (بعد نسخة احتياطية جديدة أولاً —
نفس نمط الإصدار السابق).

### 2026-08-15 (٣٨) — 🔨 إضافة "📄 الوثائق" + "🖨️ طباعة ملف الحالة" فوق نظام دورة الحياة القائم (بلا تعديل على الـWorkflow)

**الطلب**: ميزتان تُضافان **فوق** نظام دورة الحياة الكامل (البند ٣٧)
دون إعادة بناء أي زر/انتقال حالة موجود: (١) وثائق مستقلة لكل شخص
(مريض أو مرافق كلّ على حدة) — Form C أو وثيقة أخرى بمسمّى حرّ — غير
مرتبطة بحالة الإقامة إطلاقاً وتبقى محفوظة عبر كل تغييرات الحالة/إعادة
الإصدار؛ (٢) زرّ طباعة PDF واحد لملف الحالة الكامل (رأس الحالة + قسم
كامل لكل شخص: صورة شخصية، الحالة الحالية، تاريخ الانتهاء، **آخر إصدار**
بتاريخه وملفه، ثم وثائقه) ينتهي بجدول ملخّص — مع تأكيد صريح من المستخدم
أن الإصدارات القديمة **لا تُحذَف أبداً**، فقط لا تُطبَع (الطباعة للأحدث
فقط).

**التصميم**:
- `db/models.py`: جدولان جديدان بلا مساس بأي جدول قائم —
  `ResidencyDocument` (`res_documents`: `person_id, doc_type, doc_name,
  file_id, created_by, created_at`) و`ResidencyIssuance`
  (`res_issuance_history`: `person_id, expiry_date, file_id, issued_at`)
  — أرشيف كل إصدار سابق، لأن الحقول الحيّة على `ResidencyPerson`
  (`expiry_date`/`residency_file_id`) تُستبدَل بالقيمة الأحدث في كل مرة
  ولا تحتفظ بالتاريخ نفسه، فاحتاج الاحتفاظ بالإصدارات القديمة جدولاً
  مستقلاً.
- `modules/residency/models.py`: `add_document()` جديدة، و`confirm_issuance()`
  القائمة عُدِّلت بسطر واحد فقط — إضافة صفّ أرشفة في `ResidencyIssuance`
  قبل استبدال الحقول الحيّة، **بلا أي تغيير على توقيعها/شروطها/قيمتها
  المُرجَعة**.
- `modules/residency/repository.py`: `DocumentRow`/`IssuanceRow`
  (dataclasses مجمَّدة)، `get_documents_for_person()` (Form C أولاً)،
  `get_document_counts()` (لعدّاد الأزرار بلا استعلام منفصل لكل شخص)،
  `get_latest_issuance()` (لتاريخ آخر إصدار — لا يُخزَّن على الحقول
  الحيّة).
- `modules/residency/views.py`: `build_family_detail()` تعديل توافقي
  فقط — بارامتر اختياري جديد `doc_counts=None` يضيف زرّ "📄 الوثائق (N)"
  لكل شخص وزرّ "🖨️ طباعة ملف الحالة" على مستوى العائلة، **بلا مساس** بأي
  توقيع/سلوك قديم لاستدعاءات لم تمرّر البارامتر الجديد. دوال جديدة:
  `build_documents_list/build_doc_type_prompt/build_doc_name_prompt/
  build_doc_file_prompt`.
- `modules/residency/flow.py`: كولباكات جديدة تحت نفس بادئة `rn:`
  (`docs_`, `doc_add_`, `doc_add_formc_`, `doc_add_other_`, `print_`)
  — **بلا أي `CallbackQueryHandler`/`MessageHandler` جديد على مستوى
  التسجيل**، فروع إضافية فقط داخل `_dispatch_callback`/`_on_text_message`/
  `_on_media_message` الموجودة (علم انتقالي جديد `_CTX_DOC_NAME_ACTIVE`
  لالتقاط اسم وثيقة "أخرى" نصّياً، وقيمة جديدة `"doc_upload"` ضمن
  `_CTX_UPLOAD_TARGET["kind"]` الموجود — **بلا** ضبط مقاس 4×6، نفس
  معاملة `"issue_file"`).
- `services/residency_case_pdf.py` (ملف جديد مستقل): نسخة محلّية من نفس
  نمط النص العربي السليم المُثبَت في `pharmacy_evacuation_pdf.py`
  (`_ar()`/`_ar_wrap()`/`_pick_font()` بخط عربي حقيقي مسجَّل — نفس سبب
  إصلاح "الصناديق" سابقاً)، مُعاد كتابتها استقلالاً لهذا الملف فقط.
  `build_case_pdf(case: dict)` دالة **نقية متزامنة** بلا أي استدعاء
  Telegram/DB داخلها — كل بايتات الصور (الصورة الشخصية/ملف آخر إصدار/
  ملفات الوثائق) تُنزَّل مسبقاً بشكل غير متزامن في `flow.py::
  _send_case_pdf` (عبر `bot.get_file`+`download_to_memory`، تُهمَل
  بصمت عند الفشل) ثم تُمرَّر جاهزة، فتُشغَّل دالة البناء داخل
  `asyncio.to_thread(...)` بأمان دون تعارض حلقات الأحداث. الطباعة تقرأ
  حصراً من الحقول الحيّة + `get_latest_issuance()`/`get_documents_for_person()`
  — **لا تُقرَأ** `res_issuance_history` القديمة لغرض العرض إطلاقاً،
  فقط لضمان بقائها محفوظة في القاعدة.

**التحقق**: محاكاة حيّة كاملة عبر الدوال الفعلية — إنشاء طلب (مريض
+مرافق) ⇒ تفعيل حتى `ACTIVE` ⇒ إضافة Form C + وثيقة أخرى مسمّاة للمريض
ووثيقة واحدة للمرافق ⇒ تأكَّد أن وثائق كل شخص معزولة تماماً عن الآخر
(بلا أي تسرّب بالاتجاهين) ⇒ دورة إصدار كاملة (تقديم→إصدار→تأكيد) على
المريض ⇒ تأكَّد ظهور صفّ جديد في `res_issuance_history` **مع بقاء**
وثائقه المُضافة سابقاً دون أي مساس ⇒ **دورة إصدار ثانية كاملة** ⇒
تأكَّد أن صفّ الإصدار الأول ظلّ في القاعدة دون تعديل (لم يُحذَف/يُستبدَل)
وأن `get_latest_issuance()` يُعيد الأحدث فقط، ووثائق المريض لا تزال
سليمة بعد دورتَي إصدار كاملتَين ⇒ بناء PDF حقيقي بصور اختبار فعلية
(`Pillow`) للمريض والمرافق معاً (صورة شخصية + ملف إصدار + وثائق) ⇒
نجح البناء (92 كيلوبايت) واستُخرِج نصّ من الملف الناتج بلا أي استثناء
(`pypdf`) · حذف `__pycache__` بالكامل ثم `pytest -q --ignore=Archive`
(187 نجح، كالمتوقَّع تماماً) · فحص `register_all_handlers()` حقيقي عبر
`Application` فعلي — **167 معالِجاً بالإجمالي، بلا أي زيادة** عن ما قبل
هذه الميزة (نفس عدد معالِجات group 16/17/18/20 الخاصة بالإقامة تماماً)
— يؤكّد أن كل التوسعة تمّت داخل المعالِجات الموجودة فعلاً كما طُلِب
صراحةً.

لا حاجة لسكربت هجرة — `res_documents`/`res_issuance_history` جدولان
جديدان فارغان بالكامل، يُنشئهما `Base.metadata.create_all` تلقائياً
عند أول تشغيل بعد النشر.

### 2026-08-16 (٣٩) — 🐛 وثائق PDF حقيقية (Form C وغيرها) تُسقَط بصمت عند الرفع فلا تظهر في "🖨️ طباعة ملف الحالة"

**البلاغ**: "في ملف الإقامات للمريض والمرافقين، الطباعة الآن تُخرِج
بيانات وصورة شخصية فقط — الوثائق التي تم رفعها لا تظهر إطلاقاً".

**التشخيص**: خللان مركّبان في ميزة الوثائق (البند ٣٨) اكتُشِفا معاً
عند المراجعة:
1. `modules/residency/flow.py::register_handlers()` كان يسجّل معالِج
   الوسائط بفلتر `filters.PHOTO | filters.Document.IMAGE` فقط — أي أن
   أي ملف Form C/وثيقة تصل كملف PDF حقيقي (الحالة الشائعة فعلياً لوثائق
   رسمية ممسوحة ضوئياً) لا يطابق الفلتر إطلاقاً، فلا يُستدعى `_on_media_message`
   أصلاً ولا يُحفَظ أي شيء — **بصمت تام، بلا أي رسالة خطأ للمستخدم**. هذا
   يخالف السابقة المُثبَتة في بقية البوت (`user_medical_attachments.py`/
   `admin_patient_attachments_bundle.py`/`shared/uploads/collector.py`)
   التي تقبل `filters.Document.ALL` دائماً.
2. حتى لو نجح الرفع، `services/residency_case_pdf.py::_embed_image()`
   كان يحاول تحميل أي مرفق كـ`Image` عبر `ImageReader` فقط — فإن كان
   المرفق ملف PDF حقيقي لا صورة، يفشل التحميل بصمت (`except Exception`)
   ولا يظهر شيء داخل الملف الناتج.

**الإصلاح**:
- `modules/residency/flow.py`: الفلتر أصبح `filters.PHOTO |
  filters.Document.ALL`، و`_extract_file_id()` يقبل أي `message.document`
  بصرف النظر عن نوعه (لا فحص `mime_type` بعد الآن).
- `services/residency_case_pdf.py`: دالة جديدة `_classify_attachment(data)`
  تفحص البايتات فعلياً (تُصنَّف `"pdf"` عبر تحقّق ترويسة `%PDF-`،
  `"image"` عبر `ImageReader` ناجح، وإلا `"unknown"`) — الصور تُضمَّن
  مباشرة كما كانت، وملفات PDF الحقيقية **تُدمَج كصفحات كاملة في نهاية
  الملف** عبر `pypdf` (نفس نمط `admin_patient_attachments_bundle.py`
  المُثبَت)، كل واحدة مسبوقة بصفحة فاصلة تُعرِّف صاحبها والوثيقة، بدل
  إسقاطها بصمت. المرفقات من نوع "unknown" تُذكَر بملاحظة نصّية بدل
  تجاهلها تماماً — لا شيء يُسقَط بصمت بعد الآن.

**التحقق**: فحص تصنيف مباشر (`none`/`image`/`pdf`/`unknown`) على بايتات
حقيقية لكل نوع ⇒ صحيح · بناء ملف حالة بمرفقات PDF حقيقية (ملف إصدار
PDF بصفحتين + وثيقتان PDF إحداهما بصفحة واحدة + وثيقة بنوع غير مدعوم)
⇒ عدد صفحات الملف الناتج أكبر من نسخة الصور فقط، واستخراج النص أكّد
وجود محتوى كل صفحة PDF حقيقية مدمَجة حرفياً (`"Form C scan page 1/2"`،
`"Hospital letter page 1"`) داخل الملف النهائي · أُعيد تشغيل محاكاة
البند ٣٨ الكاملة (عزل الوثائق، دورتا إصدار) ⇒ لا يزال ناجحاً بلا أي
انحراف · حذف `__pycache__` بالكامل ثم `pytest -q --ignore=Archive`
(187 نجح) · فحص `register_all_handlers()` حقيقي (167 معالِجاً — بلا أي
زيادة، توسيع فئة الفلتر على نفس المعالِج المسجَّل مسبقاً فقط).

لا حاجة لسكربت هجرة أو نقل بيانات — الإصلاح في منطق القراءة/الرفع فقط،
لا تغيير في المخطط.

### 2026-08-16 (٤٠) — 🔧 تعديل فورم إدخال بيانات المرافق في "🛬 الوصول": حذف تاريخ الوصول، فصل تاريخ انتهاء التأشيرة عن الإقامة، توسيع وصف عنوان السكن

**الطلب**: بعد مراجعة فورم إدخال المرافق الحالي معي، طلب المستخدم ثلاثة
تعديلات: (١) الحقل المُسمّى "تاريخ انتهاء الإقامة" (والذي كان يكتب فعلياً
في `visa_expiry`) يُقسَّم إلى خطوتين متتاليتين — "تاريخ انتهاء التأشيرة"
أولاً (إلزامي، بلا تخطي) ثم "تاريخ انتهاء الإقامة" بعده مباشرة (بزر
تخطي)؛ (٢) حذف خطوة "تاريخ الوصول" للمرافق نهائياً لأنها تُدخَل بالفعل
أثناء بيانات المريض نفسه ولا داعي لتكرارها؛ (٣) توسيع نص "عنوان السكن"
ليطلب صراحة اسم العمارة ورقم الشقة.

**التنفيذ**:
- `modules/general_services/arrivals/session.py`: أُزيلت ملاحظة "kept
  for legacy session compat only" عن `STEP_C_RESIDENCE_EXPIRY` — كان
  الثابت معرَّفاً مسبقاً بلا أي شاشة/تسجيل فعلي (خلافاً لنظيره
  `STEP_P_RESIDENCE_EXPIRY` عند المريض، المُفعَّل بالكامل)؛ فُعِّل الآن
  بنفس نمط المريض.
- `modules/general_services/arrivals/views.py`: `build_c_visa_expiry_prompt`
  أُعيد تسميته نصّياً من "تاريخ انتهاء الإقامة" إلى "تاريخ انتهاء
  التأشيرة" (يطابق الآن اسم الثابت `STEP_C_VISA_EXPIRY` واسم عمود
  `visa_expiry` الذي يُكتَب فيه فعلياً — كان الوصف النصّي مضلِّلاً منذ
  البداية). دالة جديدة `build_c_residence_expiry_prompt` (بزر تخطي)
  تُعرَض مباشرة بعده. `build_c_passport_expiry_prompt`: زرّ "⬅️ رجوع"
  أصبح يعود لاختيار الاسم مباشرة (`back_c_name`) بدل شاشة تاريخ الوصول
  المحذوفة. `build_c_passport_prompt`: زرّ "⬅️ رجوع" أصبح يعود لشاشة
  تاريخ انتهاء الإقامة الجديدة. `build_c_residence_address_prompt`:
  النص أصبح "اكتب عنوان السكن الكامل لهذا المرافق (اسم العمارة ورقم
  الشقة)".
- `modules/general_services/arrivals/flow.py`: `_on_c_name_selected`
  ينسخ الآن `arrival_date` من `current_patient` تلقائياً للمرافق
  ويقفز مباشرة لـ`STEP_C_PASSPORT_EXPIRY` (بدل `STEP_C_ARRIVAL_DATE`).
  فرع `cal_pick` عند `STEP_C_VISA_EXPIRY` أصبح ينتقل إلى
  `STEP_C_RESIDENCE_EXPIRY` بدل القفز مباشرة لصورة الجواز؛ فرع جديد
  لـ`STEP_C_RESIDENCE_EXPIRY` يكتب `residence_expiry` وينتقل لـ
  `STEP_C_PASSPORT`. كولباكات جديدة: `c_residence_expiry_cal`/
  `c_residence_expiry_prompt`/`skip_c_residence_expiry`/
  `back_c_residence_expiry` — كلها فروع إضافية داخل المُوزِّع الموجود،
  **بلا أي** `CallbackQueryHandler` جديد على مستوى التسجيل. شاشة/كولباك
  "تاريخ الوصول" (`build_c_arrival_date_prompt`/`skip_c_arrival_date`/
  `back_c_arrival_date`) صارت غير قابلة للوصول من المسار الحي — أُبقيت
  بلا حذف (نفس نمط الشاشات اليتيمة الموجودة أصلاً في هذا الملف
  للملاحظات/الخدمات/الجهة الموصلة للمرافق).

**التحقق**: محاكاة حيّة كاملة عبر استدعاء دوال `flow.py` الحقيقية
مباشرة (`_on_c_name_selected`/`_dispatch_callback_inner`) بلا أي تمثيل
مختصر: اختيار اسم مرافق ⇒ تأكيد القفز الفوري لـ`STEP_C_PASSPORT_EXPIRY`
مع نسخ `arrival_date` من المريض تلقائياً ⇒ تخطي تاريخ انتهاء الجواز ⇒
تأكيد شاشة "تاريخ انتهاء التأشيرة" الجديدة التسمية وبقائها إلزامية (بلا
زر تخطي) ⇒ اختيار تاريخها عبر `cal_pick` حقيقي ⇒ تأكيد الانتقال لشاشة
"تاريخ انتهاء الإقامة" الجديدة (بزر تخطي، وزر رجوع صحيح) ⇒ تخطيها ⇒
تأكيد الوصول لصورة جواز السفر بزر رجوع يعود للشاشة الجديدة ⇒ اختبار
مسار الرجوع (`back_c_residence_expiry`) ⇒ اختبار المسار الثاني (اختيار
تاريخ الإقامة عبر التقويم بدل تخطيه) ⇒ كلا المسارين ينتهيان بشكل صحيح
عند صورة جواز السفر ⇒ تأكيد نص عنوان السكن الجديد يذكر صراحة "اسم
العمارة"/"رقم الشقة" · حذف `__pycache__` بالكامل ثم `pytest -q
--ignore=Archive` (187 نجح) · فحص `register_all_handlers()` حقيقي (167
معالِجاً — بلا أي زيادة).

لا حاجة لسكربت هجرة — الأعمدة `arrival_date`/`visa_expiry`/
`residence_expiry` موجودة مسبقاً في `ArrivalCompanion`، التعديل في
منطق التسلسل والتسميات فقط.

### 2026-08-16 (٤١) — 🔧 حذف محاولات ترحيل أعمدة على جدولَي res_profiles/res_companions المحذوفَين (ضجيج أخطاء في السجلّ عند كل إقلاع)

**البلاغ**: المستخدم لصق سجلّ pm2 بعد إعادة تشغيل السيرفر يُظهر 7 أخطاء
`sqlite3.OperationalError: no such table: res_profiles` / `res_companions`
مع traceback كامل لكل واحد، عند كل إقلاع للبوت.

**التشخيص**: `db/maintenance.py::check_db_health_startup()` يستدعي
`_migrate_column()` لسبعة أعمدة على `res_profiles`/`res_companions` —
وهما اسما الجدولين **القديمَين** (مخطط الإقامة الأصلي، قبل حتى "المرحلة
١" من إعادة البناء هذه الجلسة) اللذان حُذِفا نهائياً عبر
`scripts/drop_old_residency_tables.py` عند بناء `res_persons` (البند
٣٧). هذه الاستدعاءات كانت أُضيفت لترحيل أعمدة (`passport_expiry`،
`form_c_file_id`، `photo_file_id`، `tickets_file_id`) على المخطط
القديم **قبل** حذفه، ولم تُزَل بعد الحذف — فتُحاول `ALTER TABLE` على
جدول غير موجود عند كل إقلاع، تفشل (يلتقطها `try/except` الداخلي في
`_migrate_column` فلا تُسقِط البوت) لكنها تطبع 7 أخطاء كاملة بـ
traceback في كل مرة، بلا أي فائدة فعلية (الأعمدة نفسها موجودة أصلاً في
تعريف `ResidencyPerson` الحالي عبر `Base.metadata.create_all`، لا
تحتاج أي ترحيل تدريجي).

**الإصلاح**: حذف الاستدعاءات السبعة (`res_profiles.passport_expiry`،
`res_companions.passport_expiry`، `res_profiles.form_c_file_id`،
`res_profiles.photo_file_id`، `res_companions.photo_file_id`،
`res_profiles.tickets_file_id`، `res_companions.tickets_file_id`)
وتعليقاتها من `db/maintenance.py` — بلا أي مساس ببقية قائمة الترحيلات
الطويلة (كل عمود آخر في هذه الدالة لا يزال يخصّ جدولاً حياً).

**التحقق**: تأكَّدت أولاً عبر grep أن `res_profiles`/`res_companions`
غير مذكورين في أي كود حيّ (`db/models.py`، أي `modules/`) — فقط في
`scripts/migrate_to_res_persons.py`/`scripts/drop_old_residency_tables.py`
(سكربتان لمرة واحدة، نُفِّذا بالفعل على الخادم) وسجلّ الصيانة التاريخي
· بناء قاعدة اختبار فعلية بمخطط `res_persons` الحالي (بلا الجدولين
القديمين إطلاقاً، تماماً كحال قاعدة الإنتاج بعد الحذف) وتشغيل
`DatabaseMaintenance.check_db_health_startup()` الحقيقية عليها ⇒ لا
ظهور لأي خطأ `res_profiles`/`res_companions` في المخرجات بعد الآن ·
حذف `__pycache__` بالكامل ثم `pytest -q --ignore=Archive` (187 نجح، لا
تغيير في أي معالِج فهذا تعديل داخل مسار فحص القاعدة عند الإقلاع فقط).

لا حاجة لسكربت هجرة — حذف كود ميت فقط، بلا أي تغيير على المخطط أو
البيانات.

### 2026-08-16 (٤٢) — 🔨 حذف عنوان سكن المرافق المكرَّر + تنظيف "ملاحظات الدفعة" الميتة + نقل الجهة الموصلة لآخر كتلة المريض في التقرير

**الطلب**: ثلاثة تعديلات على تقرير "🛬 الوصول" المنشور: (١) لا داعي
لعنوان سكن مستقل لكل مرافق — عنوان سكن المريض يغطّي العائلة كاملة؛
(٢) يوجد سطرا ملاحظات مكرَّران ("ملاحظات الدفعة" و"ملاحظات" المريض) —
يبقى واحد فقط في آخر التقرير؛ (٣) "الجهة الموصلة" تكون آخر شيء قبل
"مختص الخدمات".

**التدقيق**: عبر `AskUserQuestion` تأكَّد المطلوب من التعديل الثالث:
تسلسل الإدخال (ملاحظات→خدمات→جهة موصلة→مختص) كان صحيحاً بالفعل، لكن
نص التقرير المنشور يعرض "🚐 الجهة الموصلة" **ضمن** كتلة كل مريض (بعد
الملاحظات، قبل الوثائق والمرافقين) بينما "👨‍⚕️ مختص الخدمات" يظهر مرة
واحدة في **آخر التقرير كله** — المستخدم اختار: تبقى الجهة الموصلة داخل
كتلة كل مريض، لكن كآخر سطر فيها (لا تُجمَّع في آخر التقرير كله).
بالنسبة لـ"ملاحظات الدفعة": تتبُّع فعلي لكل استدعاءات `STEP_BATCH_NOTES`
أثبت أنها **ميتة تماماً من الأصل** — لا مسار حيّ واحد يصل إليها (لا من
`STEP_DATE_CUSTOM`، ولا من `_finish_patient_and_advance`)، فبقي حقل
`session.batch_notes` فارغاً دائماً وسطره في التقرير يظهر بلا محتوى؛
نفس الأمر لخطوة "مختص الدفعة" القديمة (`STEP_SPECIALIST`/`build_specialist_prompt`)
التي كانت الوجهة الوحيدة لـ`STEP_BATCH_NOTES` — كتلة يتيمة كاملة من
معمارية أقدم سبقت "المختص لكل مريض".

**الإصلاح**:
- `session.py`: حذف `STEP_SPECIALIST`، `STEP_BATCH_NOTES`،
  `STEP_C_RESIDENCE_ADDRESS`، وحقل `batch_notes` من `ArrivalSession`
  بالكامل (تعريف + `save`/`load`/`create`).
- `views.py`: حذف `build_specialist_prompt`، `build_batch_notes_prompt`،
  `build_c_residence_address_prompt`، وإزالة "addr" من `_C_EDIT_FIELDS`
  (المرافق لم يعد له حقل عنوان سكن قابل للتعديل).
- `flow.py`: حذف `_show_specialist`/`_show_batch_notes`/`_show_c_residence_address`
  وكل الكولباكات المرتبطة (`back_to_batch_notes`/`back_to_specialist`/
  `skip_c_residence_address`) والفروع النصّية المرتبطة (`STEP_BATCH_NOTES`/
  `STEP_C_RESIDENCE_ADDRESS`) — صورة إقامة المرافق (`STEP_C_RESIDENCE`،
  رفعاً أو تخطياً) تنتقل الآن مباشرة لـ`_advance_after_companion_done`.
  نص التقرير المنشور: حُذف سطر "🏠 عنوان السكن" من كتلة كل مرافق، حُذف
  سطر "📝 ملاحظات الدفعة" الميت بالكامل، ونُقل سطر "🚐 الجهة الموصلة"
  ليصبح آخر سطر في كتلة كل مريض (بعد قائمة مرافقيه ووثائقهم، لا قبلها).
- **خلل حقيقي اكتُشِف أثناء الاختبار الحي وأُصلِح بالمناسبة**: تعديل
  الجلسة الماضية (البند ٤٠) حذف "تاريخ الوصول" من مسار *يدوي* واحد فقط
  (`_on_c_name_selected`، يُستدعى فقط عبر "➕ أضف مرافقاً آخر" غير
  المفعَّل حالياً) — لكن المسار **الأساسي** الفعلي لبدء كل مرافق فعلياً
  (`_start_next_companion`، يسحب المرافقين من `companion_queue` المعروف
  مسبقاً من سجلّ المريض) كان لا يزال يضبط `STEP_C_ARRIVAL_DATE` مباشرة،
  فيسأل عن تاريخ الوصول لكل مرافق حقيقي رغم الإصلاح السابق. طُبِّق نفس
  إصلاح النسخ التلقائي من تاريخ وصول المريض هناك أيضاً.

**التحقق**: محاكاة حيّة كاملة عبر استدعاء `flow.py` الحقيقية مباشرة
(`_on_c_name_selected`/`_dispatch_callback_inner`/`_handle_photo_input`/
`_start_next_companion`) — مسار مرافق يدوي وصل حتى صورة الإقامة، تخطّيها
⇒ تأكَّد الانتقال المباشر لكتلة ختام المريض بلا أي شاشة عنوان سكن ⇒
تأكَّد مسار `_start_next_companion` (السجل) أيضاً يتخطّى تاريخ الوصول
وينسخه تلقائياً ⇒ إكمال كتلة الختام (ملاحظات/خدمات/جهة موصلة عبر زرّ
حقيقي/مختص عبر زرّ حقيقي) حتى `STEP_REVIEW` ⇒ استدعاء `"confirm"`
الحقيقي كاملاً (حفظ فعلي في قاعدة اختبار + `report_publisher.publish`
حقيقية مع بوت وهمي يلتقط النص بلا أي اتصال شبكي فعلي) ⇒ فحص النص
المنشور الفعلي: لا وجود لـ"ملاحظات الدفعة" إطلاقاً، كتلة المرافق بلا
سطر عنوان سكن، و"🚐 الجهة الموصلة" هي آخر سطر فعلي في كتلة المريض قبل
الفاصل مباشرة (بعد المرافقين ووثائقهم) · حذف `__pycache__` بالكامل ثم
`pytest -q --ignore=Archive` (187 نجح) · فحص `register_all_handlers()`
حقيقي (167 معالِجاً — بلا أي زيادة).

لا حاجة لسكربت هجرة — تعديل منطق العرض/التسلسل فقط، عمود
`residence_address` في `ArrivalCompanion` يبقى في القاعدة (بيانات
قديمة محفوظة، لا تُكتَب قيمة جديدة فيه بعد الآن).

### 2026-08-17 (٤٣) — 🔨 شاشة اختيار الوثائق قبل "🖨️ طباعة ملف الحالة" + تضمين وثائق "🛬 الوصول"

**الطلب**: زر "🖨️ طباعة ملف الحالة" كان يطبع فقط الصورة الشخصية/آخر
إصدار رسمي/وثائق `res_documents` — لا وثائق رُفعت أصلاً وقت "🛬 الوصول"
(جواز، تأشيرة، إقامة، تذاكر). طُلِب: (أ) تضمين وثائق الوصول، (ب) شاشة
اختيار قبل الطباعة تحدِّد أي الفئات تُطبَع (جوازات/تأشيرات/إقامات/
تذاكر/فورم سي/الصورة الشخصية).

**التدقيق (عبر Explore agents)**: لا رابط فعلي بين `res_persons`
وجداول الوصول (`gs_arrival_patients`/`gs_arrival_companions`) —
`ResidencyPerson.arrival_patient_id` موجود بالمخطط لكنه ميت تماماً
(`create_profiles_from_arrival()` لا يكتبه، ولا طريق بنيوي لربطه أصلاً
لأن `save_arrival_batch()` لا يُعيد IDs الصفوف). الحل: **مطابقة بالاسم
الحرفي عند الطباعة فقط** — نفس دقّة `get_patient_by_name()` الحالية،
بلا أي Migration. محرِّك `shared/multiselect/engine.py` جاهز وموثَّق
بالكامل (مُستخدَم في 5+ مكان، مسجَّل عالمياً مسبقاً في
`bot/handlers_registry.py`) — أُعيد استخدامه حرفياً بدل بناء شاشة
اختيار جديدة.

**قرار توضيحي مع المستخدم حول "صورة الإقامة"** (بعد سؤالين توضيحيَّين):
شخص قد يملك مصدرَي إقامة — ملف رُفع وقت الوصول (لمريض وصل ومعه إقامة
سابقة) وملف إصدار رسمي لاحق (لمريض قُدِّم له تمديد عبر "✅ تأكيد
الإصدار"). المستخدم رفض عرض الاثنين معاً صراحةً: **"إذا يوجد إقامة
اطبع لي آخر إقامة تم رفعها سواء من الواصلين أو من الإقامة، لا أريد كل
الإقامات"** — فتُقارَن الطابعان الزمنيان (تاريخ صف الوصول مقابل
`issued_at` لآخر إصدار) ويُطبَع الأحدث فقط، بتسمية توضح مصدره.

**التنفيذ**:
- `modules/residency/repository.py`: `ArrivalDocsRow` +
  `get_arrival_patient_docs_by_name()`/`get_arrival_companion_docs_by_name()`
  — تستعلمان `db.models.ArrivalPatient`/`ArrivalCompanion` مباشرة (بلا
  استيراد بين `modules/residency` و`modules/general_services` — لا
  سابقة له في المشروع)، مطابقة `filter_by(name=...)` حرفية تامة، أحدث
  صف عند التكرار.
- `modules/residency/views.py`: `PRINT_DOC_OPTIONS` (7 فئات) لمحرِّك
  الاختيار المتعدد.
- `modules/residency/flow.py`: زرّ "print_" لم يعد يبني PDF مباشرة —
  يفتح `multiselect.open()` بكل الفئات محدَّدة مسبقاً (تأكيد فوري
  بضغطة واحدة = نفس السلوك القديم بالضبط)، ويحفظ `root_id` في
  `context.user_data` مؤقَّتاً (المحرِّك لا يحمل بيانات إضافية عبر
  الجولة). دالة جديدة `_on_print_categories` مسجَّلة عبر `result_router`
  (`register_result_routes()` جديدة تُستدعى من `routing.py`) تستقبل
  الفئات المختارة وتُكمل الطباعة. `_build_person_pdf_dict` أصبحت
  تُنزِّل كل فئة فقط إن طُلِبت، وتحسب "صورة الإقامة" الموحَّدة (مقارنة
  `get_latest_issuance()` مقابل صف الوصول، تنزيل ملف الفائز فقط — لا
  تنزيل لملف الطرف الخاسر إطلاقاً).
- `services/residency_case_pdf.py`: قسم "آخر إصدار للإقامة" القديم
  استُبدِل بقسم موحَّد "🪪 صورة الإقامة" (يعرض المصدر + التاريخ + الملف
  عبر `_attachment_flowables` الموجودة حرفياً)، وقسم جديد "📎 وثائق
  الوصول" (جواز/تأشيرة/تذاكر) — كلاهما بمفاتيح اختيارية في `case`، لا
  تغيير في توقيع `build_case_pdf` نفسه.

**التحقق**: محاكاة حيّة كاملة عبر الدوال الحقيقية — 3 حالات أشخاص:
(A) صف وصول قديم (2024) + إصدار رسمي أحدث ⇒ تأكَّد طباعة **الإصدار
الرسمي فقط**؛ (B) صف وصول بإقامة بلا أي إصدار رسمي ⇒ تأكَّد طباعة
**ملف الوصول فقط**؛ (C) لا صف وصول ولا إصدار ⇒ سطر "لا توجد إقامة
مرفوعة" بلا استثناء. تأكَّد أن إلغاء تحديد فئة (جوازات/صورة الإقامة)
يستثنيها تماماً من القاموس الناتج بينما البقية تصل سليمة ⇒ `build_case_pdf`
حقيقية على الحالات الثلاث معاً (نجحت، >2 كيلوبايت، نص عربي مستخرَج
بلا صناديق) ⇒ **جولة كاملة عبر الكولباكات الحقيقية**: `rn:print_` ⇐
يفتح شاشة الاختيار فعلاً ⇐ `msel:confirm` حقيقي عبر
`multiselect.handle_callback()` ⇐ `result_router` يوجِّه لـ
`_on_print_categories` ⇐ PDF فعلي يُرسَل (بلا أي اتصال شبكي حقيقي —
بوت وهمي يلتقط الاستدعاء). حذف `__pycache__` بالكامل ثم `pytest -q
--ignore=Archive` (187 نجح) · فحص `register_all_handlers()` حقيقي
(167 معالِجاً — بلا أي زيادة، كل التوسعة عبر `result_router`/
`multiselect` الموجودَين مسبقاً).

لا حاجة لسكربت هجرة — لا تعديل مخطط قاعدة بيانات إطلاقاً، مطابقة
بالاسم وقت التشغيل فقط.

### 2026-08-17 (٤٤) — 🔨 شاشة ملخّص بيانات الوصول قبل استكمال "🟡 معلّق من الوصول"

**الطلب**: الضغط على طلب ضمن "🟡 معلّق من الوصول" كان ينتقل فوراً لشاشة
"أرسل الصورة الآن" (بدء استكمال البيانات) بلا أي عرض لبيانات المريض
المُجمَّعة أصلاً وقت "🛬 الوصول" — لا اسم فقط. طُلِب: عرض بيانات المريض
كاملة أولاً.

**التوضيح مع المستخدم** (عبر سؤالين): (١) شاشة ملخّص مستقلة أولاً، ثم
زرّ منفصل لبدء الاستكمال — لا إضافة البيانات فوق شاشة "أرسل الصورة"
نفسها؛ (٢) الحقول المطلوبة: التواريخ (وصول/انتهاء جواز/تأشيرة/إقامة)،
حالة الوثائق المرفوعة (✅/⬜ لكل من الجواز/التأشيرة/التذاكر/الإقامة)،
وأسماء المرافقين فقط (موجودة بالفعل). لا ملاحظات/خدمات/جهة موصلة/عنوان
سكن — غير مطلوبة.

**التنفيذ**:
- `modules/residency/repository.py`: `ArrivalDocsRow` (البند ٤٣) وُسِّع
  بأربعة حقول جديدة (`arrival_date`, `passport_expiry`, `visa_expiry`,
  `residence_expiry`) بدل إنشاء دالة استعلام موازية جديدة — نفس دالتَي
  `get_arrival_patient_docs_by_name`/`get_arrival_companion_docs_by_name`
  الموجودتين مسبقاً تُستخدَمان لكلا الميزتين الآن (طباعة + ملخّص).
- `modules/residency/views.py`: `build_arrival_summary(root, arrival,
  companions)` جديدة — تعرض التواريخ وعلامات ✅/⬜ للوثائق وأسماء
  المرافقين، وزرّ "▶️ ابدأ استكمال البيانات" يستهدف كولباك
  `onboard_resume_{root_id}` **الموجود مسبقاً** (بلا أي كولباك جديد).
  عدم وجود صفّ وصول مطابق ⇒ سطر تحذير "⚠️ لا توجد بيانات وصول مطابقة"
  بدل استثناء.
- `modules/residency/flow.py`: `_show_family` — لحالة `STATUS_WAITING_ARRIVAL`،
  بدل القفز المباشر لـ`_show_onboard_step`، تُستدعى دالة جديدة
  `_show_arrival_summary` (مطابقة بالاسم عبر الدالة الموجودة، ثم عرض
  الشاشة). `_show_onboard_step` نفسها بلا أي تغيير — لا تزال الوجهة
  الوحيدة الفعلية لبدء تسلسل الصورة/التذكير، الآن تُطلَب فقط عبر الزرّ
  الصريح لا تلقائياً.

**التحقق**: محاكاة حيّة عبر الدوال الحقيقية مباشرة — شخص A (بيانات
وصول كاملة + مرافق واحد) ⇒ فتح الطلب يعرض التواريخ الأربعة وعلامات
الوثائق الصحيحة (✅ للمرفوع، ⬜ لغير المرفوع) واسم المرافق ⇒ الضغط على
"ابدأ استكمال البيانات" عبر `_dispatch_callback` الحقيقي يدخل فعلياً
شاشة "الصورة الشخصية" (لا مجرد callback صحيح شكلياً) ⇒ شخص B (لا صفّ
وصول مطابق إطلاقاً) ⇒ رسالة التحذير تظهر بلا أي استثناء. حذف
`__pycache__` بالكامل ثم `pytest -q --ignore=Archive` (187 نجح) · فحص
`register_all_handlers()` حقيقي (167 معالِجاً — بلا أي زيادة، إعادة
استخدام كولباك `onboard_resume_` الموجود مسبقاً بلا تسجيل جديد).

لا حاجة لسكربت هجرة — لا تعديل مخطط قاعدة بيانات، توسيع dataclass قراءة
فقط + شاشة عرض جديدة.

### 2026-08-17 (٤٥) — 🐛 `/start` لمستخدم جديد يسقط بـUnboundLocalError مرتين متتاليتين (logger ثم success_count)

**البلاغ**: المستخدم أرسل تقريرَي أخطاء يوميَّين. الأول (2026-08-16،
`emergency_edit.py:126`) تبيَّن أنه مُصلَح فعلاً منذ commit `aca4074`
(2026-08-06) — على الأغلب خطأ من سيرفر لم يكن قد استلم النشر وقتها،
لا ثغرة حيّة. الثاني (2026-08-15، `user_start.py:319`) كان **ثغرة
حيّة فعلية**: `UnboundLocalError: cannot access local variable
'logger' where it is not associated with a value` — تحدث لكل مستخدم
يضغط `/start` لأول مرة إطلاقاً (يُنشأ له صفّ `Translator` جديد).

**التشخيص**: دالة `user_start()` تحتوي 3 مواضع `import logging؛
logger = logging.getLogger(__name__)` **محلّية** داخل جسم الدالة نفسها
(في فروع `except`/`try` مختلفة) — رغم وجود `logger` على مستوى الملف
أصلاً (سطر 12). بما أن بايثون يحدّد أن أي اسم مُسنَد إليه **في أي مكان**
داخل دالة يُعامَل كمتغيّر محلي **لكامل الدالة** (بصرف النظر عن ترتيب
التنفيذ الفعلي)، صار `logger` محلياً لكامل `user_start` — فاستخدامه في
سطر 155 (`logger.info(...)`، قبل أي من المواضع الثلاثة) يسقط
بـ`UnboundLocalError` **دائماً** عند أول تسجيل مستخدم جديد. محاكاة حيّة
لإعادة إنتاج السيناريو نفسه (`if not tr:`) كشفت خللاً **ثانياً** من
نفس الفئة، مُقنَّعاً سابقاً لأن الأول كان يُسقِط التنفيذ قبل الوصول
إليه: `success_count` يُسنَد فقط داخل فرع `else` (عندما `ADMIN_IDS`
غير فارغة)، لكنه يُستخدَم لاحقاً بلا شرط (`if success_count ==
len(ADMIN_IDS):`) — فلو كانت `ADMIN_IDS` فارغة، يسقط أيضاً.

**الإصلاح**: حذف المواضع المحلية الثلاثة لـ`import logging`/`logger =
logging.getLogger(__name__)` بالكامل — `logger` مستوى الملف يكفي
ويُستخدَم مباشرة في كل الدالة. `success_count = 0` أصبح يُهيَّأ **قبل**
فرع `if not ADMIN_IDS:` بدل داخل `else` فقط، فيبقى معرَّفاً في الحالتين.

**التحقق**: محاكاة حيّة عبر الدالة الحقيقية مباشرة — مستخدم جديد تماماً
(لا صفّ `Translator`) يضغط `/start` مرتين: مرة بـ`ADMIN_IDS` فارغة
(المسار الذي كان يسقط) ⇒ الرسالة "📝 تم تسجيل طلبك بنجاح" تصل بلا أي
استثناء؛ ومرة بـ`ADMIN_IDS` غير فارغة (بوت وهمي يلتقط الإشعارات) ⇒
"✅ تم إشعار الإدارة بطلبك" تصل بشكل سليم (تأكيد أن الإصلاح لم يكسر
المسار الطبيعي). حذف `__pycache__` بالكامل ثم `pytest -q
--ignore=Archive` (187 نجح) · فحص `register_all_handlers()` حقيقي
(167 معالِجاً — بلا أي تغيير، إصلاح منطق داخلي فقط بلا مساس بالتسجيل).

لا حاجة لسكربت هجرة — إصلاح كود منطقي بحت، بلا أي تغيير على المخطط أو
البيانات.

### 2026-08-17 (٤٦) — 🐛 تعديل "رقم الدورة الحالية" لتقرير كيماوي/موجّه/مناعي يقفز فجأة لصيغة "الخطة العلاجية" القديمة عند وجود خطة نشطة عالقة لنفس المريض

**البلاغ**: المستخدم راجع سجل تعديلات تقرير كيماوي حقيقي (مريض يوسف
عبدالرحمن اسماعيل علي الكحلاني) — التقرير الأصلي والتعديل الأول كانا
بصيغة "رقم الدورة الحالية: 2"، لكن التعديل الأخير (المنشور فعلياً) ظهر
فجأة بصيغة مختلفة كلياً: "📋 الخطة العلاجية: 4 دورة / 📍 الدورة الحالية:
1 من 4" — رغم أن المترجم لم يُدخل أي "عدد كلي" في هذا التقرير قط.

**التشخيص**: `_apply_session_number_edit()` (مكرَّرة بنفس المنطق في
`edit_handlers/before_publish/treatment_and_endoscopy_edit.py` للتعديل
قبل النشر و`user_reports_edit.py` للتعديل بعد النشر) تكتب أولاً نصاً
يدوياً بسيطاً في `treatment_plan_summary` (سليم، مطابق لتصميم "لا
TreatmentPlan إطلاقاً لكيماوي/موجّه/مناعي" الحالي) — لكنها بعدها
**بلا أي شرط** تستدعي `get_active_plan(patient_id, treatment_key)` كـ
"احتياط توافق مع الخطط القديمة"، وإن وجدت أي صف `TreatmentPlan` نشط
لهذا المريض (حتى لو كان عالقاً من نظام سابق ولا علاقة له بهذا التقرير
تحديداً) **تستبدل النص اليدوي الذي كتبه المترجم للتو** بصيغة الخطة
القديمة الكاملة عبر `format_progress_text()`. لهذا المريض تحديداً كان
يوجد صفّ `TreatmentPlan` نشط بإجمالي 4 دورات — فبمجرد تعديل حقل "رقم
الدورة الحالية" قفز العرض لصيغته تلقائياً وبصمت. محاكاة حيّة لإعادة
الإنتاج (مريض بخطة نشطة عالقة + تقرير بصيغة يدوية بحتة) أكّدت الآلية
بالضبط.

**الإصلاح**: في كلا الملفين، فرع التطابق الناجح لـ`_DIALYSIS_SESSION_
PATTERN` (أي أن النص الحالي **مؤكَّد** أنه بالصيغة اليدوية الجديدة —
نفس الصيغة التي تكتبها `_build_manual_session_summary` عند إنشاء أي
تقرير كيماوي/موجّه/مناعي حديث) صار يُنهي الدالة فوراً (`return`) بعد
التعديل المباشر، بلا استشارة `get_active_plan`/`edit_plan` إطلاقاً.
احتياط "التوافق مع الخطط القديمة" يبقى يعمل كما صُمِّم فقط في الحالة
الحقيقية المقصودة منه: نص لا يطابق الصيغة اليدوية إطلاقاً (تقرير قديم
فعلي من قبل إلغاء `TreatmentPlan` لهذه الأنواع الثلاثة).

**التحقق**: محاكاة حيّة عبر الدالتين الحقيقيتين مباشرة — (١) مريض له
خطة `TreatmentPlan` نشطة عالقة (4 دورات) + تقرير بصيغة يدوية "رقم
الدورة الحالية: 2"؛ تعديل قبل النشر إلى "1" ثم تعديل بعد النشر إلى "3"
⇒ في كلتا الحالتين النص يبقى بالصيغة اليدوية البسيطة بلا أي قفزة لـ
"الخطة العلاجية"، والخطة العالقة نفسها لم تُمَسّ (`current_session`
بقيت 1 كما أُنشئت)؛ (٢) مريض آخر بتقرير قديم فعلي (نص "الخطة العلاجية:
6 دورة" بلا أي رقم يدوي مطابق) ⇒ التعديل ما زال يستخدم `get_active_
plan`/`format_progress_text` بنجاح كما هو مصمَّم (توافق عكسي سليم).
حذف `__pycache__` بالكامل ثم `pytest -q --ignore=Archive` (187 نجح) ·
فحص `register_all_handlers()` حقيقي (167 معالِجاً — بلا تغيير، إصلاح
منطق داخلي فقط).

لا حاجة لسكربت هجرة — إصلاح كود منطقي بحت، بلا أي تغيير على المخطط.
التقارير المنشورة سابقاً بهذا الخلل (مثل تقرير يوسف عبدالرحمن) تبقى
كما نُشرت؛ الإصلاح يمنع تكرار المشكلة في أي تعديل مستقبلي فقط.

### 2026-08-18 (٤٧) — 🔍 مراجعة شاملة لكامل المشروع (111,826 سطر) + إصلاح 7 مشاكل مؤكَّدة

**الطلب**: مراجعة كل ملفات الكود وتلخيص المشاكل/التحسينات/الثغرات
المتبقية. نُفِّذت عبر 5 مراجعات متوازية (وكلاء) تغطي كل قسم رئيسي
(`bot/handlers/admin`، `bot/handlers/user` بما فيها نظام التقارير
الكامل، `modules/`، `services/`، `core`+`shared`+`db`)، ثم تحقّق مباشر
(قراءة كود فعلي + محاكاة حيّة) لكل نتيجة حرجة قبل اعتمادها — بلا تخمين.

**النتائج المؤكَّدة والمُصلَحة في هذه الجلسة**:

1. **`bot/handlers/admin/admin_start.py:143`** (`handle_user_approval`) —
   نفس نمط خلل `logger`/`UnboundLocalError` المُصلَح سابقاً في
   `user_start.py`: `logger` محلي مُعرَّف فقط داخل فرع "قبول"، فيسقط فرع
   "رفض" (سطر 168) عند فشل إرسال إشعار الرفض. أُزيل التعريف المحلي —
   `logger` مستوى الملف يكفي.

2. **`bot/handlers/user/user_reports_edit.py:2830`**
   (`show_field_selection`) — نفس النمط: `logger` محلي يُعرَّف **بعد** 6
   مواضع `except` تستخدمه مسبقاً (لأنواع: استشارة+عملية، عملية، خروج من
   المستشفى، علاج طبيعي، أجهزة تعويضية، ترقيد) — أي خطأ تحليل نص قرار
   الطبيب يُسقط شاشة "تعديل التقرير بعد النشر" بالكامل بدل تسجيله
   بصمت كما هو مقصود. أُزيل التعريف المحلي.

   **التحقق (كلا الإصلاحين)**: إثبات حتمي عبر فحص `__code__.co_varnames`
   للدالتين — `logger` لم يعد ضمن المتغيرات المحلية إطلاقاً، فبايثون
   يحلّه دائماً كاسم مستوى الوحدة (قرار وقت الترجمة، لا يعتمد على مسار
   التنفيذ) — يستحيل بنيوياً أن يسقط بـ`UnboundLocalError` بعد الآن.

3. **`services/case_summary_service.py`** — لا يوجد أي `escape_markdown`
   رغم حشر نص حر (شكوى/تشخيص/قرارات/توصيات المترجم) مباشرة في رسالة
   `parse_mode="Markdown"` (شاشة "📋 ملخص الحالة"، `user_case_summary.py`).
   نفس فئة الخلل المُصلَحة سابقاً في 16 ملفاً عبر `escape_md_v1()`. أُضيف
   الهروب داخل `_trim()` — نقطة العبور الوحيدة لكل نص حر في هذا الملف
   (20 موضع استدعاء)، فتغطية كاملة بتعديل سطر واحد.

4. **`services/tomorrow_appointments.py`، `services/schedule_tracker.py`،
   `services/pending_reports_service.py`** — نفس فجوة الهروب من
   Markdown لأسماء مرضى/أقسام/مترجمين حرة تُرسَل للأدمن يومياً؛ أُضيف
   `escape_markdown` عند كل نقطة حشر. في `schedule_tracker.py` استُبدلت
   أيضاً 4 مواضع `print()` بـ`logger.error()` — فشل الإرسال كان مرئياً
   فقط في طرفية التشغيل التفاعلية، لا في سجلّ الخادم الفعلي (pm2).

5. **`modules/healthcare/medical_followup/flow.py:886-917`** — معالِج
   `_handle_hc_callback` (نمط `^hc:`) مسجَّل في نفس المجموعة (group=1)
   التي يسجّل فيها `woundcare/flow.py` نفس النمط **أولاً** — فهذه النسخة
   كانت ميتة دائماً (PTB تتوقف عند أول تطابق)، وأيضاً قديمة (تعرض قائمة
   "المتابعة" الوسيطة بدل بدء التدفق مباشرة كما في نسخة woundcare
   الحيّة الحالية). حُذفت بالكامل (الدالة + تسجيلها + الاستيرادات التي
   باتت ميتة معها: `HC`، `build_followup_menu`) لتفادي مفاجأة صامتة لو
   تغيّر ترتيب التسجيل يوماً.

**بنود رُصِدت لكنها أُرجِئت بقرار المستخدم** (تحتاج قرارات تصميم أوسع):
معالِجات `result_router`/`multiselect` (مستخدَمة في كل الوحدات) لا تُعيد
التحقق من صلاحية الوحدة عند اكتمال الاختيار — فقط
`modules/residency/flow.py::_on_print_categories` يفعل ذلك صراحةً؛
ومطابقة وثائق الوصول بالإقامة بالاسم الحرفي (`modules/residency/
repository.py`) قد تُطابق شخصاً خاطئاً بصمت عند تشابه الأسماء.

**التحقق الشامل**: محاكاة حيّة مباشرة لكل إصلاح (بايت-كود للـscoping،
مقارنة نص فعلي قبل/بعد `escape_markdown` للهروب، `get_pending_reports_
summary_text()` كامل بنص محارف خاصة تجريبي) · حذف `__pycache__` بالكامل
ثم `pytest -q --ignore=Archive` (187 نجح) · فحص `register_all_handlers()`
حقيقي — **166 معالِجاً (بدل 167) بفرق -1 بالضبط**، مطابق تماماً لحذف
التسجيل المكرر الميت ولا شيء غيره.

لا حاجة لسكربت هجرة — كل الإصلاحات كود منطقي بحت بلا أي تغيير على
المخطط.

### 2026-08-18 (٤٨) — 🔨 مقاسات صور الوثائق صغيرة جداً في "🖨️ طباعة ملف الحالة" (بقية الصفحة فاضية)

**البلاغ**: "الطباعه تمام لاكن المقاسات للوثواق صغيره وبقيه الصفحه
فاضي" — صور الوثائق (جواز/تأشيرة/إقامة، عادة مصوَّرة بالهاتف بوضع
طولي/Portrait) تظهر صغيرة جداً وسط مساحة فارغة كبيرة في PDF ملف الحالة.

**السبب**: `services/residency_case_pdf.py::build_case_pdf` — الحد
الأقصى لارتفاع صورة أي وثيقة (`file_max`) كان `9 * cm` فقط، بينما
العرض المسموح كان `content_width * 0.9` (يقارب عرض الصفحة كاملاً). أي
صورة طولية تتقيّد بالارتفاع القصير أولاً (نسبة العرض/الارتفاع تُحافَظ
عليها)، فتظهر ضيقة جداً رغم توفر عرض صفحة كامل غير مُستغَل.

**الإصلاح**: رُفِع `file_max` إلى `(content_width * 0.95, 22 * cm)` —
ارتفاع يقارب المساحة القابلة للطباعة فعلياً في الصفحة، فتستغل الصور
الطولية العرض المتاح بالكامل بدل التوقف عند حد ارتفاع ضيق. عيّنة PDF
بصور بورتريه واقعية أُرسِلَت للمستخدم للمراجعة البصرية قبل الشحن،
وأُبقي التغيير غير موثَّق/غير مرفوع حتى تأكيده الصريح.

**التحقق**: `python -m py_compile` نظيف · `pytest -q --ignore=Archive`
(187 نجح، بلا انحدار — الملف لا اختبارات وحدة مخصَّصة له، التحقق
البصري عبر العيّنة المُرسَلة هو الحاسم هنا).

لا حاجة لسكربت هجرة — تعديل ثابت عرض/ارتفاع بحت، بلا أي تغيير على
المخطط أو منطق التصنيف/الدمج.

### 2026-08-18 (٤٩) — 🏗️ توحيد تهريب Markdown في مكان واحد (`shared/text_safety.py`)

**السياق**: سؤال معماري من المستخدم — كيف نقلّل تكرار نفس المنطق عبر
ملفات متعددة مع تكبّر البوت. أول تطبيق عملي: تهريب Markdown كان
مُنفَّذاً في **مكانين مستقلين** (`utils.py::escape_md_v1` بـ17+ موضع
استدعاء، و`broadcast_service.py::escape_markdown` بـ68 موضع استدعاء)
+ **7 ملفات أخرى** كانت تستورد `telegram.helpers.escape_markdown`
مباشرة كل مرة (بينها 4 ملفات أُضيف لها هذا مؤخراً في الإدخال ٤٧ —
دليل حيّ على أن غياب مكان موحَّد يُنتج نفس مشكلة النسيان مجدداً حتى مع
معرفة الحل).

**التنفيذ**: ملف جديد `shared/text_safety.py` يحتوي التنفيذ الفعلي
الوحيد الآن (`escape_markdown_v1(text) -> str`، يستدعي
`telegram.helpers.escape_markdown(text, version=1)` — التطبيق الرسمي
لقواعد Telegram). الأسماء القديمة أصبحت أغلفة رقيقة تستدعيه:
- `utils.py::escape_md_v1` — نفس التوقيع، بلا تعديل على 17+ موضع
  الاستدعاء القائمة.
- `broadcast_service.py::escape_markdown` — نفس التوقيع **وسلوك
  الحارس الأصلي** (`if not text: return text` يبقى كما هو، للحفاظ على
  سلوك الـ68 موضع القائمة بلا أي تغيير طفيف).
- 5 ملفات أخرى (`case_summary_service.py`, `tomorrow_appointments.py`,
  `schedule_tracker.py`, `pending_reports_service.py`,
  `patient_handlers.py`) نُقلت لاستيراد `escape_markdown_v1` مباشرة من
  المكان الموحَّد بدل `telegram.helpers` مباشرة.
- ملفان (`user_patient_search_inline.py`,
  `user_reports_add_new_system/common_imports.py`) كان فيهما استيراد
  **ميت تماماً** لـ`escape_markdown` (بلا أي موضع استدعاء فعلي) — حُذف
  بدل إعادة توجيهه، تماشياً مع سياسة إزالة الكود الميت المُتَّبعة في
  هذا المشروع.

**قاعدة للمستقبل**: أي كود جديد يبني رسالة Markdown من نص حر يجب أن
يستورد `escape_markdown_v1` من `shared/text_safety.py` مباشرة — لا
تُنشأ نسخة جديدة ولا يُستورد `telegram.helpers.escape_markdown` مباشرة
بعد الآن.

**التحقق**: مقارنة صارمة (بايت مقابل بايت) لمخرَجات الثلاثة أسماء
(`escape_markdown_v1`, `escape_md_v1`, `broadcast_service.escape_
markdown`) على نص فيه كل المحارف الخاصة `_ * `` [` — متطابقة تماماً
بلا استثناء، بما فيها سلوك `None`/النص الفارغ الخاص بكل غلاف · حذف
`__pycache__` بالكامل ثم `pytest -q --ignore=Archive` (187 نجح) · فحص
`register_all_handlers()` حقيقي (166 معالِجاً — بلا تغيير، الملف الجديد
لا يسجّل أي معالِج).

لا حاجة لسكربت هجرة — إعادة تنظيم استيراد بحتة، الدالة الفعلية لم
يتغيّر سلوكها إطلاقاً.

### 2026-08-18 (٥٠) — 🏗️ حذف قاموس `field_names` اليدوي المكرَّر في `handle_field_selection()` — أحد بنود "النمط الجذري المتكرر" أُغلق

**السياق**: تتمة سؤال المستخدم المعماري — بعد توحيد تهريب Markdown
(الإدخال ٤٩)، العودة لبند "قائمة الـ6/7 ملفات" الموثَّق أعلى هذا
السجل. أول بند مُختار: صف "قاموس `field_names`" — كان مرشَّحاً مثالياً
لأن `field_registry.py` (بُني في مهام سابقة #117-125) **يحتوي بالفعل**
كل تسمية عربية لكل حقل لكل نوع تقرير، وحتى الدالة التي تبني أزرار
الحقول أصلاً (`get_editable_fields_by_action_type()`) كانت مسجَّلة
تسحب منه — لكن `handle_field_selection()` (الدالة التي تُستدعى **عند
الضغط على أحد تلك الأزرار بالذات**) كانت تحمل نسخة يدوية منفصلة
بأكثر من 40 سطراً، بعضها **ميت فعلياً** (`doctor_decision`/
`treatment_plan`/`medications` كمفاتيح — مفاتيح `field_registry.py`
الحقيقية أصبحت `decision`/`recommendations`/`tests` منذ توحيد الأعمدة
في المهام السابقة، فهذه الإدخالات لا تُطابَق أبداً)، وبعضها بتسميات
عامة بلا سياق نوع التقرير (`'decision': 'قرار الطبيب'` دائماً، بينما
`field_registry.py` يملك تسمية أدق لكل نوع: "قرار الطبيب وتفاصيل
العملية" لاستشارة العملية، "قرار الطبيب وماذا تم" للطوارئ...).

**الإصلاح**: حذف القاموس بالكامل. `field_display` الآن:
```python
medical_action = context.user_data.get('current_report_data', {}).get('medical_action', '')
field_display = dict(get_editable_fields_by_action_type(medical_action)).get(field_name, field_name)
```
— نفس الدالة بالضبط التي بَنَت الزر المضغوط أصلاً، فتضمن تطابق تسمية
شاشة التعديل مع تسمية الزر دائماً، ولا يمكن أن تتباعدا بصمت بعد الآن.

**التحقق**: محاكاة حيّة عبر الدوال الحقيقية — عرض شاشة اختيار الحقول
لتقرير كيماوي حقيقي (`show_field_selection`) ثم ضغط زر "رقم الدورة
الحالية" (`handle_field_selection`) ⇒ التسمية المعروضة "🔢 رقم الدورة
الحالية" مطابقة تماماً لمصدر الأزرار؛ حقل غير معروف كلياً ⇒ تراجع سليم
للاسم الخام بلا أي استثناء (نفس سلوك `.get(x, x)` القديم)؛ نفس المفهوم
(`case_status`) في نوع تقرير مختلف (طوارئ) ⇒ تسمية سياقية مختلفة "🏥
وضع الحالة" — إثبات مباشر أن التوحيد صحّح غياب السياق لا فقط أزال
التكرار · حذف `__pycache__` بالكامل ثم `pytest -q --ignore=Archive`
(187 نجح) · فحص `register_all_handlers()` حقيقي (166 معالِجاً — بلا
تغيير).

**ما تبقّى من "النمط الجذري المتكرر"** (أعلى هذا السجل) لجولة لاحقة:
`before_publish/router.py`، `handle_report_selection()`: قاموس
`current_report_data`، `execute_smart_state_action.py`،
`broadcast_service.py::format_report_message()`، وقوائم
`valid_flow_types` (3 مواضع) + `get_translator_state()`/
`get_confirm_state()` في `flows/shared.py` — كل بند يحتاج فحصاً منفصلاً
لتحديد إن كان قابلاً للتوحيد عبر `field_registry.py` بنفس الأمان، أو
يحتاج تصميماً مختلفاً.

لا حاجة لسكربت هجرة — إصلاح كود منطقي بحت، بلا أي تغيير على المخطط.

### 2026-08-18 (٥١) — 🏗️ توحيد قوائم `valid_flow_types` الثلاث المكرَّرة في `flows/shared.py`

**السياق**: تتمة مباشرة للإدخال ٥٠ — البند التالي من "النمط الجذري
المتكرر". قائمة `valid_flow_types` (23 قيمة بالضبط) كانت مكرَّرة
**حرفياً بشكل منفصل في 3 دوال مختلفة**: `handle_final_confirm`،
`save_report_to_database`، `handle_translator_page_navigation` — كل
واحدة مصحوبة بنسخة منفصلة من قاموس `more_specific_flows`
(`{"followup": ["periodic_followup", "inpatient_followup"]}`). تحقّقت
برمجياً: محتوى القوائم الثلاث **متطابق تماماً** (23/23)، لكن منطق
الـfallback حولها **اختلف فعلياً**: نسختا `save_report_to_database`
و`handle_translator_page_navigation` تتراجعان لـ`new_consult` صراحة
إن كان كل من flow_type وcurrent_flow غير صالحين، بينما نسخة
`handle_final_confirm` **كانت تفتقد هذا الـfallback الأخير** — دليل
حي على أن التكرار اليدوي بدأ يتباعد فعلاً ولو بصمت (لا أثر عملي هنا
لأن `get_confirm_state`/`get_translator_state` تفشلان بأمان لأي
flow_type غير معروف عبر `.get(x, NEW_CONSULT_*)`، لكنه فرق سلوك حقيقي
كان يمكن أن يصبح خطراً لو تغيّرت تلك الدوال مستقبلاً).

**الإصلاح**: `field_registry.py` يحتوي الآن:
- `VALID_FLOW_TYPES` — مُشتقّة آلياً من `REPORT_FIELD_REGISTRY.keys()`
  (مطروحاً منها المفتاحان المرادفان `rehab_physical_short`/
  `app_reschedule`، ومضافاً إليها `"followup"` كقيمة قديمة عامة موثَّقة
  صراحة) — تحقّقتُ أنها تطابق الاتحاد الأصلي للقوائم الثلاث حرفاً بحرف.
- `resolve_flow_type_for_confirmation(flow_type, current_flow)` — يحل
  محل كل نسخ منطق "الأكثر تحديداً له الأولوية، وإلا تحقق الصلاحية، وإلا
  new_consult" الثلاث، بالسلوك **الأكثر أماناً** من النسخ الثلاث (يشمل
  fallback الـnew_consult الذي كان مفقوداً في `handle_final_confirm`).

الدوال الثلاث في `flows/shared.py` تستدعي هذه الدالة الواحدة الآن بدل
القوائم المحلية.

**التحقق**: مقارنة برمجية صارمة أن `VALID_FLOW_TYPES` يطابق اتحاد
القوائم الثلاث الأصلية (23/23) · اختبار `resolve_flow_type_for_
confirmation` مباشرة لكل السيناريوهات (نوع صالح، نوع عام مع بديل أكثر
تحديداً، نوع غير معروف مع/بلا current_flow صالح) · محاكاة حيّة كاملة
عبر `handle_translator_page_navigation` الفعلية (3 سيناريوهات: بديل
أكثر تحديداً، نوع صالح مباشرة، نوع غير معروف كلياً) — الثلاثة نجحت بلا
استثناء · حذف `__pycache__` بالكامل ثم `pytest -q --ignore=Archive`
(187 نجح) · فحص `register_all_handlers()` حقيقي (166 معالِجاً — بلا
تغيير).

**ما تبقّى** من "النمط الجذري المتكرر": `before_publish/router.py`،
`handle_report_selection()`: قاموس `current_report_data` (⚠️ هذا البند
تحديداً متشابك مع كود تحليل `doctor_decision` المركّب القديم — نفس
الكود الذي تستهدفه المهمة المعلَّقة #124 "حذف كود التحليل المركّب
الميت بعد التأكد من الترحيل"، فيحتاج فحصاً حذراً لمخاطر بيانات حقيقية
لا مجرد إعادة هيكلة، وليس بنفس أمان هذا الإصلاح)، `execute_smart_
state_action.py`، `broadcast_service.py::format_report_message()`،
و`get_translator_state()`/`get_confirm_state()` (قاموسا حالة منفصلان
لم يُلمسا).

لا حاجة لسكربت هجرة — إصلاح كود منطقي بحت، بلا أي تغيير على المخطط.

### 2026-08-18 (٥٢) — 🐛 `BadRequest: Message is not modified` يُسجَّل كخطأ زائف في تقرير الأخطاء اليومي

**البلاغ**: تقرير أخطاء اليوم (`error-report-2026-08-17.txt`) — خطأ
واحد: `BadRequest @ action_type_handlers.py:344`، الدالة
`handle_action_category`: "Message is not modified: specified new
message content and reply markup are exactly the same as a current
content and reply markup of the message".

**التشخيص**: هذا سلوك **طبيعي متوقَّع من تيليجرام نفسه**، ليس خطأ
حقيقي — يحدث عند ضغط نفس زر تصنيف نوع الإجراء مرّتين متتاليتين (أو أي
سيناريو تكون فيه الشاشة الناتجة مطابقة تماماً لما هو معروض بالفعل).
نفس المبدأ مُطبَّق فعلاً وبشكل صحيح في `admin_pending_reports.py::
_handle_render_error` (يعرض تنبيهاً خفيفاً بدل التسجيل كخطأ) — لكن
`handle_action_category` كانت تستخدم `except Exception as e:
logger.error(...)` عامة تلتقط هذه الحالة الحميدة مع الأخطاء الحقيقية
معاً، فتظهر في تقرير الأخطاء اليومي كضجيج رغم عدم وجود أي أثر فعلي على
المستخدم (الشاشة المعروضة صحيحة أصلاً، لا حاجة لإعادة رسمها).

**الإصلاح**: تمييز رسالة "message is not modified" (case-insensitive)
داخل نفس معالِج `except` — تُسجَّل الآن بمستوى `debug` بدل `error`، بينما
أي استثناء آخر حقيقي يبقى يُسجَّل كـ`error` مع `exc_info=True` كالمعتاد
(لا تراجع في رصد الأخطاء الحقيقية). لم يُستخدَم `safe_edit_message()`
الجاهز في `bot/safe_callback.py` رغم حله لنفس المشكلة، لأن نمط تسجيله
للأخطاء الأخرى (`print()` بلا `exc_info`) أضعف من `logger.error(...,
exc_info=True)` الحالي — استبداله كان سيقلّل من رصد أخطاء حقيقية أخرى
مستقبلية، فأُبقي المنطق محلياً بنفس مبدأ `admin_pending_reports.py`.

**التحقق**: محاكاة حيّة عبر الدالة الحقيقية بـ3 سيناريوهات — (١) نفس
رسالة الخطأ المُبلَّغة بالضبط ⇒ لا شيء يُسجَّل بمستوى ERROR، فقط DEBUG
صامت؛ (٢) `BadRequest` حقيقي مختلف ("Chat not found") ⇒ يبقى يُسجَّل
كـERROR كالمعتاد (تأكيد عدم انكسار رصد الأخطاء الحقيقية)؛ (٣) نجاح
التعديل الطبيعي بلا استثناء ⇒ لا أي تسجيل إطلاقاً · حذف `__pycache__`
بالكامل ثم `pytest -q --ignore=Archive` (187 نجح) · فحص
`register_all_handlers()` حقيقي (166 معالِجاً — بلا تغيير).

لا حاجة لسكربت هجرة — إصلاح كود منطقي بحت، بلا أي تغيير على المخطط.

### 2026-08-18 (٥٣) — 🏗️ توسيع مراقبة الأخطاء: أزرار بلا معالِج مختص تصل الآن لتقرير الأخطاء اليومي

**الطلب**: توسيع مراقبة الأخطاء لتشمل أزراراً "مكسورة" (لا تفعل شيئاً
عند الضغط بلا أي استثناء بايثون ظاهر) عبر مراحل التقرير الثلاث
(إدخال/تعديل قبل النشر/تعديل بعد النشر).

**التشخيص**: `universal_fallback.py::handle_any_callback` (المعالِج
الاحتياطي الأخير، group 999) يكتشف فعلاً أي زر لا يطابقه أي نمط في
`KNOWN_CALLBACKS` — لكنه كان يسجّله بمستوى `WARNING` فقط، و
`services/error_digest.py` (مولّد التقرير اليومي) يلتقط `ERROR` فأعلى
حصراً — فالكشف كان يحدث فعلياً لكن نتيجته تضيع بصمت قبل الوصول لأي
تقرير يراه أحد.

**التدقيق قبل التفعيل** (لتفادي إغراق التقرير اليومي بإيجابيات كاذبة
فور الرفع لـERROR): بُنيت أداة مقارنة برمجية بين كل قيم `callback_data`
الحرفية المبنية فعلياً في المشروع (840 قيمة) وكل نمط CallbackQueryHandler
مسجَّل فعلياً عبر `register_all_handlers()` حقيقي + قائمة
`KNOWN_CALLBACKS` — **صفر فجوة**. تدقيق أعمق لعيّنة من الأزرار التي بدت
"غير مغطّاة" بأنماط حقيقية فقط (32 حالة) كشف أنها جميعاً إيجابيات كاذبة
من نمط بايثون تحديداً: `CallbackQueryHandler(handler)` **بلا** `pattern`
مسجَّل داخل حالة (state) محدَّدة من `ConversationHandler` — توجيه حقيقي
وصحيح يعتمد على حالة المحادثة لا على مطابقة نصية، فيبدو "بلا نمط" لأداة
تفحص الأنماط فقط. مُستخدَم بكثافة ومقصود (مثال: `handle_confirm_edit`
في `user_reports_edit.py` يوجّه داخلياً بـ`if query.data == "..."`).

**اكتشاف حقيقي أثناء التدقيق** (موثَّق أدناه كتدقيق نظيف — ليس بندَ
إصلاح): ملفا `user_inline_menu.py` و`user_shortcuts.py` (نظام قوائم
Inline + اختصارات بديل) **معطَّلان عمداً بالكامل** —
`bot/handlers_registry.py:143-147` يحمل تعليقاً صريحاً "المستخدم يفضل
الأزرار الثابتة" وكلا استدعائي `register()` معلَّقان. تحقّقتُ أن كلا
الملفين **بلا أي مستدعٍ حي آخر** (لا نقطة دخول بديلة)، فلا مستخدم حقيقي
يصل لأزرارهما إطلاقاً — كود ميت مقصود، لا خلل حي.

**الإصلاح**: سطر واحد — `logger.warning` ← `logger.error` في
`handle_any_callback` عند اكتشاف زر بلا معالِج مختص. تجربة المستخدم لم
تتغيّر (لا يزال يستلم نفس تنبيه "⚠️ هذا الزر غير متاح حالياً")؛ الفرق
الوحيد أن الحدث يصل الآن لتقرير الأخطاء اليومي.

**التحقق**: محاكاة حيّة مباشرة — زر غير معروف كلياً ⇒ يظهر في ملف
`errors-YYYY-MM-DD.jsonl` بمستوى ERROR، والمستخدم يستلم تنبيهه المعتاد
بلا تغيير؛ زر معروف (`nav:back`) ⇒ لا يُسجَّل إطلاقاً (بلا ضجيج) · حذف
`__pycache__` بالكامل ثم `pytest -q --ignore=Archive` (187 نجح) · فحص
`register_all_handlers()` حقيقي (166 معالِجاً — بلا تغيير).

**محاولة مرفوضة**: بُني أولاً اختبار pytest دائم (`test_callback_
coverage.py`) لمقارنة كل زر بكل نمط تلقائياً عند كل تشغيل — أُلغِي
ولم يُشحَن لأن نفس مشكلة "بلا pattern داخل state" أعلاه تجعله يُنتِج
إيجابيات كاذبة كثيرة (467 من أصل 840 في أول تشغيل) تحتاج نمذجة حقيقية
لحالات ConversationHandler ليكون دقيقاً — خارج نطاق معقول لهذه الجلسة.
المراقبة الحيّة (هذا الإصلاح) كافية وأكثر أماناً: تعتمد على السلوك
الفعلي وقت التشغيل لا تحليلاً ثابتاً للكود.

**تنبيه للمتابعة**: راقب تقارير الأخطاء القادمة أياماً قليلة — إن ظهر
أي "Unhandled callback" متكرر لزر لا تعرفه، أبلغني به مباشرة للتحقيق؛
هذا بالضبط الهدف من هذا التوسيع.

لا حاجة لسكربت هجرة — إصلاح كود منطقي بحت، بلا أي تغيير على المخطط.

### 2026-08-18 (٥٤) — 🐛 عطلان من تقرير أخطاء اليوم: إنذار كاذب متكرر (28×) + شاشة "رقم الغرفة" تسقط دائماً

تقرير `error-report-2026-08-18.txt` — أول تقرير بعد تفعيل مراقبة الأزرار
(إدخال ٥٣)، وأثبت قيمته فوراً: كشف عطلاً حقيقياً كان مخفياً، وكشف قصوراً
في تحققي أنا نفسه.

**١) إنذار كاذب: `Unhandled callback received: medrep_count:1` (28 مرة)**

`medrep_count:` (أزرار عدد الفحوصات المنتظرة بعد "🟡 لم يجهز بعد") **له
معالِج حقيقي مسجَّل فعلاً** (`conversation_handler.py:830`، حالة
`MEDICAL_REPORT_PENDING_COUNT`) — الزر يعمل بشكل سليم تماماً. لكنه كان
غائباً عن قائمة `KNOWN_CALLBACKS` اليدوية في `universal_fallback.py`
(شقيقاه `medrep:` و`medrep_done:` موجودان فيها!). وبما أن PTB يُمرّر كل
تحديث لكل مجموعة معالِجات بالتتابع، فمعالِج الفلبَك (group 999) يستقبل
الزر أيضاً حتى بعد نجاح معالجته، ويعتبره "غير معالَج" لغياب النمط عن
القائمة.

**قصور في تحققي (إدخال ٥٣)**: دقّقتُ حينها كل قيم `callback_data` مقابل
**اتحاد** (الأنماط المسجَّلة فعلياً + `KNOWN_CALLBACKS`) فبدت التغطية
كاملة بلا فجوة. لكن إسكات الفلبَك يعتمد على `KNOWN_CALLBACKS` **وحدها**
لا على وجود معالِج حقيقي — وهو الفحص الصحيح الذي لم أُجرِه. إعادة
التدقيق بالمعيار الصحيح كشفت فجوات أخرى مماثلة كانت ستظهر تباعاً
(`action_cat:`، `chemo_uniform:`، `dlx_cal_*` وغيرها).

**السبب الجذري**: `KNOWN_CALLBACKS` كانت **نسخة يدوية مكرَّرة** من
تسجيلات المعالِجات الحقيقية — نفس نمط "المنطق نفسه في مكانين يتباعدان"
الذي عالجناه هذه الجلسة في تهريب Markdown (٤٩) وقوائم `valid_flow_types`
(٥١). أي نمط جديد يُضاف للبوت ويُنسى هنا = إنذار كاذب دائم لكل ضغطة.

**الإصلاح**: `_snapshot_registered_patterns(app)` جديدة تلتقط آلياً أنماط
كل `CallbackQueryHandler` مسجَّل فعلياً (بما فيها المتداخلة داخل
`ConversationHandler`) لحظة تسجيل الفلبَك — وهو **آخر ما يُسجَّل** في
`handlers_registry.py`، فكل المعالِجات موجودة وقتها. `is_known_callback`
تستشير الأنماط المُشتقّة آلياً أولاً ثم القائمة اليدوية (تبقى تكملةً
للمعالِجات المقيَّدة بحالة محادثة بلا `pattern`، وهي غير قابلة للاشتقاق).
النتيجة: **يستحيل تكرار هذا الانحراف** — أي معالِج جديد له نمط يُسكَت
تلقائياً بلا صيانة يدوية.

**٢) عطل حقيقي: `BadRequest: Inline keyboard expected`
(`execute_smart_state_action.py`)**

عند ضغط "🔙 رجوع" للعودة لخطوة "رقم الغرفة والطابق" في مسار **متابعة في
الرقود**، كان الكود يمرّر `ReplyKeyboardMarkup([["تخطي"]])` إلى
`edit_message_text` — وتيليجرام لا يقبل في تعديل الرسائل إلا لوحة
**مضمَّنة** (`InlineKeyboardMarkup`). فكان يسقط **دائماً** (لا أحياناً)
ويرى المستخدم "❌ حدث خطأ في إعادة العرض" بدل الشاشة. لوحة "تخطي" نفسها
سليمة في المسار الأمامي (`flows/followup.py`) لأنها تُرسَل عبر
`reply_text` لا `edit_message_text`. استُبدلت بأزرار التنقل المضمَّنة
المستخدَمة في **كل** الفروع الشقيقة داخل نفس الدالة — ونص الرسالة أصلاً
لا يذكر "تخطي"، فلا يفقد المستخدم أي خيار.

**التحقق**: محاكاة حيّة تعيد إنتاج العطلين ثم تثبت زوالهما — (أ)
`medrep_count:1` صار مُسكَتاً تلقائياً، وكذلك بقية الفجوات المكتشَفة، مع
تأكيد **عدم الانحدار** (كل الأزرار المُسكَتة سابقاً باقية) و**بقاء قدرة
الكشف** (زر وهمي غير مسجَّل ما زال يُكشَف — الغرض الأساسي محفوظ)؛ (ب)
شاشة "رقم الغرفة" تُعرَض بلوحة مضمَّنة بلا استثناء، مع إثبات أن المحاكاة
نفسها **كانت سترفض** اللوحة غير المضمَّنة تماماً كتيليجرام (اختبار صادق
لا يمرّ بالمصادفة) · حذف `__pycache__` بالكامل ثم `pytest -q
--ignore=Archive` (187 نجح) · فحص `register_all_handlers()` حقيقي (166
معالِجاً — بلا تغيير، مع تأكيد التقاط 267 نمطاً وقت الإقلاع).

**أخطاء أُهملت عمداً**: `Bad Gateway`/`NetworkError` (2× كل منهما، 03:11)
— انقطاع شبكة عابر من تيليجرام نفسه، لا علاقة له بالكود.

لا حاجة لسكربت هجرة — إصلاحا كود منطقي بحت، بلا أي تغيير على المخطط.

### 2026-08-18 (٥٥) — 🐛 قائمة حقول تعديل المسودة تظهر **فارغة تماماً** بسبب دورة استيراد تفشل بصمت عند الإقلاع

**البلاغ**: سطر تحذير في سجل الإقلاع (pm2) لفت انتباه المستخدم:
`⚠️ Cannot import get_editable_fields_by_action_type from user_reports_edit`
— كان يظهر عند **كل** إقلاع (وفي كل تشغيل اختبار طوال هذه الجلسة).

**الأثر الحقيقي على المستخدم** (ليس ضجيجاً كما قد يبدو): في
`edit_handlers/draft/handlers.py` كان الاستيراد على مستوى الملف داخل
`try/except ImportError` يسقط لبديل `lambda x: []`. ولأن الربط على مستوى
الوحدة يحدث **مرة واحدة**، بقي البديل الفارغ مربوطاً نهائياً طوال عمر
العملية. النتيجة في `handle_draft_field_input`: بعد أن يعدّل المترجم أي
حقل في المسودة ويُدخل القيمة الجديدة، تُحفَظ القيمة وتظهر رسالة
"✅ تم تحديث ... بنجاح!\n\n📝 **اختر حقلاً آخر للتعديل**" ثم تُعرَض قائمة
**بلا أي زر حقل إطلاقاً** (فقط "انتهيت/رجوع/إلغاء") — فيستحيل عليه تعديل
حقل ثانٍ في نفس الجولة.

**السبب الجذري (دورة استيراد حقيقية)**: `user_reports_edit` يستورد
`...add_new_system.utils`، وهذا يستلزم تنفيذ `__init__.py` لحزمة
`add_new_system`، وهي (عبر `conversation_handler.py`) تستورد
`edit_handlers/draft/handlers.py` نفسه — فتفشل الحلقة.

**دقّة مهمة**: الفشل **يعتمد على ترتيب الاستيراد**. باستيراد الملف
منفرداً ينجح تماماً (وهذا ما يخفي العطل في أي فحص سطحي)؛ لكن بترتيب
الاستيراد **الإنتاجي الفعلي** عبر `register_all_handlers()` يفشل دائماً.
أُثبِت هذا تجريبياً بتشغيل التسجيل الحقيقي ثم فحص الاسم المربوط فعلاً في
الوحدة (`__name__ == "<lambda>"` ⇒ True، ونتيجة الاستدعاء `[]`).

**الإصلاح**: تحويل الاستيراد إلى **محلي داخل الدالة** (يقع وقت الاستدعاء
لا وقت التحميل، فتُكسَر الدورة) — نفس الحل الموثَّق والمُطبَّق فعلاً في
الطرف المقابل: `user_reports_edit.py::get_editable_fields_by_action_type`
تستورد `field_registry` محلياً للسبب ذاته بالضبط. كما رُفِع مستوى تسجيل
أي فشل مستقبلي من `warning` إلى `error` مع `exc_info=True` حتى يصل تقرير
الأخطاء اليومي بدل الضياع بصمت.

**التحقق**: (١) تحذير الإقلاع اختفى تماماً من سجلّ `register_all_
handlers()` الحقيقي؛ (٢) الدالة تُرجع الآن 8 حقول حقيقية لـ"استشارة
جديدة" **مطابقة تماماً** لمصدر الحقيقة (`field_registry`)؛ (٣) محاكاة
حيّة للشاشة المكسورة نفسها (`handle_draft_field_input` الفعلية) ⇒ القائمة
تعرض **3 أزرار حقول قابلة للتعديل** بعد أن كانت **صفراً** · حذف
`__pycache__` بالكامل ثم `pytest -q --ignore=Archive` (187 نجح) · فحص
`register_all_handlers()` حقيقي (166 معالِجاً — بلا تغيير).

**درس عام**: `try/except ImportError` على مستوى الوحدة مع بديل صامت
يُحوّل دورة استيراد إلى **عطل وظيفي دائم بلا أي أثر ظاهر** سوى سطر تحذير
واحد وقت الإقلاع. عند رؤية أي تحذير استيراد في سجل الإقلاع: افحص ما إذا
كان البديل مُستخدَماً فعلياً في مسار حي — لا تفترض أنه ضجيج.

لا حاجة لسكربت هجرة — إصلاح كود منطقي بحت، بلا أي تغيير على المخطط.

### 2026-08-19 (٥٦) — 🐛 مُرشِّح ضجيج تقرير الأخطاء لا يحجب أخطاء الشبكة رغم إدراجها في قائمة التجاهل

**نتيجة إيجابية أولاً**: تقرير `error-report-2026-08-19.txt` **خالٍ تماماً
من أخطاء التطبيق** — كل ما أُصلح في الإدخالات ٥٢/٥٤/٥٥ اختفى فعلياً من
الإنتاج: `medrep_count:` (كانت 28×)، `Inline keyboard expected`، وتحذير
`Cannot import get_editable_fields_by_action_type`.

**البلاغ**: البقية (14 خطأ) كلها انقطاعات شبكة عابرة من تيليجرام
(`httpx.ReadError` / `NetworkError`) — **رغم أن `_IGNORE_SUBSTRINGS` في
`services/error_digest.py` تحتوي `"httpx"` و`"NetworkError"` صراحةً**.
أي أن مُرشِّح الضجيج نفسه لم يكن يعمل، ويُغرق التقرير بما يُفترَض حجبه.

**التشخيص (سببان مستقلان، أُثبِتا تجريبياً بمحاكاة أمينة لطريقة تسجيل
PTB)**:
1. **`record.msg` قالب خام لا نص نهائي**: PTB يسجّل
   `_LOGGER.exception("Error while getting Updates: %s", exc)` — فيبقى
   `record.msg == "Error while getting Updates: %s"` والقيمة الفعلية
   (`"httpx.ReadError"`) في `record.args`. والمُرشِّح كان يفحص
   `f"{record.name} {record.msg}"` أي القالب الخام، فلا يرى كلمة `httpx`
   إطلاقاً.
2. **بعض السجلات بلا أي كلمة مفتاحية في نصها**: الرسالة الثانية
   (`"Exception happened while polling for updates."`) لا تحوي أي مصطلح
   من قائمة التجاهل؛ الدليل الوحيد على كونها ضجيج شبكة هو **نوع
   الاستثناء** (`NetworkError`) في `exc_info` — ولم يكن المُرشِّح يفحصه
   إطلاقاً.

**الإصلاح**: يُبنى نص المطابقة الآن من ثلاثة مصادر معاً — اسم المُسجِّل +
`record.getMessage()` (**بعد دمج المعاملات**، داخل `try/except` لأن
الدمج قد يفشل عند عدم تطابق المعاملات) + **اسم نوع الاستثناء** من
`exc_info`. (`_redact` في نفس الملف كانت تستخدم `getMessage()` بشكل صحيح
أصلاً — التناقض كان في `emit` وحدها.)

**التحقق**: إعادة إنتاج الحالتين بالضبط كما تُسجَّلان في PTB ⇒ كلتاهما
مُحجَبة الآن؛ **وضمان عدم الإفراط في الحجب**: خطأ تطبيقي حقيقي
(`BadRequest` باستثناء كامل) ما زال يُسجَّل بنوعه الصحيح، وخطأ نصّي بلا
`exc_info` إطلاقاً (مثل سجلّ مراقبة الأزرار في الإدخال ٥٣) ما زال يصل
التقرير · حذف `__pycache__` بالكامل ثم `pytest -q --ignore=Archive`
(187 نجح) · فحص `register_all_handlers()` حقيقي (166 معالِجاً — بلا
تغيير).

**الأثر المتوقَّع**: تقارير الأيام القادمة يجب أن تكون **فارغة تماماً**
ما لم يقع خطأ تطبيقي حقيقي — وهذا هو الغرض: أي سطر يظهر فيها من الآن
فصاعداً يستحق التحقيق فوراً، بلا ضجيج يُخفيه.

لا حاجة لسكربت هجرة — إصلاح كود منطقي بحت، بلا أي تغيير على المخطط.

### 2026-08-21 (٥٧) — 🔨 "🧳 أرشيف المسافرين": إخفاء أسماء من سافروا عن قوائم المستخدمين بلا حذف أي بيانات

**الطلب**: قائمة أسماء المرضى تكبر باستمرار، وفيها من سافر ومن هو مقيم.
الأسماء الكثيرة غير الضرورية تُرهق المستخدمين، **والحذف غير ممكن** لأن
بياناتهم مطلوبة في التقارير والإحصائيات. المطلوب: زر أرشيف في الأدمن
يعرض الأسماء، والضغط على اسم يعني "سافر" فلا يعود يظهر للمستخدمين.

**قرار التصميم — أرشفة ناعمة لا حذف**: عمود جديد `Patient.archived_at`
(DateTime، NULL = نشط) بدل قيمة جديدة في `patient_type` — لأن الأخير
كان سيمحو نوع المريض الأصلي (`pharmacy_only`/`chennai`/`companion`)
ويُفقِد تصنيفه عند عودته. واختير عمود **تاريخ** لا Boolean ليحمل معلومة
"متى سافر" (مفيدة للإحصائيات) والصدق مشتقّ منه مباشرة.

**نطاق الإخفاء** (قرار صريح مُبلَّغ للمستخدم): يشمل **كل قوائم اختيار
المرضى** — التقارير، الرعاية الصحية، الصيدلية، الخدمات العامة، والإقامة
— لأن من سافر لا يُفترض أن يستقبل أي خدمة جديدة. **لا يشمل إطلاقاً** أي
قراءة تاريخية: `Report`، `get_patient_by_id/by_name`، والتقارير
والإحصائيات كلها تعمل كما هي بلا أي تغيير.

**لماذا كان التنفيذ منخفض المخاطر**: تدقيق المعمارية كشف أن **كل** مواضع
سرد/بحث المرضى (3 مواضع في تدفق التقارير + البحث inline + المنتقي
المشترك) تمرّ بالفعل عبر **دالتَي حقيقة وحيدتين**:
`report_flow_patient_visible` و`_type_visible` في
`shared/selectors/patient_selector/_data.py`. فأُضيف الفلتر داخلهما
(بأعلى أولوية، قبل فلتر المدينة) ووصل لكل القوائم تلقائياً. تحقّقتُ
أيضاً أن **تدفق المغادرة لا يتأثر** لأنه يقرأ جدول الوصول
(`get_active_arrivals`) لا سجلّ المرضى.

**الشاشات المُضافة** (`admin_schedule_management.py`، بادئة `parch:`):
- زر جديد "🧳 أرشيف المسافرين (العدد)" داخل "📝 إدارة أسماء المرضى"،
  وترويسة الشاشة صارت تفصل "الأسماء النشطة" عن "المسافرون".
- "✈️ تحديد مريض مسافر" — قائمة النشطين مرقَّمة الصفحات، الضغط على اسم
  يؤرشفه فوراً ثم **يُعيد عرض نفس الصفحة** (تحديد عدة مسافرين تباعاً بلا
  رجوع يدوي).
- "📦 عرض المسافرين" — المؤرشفون مع تاريخ سفر كل منهم، والضغط يُعيده
  للقوائم فوراً (**العملية عكوسة بالكامل**).

**ملاحظتان تقنيتان أثناء التنفيذ**: `CallbackQuery` غير قابل للتعديل في
PTB v20 (فلا يصحّ تمرير الصفحة بإعادة كتابة `query.data`)، وتيليجرام
يسمح بالرد على الاستعلام **مرة واحدة فقط** — لذلك فُصِل الرسم في دالتَي
`_render_archive_pick`/`_render_archive_list` نقيّتين لا تستدعيان
`answer()`، تستدعيهما المعالِجات ودالة التبديل معاً.

**التحقق**: محاكاة حيّة عبر الدوال والشاشات الحقيقية — (١) قبل/بعد
الأرشفة: الاسم يختفي من **كل** القوائم الخمس ومن تدفق التقارير والبحث
inline، بينما بقية المرضى (عادي/صيدلية/مرافق) ظاهرون بلا أي تغيير؛
(٢) **البيانات سليمة**: الصف موجود، `get_patient_by_id/by_name` يعملان،
وتقريره التاريخي كما هو؛ (٣) الإرجاع يُعيده لكل القوائم فوراً؛
(٤) **الهجرة على قاعدة قائمة**: أُنشئت قاعدة "قديمة" بلا العمود ثم
شُغِّل `check_db_health_startup()` الحقيقي ⇒ أُضيف العمود وبقي كل
المرضى نشطين (NULL)؛ (٥) شاشات الأدمن الأربع من طرف لطرف (عرض/أرشفة/
إعادة عرض/إرجاع/العدّادات) · حذف `__pycache__` بالكامل ثم `pytest -q
--ignore=Archive` (187 نجح) · فحص `register_all_handlers()` حقيقي:
**170 معالِجاً (بدل 166) بفرق +4 بالضبط** = المعالِجات الأربعة الجديدة،
و271 نمطاً (بدل 267) التُقِطت **تلقائياً** في إسكات الفلبَك (تأكيد عملي
لفائدة الاشتقاق الآلي المُضاف في الإدخال ٥٤ — بلا أي صيانة يدوية).

لا حاجة لسكربت هجرة منفصل — العمود يُضاف تلقائياً عند أول إقلاع عبر
`_migrate_column`، وكل الصفوف القائمة تبقى نشطة (NULL) فلا يتغيّر أي
سلوك قائم حتى يحدّد الأدمن مسافراً بنفسه.

### 2026-08-21 (٥٨) — 🔒 تنقية توكن البوت من السجلات **لم تكن تعمل إطلاقاً** + اختصار ضجيج أخطاء الشبكة في pm2

**المصدر**: المستخدم أرسل سطر خطأ شبكة من سجل pm2
(`httpx.ReadError` أثناء long-polling) — حدث عابر غير ضار (PTB يعيد
المحاولة تلقائياً ويستأنف الاستقبال بلا تدخّل)، لكنه يطبع **أثراً كاملاً
بنحو 60 سطراً** في كل مرة فيُغرق السجل. أثناء فحص مسار التسجيل ظهر عطب
أمني أهمّ بكثير.

**١) 🔒 عطب أمني: `_RedactSecretsFilter` في `app.py` لا يطابق شيئاً أبداً**

الفلتر مكتوب خصيصاً لإخفاء توكن البوت من روابط
`https://api.telegram.org/bot<TOKEN>/...` التي تظهر في سجلات مكتبة
تيليجرام (وتعليقه في الكود يقول ذلك صراحةً: "these are the lines that
leak the token"). لكن نمطه كان بشرطات مائلة **مضاعفة داخل سلسلة خام**:
`r"(https?://api\\.telegram\\.org/bot)(\\d+:)[^/\\s]+"` — وهو ما يعني
للمحرِّك "شرطة مائلة **حقيقية** ثم أي محرف"، ولا يظهر ذلك في أي رابط
فعلي. ونص الاستبدال `r"\\1\\2***REDACTED***"` به نفس العطب (كان سيكتب
`\1\2` حرفياً بدل مجموعتَي المطابقة).

**النتيجة**: التنقية كانت **صامتة وبلا أي أثر** — أي سطر سجلّ يحوي رابط
API كاملاً كان يطبع التوكن بنصّه الصريح في pm2 (ومنه إلى لقطات الشاشة
والنسخ واللصق).

**الإثبات** (لا إعادة كتابة يدوية للنمط): استُخرج النمط الفعلي من
`app.py` عبر `ast` واختُبر على رابط حقيقي ⇒ `search()` تُعيد `None` قبل
الإصلاح، ومطابقة صحيحة بعده.

**تصحيح لتشخيص أوّلي خاطئ**: ظننتُ ابتداءً أن الفلتر مُركَّب على
**المُسجِّل** الجذر فقط (وهو موضع لا يرى السجلات المنتشرة من مسجّل ابن —
تحقّقتُ من هذه القاعدة تجريبياً وهي صحيحة). لكن قراءة الكود كاملاً بيّنت
أنه يُضاف على **معالِجات** الجذر أيضاً (`for h in root.handlers:
h.addFilter(flt)`) — أي أن موضع التركيب سليم أصلاً، والعطب الحقيقي في
النمط وحده.

**٢) 🔇 اختصار ضجيج أخطاء الشبكة العابرة في سجل pm2**

فلتر جديد `_CollapseTransientNetworkErrors` يختصر أخطاء
`httpx.ReadError/ConnectError/ReadTimeout/...` و`Bad Gateway` الصادرة من
مسجّلات `telegram*` إلى **سطر واحد موجز** بمستوى `WARNING` بلا أثر:
"⚠️ انقطاع شبكة عابر مع تيليجرام (…) — إعادة المحاولة تلقائياً، لا إجراء
مطلوب."

**لا يُخفي الحدث** (يبقى سطر واضح)، ويحذف فقط الـtraceback عديم الفائدة
التشخيصية هنا. مستقل تماماً عن حجب التقرير اليومي (إدخال ٥٦) الذي يخصّ
`error_digest.py` — هذا للسجل الحيّ وحده.

**التحقق** (باستخراج الصنفين من `app.py` نفسه عبر `ast`، لا نسخ يدوي):
(١) إعادة إنتاج سطر pm2 المُبلَّغ حرفياً ⇒ سطر واحد بلا `Traceback`
بمستوى WARNING؛ (٢) **لا إفراط في الاختصار**: خطأ تطبيقي حقيقي
(`ValueError`) يبقى كاملاً بأثره ومستواه ERROR، وخطأ تيليجرام غير عابر
(`BadRequest: Chat not found`) يبقى كاملاً أيضاً؛ (٣) التوكن يُنقَّى
فعلياً الآن (`***REDACTED***`) مع إثبات مباشر أن النمط القديم لا يطابق
الرابط إطلاقاً · `pytest -q --ignore=Archive` (187 نجح) · فحص
`register_all_handlers()` حقيقي (170 معالِجاً — بلا تغيير).

**درس عام**: الشرطات المائلة المضاعفة داخل سلسلة خام (`r"\\."`) عطب
صامت متكرر — الفلتر "موجود ويعمل" ظاهرياً لكنه لا يطابق شيئاً. أي فلتر
أمني يجب أن يُختبَر بعيّنة حقيقية تُثبِت أنه **يمنع** ما وُضِع لمنعه، لا
أن يُفترَض عمله من وجوده.

لا حاجة لسكربت هجرة — تعديل تسجيل بحت، بلا أي تغيير على المخطط.

### 2026-08-22 (٥٩) — 🔨 "🏠 الحالات الموجودة": إدخال المرضى القدامى لبوتَي الخدمات والإقامة — **المرحلة ١**

**الطلب**: وحدتا "🪪 الإقامة" و"🔧 الخدمات العامة" فُعِّلتا للمرضى الجدد
الذين يمرّون بتدفق "🛬 الوصول"، لكن المرضى الموجودين **قبل** التفعيل لم
يمرّوا به إطلاقاً فلا وجود لهم فيهما. المطلوب مسار يدوي من الأدمن
لإدخالهم — مهمة مؤقتة تنتهي بانتهاء إدخال القدامى.

**التحليل المعماري (قبل أي كود)**: المريض القديم يحتاج التسجيل في
**ثلاثة أنظمة منفصلة لا رابط بينها**:
| النظام | مصدر بياناته | ما يلزم |
|---|---|---|
| 🛫 المغادرين | `gs_arrival_patients` | صف `ArrivalPatient` بحالة `active` |
| 🔧 الخدمات العامة | سجلّ `patients` | ظهور تحت `only_companion_flow` |
| 🪪 الإقامة | `res_persons` | صف `ResidencyPerson` |

**⚠️ التعارض الحقيقي المُكتشَف قبل التنفيذ**: ظهور المريض في "الخدمات
العامة" يتطلَّب `patient_type ∈ (companion_parent, companion)`. وتغيير
نوع مريض قديم إلى ذلك كان **سيكسر تصنيفاً حقيقياً**: مريض `chennai`
يختفي من قسم "🏙️ تقارير تشناي" ويظهر خطأً في التقارير الاعتيادية، ومريض
`pharmacy_only` يخرج من قصره على الصيدلية.

**الحل**: علَم مستقل `Patient.gs_onboarded_at` (بنفس نمط `archived_at`
في الإدخال ٥٧) ووُسِّع شرط `only_companion_flow` في `_type_visible`
ليقبله — فيبقى `patient_type` الأصلي **سليماً تماماً بلا أي مساس**.
المرافقون الجدد يُنشأون كصفوف `Patient` جديدة بنوع `companion` (لا خطر،
فهم جدد لا تصنيف سابق لهم).

**الحالة السادسة في وحدة الإقامة**: `STATUS_LEGACY_PENDING`
("🏠 معلّقات من الحالات السابقة"). واجهة الإقامة كلها مبنية على
`STATUS_ORDER`، فالزر والعدّاد وشاشة القائمة ظهرت **تلقائياً** بلا لمس
أي ملف واجهة — نقطة تكامل نظيفة.

**عزل المخاطر**: بُنيت الميزة في ملف مستقل تماماً
(`bot/handlers/admin/admin_legacy_onboarding.py`، بادئة `lego:`،
مجموعتان 21/22 خاصتان به) يعيد استخدام البدائل المشتركة
(`shared/calendar_picker`) — **بلا أي تعديل على تدفق الوصول الحيّ**، فلا
خطر انحدار عليه إطلاقاً. معالِجا النص/الوسائط يخرجان فوراً إن لم توجد
جلسة `lego` نشطة حتى لا يبتلعا رسائل التدفقات الأخرى.

**الفورمة** (بطلب المستخدم حرفياً، بلا حقول إضافية — أكّد ذلك صراحةً):
تأشيرة (تقويم بقفز سنوات/شهور، **بلا زر تخطٍّ**) → جواز (نفس التقويم) →
صورة الجواز → صورة التأشيرة مع ختم الدخول → صورة الإقامة (**الحقل
الاختياري الوحيد**) → السكن ورقم الشقة → مرافقون؟ كم؟ → لكل مرافق: الاسم
+ نفس الفورمة → **مراجعة** → حفظ.

**تحسينات أُضيفت على الفكرة الأصلية** (بموافقة المستخدم): حارس ضد
الإدخال المزدوج، إخفاء من أُدخِل مسبقاً من قائمة المرشحين، شاشة مراجعة
قبل الحفظ، وعدّاد تقدّم (المتبقّون/أُدخِلوا).

**التكامل مع أرشيف المسافرين (٥٧)**: قائمة المرشحين تستبعد
`archived_at IS NOT NULL` — فمن سافر لا يُدخَل. تحقّقتُ من ذلك صراحةً.

**التحقق**: محاكاة حيّة كاملة عبر المعالِجات الحقيقية تمشي الفورمة خطوة
بخطوة (تقويمان، ثلاث مرفقات، تخطي الإقامة، السكن، مرافق كامل، مراجعة،
حفظ) ثم تتحقق من **الأنظمة الثلاثة** في القاعدة: صف وصول `active` مع
مرافقه؛ علَم `gs_onboarded_at` مع **تأكيد أن `patient_type` لم يتغيّر**؛
شخصا إقامة بحالة `LEGACY_PENDING` مع سجلّ انتقال لكلٍّ منهما. وأيضاً:
ظهورهما في `fetch_all(only_companion_flow=True)`؛ **سلامة تصنيف تشناي**؛
بقاء المريض في التقارير الاعتيادية؛ عمل الحارس ضد التكرار؛ اختفاؤه من
قائمة المرشحين بعد الإدخال؛ استبعاد المسافر · حذف `__pycache__` ثم
`pytest -q --ignore=Archive` (187 نجح، بلا تصادم مجموعات) · فحص
`register_all_handlers()` حقيقي: **173 معالِجاً (بدل 170) بفرق +3
بالضبط**، ونمط `lego:` التُقِط تلقائياً في إسكات الفلبَك.

**🐛 تسريب رُصِد وأُصلح فور الشحن (سؤال المستخدم "كيف سيظهر في بوت
الخدمات؟")**: كلٌّ من "🛬 الوصول" و"🔧 الخدمات العامة" يستدعي منتقي
الاسم بـ`only_companion_flow=True`. فبمجرد أن جعلنا العلَم
`gs_onboarded_at` كافياً للظهور تحت ذلك الشرط، ظهر المريض القديم في
منتقي اسم **الوصول** أيضاً — مناقضاً القاعدة صراحةً ("لأنهم ليسوا من
الوصول")، وكان سيسمح بتسجيل وصول لمن هو هنا أصلاً. أُثبِت التسريب
تجريبياً ثم أُصلح بعلَم طلب صريح `include_onboarded` يُمرَّر عبر
`enter()` ويُحفَظ في جلسة المنتقي (فتحترمه إعادة الجلب والبحث inline
معاً): "الخدمات العامة" وحدها تمرّره `True`، و"الوصول" يبقى `False`.
تحقّق بعد الإصلاح: القديم مخفي عن الوصول، ظاهر في الخدمات العامة،
والواصلون (companion_parent/companion) بلا أي انحدار — في القائمة
المرقَّمة والبحث معاً.

**تصحيح ثانٍ (مراجعة المستخدم)**: منتقي "🔧 الخدمات العامة" كان يعرض
**مرافقي** المريض القديم كأسماء مستقلة أيضاً — بينما القاعدة المطلوبة في
الطلب الأصلي: "تظهر أسماء المرضى فقط بدون المرافقين". صُحِّح: من أُدخِل
عبر "🏠 الحالات الموجودة" يظهر **المريض وحده** في منتقي الخدمات
(`return include_onboarded and pt != _COMPANION`). صفوف المرافقين تبقى في
السجلّ كما هي — يحتاجها عرض الأدمن ووحدة الإقامة — ولا تُحذف.
**وحدة الإقامة كانت سليمة أصلاً** بلا أي تعديل: `get_requests_by_status`
تجمع حسب المريض الجذر، فيظهر المرافقون **داخل** ملف المريض لا كأسماء
مستقلة (تحقّق مباشر: قائمة الحالة تُخرِج اسم المريض وحده).
تحقّق بعد التصحيح: الخدمات تعرض المريض القديم وحده؛ **والواصلون عبر
التدفق الطبيعي (مريضاً ومرافقاً) بلا أي انحدار** في القائمة والبحث معاً.

**🔍 بحث سريع (طلب لاحق من المستخدم)**: مع أكثر من مئة اسم، تصفّح
الصفحات (٨ لكل صفحة) بطيء. أُضيف زر "🔍 بحث بالاسم" داخل الشاشة: يطلب
كتابة الاسم أو جزءاً منه (حرفان فأكثر) ثم يعرض المطابق، مع زر "📋 عرض
كل الأسماء" للعودة، والتنقّل بين صفحات النتائج يحافظ على الاستعلام.

⚠️ **لماذا لم يُستخدَم بحث تيليجرام المدمج** (`switch_inline_query`) الذي
يستخدمه المترجمون رغم طلب المستخدم "نفس المترجمين": معالِجه المشترك
(`user_patient_search_inline.py`) يطبّق قواعد **تدفق التقارير** لا قواعد
هذه الشاشة، فكان سيعرض أسماءً مؤرشفة (مسافرين) أو مُدخَلة مسبقاً —
وهي بالضبط ما تستبعده هذه الشاشة. البحث المحلي يعيد استخدام
`_fetch_candidates` نفسها فيرث فلاترها حرفياً بلا ازدواج منطق.

حالة البحث محفوظة خارج جلسة الفورمة (`_lego_search_wait`/`_lego_search_q`)
لأن البحث يقع **قبل** اختيار المريض. تحقّق: زر البحث ظاهر؛ حرف واحد
مرفوض مع بقاء حالة الانتظار؛ النتائج تستبعد المُدخَل مسبقاً والمسافر؛
التنقّل يحافظ على الاستعلام؛ "عرض كل الأسماء" يلغيه؛ الاختيار من النتائج
يبدأ الفورمة؛ و**النص خارج حالة البحث/الجلسة لا يُبتلع** (لا يعطّل أي
تدفق آخر).

**المرحلة ٢ (لم تُنفَّذ بعد — بعد تجربة المرحلة ١ فعلياً)**: زر
"🏠 معلّقات من الحالات السابقة" داخل وحدة الإقامة، وبداخله زرّان: "توجد
إقامة/تمديد" (تاريخ آخر إصدار + تاريخ الانتهاء + تاريخ التنبيه القادم +
صورة آخر إقامة + صورة شخصية) أو "لا يوجد تمديد" (تاريخ الانتهاء فقط)،
ثم نقل الشخص ومرافقيه للحالة المناسبة ضمن الحالات الخمس القائمة.

لا حاجة لسكربت هجرة — العمود يُضاف تلقائياً عند أول إقلاع عبر
`_migrate_column`، وكل الصفوف القائمة تبقى `NULL` فلا يتغيّر أي سلوك.

### 2026-08-22 (٦٠) — 🔨 "🏠 معلّقات من الحالات السابقة": معالجة إقامة المرضى القدامى — **المرحلة ٢**

تكملة الإدخال ٥٩. المرضى القدامى المُدخَلون عبر شاشة الأدمن يصلون وحدة
الإقامة بحالة `LEGACY_PENDING` (بياناتهم الأساسية مكتملة، وبيانات
**الإقامة** نفسها ناقصة). هذه المرحلة تُدخِلها ثم تنقلهم لحالتهم الطبيعية.

**المساران** (بطلب المستخدم حرفياً) — زرّان على مستوى **العائلة** داخل
شاشة تفاصيل الطلب:
- **✅ توجد إقامة / تمديد**: تاريخ آخر إصدار → تاريخ الانتهاء → تاريخ
  التنبيه القادم → صورة آخر إقامة → صورة شخصية.
- **❌ لا يوجد تمديد**: تاريخ الانتهاء فقط.

ثم شاشة **اختيار القائمة** التي ينتقل إليها المريض ومرافقوه معاً — مبنية
من `STATUS_ORDER` مطروحاً منها `LEGACY_PENDING` نفسها، فتتبع أي تغيير
مستقبلي على الحالات تلقائياً.

**قرارات تصميم**:
- **طابور مشتقّ من القاعدة لا جلسة**: الموضع (أي شخص/أي خطوة) يُحتسَب من
  الحقول المملوءة فعلاً عبر `_build_legacy_state` — نفس نمط
  `_build_onboard_state` القائم. النتيجة: المسار **قابل للاستئناف** بعد
  أي انقطاع أو إعادة تشغيل، بلا حالة ضائعة.
- **الوضع (ext/noext) يُحمَل في هدف التقويم/الرفع** لا في مفتاح سياق
  مستقل: مسار "لا يوجد تمديد" لا يطلب صوراً ولا تنبيهاً، فلا يمكن تمييزه
  من الحقول الفارغة وحدها. (أُنشئ مفتاح `_CTX_LEGACY` ابتداءً ثم **حُذِف**
  عند اكتشاف أنه لا يُقرَأ إطلاقاً — حالة ميتة.)
- **تقويم بقفز السنوات/الشهور** لهذه التواريخ (قد تبعد سنة أو أكثر،
  فالتنقّل شهراً شهراً غير عملي). وُسِّع `_handle_calendar_action` ليدعم
  `cal_years/cal_setyear/cal_setmonth/cal_yprev/cal_ynext` — إضافة بحتة:
  المسارات القديمة تبني تقويمها بلا `quick_jump` فلا تصلها هذه الأفعال
  إطلاقاً.
- **أرشفة الإصدار تلقائياً**: من يملك تاريخ انتهاء **وملف إقامة** معاً
  يُضاف له سطر في `ResidencyIssuance` عند النقل — فيظهر في "🖨️ طباعة ملف
  الحالة" وسجلّ الإصدارات كأي إصدار رسمي. مسار "لا يوجد تمديد" (بلا ملف)
  لا يُؤرشَف — تحقّق صريح من ذلك.
- **الصورة الشخصية تُضبَط على 4×6** بنفس معالجة صورة الوصول (تصلح
  للطباعة)، وصورة الإقامة تُحفَظ كما هي (مستند رسمي).

**عمود جديد**: `ResidencyPerson.last_issue_date` (تاريخ آخر إصدار — إقامة
أُصدرت قبل تفعيل الوحدة فلا سجلّ لها)، مُهاجَر عبر `_migrate_column`.

**التحقق**: محاكاة حيّة كاملة عبر معالِجات الوحدة الحقيقية للمسارين —
(١) مسار التمديد: الزرّان يظهران؛ التقويم بقفز السنوات؛ قفز السنة←الشهر←
اليوم؛ الحقول الخمسة محفوظة بالقاعدة؛ **انتقال الطابور تلقائياً للمرافق**؛
شاشة الاختيار تعرض 5 حالات بلا الحالة الحالية؛ النقل يحرّك المريض ومرافقه
**معاً** مع سجلّ انتقال لكلٍّ منهما وأرشفة إصدارَين؛ واختفاؤهما من قائمة
المعلّقات وظهورهما في الوجهة. (٢) مسار "لا يوجد تمديد": يبدأ بالانتهاء
مباشرة، ينتقل للمرافق بلا طلب صور/تنبيه، ويُنقَل بلا أرشفة إصدار.
(٣) حارس: حالة هدف غير صالحة تُرفَض بلا أي تغيير · `pytest -q
--ignore=Archive` (187 نجح) · `register_all_handlers()` حقيقي (173
معالِجاً — بلا تغيير، كل التوسعة داخل معالِج `rn:` القائم) · إعادة تشغيل
تحقّقَي المرحلة ١ والهجرة: سليمان بلا انحدار.

**إضافة (طلب لاحق)**: مسار "❌ لا يوجد تمديد" لا يجمع تاريخ تنبيه أصلاً،
لكن الانتقال إلى "🟢 الحالات النشطة" يستلزمه — وإلا لن يظهر أي تنبيه لهذا
المريض لاحقاً. صار اختيار "الحالات النشطة" يطلب تاريخ التنبيه **لكل شخص
ينقصه** (المريض ثم مرافقوه) قبل تنفيذ النقل، ثم يُنفَّذ النقل تلقائياً عند
اكتمال الجميع. بقية الحالات تُنقَل مباشرة كما كانت.

⚠️ الفحص يقوم على **الحقل الفعلي** (`reminder_date` فارغ) لا على الوضع
(ext/noext) — فيغطّي أي نقص مهما كان مصدره، ولا يسأل مرتين في مسار
التمديد الذي جمعه أصلاً (تحقّق صريح من الحالتين). الحالة الهدف تُحفَظ في
`_CTX_LEGACY_MOVE` أثناء الجمع، ويُنظَّف ضمن `_clear_transient_state` حتى
لا تبقى حالة نقل معلّقة لعائلة أخرى بعد مغادرة الشاشة.

**تصحيح (مراجعة المستخدم)**: شاشة تفاصيل الطلب لحالة `LEGACY_PENDING`
كانت تعرض أيضاً أزرار "📄 الوثائق" لكل شخص و"🖨️ طباعة ملف الحالة".
أُخفيت: هذه **حالة عبور** غرضها الوحيد إدخال بيانات الإقامة الناقصة ثم
النقل — ووثائق المريض القديم لم تُرفَع بعد عبر هذه الوحدة، وطباعة الملف
قبل اكتمال البيانات تُخرِج ملفاً ناقصاً. الشاشة الآن: الزرّان + رجوع فقط.
**تعود كل الأزرار تلقائياً** بمجرد انتقاله لأي من الحالات الخمس الطبيعية
(تحقّق صريح من الاتجاهين).

لا حاجة لسكربت هجرة — العمود يُضاف تلقائياً عند أول إقلاع، والصفوف
القائمة تبقى فارغة فلا يتغيّر أي سلوك.

### 2026-08-22 (٦١) — 🗑️ حذف كامل لاسم أُضيف عبر "🤝 مريض جديد مع مرافقين"

**البلاغ**: أسماء تجريبية أُنشئت أثناء بناء زر "مريض جديد مع مرافقين" بقيت
ظاهرة في **بوت الإقامات** و**الخدمات العامة**، ولا سبيل لإزالتها.

**السبب**: `delete_patient` العادية (زر "🗑️ حذف اسم") تحذف صفوف `Patient`
وتنظّف أشخاص الإقامة **غير المُستكمَلين حصراً**
(`delete_stub_person_by_name`: `WAITING_ARRIVAL` بلا صورة). فأي اسم مرّ
بتدفق الوصول أو أُدخِل عبر "🏠 الحالات الموجودة" تبقى له:
- صفوف `ResidencyPerson` بأي حالة أخرى ⇒ تظل في بوت الإقامة
- صفوف `ArrivalPatient`/`ArrivalCompanion` ⇒ تظل في 🛫 المغادرين
بلا أي مريض يشير إليها — أسماء يتيمة دائمة.

**الإضافة**: زر "🗑️ حذف اسم (مريض + مرافقين)" في "📝 إدارة أسماء المرضى"
(بادئة `pcdel:`) يحذف الاسم ومرافقيه من **الأنظمة الثلاثة دفعة واحدة**،
مع توابع الإقامة كلها (`ResidencyStatusLog`/`ResidencyDocument`/
`ResidencyIssuance`) فلا تبقى صفوف يتيمة.

**🔒 حارس البيانات (قرار تصميم)**: يُمنَع الحذف **نهائياً** إن وُجد أي
تقرير طبي مرتبط بالاسم — تعرض الشاشة عدد التقارير، **ولا تعرض زر الحذف
إطلاقاً**، وتوجّه الأدمن إلى "🧳 أرشيف المسافرين" (إخفاء بلا فقدان
بيانات). هذا نفس المبدأ الذي بُني لأجله الأرشيف في الإدخال ٥٧: لا نحذف من
له بيانات. الحارس مُطبَّق في **طبقة الخدمة** أيضاً لا في الواجهة وحدها —
تحقّق صريح باستدعاء `purge_companion_flow_family` مباشرةً على اسم له
تقرير: تُرفَض ولا يُمَسّ أي صف.

**شاشة تأكيد صريحة**: تُظهر قبل الحذف ما سيُحذَف بالضبط بالأرقام (كم اسماً
من سجلّ المرضى، كم شخصاً من الإقامة، كم صف وصول ومرافقاً) — لا حذف
"أعمى".

**التحقق**: بُنيت عائلتان كاملتان عبر الأنظمة الثلاثة كما ينتجهما
الاستخدام الحقيقي (سجلّ مرضى + إقامة بتوابعها + وصول بمرافقيه)، إحداهما
لها تقرير طبي — (١) القائمة تعرض الاسمين؛ (٢) شاشة التأكيد تُظهر الأرقام
الصحيحة؛ (٣) **الحارس يمنع** ذات التقرير ولا يعرض زر الحذف، ويرفض أيضاً
عند تجاوز الواجهة؛ (٤) الاسم النظيف يُحذَف من الأنظمة الثلاثة بالكامل
**بلا أي صف يتيم** (فحص: كل صف تابع باقٍ يعود لشخص إقامة موجود فعلاً)؛
(٥) الاسم الآخر وبياناته **لم تتأثر إطلاقاً**؛ (٦) اختفى من القائمة ·
`pytest -q --ignore=Archive` (187 نجح) · `register_all_handlers()` حقيقي:
**176 معالِجاً (بدل 173) بفرق +3 بالضبط**.

لا حاجة لسكربت هجرة — لا تغيير على المخطط.

### 2026-08-22 (٦٢) — 🏗️ توحيد دوال النص العربي في مولّدات PDF (`services/pdf_arabic.py`) + إصلاح خلل "None"

**السياق**: بطلب المستخدم — متابعة تقليل تكرار المنطق. اختير ملفّات PDF
أولاً: `_ar` (إعادة تشكيل + bidi) مكرَّرة **6 مرات**، `_ar_wrap` **4 مرات**،
`_pick_font` **5 مرات** — وتعليقات تلك الملفات نفسها تعترف بالنسخ ("نفس
نمط ... في services/X.py").

**⚠️ تصحيح منهجي أثناء العمل**: أول اختبار سلوكي كتبته استخرج الدوال عبر
AST ونفّذها **معزولة عن وحداتها**، فسقطت كلها بـ`NameError: _ARABIC_RE`
وتطابقت النتائج تطابقاً زائفاً ("كلها متكافئة"). اكتُشِف ذلك بطباعة القيم
الفعلية بدل الاكتفاء بمقارنتها. الاختبار الصحيح يستورد الوحدات الحقيقية
بكل تبعياتها — **درس: أي مقارنة سلوكية يجب أن تُطبَع قيمها لا أن تُقارَن
فقط، وإلا قد يكون التطابق تطابقَ أخطاء.**

**التباعد الحقيقي المُثبَت** (بعد تصحيح الاختبار):
- 🐛 **خلل فعلي**: `modules/healthcare/evaluation/pdf_builder.py::_ar`
  كان `str(text)` بلا حارس، فيُخرِج كلمة **"None" حرفياً** للقيمة الفارغة
  — أي أن "None" كانت **تُطبَع فعلياً** داخل تقرير تقييم الرعاية الصحية،
  بينما النسخ الخمس الأخرى تُخرِج "".
- **نطاق المحارف اختلف**: ثلاث نسخ تفتقد كتلة Arabic Extended-A
  (U+08A0–U+08FF) الموجودة في نسختين — فنص بتلك المحارف يمرّ بلا إعادة
  تشكيل في بعض التقارير دون بعض.

**التنفيذ**: ملف جديد `services/pdf_arabic.py` يحمل التنفيذ الوحيد
(`ar`/`ar_wrap`/`pick_font` + `ARABIC_RE`) موحَّداً على **الأوسع والأسلم**:
اتحاد كل كتل المحارف الخمس، وحارس يمنع طباعة "None". النسخ الست بقيت
كأغلفة رقيقة تستدعيه (نفس نمط `shared/text_safety.py` في الإدخال ٤٩) فلم
تُلمَس أي من مئات مواضع الاستدعاء. حُذِفت أيضاً تعريفات `_ARABIC_RE`
الستّ بعد أن صارت ميتة (عبر AST لا مطابقة نصية).

**التحقق**: مقارنة سلوكية على **الوحدات الحقيقية** قبل/بعد — قبل: اختلاف
مؤكَّد في `None`؛ بعد: **الست متطابقة تماماً** على كل العيّنات (عربي/
إنجليزي/مختلط/Supplement/Extended-A/فارغ/None/أرقام/نِسَب) · توليد PDF
فعلي من `residency_case_pdf` (ملف صالح يبدأ بـ`%PDF-`) · `_ar_wrap` يلفّ
عبر 6 أسطر · `_pick_font` يعمل عبر المصدر الموحّد · تأكيد أن
`pdf_builder._ar(None) == ""` وأنه ما زال يعيد التشكيل · `pytest -q
--ignore=Archive` (187 نجح) · `register_all_handlers()` حقيقي (176
معالِجاً — بلا تغيير).

**الأثر**: **−213 سطراً صافية** (75 مُضافاً مقابل 288 محذوفاً) عبر 6 ملفات.

**ما تبقّى من تقليل التكرار** (لجولات قادمة): ترقيم الصفحات المكرَّر 8
مرات في شاشات الأدمن رغم وجود `shared/ui_primitives.py::paginate()`
جاهزة وغير مستخدَمة؛ و~750 سطراً من آلية الجلسة المكرَّرة 7 مرات في
وحدات الرعاية الصحية والخدمات العامة.

لا حاجة لسكربت هجرة — إعادة تنظيم بحتة، بلا أي تغيير على المخطط.

### 2026-08-22 (٦٣) — 🏗️ قائمة مختصي الخدمات صارت مُشتقّة من الصلاحيات بدل أسماء مكتوبة في الكود

**الطلب**: حذف "رضاء" و"علي صالح" من بوت الخدمات، والاعتماد على الآلية
الجديدة: المستخدم يقدّم طلباً → يوافق الأدمن ويكتب له اسماً → يفتح له
صلاحية الخدمات العامة ⇒ **يظهر اسمه تلقائياً**.

**ما كان**: `STAFF_MAP` في `general_services/constants.py` قاموس ثابت
باسمين مكتوبين في الكود. أي موظف جديد يستلزم تعديل كود ونشراً، وأي موظف
يغادر يبقى اسمه ظاهراً حتى يُحذَف يدوياً.

**ما صار**: `modules/general_services/staff.py::get_staff_map()` يبني
`{sp_<tg_id>: الاسم}` من **من مُنح وحدة `general_services`** في إدارة
الوصول، بالاسم المعتمَد من `TranslatorDirectory` (نفس الاسم الذي يكتبه
الأدمن عند الموافقة). مرتَّبة أبجدياً ليثبت ترتيب الأزرار.

**إعادة استخدام لا تكرار**: بُنيت فوق `access_service.list_users_with_module`
و`translators_service.get_all_translators` القائمتين — لا استعلام جديد ولا
مُحلِّل أسماء جديد (نفس نمط `_names_by_city` المُتَّبع أصلاً لفلترة مترجمي
تشناي بالصلاحيات).

**قرار**: الأدمن **لا يُدرَج تلقائياً** رغم أن `is_admin` يمنحه وصولاً لكل
الوحدات — كونه أدمناً لا يعني أنه مختص خدمات يُنسَب إليه العمل. القائمة
تقتصر على من مُنح الوحدة صراحةً، وهو بالضبط معنى "فتح الصلاحيات له".

**السجلات القديمة لا تتأثر**: `specialist_label` مُخزَّن مع كل صف
(ArrivalPatient/DepartureRecord/PublicService)، فأسماء من غادر تبقى ظاهرة
في سجلّاته التاريخية بعد اختفائه من قائمة الاختيار. تحقّقتُ أيضاً أن
`STAFF_MAP` كانت تُستخدَم **وقت الاختيار فقط** (بناء الأزرار + تحويل
معرّف الزر المضغوط لاسم) لا في عرض السجلات.

**⚠️ عطب أثناء التنفيذ وتصحيحه**: سكربت الاستبدال أدرج سطر الاستيراد
الجديد اعتماداً على "آخر سطر يبدأ بـ`from modules.general_services`" —
فوقع **داخل كتلة استيراد متعددة الأسطر** في 5 ملفات من 6 وكسرها نحوياً.
اكتُشِف بفحص `ast.parse` لكل ملف بعد التعديل (لا بالثقة في نجاح السكربت)،
وأُصلح بإعادة الإدراج بعد **نهاية آخر عقدة استيراد كاملة** عبر AST.
**درس: أي تعديل آلي على الاستيرادات يجب أن يُتحقَّق منه بـ`ast.parse` لكل
ملف مباشرةً، لا بعدّ الاستبدالات وحده.**

**التحقق**: منح الصلاحية ⇒ ظهور الاسم فوراً؛ منح ثانٍ ⇒ ترتيب أبجدي ثابت؛
من له اسم بلا صلاحية **لا يظهر**؛ صلاحية وحدة أخرى (الإقامة) **لا تُدرجه**
هنا؛ سحب الصلاحية ⇒ اختفاء فوري؛ وبناء الأزرار فعلياً في **الشاشات الثلاث**
(الوصول/المغادرين/الخدمات العامة) من القائمة الديناميكية، مع اختفائها
جميعاً بعد سحب كل الصلاحيات · تأكيد اختفاء "رضاء"/"علي صالح" من الكود ·
`pytest -q --ignore=Archive` (187 نجح) · `register_all_handlers()` حقيقي
(176 معالِجاً — بلا تغيير).

لا حاجة لسكربت هجرة — لا تغيير على المخطط.

### 2026-08-22 (٦٤) — 🔧 توحيد قاعدة "الخدمات العامة = مرضى فقط" لكل المصادر + تسمية العلَم باسمه الحقيقي

**السؤال الذي كشف التعارض**: سأل المستخدم "هل يظهر اسم من زر «🤝 مريض جديد
مع مرافقين» في الخدمات العامة؟". التحقق التجريبي أجاب: **نعم**، لكنه كشف
أن **المرافق يظهر أيضاً** — مخالفاً قاعدة المستخدم السابقة ("لا داعي
لإظهار المرافقين، فقط المرضى")، وموجِداً **سلوكين مختلفين لنفس الشاشة**:

| مصدر الاسم | المريض | المرافق |
|---|---|---|
| 🏠 الحالات الموجودة | ظاهر | مخفي |
| 🤝 مريض جديد مع مرافقين | ظاهر | **ظاهر** ← غير متسق |

سببه أنني طبّقت القاعدة سابقاً (إدخال ٥٩) على المرضى القدامى وحدهم تحفّظاً
على السلوك الحيّ. بقرار المستخدم صراحةً: **تُوحَّد على كل المصادر**.

**التنفيذ**:
- **إعادة تسمية `include_onboarded` → `services_scope`** عبر كل السلسلة
  (`_data`/`selector`/`_session`/البحث inline/`public_services`). الاسم
  القديم صار مضلِّلاً بعد أن أصبح العلَم يحدّد **نطاق الشاشة** (خدمات مقابل
  وصول) لا مجرد "تضمين القدامى".
- **إعادة بناء منطق `only_companion_flow`** ليكون صريحاً بحالاته الثلاث
  (مرافق / مريض تدفّق المرافقين / مريض قديم) بدل تداخل شرطين.

**🐛 تسريب رصده التحقق أثناء البناء**: النسخة الأولى من المنطق الموحَّد
أظهرت **مرافق المريض القديم في منتقي "🛬 الوصول"** — وهو لم يصل عبر ذلك
التدفق أصلاً فلا يصحّ تسجيل وصول له. أُصلح بحارس صريح: مرافق يحمل
`gs_onboarded_at` لا يظهر في أيٍّ من الشاشتين.

**⚠️ تصحيح في التحقق نفسه**: أحد اختباراتي زعم "انحدار: مريض تشناي ظهر في
التقارير" — وكان **الاختبار خاطئاً** لا الكود: قاس عبر `fetch_all()` العام
(الذي يُظهر مرضى تشناي عمداً لشاشات الرعاية/الصيدلية) بدل
`report_flow_patient_visible` قاعدةِ تدفّق التقارير الفعلية. صُحِّح الاختبار
ليقيس بالقاعدة الصحيحة.

**النتيجة النهائية** (مؤكَّدة تجريبياً):
- 🔧 **الخدمات العامة**: المرضى فقط — من الوصول ومن القدامى، بلا أي مرافق.
- 🛬 **الوصول**: يحتفظ بمرافقيه (يحتاج اختيار اسم المرافق لتسجيل بياناته)،
  وبلا المرضى القدامى ولا مرافقيهم.
- 📋 **تدفّق التقارير** و🏙️ **قسم تشناي**: بلا أي انحدار.
- 🔍 **البحث inline** يتبع نفس القاعدة بالضبط في الشاشتين.

**التحقق**: سيناريو بخمسة أنواع أسماء (قديم+مرافقه، تدفّق المرافقين+مرافقه،
عادي، تشناي) عبر أربع شاشات + البحث · إعادة تشغيل تحقّقَي المرحلة ١
والمختصين الديناميكيين · `pytest -q --ignore=Archive` (187 نجح) ·
`register_all_handlers()` حقيقي (176 معالِجاً — بلا تغيير).

لا حاجة لسكربت هجرة — لا تغيير على المخطط.

### 2026-08-24 (٦٥) — 🗑️ شاشة الحذف تصل الآن لكل مصدر (كانت تغطّي ثلثاً واحداً)

**الشكوى**: "في بوت الخدمات لا تزال تظهر أسماء قديمة، وكذلك في بوت الإقامات
في زر معلّق انتهاء الإقامة — كيف نتخلّص منها؟"

**التشخيص** (بأربعة أسماء اصطناعية عبر الشاشات الحقيقية): الشاشة كانت
تُدرِج `patient_type == "companion_parent"` وحده وتُعرِّف كل شيء
بـ`patient_id` — فبقي **مصدران كاملان بلا أي طريق للحذف**:

| الاسم | يظهر في | أداة الحذف |
|---|---|---|
| مريض زر المرافقين | الخدمات | ✅ يصله |
| مريض "🏠 الحالات الموجودة" (`gs_onboarded_at`) | الخدمات | ❌ **لا يصله** |
| **شخص إقامة يتيم** (لا صف مريض مطابق) | معلّق انتهاء الإقامة | ❌ **لا يصله** |
| شخص إقامة لمريض عادي | معلّق انتهاء الإقامة | ❌ **لا يصله** |

اليتيم كان أعمق من مجرد مُرشِّح ناقص: لا وجود لـ`patient_id` أصلاً ليُبنى
عليه أي إجراء — فالبنية نفسها كانت تمنع حذفه لا الاستعلام وحده.

**الإصلاح**: المُعرِّف صار **مفتاحاً نصّياً يحمل مصدره** — `p-<patient_id>`
أو `r-<res_person_id>` (شرطة لا نقطتان: `callback_data` مُقسَّمة بـ":").
- `get_deletable_families` تجمع المصادر الثلاثة بلا تكرار (الاسم المُغطّى
  بصف مريض لا يتكرّر كجذر إقامة)، وتعرض المصدر بجانب كل اسم.
- `_resolve_family` تترجم المفتاح ⇒ (الاسم, المرافقون, patient_id أو None)؛
  مرافقو مفتاح `r-` هم أبناء العلاقة الذاتية (`parent_id`).
- الحذف يشمل صفوف سجلّ المرضى **بالاسم** أيضاً لا بالرابط وحده — وإلا لبقي
  صف المريض العادي بعد حذف شخص الإقامة المطابق له.
- تسمية الزر: "🗑️ حذف اسم (مريض + مرافقين)" ⇐ "🗑️ حذف اسم نهائياً".

**🔴 فخّ كاد يمرّ صامتاً**: أنماط التسجيل كانت
`^pcdel:pick:\d+:\d+$` — وهي **ترفض** `pcdel:pick:p-12:0`. لولا تعديلها
لسقطت كل ضغطة لمعالِج الاحتياط العام بلا أي أثر ظاهر للأدمن (الزر "لا
يعمل" بلا رسالة خطأ). أُضيف تأكيد صريح في التحقق يطابق كل مفتاح مُولَّد
على الأنماط **المسجَّلة فعلياً** في `Application` لا على نسخة منها.

**الحارس بلا تغيير**: أي اسم له تقرير طبي واحد فأكثر **يُرفَض حذفه** ويُوجَّه
الأدمن لـ🧳 أرشيف المسافرين (إخفاء بلا فقدان بيانات). مؤكَّد تجريبياً:
الاسم ذو التقرير بقي في الشاشتين بعد حذف الثلاثة الآخرين.

**⚠️ تصحيح في التحقق نفسه**: أحد تأكيداتي توقّع خلوّ شاشة الخدمات تماماً
بعد الحذف — وهو **خطأ في الاختبار لا في الكود**: صاحب التقرير الطبي يبقى
عمداً بفعل الحارس. صُحِّح التوقّع.

**التحقق**: أربعة أسماء (المصادر الثلاثة + حالة الحارس) عبر
`handle_pcdel_pick`/`handle_pcdel_confirm` الحقيقيين · فحص عدم بقاء صفوف
يتيمة (مرافقو الإقامة، `ResidencyStatusLog`، صفوف الوصول، مرافقو السجلّ) ·
سلامة التقرير الطبي · `pytest -q --ignore=Archive` (187 نجح) ·
`register_all_handlers()` حقيقي (176 معالِجاً — بلا تغيير).

لا حاجة لسكربت هجرة — لا تغيير على المخطط.

## 📋 كيفية استخدام هذا السجل

- قبل إضافة أي نوع إجراء/مسار جديد للبوت: راجع الجدول أعلاه وسجِّله في
  كل الملفات الستة.
- بعد كل جلسة صيانة: أضف قسماً جديداً بالتاريخ تحت "ما أُصلح"، واحذف
  البند المقابل من "مفتوح" إن أُغلق بالكامل.
- عند بدء جلسة صيانة دورية: ابدأ بقسم "🔓 مفتوح" أعلاه.
