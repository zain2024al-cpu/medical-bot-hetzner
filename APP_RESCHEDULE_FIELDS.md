# 📋 حقول مسار "تأجيل موعد"

## 🔹 نظرة عامة
مسار "تأجيل موعد" هو أحد أنواع الإجراءات المتاحة في البوت. يحتوي على **3 حقول رئيسية**.

---

## 📝 الحقول بالترتيب:

### 1️⃣ **سبب تأجيل الموعد** (APP_RESCHEDULE_REASON)
- **الحالة:** `APP_RESCHEDULE_REASON`
- **النوع:** نص (Text Input)
- **الوصف:** يطلب من المستخدم إدخال سبب تأجيل الموعد
- **الحد الأدنى:** 3 أحرف
- **المفتاح في البيانات:** `app_reschedule_reason`
- **الرسالة:** "يرجى إدخال سبب تأجيل الموعد:"

### 2️⃣ **تاريخ العودة الجديد** (APP_RESCHEDULE_RETURN_DATE)
- **الحالة:** `APP_RESCHEDULE_RETURN_DATE`
- **النوع:** تقويم (Calendar Picker)
- **الوصف:** اختيار تاريخ العودة الجديد من التقويم
- **المفتاح في البيانات:** `app_reschedule_return_date` و `followup_date`
- **الرسالة:** "📅 **اختر تاريخ العودة الجديد:**"
- **ملاحظة:** ⚠️ **لا يحتوي على وقت** - فقط التاريخ

### 3️⃣ **سبب العودة** (APP_RESCHEDULE_RETURN_REASON)
- **الحالة:** `APP_RESCHEDULE_RETURN_REASON`
- **النوع:** نص (Text Input)
- **الوصف:** يطلب من المستخدم إدخال سبب العودة في التاريخ الجديد
- **الحد الأدنى:** 3 أحرف
- **المفتاح في البيانات:** `app_reschedule_return_reason` و `followup_reason`
- **الرسالة:** "يرجى إدخال سبب العودة:"

### 4️⃣ **اختيار المترجم** (APP_RESCHEDULE_TRANSLATOR)
- **الحالة:** `APP_RESCHEDULE_TRANSLATOR`
- **النوع:** قائمة اختيار (Inline Keyboard)
- **الوصف:** اختيار المترجم من القائمة
- **المفتاح في البيانات:** `translator_name`

### 5️⃣ **التأكيد والحفظ** (APP_RESCHEDULE_CONFIRM)
- **الحالة:** `APP_RESCHEDULE_CONFIRM`
- **النوع:** تأكيد نهائي
- **الوصف:** عرض الملخص النهائي وخيارات الحفظ أو التعديل

---

## 📊 الملخص النهائي (في show_final_summary):

عند عرض الملخص النهائي، يتم عرض:

```
📋 **ملخص التقرير**

📅 **التاريخ:** [تاريخ التقرير]
👤 **المريض:** [اسم المريض]
🏥 **المستشفى:** [اسم المستشفى]
🏷️ **القسم:** [اسم القسم]
👨‍⚕️ **الطبيب:** [اسم الطبيب]

⚕️ **نوع الإجراء:** تأجيل موعد

📅 **سبب تأجيل الموعد:** [سبب التأجيل]
📅 **تاريخ العودة:** [تاريخ العودة]  ⚠️ بدون وقت
📝 **سبب العودة:** [سبب العودة]

👤 **المترجم:** [اسم المترجم]
```

---

## ⚠️ ملاحظات مهمة:

1. **لا يوجد حقل وقت:** مسار "تأجيل موعد" **لا يحتوي على حقل وقت** - فقط التاريخ
2. **حقلان منفصلان للسبب:**
   - `app_reschedule_reason` - سبب تأجيل الموعد
   - `app_reschedule_return_reason` - سبب العودة في التاريخ الجديد
3. **تاريخ العودة:** يتم حفظه في `app_reschedule_return_date` و `followup_date`

---

## 🔧 الكود المرجعي:

### الملفات الرئيسية:
- `bot/handlers/user/user_reports_add_new_system.py` (السطور 7221-7378)
- `bot/handlers/user/user_reports_add_new_system/flows/shared.py` (السطور 1239-1253)

### الدوال الرئيسية:
- `start_appointment_reschedule_flow()` - بدء المسار
- `handle_app_reschedule_reason()` - معالجة سبب التأجيل
- `_show_reschedule_calendar()` - عرض التقويم
- `handle_reschedule_calendar_day()` - اختيار التاريخ
- `handle_app_reschedule_return_reason()` - معالجة سبب العودة

---

## 📌 الخلاصة:

مسار "تأجيل موعد" يحتوي على **3 حقول إدخال**:
1. ✅ سبب تأجيل الموعد (نص)
2. ✅ تاريخ العودة الجديد (تقويم - بدون وقت)
3. ✅ سبب العودة (نص)

**لا يوجد حقل وقت في هذا المسار!**





